[
  {
    "objectID": "MyBlog/BlogFull/index.html",
    "href": "MyBlog/BlogFull/index.html",
    "title": "Week of Intellectual Adventure",
    "section": "",
    "text": "I got nominated and eventually selected for the NUS Young Fellowship Program, but I did not think that this opportunity would become one of the best experiences of my academic life so far. As one of the five students picked from India, alongside some brilliant minds from IIT Bombay and IIT Madras, I was nervously excited. My essay titled “Guardrails” (check it out on under the Madhav_Speaks) had managed to impress the selection committee and opened the door to this incredible journey. This program brought together nearly 90 students from 20+ countries. We were from India, China, USA, UK, Hungary, Japan, South Korea, Malaysia, Australia, Singapore, Saudi Arabia, Philippines, Canada, Vietnam, Thailand, and more. When I packed my bags for this week-long adventure in learning, I knew I was in for something special. My dad came with me till Chhatrapati Shivaji Maharaj International Airport Mumbai as it was first time I was travelling international.",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#introduction",
    "href": "MyBlog/BlogFull/index.html#introduction",
    "title": "Week of Intellectual Adventure",
    "section": "",
    "text": "I got nominated and eventually selected for the NUS Young Fellowship Program, but I did not think that this opportunity would become one of the best experiences of my academic life so far. As one of the five students picked from India, alongside some brilliant minds from IIT Bombay and IIT Madras, I was nervously excited. My essay titled “Guardrails” (check it out on under the Madhav_Speaks) had managed to impress the selection committee and opened the door to this incredible journey. This program brought together nearly 90 students from 20+ countries. We were from India, China, USA, UK, Hungary, Japan, South Korea, Malaysia, Australia, Singapore, Saudi Arabia, Philippines, Canada, Vietnam, Thailand, and more. When I packed my bags for this week-long adventure in learning, I knew I was in for something special. My dad came with me till Chhatrapati Shivaji Maharaj International Airport Mumbai as it was first time I was travelling international.",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-0-touchdown-in-the-lion-city",
    "href": "MyBlog/BlogFull/index.html#day-0-touchdown-in-the-lion-city",
    "title": "Week of Intellectual Adventure",
    "section": "Day 0: Touchdown in the Lion City",
    "text": "Day 0: Touchdown in the Lion City\n\n\n\nChangi Airport on arrival\n\n\nStepping off the plane in Singapore, I was hit by two the humidity (whew!) and the efficiency of Changi Airport. This place runs like a well-oiled machine! Although NUS had arranged transportation, I decided to grab(yes) a taxi. As we zipped past skyscrapers and lush greenery, I was excited for the week ahead. The NUS campus welcomed me with open arms as campus is part of city and not a isolated place (and comfy accommodation). As I settled in, I decided to take a stroll around University town with other fellows and had intros and handshakes with a lot of people.\n\n\n\nArrival of students",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-1-diving-into-the-deep-end",
    "href": "MyBlog/BlogFull/index.html#day-1-diving-into-the-deep-end",
    "title": "Week of Intellectual Adventure",
    "section": "Day 1: Diving into the Deep End",
    "text": "Day 1: Diving into the Deep End\n\n\n\nProf Jessical Pan in inaugural address\n\n\n\nMorning: Breaking the Ice and Blowing Our Minds\nThe program kicked off with some ice-breakers. It was here that I met my squad: Rena, Yi Xin, Xinming, and Swastika. We were dubbed Group 12. Our first lecture was by Professor Chai Kah Hin, he kept pushing us to ask “So what?” – making us really think about the impact and relevance of our research ideas. He introduced us to this cool concept called the “ghost deck” which basically is about creating a storyboard of your presentation before getting into the nitty-gritty. Apparently, it’s a trick used by big shots like McKinsey to keep their presentations tight and logical. (Mental note: must try this for my next class project!)\n\n\n\nInaugural session\n\n\n\n\nAfternoon: Problem-Solving Like a Boss\nProf. Chai wasn’t done with us yet. His mantra was “Rough, Rapid, Right”– the idea that making quick mistakes is actually a good thing. It’s all about learning fast and adapting faster. He also talked about horizontal and vertical logic in presentations, emphasizing the need for a clear, newspaper-like summary to hook your audience from the start.\n\nPro tip: If you ever find yourself in Singapore, don’t miss out on trying the local kopi (coffee).\n\nWe rounded off the afternoon with a quick but eye-opening lecture by Dr. Amirhassan Monajemi on Generative AI. While a lot of it was familiar territory for us computer science geeks, seeing how it’s being applied across different fields at NUS was fascinating. It’s one thing to code an AI model, but seeing it potentially reshape entire industries? Although I found myself yawning during this lecture, I went out and took a Kopi to keep myself fresh. Actually Luo helped me to get to know about Kopi. I am attaching one guide here for your reference that I used over remaining week.\n\n\n\n\n\n\nKopi guide\n\n\n\n\n\n\n\nFirst Lunch\n\n\n\n\n\n\n\nEvening: The Ethics of AI\nThe highlight of the day was hands down the session on AI and ethics by Simon Chesterman, Dean of the NUS Faculty of Law. He didn’t just lecture; he made us question everything we thought we knew about education and technology. He painted a vivid (and slightly unsettling) picture of the challenges universities face in the AI era. He threw some real curveballs at us: Are students customers or products? How do we evaluate people when AI can basically be a performance-enhancing drug for the mind? He started by addressing the fear of AI turning into Skynet and taking over the world. While he didn’t completely rule out the possibility (gulp!), he was more concerned about how we humans might misuse this insanely powerful tech.\n\n\n\nAddress of Dean Simon Chesterman\n\n\nThe role of universities in this brave new AI world was a big talking point. He told us in clear terms that the age-old notion of professors knowing more than students is becoming outdated and with all the world’s knowledge at our fingertips, universities need to focus on teaching us how to think, not just what to think. We also dove into the ethical minefield of AI in various fields. In research, for instance, AI is doing some pretty cool stuff like speeding up drug discovery. But it also raises some tricky questions about plagiarism and the temptation to take shortcuts. He also stressed the importance of separating the hype from reality when it comes to AI’s capabilities. One of the most thought-provoking parts of the discussion was about responsibility in the AI era. Who’s to blame when an AI system messes up? The developers? The users? The data it was trained on?\nAfter the session, some graduate students took us for a guided campus tour where I got to know about a lot of folks and had some interesting conversations with friends from Japan and South Korea.\n\n\n\nCampus Tour later that day\n\n\nOver dinner at a local hawker center near Clementi mall which is very close to University Town, our group, along with a couple of others, started with debate about the future of creativity and AI’s role in the arts. The diversity of viewpoints, influenced by our varied cultural backgrounds, made for a conversation which later ended up into a conversation of stories, our stories to each other. I walked nearly 18km that day!! (eventually, I breached 30km a day mark).",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-2-from-phd-dreams-to-workplace-scenarios",
    "href": "MyBlog/BlogFull/index.html#day-2-from-phd-dreams-to-workplace-scenarios",
    "title": "Week of Intellectual Adventure",
    "section": "Day 2: From PhD Dreams to Workplace Scenarios",
    "text": "Day 2: From PhD Dreams to Workplace Scenarios\n\nMorning: PhD Tales That’ll Make You Laugh, Cry, and Think\nThe day started with a panel discussion featuring PhD candidates and recent graduates. I was particularly struck by Dr. Boyd Anderson from New Zealand. This guy went from tinkering with embedded systems to revolutionizing how we teach. Choosing the right supervisor and institution is like picking a life partner, it can make or break your academic journey. As one panelist put it, “The institution matters, but within it, your supervisor matters most.” (Note to self: choose wisely!)\n\n\nAfternoon: The Dark Side of Office Life\nProfessor Sandy Lim’s session was on Workplace Incivility. Using Structured Equation Modeling, she told us about how rudeness can wreck a workplace. 1. Workplace incivility is more common than bad coffee in offices, 91% of employees in Asia reported experiencing it. 2. Your boss being rude affects you more than your annoying cubicle mate when it comes to wanting to quit. 3. Rudeness can spread through an office faster than gossip if left unchecked.\nIn the evening, we visited robotics lab and nano-materials lab. I got to handle an electron microscope and also play with a robotic car. That day, I along with other vegeterian friends, visited Anand Bhawan (A2B) in Little India and had very home like dinner whereas our non-vegeterian friends enjoyed their meals in a restaurant inside NUS. We also went to clark quay for a walk.\n\n\n\n\n\n\nIn the nanomaterials lab\n\n\n\n\n\n\n\nRobotics lab\n\n\n\n\n\n\n\n\n\nIncubation center for converting ideas to products\n\n\n\n\n\n\n\nRobotics lab",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-3-from-nano-wonders-to-urban-jungles",
    "href": "MyBlog/BlogFull/index.html#day-3-from-nano-wonders-to-urban-jungles",
    "title": "Week of Intellectual Adventure",
    "section": "Day 3: From Nano-Wonders to Urban Jungles",
    "text": "Day 3: From Nano-Wonders to Urban Jungles\n\nMorning: The Invisible Tech Running Our World\nProfessor Aaron Thean’s lecture was on semiconductors He started with : “If you don’t have semiconductors, you don’t have technology.”\nI was listening very mindfully that I was able to note these points only.\n\nMoore’s Law isn’t just about cramming more transistors onto a chip; it’s a testament to human ingenuity and stubbornness.\nCurrent AI, with far fewer parameters than our brains, guzzles huge energy.\nHis research on rewritable chips, mimicking our brain’s plasticity, hinted at a future where our gadgets might think more like us.\n\nI thought about future devices that might blur the line between silicon and neurons.\n\n\nMorning: Harnessing Nature’s Power (Without Angering the Weather Gods)\nDr. Ghim Wei Ho’s session was on solar technologies for tapping into low-grade heat fluctuations.\n\n\n\nDr. Ghim Wei Ho addressing us\n\n\nShe stated that roof gardens can actually make solar panels work better and last longer? It’s like giving your panels a cool, green hat. Niladri, a undergrad from UMich asked a ton of questions in this session.\n\n\nAfternoon: Transforming Concrete Jungles into Digital Paradises\nErvine Lin’s lecture on digitizing landscapes. He challenged us to see beyond trees and buildings to the designed environments shaping our cities and lives.\n\n\n\nVery interesting session by Ervine Lin\n\n\nSome “cool” takeaways:\n\nSingapore’s urban planning philosophy evolved from “Garden City” to “City in Gardens” a subtle difference with mindset shift!\n3D scanning and reality capture tech are revolutionizing landscape architecture.\nUnderstanding topography is crucial in urban planning, especially for flood prevention.\n\n\n\n\nAsking question\n\n\nAfter the lectures, we hopped on NUS buses for a tour of Singapore’s highlights like Gardens by the Bay, cloud forest, flower dome(nature on steroids!), the iconic Merlion (half-lion, half-fish, 100% Instagram-worthy although I don’t use instagram yet), and some architectural marvels that made my neck hurt from looking up so much.\n\n\n\nVisit to Gardens by the bay\n\n\nThe evening was a culinary delight through Little India and Chinatown. As we stuffed ourselves with everything we liked, I loved how this tiny island nation had become a melting pot of cultures and cuisines.\n\n\n\n\n\n\nNear Merlion\n\n\n\n\n\n\n\nMarine Bay sands\n\n\n\n\n\n\n\n\n\nIn front of Merlion\n\n\n\n\n\n\n\nInside flower dome\n\n\n\n\n\n\n\n\n\nInside Marine Bay Sands Casino",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-4-health-wealth-and-the-art-of-not-boring-people-to-death",
    "href": "MyBlog/BlogFull/index.html#day-4-health-wealth-and-the-art-of-not-boring-people-to-death",
    "title": "Week of Intellectual Adventure",
    "section": "Day 4: Health, Wealth, and the Art of Not Boring People to Death",
    "text": "Day 4: Health, Wealth, and the Art of Not Boring People to Death\n\nMorning: AI: Your New Healthcare Bestie?\nProfessor Dean Ho’s session on N-of-1 Digital Health felt like peeping into the future of medicine. He explained us WisDM and CURATE platform, which uses AI to personalise treatments to the level of dosage quantity to individual patients based on their individual history.\n\nThe old “one-size-fits-all” approach to medicine is outdated.\nAI could be the key to cracking the code on personalized cancer treatments. Take that, cancer!\nIn healthcare data, “stories over snapshots”\n\n\n\n\nAfter talking with Prof. Ho\n\n\nProf. Ho also dove into the gamification of healthcare. Imagine earning points for taking your meds or hitting your step goal, being healthy becomes as addictive as your favorite mobile game! I bombarded him with questions after the session (sorry, not sorry!), and to his credit, he answered every single one with patience and enthusiasm. It’s not every day you get to pick the brain of someone like him!\n\n\nAfternoon: Economics in the Age of AI\nThe lecture on economic strategies for the Industrial revolution 5.0. The key takeaways were:\n\nWe need to rewire our brains when it comes to economic development. The old rules are irrelevant.\nIt’s time to question everything we thought we knew about economics.\nCrafting economic strategies that play nice with AI and automation is the new holy grail.\n\nAs I sat there, furiously scribbling notes, I was thinking: in this absolute new world, would my future job/business even exist? Or would I be competing with a hyper-intelligent AI? (Note to self: start being nicer to Claude, just in case.)\n\n\nAfternoon: The Art of Not Boring People (In Three Minutes or Less)\nThe 3-Minute Thesis (3MT) workshop by Rafi Rasheed and Jason Banta was a crash course in not putting people to sleep while talking about your research. Their golden rules:\n\nStart with a bang! Your opening line should grab attention faster than a cat video on social media.\nUse stories that people can relate to.\nProblem, solution, impact are the essentials of any good pitch. If you can’t explain it to your grandparentts, you’re doing it wrong.\n\nLater before evening Prof Johannes Widodo took us to architectural tour of academic building of department of architecture.\n\n\n\nDuring the tour with Prof Johannes\n\n\nWe spent the evening practicing our pitches, giving each other feedback, and trying not to panic at the thought presenting the poster in front of everyone. It was challenging, hilarious, and oddly exhilarating. Since next day was presentation day, we did not go for any outdoor adventures. We visited the merchandise store in NUS, bought few things, had some snacks and started working on presentations. Although our group did not make it to finals (we missed it with very small margin becasue of too much text on poster), we enjoyed the work we did and got to learn a lot from posters of other groups.\n\n\n\n\n\n\nPreparing Poster Presentation\n\n\n\n\n\n\n\nAfter all nighter work",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#day-5-battling-urban-heat-and-jamming-with-ai",
    "href": "MyBlog/BlogFull/index.html#day-5-battling-urban-heat-and-jamming-with-ai",
    "title": "Week of Intellectual Adventure",
    "section": "Day 5: Battling Urban Heat and Jamming with AI",
    "text": "Day 5: Battling Urban Heat and Jamming with AI\n\nMorning: Saving the World, One Cool Roof at a Time\nProfessor Wong Nyuk Hien’s lecture was on the CoolNUS Project on urban climate challenges. Cool facts (pun intended):\n\nTall buildings have a urban heat island effect.\nIn Singapore, 1°C in temperature can spike cooling demand by 7-10%.\nUrban greenery is more than just for beauty.\n\n\n\nAfternoon: When AI Drops the Beat\nThe program finale with Francis Tan exploring AI and Music was fun. We played around with tools like SUNO and UDIO. Authenticity, Connection and Catchiness are key ingredients even for AI generated music. As we experimented with AI-generated tunes, everyone debated about the future of creativity. Can a machine really capture the soul of music? Or are we on the brink of a new era of human-AI collaboration in art? The jury’s still out, but one thing’s for sure is that the content industry is in for one wild ride!\n\n\n\nPoster Presentation on final day",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/BlogFull/index.html#the-grand-finale-posters-presentations-and-goodbyes",
    "href": "MyBlog/BlogFull/index.html#the-grand-finale-posters-presentations-and-goodbyes",
    "title": "Week of Intellectual Adventure",
    "section": "The Grand Finale: Posters, Presentations, and Goodbyes",
    "text": "The Grand Finale: Posters, Presentations, and Goodbyes\nAs the program neared its end, our group pulled an all-nighter on second last night to put the finishing touches on our poster and presentation. Fueled by a mix of excitement, caffeine, and the fear of embarrassing ourselves in front of some of the brightest minds in academia, we worked through the night. It was exhausting, exhilarating, and oddly fun ; a perfect microcosm of the entire week.\n\n\n\n\n\n\nEvaluation of Poster Presentation\n\n\n\n\n\n\n\nAfter the last session greeting to everyone and wishing them good luck!\n\n\n\n\n\n\n\n\n\nWith Dean Jessica Pan and Prof Kah-Hin Chai\n\n\n\n\n\n\n\nSentosa Islands\n\n\n\n\n\nThe next day, everyone presented their projects, I loved the creativity and innovation on display. From AI-powered solutions for sustainable agriculture to novel approaches in quantum computing, the range of ideas was staggering. As the program officially came to a close, a bittersweet feeling settled over us all. We’d come as strangers from different universities but were bonded so close during such short period. That evening we went to an extra mile to squeeze out joy from every moment. We visited so many tourist attractions. We also went to Casino as entry was free for foreginers (just to see it from inside!). This is the day which made me breach 30km mark. Also, after coming back to U Town around 2AM, we wandered inside U Town till 4:30 AM. Next day, half of us came together to the aiport, bid farewell to each other. Everyone became so emotional at this point that some among us were actually sobbing. I said goodbye to everyone. But the jounery together was not over, we will still spend some time together remotely, to prepare our 3MTs and will present them to the jury.\nTo anyone considering applying to the NUS Young Fellowship Program : do it! It’s an unparalleled opportunity to immerse yourself in NUS research environment, engage with leading academics, and connect with exceptional peers from around the globe.\n\n\n\nRoaming around airport\n\n\nUpdate : 3MT is done. It was really really good to listen to knowledge-dense pitches.\n\n\n\nLeaving for the airport together\n\n\nAfter farewell, I roamed around the airport for an hour or two with few friends who were still to board their flights. Then I sat at starbucks for 2 hours since my flight was delayed beause of bad weather. It was here that I finetuned my digital journal. I boarded my flight to Mumbai. I thought about this week and interactions and as I started seeing Mumbai from Window, I played Swades (Yeh Jo Des Hei Tera) song. When I deboarded, I was overwhelmed with emotions and filled eyes. The NUS Young Fellowship Program showed me cutting edge of research and innovation and a journey that can be taken by cities and even entire nation together.\n\n\n\nSwastika, Yi, Lili (Luo Xinming), Rena and Me\n\n\nNote : My notes were very scattered, I have taken help from Claude Sonnet 3.5 on 30 Sept 2024 to make them coherent.",
    "crumbs": [
      "Madhav_Speaks",
      "Week of Intellectual Adventure"
    ]
  },
  {
    "objectID": "MyBlog/Talk-by-Sayak/index.html",
    "href": "MyBlog/Talk-by-Sayak/index.html",
    "title": "Sayak Paul from Hugging Face",
    "section": "",
    "text": "What Sayak talked about??\n\nDiffusers library is his recent work. He loves wasting GPU hours and he thinks Vision Transformers are very Robust Learners. Take everything with grain of salt since he is highly optiniated. When he learned the stuff, No Fast.ai then, Andrew Ng course was start. How school maths meant the things in ML was initial hook for him. He didn’t dabble in other subjects.\nAndrej Karpathy Andrej Karpathy says:- Spend 10000 hours quote, He wasn’t dubious in his choice, he was 100 percent sure that he wanted to just go into ML. Surface level knowledge just leads to suffering in which case if you are even able to build models, Models might fail silently and you won’t be able to understand why if you are not learning in deep. Spend time with subject you like, Maths is necessary but not university level, Calculus Linear Alg Prob, CS231n, Andrew NG, FastAI, Deep Learning Book Ean Goodfellow, Deep Learning with python, Grokking Machine Learning.\nImplementation is crux of most of the things, (Which paper did he implement?) Implementing the papers that align/ some part of those paper, gives a lot of learning, Balancing off understanding, intution, and how things work in practice, grokking/cramming alone never help, deep learning is often alchemy than science, Curious mind is necessary.\nHe didn’t start with a ML heavy job but it was clear to him totally that he will eventually. Having public record of our understanding is important so he started to write technical blogs, one way to write blog is write self reference, It was public record that got attention of employees (Where did he write ?) Forced himself on engineering side of ML, like deploying the mode, Docker/Kubernetes, Mentor at GSoC at TensorFlow, Publishing the tier-1 paper is really crazy, Got into hugging face because of public work, Open-Source,\nHe was not interested more education, He didn’t want to be research scientist, non-trivial public portfolio is really a thing that always helps, Matured Work Experience &gt;&gt;&gt; academic affliaition, Someone who have tensorflow contributions will always get more pref over stanford grad, Difficult Stuff =&gt; learning opportunities, You will always feel overwhelmed but carry on with those moments, Do not settle, Don’t be complecent, Sharpening the Sword - Have the same mindset as the one you had at the beginning. Reach out to other collaborators,\nA very nice doc of faqs he compiled, can be found at bit.ly/sayak-faqs\nQuestions :\n\nHe was working with Roberta and Bert sized model, He got comfortable then with codebase so he contributed to open issues, Connext took it him inside and after 6 months they reached out to him&gt;\nDoing things for open source is crux of HF\nYou have to build your case for yourself, make something that you can show to tell that I have expertise, Interview is not the way to go.\nOn his personal site he used to post blogs, it helps your own understanding and articulate it properly.\nInception you need to go deeper, regularization, SAM, 2018, High threshold for patience,\nProject should have enough thickness to show your expertize,\nEx: Finetuning is really easy but you can show maturity if you can tell why did you finetune&gt; Like prompt engineering didn’t work, RAG didn’t suffice then You curated custom dataset, then finetuned/\nKaggling is good, but focus on learning aspect rather than winning aspects.\nHow to Flash-Attention 2, Why not in C? How to find papers to be implemented. Youwill learn with time . During intital time randomly kicling a paper alinging with your field.\n\nNo cracked failure, its a Back and forth process, Focus on representativeness, no corruptions, check and check again. Tuning CV models with task rewards, RAG is beneficial for factuality, Do a Blog on YOLO, D2L.ai. Detection is far from saturation, contribute detection model to transformer.\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“Sayak Paul from Hugging Face.” 2024. April 11, 2024. https://madhaviit.github.io/MyBlog/Talk-by-Sayak/.",
    "crumbs": [
      "Madhav_Speaks",
      "Sayak Paul from Hugging Face"
    ]
  },
  {
    "objectID": "MyBlog/index.html",
    "href": "MyBlog/index.html",
    "title": "Madhav_Speaks",
    "section": "",
    "text": "India@100\n\n\n\nOpinion\n\nThought\n\n\n\n\n\n\nJan 1, 2026\n\n\n\n\n\n\n\nBroken Silence\n\n\n\nMicrofiction\n\n\n\n\n\n\nFeb 27, 2025\n\n\n\n\n\n\n\nWeek of Intellectual Adventure\n\n\n\nExperience\n\nGenAI\n\nResearch\n\n\n\n\n\n\nOct 1, 2024\n\n\n\n\n\n\n\nHow to CS?\n\n\n\nOpinion\n\nThought\n\n\n\n\n\n\nJul 25, 2024\n\n\n\n\n\n\n\nGuardrails\n\n\n\nArtificial Intelligence\n\nSocial Networks\n\nPrivacy\n\nGenAI\n\n\n\n\n\n\nApr 22, 2024\n\n\n\n\n\n\n\nSayak Paul from Hugging Face\n\n\n\nTalk\n\nML\n\nHugging Face\n\n\n\n\n\n\nApr 11, 2024\n\n\n\n\n\n\n\nPreparation for Internships\n\n\n\nTalk\n\nSeniors\n\nResume\n\n\n\n\n\n\nMay 11, 2023\n\n\n\n\n\n\n\nIndia@75\n\n\n\nOpinion\n\nThought\n\n\n\n\n\n\nAug 15, 2022\n\n\n\n\n\n\n\nवारकरी संप्रदाय:एक समृद्ध जीवनधारा\n\n\n\nOpinion\n\nThought\n\n\n\n\n\n\nJan 7, 2019\n\n\n\n\n\nNo matching items\nReuseCC BY-NC-ND 4.0",
    "crumbs": [
      "Home",
      "Blog"
    ]
  },
  {
    "objectID": "MyBlog/Varkari/index.html",
    "href": "MyBlog/Varkari/index.html",
    "title": "वारकरी संप्रदाय:एक समृद्ध जीवनधारा",
    "section": "",
    "text": "भारतीय संस्कृति के उद्यान में अनेक पंथ और संप्रदाय फूलों की तरह खिले हुए हैं। इन मनोहारी पुष्पों में से एक है वारकरी संप्रदाय, जिसकी सुगंध बारहवीं शताब्दी से दक्कन की धरती को महका रही है। यह संप्रदाय भगवान विठ्ठल की भक्ति का अमृत पान कराता है, जिनकी दिव्य मूर्ति पंढरपुर में विराजमान है। “वारकरी” शब्द स्वयं में एक कहानी छिपाए है - ‘वारी करने वाला’। वारी एक ऐसी तीर्थयात्रा है जो विभिन्न गाँवों से प्रारंभ होकर पंढरपुर की पावन भूमि पर समाप्त होती है। यह यात्रा केवल पैरों की नहीं, बल्कि आत्मा की भी यात्रा है। इस आध्यात्मिक उद्यान के मुख्य माली रहे हैं - ज्ञानेश्वर, तुकाराम, नामदेव और एकनाथ जैसे महान संत। इन दिव्य आत्माओं ने भक्ति के बीज बोए, जिन्होंने सत्य और निष्ठा की जड़ों को गहरा किया। वारकरी इसी पथ पर चलते हुए अपने जीवन रूपी वृक्ष को पल्लवित करते हैं।\nइस संप्रदाय की विशेषता है इसकी समानता की भावना। यहाँ छोटे-बड़े का भेद मिट जाता है, जैसे समुद्र में सभी नदियाँ एकाकार हो जाती हैं। यहाँ मान्यता है कि ईश्वर के नाम के जप से ही पुण्य का फल प्राप्त होता है। वारकरी परंपरा की समृद्धि इसके साहित्य, सिद्धांतों और इतिहास में निहित है, जैसे किसी कलश में अमृत भरा हो। साहित्य में सदियों की ऐसी कविताओं का भंडार है, जो सरल और मधुर भाषा में वह ज्ञान संप्रेषित कर पाती हैं, जिसे सैकड़ों महाकाव्य भी शायद नहीं व्यक्त कर पाते। उन कविताओं में जीवन का सत्य और अनुभवों की गहराई इस सहजता से रची है कि वे मन को छू जाती हैं, जैसे शीतल हवा आत्मा को शांति प्रदान करती है। यह संप्रदाय आत्मा और ईश्वर के अस्तित्व को स्वीकार करता है, मानो दो चिरंतन सत्य हों। इस आध्यात्मिक इमारत के निर्माण में अनेक संतों ने अपना योगदान दिया है। इसका सुंदर चित्रण इन पंक्तियों में किया गया है:\n\n“भागवतधर्मे कृपा झाली इमारत फळा आली ।\nज्ञानदेवे रचिला पाया उभारिले देवालया ।\nनामा तयाचा किंकर तेणे केला हा विस्तार ।\nजनार्दन एकनाथ खांब दिला भागवत ।\nतुका झालासे कळस भजन करा सावकाश ।\nबहिणी फडकती ध्वजा निरूपण आले ओजा ।”\n\nइन पंक्तियों में बताया गया है कि भागवत धर्म की कृपा से इस संप्रदाय की इमारत खड़ी हुई। ज्ञानदेव ने इसकी नींव रखी, नामदेव ने इसका विस्तार किया, एकनाथ ने इसे मजबूती प्रदान की, तुकाराम इसके शिखर बने, और बहिणाबाई ने इस पर ध्वज फहराया। संत ज्ञानेश्वर ने मानवता के महत्व को पसायदान के इन अमर शब्दों में व्यक्त किया:\n\n“जे खळांची व्यंकटी सांडो । तया सत्कर्मी रती वाढो ।\nभूतां परस्परे जडो । मैत्र जीवांचे ।।”\n\nअर्थात, दुष्टों की दुष्टता समाप्त हो और सत्कर्म में रुचि बढ़े, और सभी प्राणियों के बीच मैत्री का भाव जागे। वारकरी परंपरा में एक प्रसिद्ध दंतकथा है जिसमें कहा जाता है कि भगवान विठ्ठल भक्त पुंडलिक के लिए ईंट पर खड़े हो गए। यह कथा माता-पिता की सेवा के महत्व को उजागर करती है, मानो यह कह रही हो कि मनुष्य की सेवा ही ईश्वर की सेवा है। प्रकृति के प्रति प्रेम और सम्मान की भावना इन पंक्तियों में झलकती है:\n\n“वृक्षवल्ली आम्हां सोयरे वनचरे । पक्षीही सुस्वरे आळविती।”\n\nइसका अर्थ है कि वृक्ष और लताएँ हमारे रिश्तेदार हैं, वन के जीव हमारे मित्र हैं, और पक्षी भी मधुर स्वर में इसी बात को गाते हैं। यह पंक्ति पर्यावरण संतुलन के महत्व को रेखांकित करती है। गुरु का महत्व इस संप्रदाय में सर्वोपरि है। संत ज्ञानेश्वर के शब्दों में:\n\n“भावेविण देव न कळे निःसंदेह गुरूविण अनुभव कैसा कळे।”\n\nअर्थात, बिना भाव के ईश्वर का ज्ञान नहीं होता, और बिना गुरु के अनुभव कैसे प्राप्त हो सकता है। जीवन की पवित्रता पर बल देते हुए एक अभंग कहता है:\n\n“नाही निर्मळ जीवन काय करील साबण”\n\nइसका अर्थ है कि यदि जीवन निर्मल (पवित्र) नहीं है, तो साबुन क्या करेगा। यह पंक्ति हमें याद दिलाती है कि बाहरी शुद्धता से अधिक महत्वपूर्ण है आंतरिक शुद्धता।\nवारकरी संप्रदाय की शिक्षाएं सरल और व्यावहारिक हैं, जैसे किसी माँ का स्नेहिल मार्गदर्शन। ये शिक्षाएँ कहती हैं - अपने कर्तव्यों का पालन करते हुए भगवान को न भूलें। प्रातःकाल जल्दी उठें, स्नान के बाद गोपीचंदन का तिलक लगाएं, नियमित रूप से हरिपाठ का पाठ करें, संतों के ग्रंथों का अध्ययन करें, भगवान के दर्शन करें, पंढरपुर की यात्रा करें। सात्विक भोजन करें, हिंसा न करें, मांसाहार न करें। अच्छा आचरण रखें, परोपकारी बनें, परमार्थ की साधना करें। जीवन के बंधनों से धीरे-धीरे मुक्त होकर ईश्वर के साथ एकरूप होने का प्रयास करें।\nवारकरी संप्रदाय की एक उल्लेखनीय विशेषता यह है कि इसमें भागवतधर्म का अनुयायी बनना जन्मजात अधिकार नहीं है, अपितु यह व्यक्ति की स्वेच्छा पर निर्भर करता है। यह दृष्टिकोण इस संप्रदाय को अधिक समावेशी और उदार बनाता है, जहाँ किसी भी पृष्ठभूमि का व्यक्ति अपनी आध्यात्मिक यात्रा शुरू कर सकता है।\nइस संप्रदाय के सार को इन पंक्तियों में व्यक्त किया जा सकता है:\n\n“अध्यात्माने तृप्त जीवन । नाही कुठली खंत\nकरील मानवतेचे रक्षण । हा वारकरीपंथ ।।”\n\nअर्थात, वारकरी संप्रदाय एक ऐसा मार्ग है जो अध्यात्म से जीवन को तृप्त करता है और जो मानवता की रक्षा करता है। इस प्रकार, वारकरी संप्रदाय केवल एक धार्मिक आंदोलन नहीं, बल्कि एक समृद्ध जीवनधारा है जो सदियों से महाराष्ट्र की संस्कृति और समाज को प्रभावित कर रही है। यह संप्रदाय आध्यात्मिकता, मानवता और सामाजिक समरसता का एक अनूठा संगम है, जो आज के समय में भी उतना ही प्रासंगिक है जितना कि सदियों पहले था। वारकरी संप्रदाय की समृद्धि उसके अध्यात्म में है, और उसका अध्यात्म उसकी समृद्धि में। यह एक ऐसा पथ है जो अपने अनुयायियों के वैचारिक स्तर को उन्नत करने की अद्भुत क्षमता रखता है। वारकरी संप्रदाय एक ऐसा आध्यात्मिक पथ है जो हमें सिखाता है कि कैसे जीवन के प्रत्येक क्षण को एक तीर्थयात्रा में बदला जा सकता है। यह हमें याद दिलाता है कि सच्चा मंदिर हमारे हृदय में है, और सच्ची पूजा मानवता की सेवा में। यह संप्रदाय हमें आमंत्रित करता है कि हम भी इस आध्यात्मिक यात्रा में शामिल हों, जहाँ प्रत्येक कदम हमें ईश्वर के और करीब ले जाता है।\n\n\nThis article was originally written in Marathi without help from any GenAI models. Claude is used for converting it to Hindi.\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“&lt;Span Class=‘hindi-Kalam’&gt;वारकरी संप्रदाय:एक\nसमृद्ध जीवनधारा&lt;/Span&gt;.” 2019. January 7, 2019. https://madhaviit.github.io/MyBlog/Varkari/.",
    "crumbs": [
      "Madhav_Speaks",
      "<span class=\"hindi-kalam\">वारकरी संप्रदाय:एक समृद्ध जीवनधारा</span>"
    ]
  },
  {
    "objectID": "MyBlog/India@100/index.html",
    "href": "MyBlog/India@100/index.html",
    "title": "India@100",
    "section": "",
    "text": "We have already burned through a quarter of this century. India stood at its start with a promise to own it. I remember when we were all charged up as kids that she would be a superpower by 2020, a goal set by Dr. APJ Abdul Kalam. It was ambitious and inspiring, and it gave us something to strive for collectively as a nation. We indeed got many things right and must be proud of those achievements, but we also missed many opportunities. Now, we cannot rest on partial victories. The mission is Viksit Bharat @2047. We must at least become a upper-middle-income nation, but that is not the end goal. The life of citizens must be the north star. I propose a framework with a three-prong heuristic through which most policy and directives can take cue. First, prevent unnatural death. Second, increase life expectancy. Third, enrich that long and healthy life.\n\nThe First Prong: Prevent Unnatural Death\nThe most immediate duty of governance is the preservation of life. We must aggressively arrest any and all deaths. This encompasses everything from maternal mortality to road accidents, deaths from malnutrition and preventable diseases, from farmer suicides and industrial accidents. Every single unnatural death is in some way a failure of governance.\nWhen accidents claim lives that could have been saved, or disasters strike without adequate response systems, we fail at the basics (We need automated SOS alarm systems at scale that can respond to emergencies swiftly and effectively). We extend this concept to economic survival as well. The surge in regional inequality that has forced millions to migrate must be reversed through townification. More of our population has to shift out from agriculture, and we must modernize our farming with machines and automation to reduce the burden on farmers struggling under economic distress.\nNational security is equally vital to preventing the loss of life and sovereignty. Digging a well when thirsty cannot be the national strategy. Two things India heavily imports are defense equipment and energy. We cannot afford to be highly dependent on others for our security or our energy needs. We must be competent in our strengths for a free-market world, and simultaneously, we must build self-reliance in every sector. A bit far fetched under this prong, we also need to ramp up space exploration.\n\n\nThe Second Prong: Extending Life Span\nOnce survival is secured, we must focus on the span of existence. Increasing life expectancy requires a holistic approach to health and environment. It begins before birth. For a nation with demography as one of its strongest assets, every child born must have access to proper nutrition in the childhood and befor birth as well. This is when the foundation for a healthy, productive life is laid.\nThe reach of healthcare services must expand through targeted interventions. We need more primary health centers, more trained medical professionals in rural areas, and better health insurance coverage. Preventive healthcare should be prioritized over curative healthcare.\nHowever, medicine alone cannot guarantee a long life if our environment is toxic. Our cities need to be livable, breathable, and beautiful. No more should our cities be designed for cars. They should be designed for people, for walking, cycling, and using public transit, with parks, lush greenery, lakes, and playgrounds. Clean air and green spaces directly impact how long and how well we live. The transition to renewable or clean energy is also about securing our future in a world increasingly constrained by climate change.\nWe must also guard against the forces that shorten life from within. Addictions of different kinds are giving today’s youth a slow death. Whether substance abuse, screen addiction, or the more subtle addictions to instant gratification, we are losing a generation to these forces.\n\n\nThe Third Prong: Enriching the Life\nYet, what is the point of living long if that life lacks meaning, opportunity, beauty, and dignity? Education, cultural enrichment, economic opportunity, social harmony, and environmental aesthetics make life worth living.\nThis enrichment begins with education. The promise of education as the great equalizer remained largely unfulfilled. We need great public schooling, and institutions like Navodaya need big upgrades. The school curriculum needs to be more explorative but also harder. We can make it accessible without making it easy or dumbing it down. Higher education needs an absolute revolution. The way to improve education in India is to put the best national and international minds as faculties. They won’t do it for living, they will do it because they care. Teaching in native languages and Ancient Indian Knowledge are good initiatives but are least impactful when fundamentals like basic infrastructure, quality teaching, student-teacher ratios, and learning outcomes are not fixed. Primary and secondary education must sensitise young adults on essential topics. We must arrest vulgarity in society spreading through mass media. Scientific temperament needs to reclaim front seat again.\nWe need to build an ecosystem where talent thrives. We need cities or even remote locations where foreign top talent can come, stay, and work. Brain drain has worsened because our best minds perceive limited pathways at home. When our environment fails to retain talent or attract global expertise, we compromise our ability to extend both the length and quality of life for all citizens.\nProsperity is the engine of a rich life (Aim is to have at least three spots out of the top 10 valued companies in the world). The means to prosperity is moving low-skilled labor to high-skilled jobs, which requires rethinking vocational training and labor laws. We must look beyond IT and services to green technology, biotechnology, and advanced manufacturing. Artificial Intelligence and robotics hold immense promise. We must aggressively push for space exploration. Furthermore, as a key data source in the modern world, we should monetize our data or control it with strong laws. Our demographic dividend can be our data dividend.\nCultural and aesthetic richness is equally important. Indian tourism is woefully underexploited. We have culture, history, and diversity that almost no country can match. Promoting tourism and our soft power through cinema, music, food, spirituality can open doors that hard power cannot. At the same time, we have to start travelling more internationally and exposing ourselves more to other cultures. The world is full of beauty and we should embrace it in our lifetimes. We want efficiency, but we also crave beauty. Jumbled electric wires are aesthetically ugly. Our cities need to be pleasing to the eye, as aesthetics impact our mental well-being.\nFinally, a rich life requires a just society. Making sure that half of India gets representation in parliament is important, the number of women MPs has to increase rapidly. We need to strive for a society where individuals vote as citizens first, as Indians, not as members of narrower identities. The communalism offered to aspiring youthful India must be ended. Civil servants should have the courage and integrity to suggest what is best for the people. We need technologies like blockchain and ZKPs to make governance more efficient, transparent, and accountable.\n\n\nCompounding\nCompounding has immense power. To allow compounding of national progress, policies by and large should outlive the governments. Dialogue and broad consensus, decentralisation of power with clearer responsibilities helps ensure compounding continues by avoiding a single point of failure. Also, to not discourage thriving states, we need a better fund distribution algorithm which rewards good governance but also helps lagging states to catch up in other than financial means. Freebies, especially on backdrop of elections are extremely harmful as they take away the funds otherwise available for improving economic health. Radical technological innovation catalyses compounding manifold and IP protection, easier access to capital, no unnecessary bureaucratic red tape, culture of celebrating risk-taking and forgiving failures will help this happen.\n\n\nThe Pledge of 2047\nWe should ask, as a net result, does something help improve life? If the answer to all three prongs is no, we should reconsider. We don’t need premature juvenile pompous declarations of victory, What India truly needs is a silent redemption that is patient, persistent work where all arms of democracy fulfill their duties diligently. We need scientists in their labs, teachers in their classrooms, doctors in their clinics, engineers at their drawing boards, farmers in their fields, civil servants at their desks, and everyone and all working towards making India live better. The clock is ticking, and our destination 2047 is not as far away as it seems. Let us make sure we enjoy the journey. This is a road we must take because India deserves better, and her children deserve better. Humanity deserves better. Let us Make India Great Again, a country that is prosperous, just, free, and beautiful. By the end of 2047, when India celebrates a century of independence, we should be able to say that we indeed redeemed the pledge our forefathers made. The ambition of the greatest beings of our generation now, must be to wipe every tear from every eye and then bring a smile to every lip. That is the north star. That is the ultimate filter.\n\n\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“India@100.” 2026. January 1, 2026. https://madhaviit.github.io/MyBlog/India@100/.",
    "crumbs": [
      "Madhav_Speaks",
      "India@100"
    ]
  },
  {
    "objectID": "Poems/Marathi/विटी.html",
    "href": "Poems/Marathi/विटी.html",
    "title": "विटी",
    "section": "",
    "text": "एकदा मी बसलो होतो पिंपळाच्या झाडाखाली\nतेवढ्यात पोरे गावची माझ्याभोवती गोळा झाली\n\nमला म्हणाली दादा विटी आमची झाडावर अडकली\nविजा गेलाय काढायला त्याला एक फांदी थडकली\n\nमी म्हणालो बोला आहे काय रे इच्छा तुमची\nपोरे म्हणाली दादा द्या विटी हो काढून आमची\n\nठीक म्हणून पॅडल मारून मी गेलो झाडापाशी\nतेव्हा मला दिसली तिथे एक मधुमाशी\n\nमुलांनी का बोलावलं तेव्हा मला समजलं\nआपलं काही खरं नाही तेव्हाच मला उमजलं\n\nदगड अडकवून गलोलीला विटीला मी मारला\nतेव्हा मात्र आमचा सगळा घोळपा गंजीत लपला\n\nनशीब आमचं पोळ्याला दगड लागला नाही\nविटी मिळाली डंक बरा त्यांचा मिळाला नाही\n\nएक तात्पर्य सांगतो वेळ काळ बघून घ्यावा\nपरिस्थिती जाणून मगच होकार तो द्यावा\n\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">विटी</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/विशाद.html",
    "href": "Poems/Marathi/विशाद.html",
    "title": "विशाद",
    "section": "",
    "text": "भोवताली घडणाऱ्या आणि रोजच्या वाटणाऱ्या घटना, भोवतालच विश्व,\nयावर विश्वासून, जीवनाच, आकलन आणि आरेखन करताना,\nएक दिवस, सबंध भोवताल तुमच्यावर आक्रमून येतो,\nज्या वाटखुणांवर तुम्ही विश्वास टाकला, त्या एका दिवशी वादळामध्ये विरून जातात,\nपाऊलखुणांची पायवाट, त्या दिवशी एकाएकी अदृश्य होते,\nडोळे भरून जातात, गळा दाटून येतो,\nकोणी माझे नाही, मी कोणाचा नाही, विशाद पेटून येतो\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">विशाद</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/धर्मवीर.html",
    "href": "Poems/Marathi/धर्मवीर.html",
    "title": "धर्मवीर",
    "section": "",
    "text": "धर्मासाठी जगला\nआणि धर्मासाठी झिजला\nशिवरायांचा पुत्रचि तो\nएक राजा होऊन गेला\nशिवरायांसोबत त्यांनी तलवार फिरवली\nजिजाऊंसोबत त्यांनी अक्षरे गिरवली\nहिंदवी स्वराज्य रक्षण करण्या\nएक विद्वान जन्मला\nमुघलांना जेरीस आणले त्यांच्या मुलखात जाऊन\nदोन महिने रूप बदलले मथुरेत राहून\nआजीच्या देखरेखी खाली\nएक मुत्सद्दी वाढला\nऔरंगजेब चालून आला त्याला ज्यांनी चिरडला\nकेवळ स्वकियांच्या गद्दारीने अटकेत जो गेला\nमर्द मराठी मातीचा\nजणू गंधच हरपला\nइस्लाम स्वीकार सरदार करतो औरंगजेब बोलला\nसह्याद्रीचा वाघ तो त्या अतिशहाण्यावर गरजला\nडोळे काढले जीभ कापली उपाशी मारले\nत्वचा सोलली तरी मस्तक ताठ ठेवले\nदृढ मनाने धर्माभिमानी\nइतिहास रचला\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">धर्मवीर</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/महाराष्ट्रभूमी.html",
    "href": "Poems/Marathi/महाराष्ट्रभूमी.html",
    "title": "महाराष्ट्रभूमी",
    "section": "",
    "text": "माझी महाराष्ट्र भूमी आहे थोरचि अपार\nशौर्य वीरांचे घर समृद्धीचे खुले द्वार\nलढले येथे छत्रपती रक्ते वीरांची सांडली\nमाझ्या महाराष्ट्र भूमीत एकता नांदली\nमाझी महाराष्ट्र भूमी सदा दुर्जनांशी झुंजली\nशाहिरांचे पोवाडे नि कविता येथे गुंजली\nमराठी भाषेवर केले सर्व रयतेने प्रेम\nअतिथि देवो भव हाचि आमुचा नेम\nकांदा भाकर पुरणपोळी तोंडी वाळके नि मुळा\nजुनी संस्कृती आमुची अहो आहे नाद खुळा\nराष्ट्राच्या विकासासाठी रयत तयार मरणाला\nमाझे अनंत प्रणाम मायभूमीच्या चरणाला\nआहेत ध्यास नवे आहे संकल्प नवा\nश्वासाश्वासामधून वाहे नाविन्याची हवा\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">महाराष्ट्रभूमी</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/गणेशोत्सव.html",
    "href": "Poems/Marathi/गणेशोत्सव.html",
    "title": "गणेशोत्सव",
    "section": "",
    "text": "टिळकांनी सुरू केले जयाला तो खरा गणेशोत्सव चाललाय लयाला\nढोल ताशे गायब झाले तेथे डॉल्बी साहेब आले\nमुल्ये गेली बाराच्या भावात 100 मंडळी एका गावात\nविधायक कामे मागे पडली देणगीच्या जीवावर पार्टी झडली\nएकतेचे धोरण गेले हरपुण नेते गेले तूप वरपून\nउत्सवाची सर्वांनी पहावी स्थिती गणेशाची आराधना होतेच किती\nनतमस्तक व्हा एकदंताच्या चरणी करा प्रार्थना भक्ती ठेवा मनी\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">गणेशोत्सव</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/आषाढ.html",
    "href": "Poems/Marathi/आषाढ.html",
    "title": "आषाढ",
    "section": "",
    "text": "हा ऊन पावसाचा खेळ\nचहूकडे पसरली हिरवळ\nफुलून गेले बोडके माळ\nपेरले गेले ज्वारी नि साळ\nआषाढाचा खेळ नवा\nवासुदेव वाजवी पवा\nचालती वाजवत टाळ\nवारकरी लेवूनी तुळशीची माळ\nहे पिवळे पिवळे ऊन कोवळे\nहे ढग आता काळे काळे\nतेव्हा सदरा कधी न वाळे\nऊन पावसाचा खेळच चाले\nअचानक आला हलकासा पाऊस\nऊन पडले कोवळे तांबूस\nतेव्हा आई मला ओरडे\nपहिले कर तुझे अंग कोरडे\nझाली पृथ्वी हिरवीगार\nवाहू लागलं वारं गार\nआता हिवाळ्याची ओढ असे\nलिहावे तेवढे थोडं असे\n— अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">आषाढ</span>"
    ]
  },
  {
    "objectID": "Poems/English/Bapu.html",
    "href": "Poems/English/Bapu.html",
    "title": " Bapu ",
    "section": "",
    "text": "Though there is dark but as you are lying in our minds the bark of dogs will show us the way\nThough there is extremism but as you are lying in our minds the shots of gun will teach us tolerance\nThough many in Indian villages sleep without dinner but as you are lying in our mind we will be winner against hunger\nThough we have reached the Mars the world is still under the threat of wars but as you are lying in our minds the kind and sensible people will come together\nBapu though many of us are blind but as you are lying in our mind the kind god will blow the wind and we will find our way\n-Avikskhit Nachiketa",
    "crumbs": [
      "Home",
      "Poems",
      "English",
      "<span onmousedown='return false;' onselectstart=\"return false;\" oncopy=\"return false;\" onpaste=\"return false;\" oncut=\"return false;\"> Bapu </span>"
    ]
  },
  {
    "objectID": "Poems/index.html",
    "href": "Poems/index.html",
    "title": "Poems",
    "section": "",
    "text": "विशाद\n\n\n\nMarathi\n\n\n\n\n\n\nFeb 2025\n\n\n\n\n\n\n\nकर्मर्तव्य\n\n\n\nMarathi\n\n\n\n\n\n\nAug 2024\n\n\n\n\n\n\n\nपरिस्थिती\n\n\n\nHindi\n\n\n\n\n\n\nMay 2024\n\n\n\n\n\n\n\nमोहन\n\n\n\nHindi\n\n\n\n\n\n\nJan 2024\n\n\n\n\n\n\n\nऔकात\n\n\n\nHindi\n\n\n\n\n\n\nDec 2023\n\n\n\n\n\n\n\nअसफलता\n\n\n\nHindi\n\n\n\n\n\n\nSep 2023\n\n\n\n\n\n\n\n Bapu \n\n\n\nEnglish\n\n\n\n\n\n\nOct 2018\n\n\n\n\n\n\n\nविटी\n\n\n\nMarathi\n\n\n\n\n\n\nJan 2018\n\n\n\n\n\n\n\nकविता खरंच काय असते\n\n\n\nMarathi\n\n\n\n\n\n\nSep 2017\n\n\n\n\n\n\n\nआषाढ\n\n\n\nMarathi\n\n\n\n\n\n\nAug 2017\n\n\n\n\n\n\n\nशेतकऱ्याचे मनोगत\n\n\n\nMarathi\n\n\n\n\n\n\nMay 2017\n\n\n\n\n\n\n\nशिवबा\n\n\n\nMarathi\n\n\n\n\n\n\nApr 2017\n\n\n\n\n\n\n\nधर्मवीर\n\n\n\nMarathi\n\n\n\n\n\n\nDec 2016\n\n\n\n\n\n\n\nमी शेतकरी\n\n\n\nMarathi\n\n\n\n\n\n\nMay 2016\n\n\n\n\n\n\n\nदिवाळी\n\n\n\nMarathi\n\n\n\n\n\n\nDec 2015\n\n\n\n\n\n\n\nमहाराष्ट्रभूमी\n\n\n\nMarathi\n\n\n\n\n\n\nDec 2015\n\n\n\n\n\n\n\nगणेशोत्सव\n\n\n\nMarathi\n\n\n\n\n\n\nSep 2015\n\n\n\n\n\nNo matching items",
    "crumbs": [
      "Home",
      "Poems"
    ]
  },
  {
    "objectID": "Poems/Hindi/मोहन.html",
    "href": "Poems/Hindi/मोहन.html",
    "title": "मोहन",
    "section": "",
    "text": "वर्षों तक जग में घूम-घूम\nवियोग विघ्नों को चूम चूम\nसह अब शब्द अपमान प्रखर\nमोहन आया कुछ और निखर \nसौहार्द की राह बताने को\nसबको एक साथ लाने को\nअंग्रेजों को समझाने को\nऔर भारत से हटवाने को\nगांधीजी वापस भारत आए\nसत्याग्रह की महारथ लाए\n\nसंभाले संग्राम की डोर चले\nपकड़े अहिंसा की छोर चले\nतिलक गोखले साथ थे\nसर पर भारत मां के हाथ थे \nअंग्रेज चाहे नील उगाना\nऔर असंगत कर भराना\nचंपारण में पहुंचे गांधी\nउठाव की जो लाई आंधी\nअंग्रेजों को रुकना पड़ा\nनिवासियों सामने झुकना पड़ा\n\nखिलाफत की रमजान मनाई\nरामराज्य की याद दिलाई\nडायर ने जब जीवन जलाया\nगांधी ने असहयोग चलाया\nअपनों को भी बहुत सुनाया\nस्वराज का सपना आगे बढ़ाया\nउपोषण किया स्वदेशी चलाया\nप्रेम एवं सत्य का रास्ता दिखाया \nगांधी ने हुंकार किया\nसविनय अवज्ञा का प्रचार किया\nदांडी मार्च पर चले गांधी\nसंपूर्ण स्वराज बोले गांधी\n\nगोलमेज का मेल चला\nतोड़ो हुकूमत करो का खेल चला\nजेल में उपवास चलाया\nमंदिरों में छुआछूत जलाया\n\nछोड़ो भारत की साख दिखाई\nकरो या मरो की आंख दिखाई\nअंग्रेजों और अब सह ना सके\nअखंड भारत की राह ले ना सके\nसांप्रदायिकता की आग लगा के\nभाई को भाई से भीड़ा के\nकिसी का सपना साकार हुआ\nगांधी का मन निराकार हुआ\n\nसब कुछ छोड़ने चले गांधी\nसमाज को जोड़ने चले गांधी\nकट्टर पंथियों को अब जाग आई\nगांधी के निषेध की मांग आई\nहितवचन इन्हें न समझ आया\nराम के उपासक को रावण बताया\nगांधी के विचार को मारने चले\nजो असाध्य था साधने चले\nयुवा हत्यारे 78 साल के बूढ़े से डर गए\nराम का नाम लेते-लेते गांधी मारे नहीं अमर हो गए\n\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Hindi",
      "<span class=\"hindi-kalam\">मोहन</span>"
    ]
  },
  {
    "objectID": "Poems/Hindi/औकात.html",
    "href": "Poems/Hindi/औकात.html",
    "title": "औकात",
    "section": "",
    "text": "हम खुशियां बांटने निकले थे एक सौगात बताई गई\nजानके हो या अनजाने में हमे हमारी औकात बताई गई\nलेकिन औकात तो कोई नियतांक नही वक्त का फलन है\nताकत के वर्ग का दौलत से गुणन है\nकिंतु जब इस समीकरण पे शिक्षा और सादगी का घात छा जाएगा\nहम बताएंगे नही तब पर सबको अपने आप समझ आ जायेगा\nकी ज्ञान दौलत सत्ता भगवान देता बांटने हेतु\nरिश्ते नाते समाजों में बांधने सौहार्द के सेतु\nयही सत्ता और शौहरत तों बहार का तौर है, आज नही तो कल झड़ जायेगा\nकौशल और ज्ञान तो अशोक की वे जड़ें है, जिनसे इंसान किसीभी हालात से लड़ जायेगा\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Hindi",
      "<span class=\"hindi-kalam\">औकात</span>"
    ]
  },
  {
    "objectID": "CV.html",
    "href": "CV.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "Indian Institute of Technology (IIT) Indore Nov 2021 – May 2025\nB.Tech in Computer Science and Engineering with minor in Economics · Transcript\nGPA: 9.39/10 · Recipient of The President of India Gold Medal\n\nSanjay Godawat Junior College Apr 2021\nSenior Secondary (12th Grade) · 97.17% · GPA 10/10\n\nS K Pant Walavalkar High School Mar 2019\nSecondary (10th Grade) · 97.60% · GPA 10/10 · Ideal Student Award 2019"
  },
  {
    "objectID": "CV.html#education",
    "href": "CV.html#education",
    "title": "Madhav Kadam",
    "section": "",
    "text": "Indian Institute of Technology (IIT) Indore Nov 2021 – May 2025\nB.Tech in Computer Science and Engineering with minor in Economics · Transcript\nGPA: 9.39/10 · Recipient of The President of India Gold Medal\n\nSanjay Godawat Junior College Apr 2021\nSenior Secondary (12th Grade) · 97.17% · GPA 10/10\n\nS K Pant Walavalkar High School Mar 2019\nSecondary (10th Grade) · 97.60% · GPA 10/10 · Ideal Student Award 2019"
  },
  {
    "objectID": "CV.html#scholastic-achievements",
    "href": "CV.html#scholastic-achievements",
    "title": "Madhav Kadam",
    "section": "Scholastic Achievements",
    "text": "Scholastic Achievements\n\nRecipient of The President of India Gold Medal for the best academic performance among all UG graduating students\nSelected for Arizona State University Summer Research Internships (SURI) Program by School of Computing and Augmented Intelligence\nNational Finalist in the Goldman Sachs India Hackathon in Computer Science 2024\nSelected for National University of Singapore Young Fellowship Study Award 2024 · Blog\nLed team to Bronze Medal (3rd place) at Inter-IIT Tech Meet 12.0, IIT Madras, Dec 2023, for Adobe’s Behaviour Simulation Challenge\nSelected for the highly competitive Amazon ML Summer School 2023\nAchieved Gold Medal (1st place) at Inter-IIT Tech Meet 11.0, IIT Kanpur, Feb 2023, for Cloudphysician Healthcare’s Vitals Extraction Challenge\nAll India Rank 1140 in JEE (Advanced) 2021 among over 1 million applicants\n100 percentile score in Mathematics for JEE (Mains)\nAchieved All Maharashtra Rank 30 in MHT-CET 2021 among 250,000 applicants\nWinner of Annual Debating Championship, Best Self-Written Poetry Award, IIT Indore\nExpert on Codeforces and Knight on Leetcode · Leetcode Biweekly Contest 137 (World Rank 346) · Codeforces Round 990 (World Rank 888 / India Rank 140)"
  },
  {
    "objectID": "CV.html#publications",
    "href": "CV.html#publications",
    "title": "Madhav Kadam",
    "section": "Publications",
    "text": "Publications\nScalable Distributed Laplacian Score for Feature Selection Dec 2024\nInternational Conference on Neural Information Processing (ICONIP), Auckland, New Zealand · Paper"
  },
  {
    "objectID": "CV.html#work-experience",
    "href": "CV.html#work-experience",
    "title": "Madhav Kadam",
    "section": "Work Experience",
    "text": "Work Experience\nSoftware Engineer — Samsung Research Jul 2025 – present\nDigital Wallet Team (Samsung Pay), Bengaluru, India\n\nDeveloped POC for OCR-based Add Anything to Wallet tool as an independent project and integrated it with Samsung Wallet. Implemented solution for securely digitising NFC-based Office IDs for internal use.\nDeveloped LLM as an on-device judge pipeline for multimodal use case.\nTo improve engineering efficiency, developed fully automated Figma-to-code in-house solution.\nReceived Samsung Quarterly Excellence Award and Annual Award 2025 for team contribution in strategic expansion of Samsung Wallet.\n\n\nResearch Intern — SURI, Arizona State University May 2025 – Jul 2025\nSchool of Computing and Augmented Intelligence, ASU\n\nConducted literature reviews on Privacy and Network Security of various categories of Android applications.\nApplied static and dynamic analysis methodologies to evaluate and report on the network security and privacy posture of shortlisted rental applications.\n\n\nSummer Intern — Samsung Research Jun 2024 – Aug 2024\nDigital Wallet Team (Samsung Pay), Bengaluru, India · Experience Letter\n\nDeveloped AI-driven image generation models aligned with Samsung theme for NRU promotion campaign.\nAutomated entire end-to-end promotional pipeline; researched and proposed a plan to develop an LLM agent capable of performing tasks for Samsung Wallet.\nReceived Advanced Developer Pre-Placement Offer.\n\n\nResearch Intern — Computational Social Science Group Dec 2023 – Dec 2024\nInstitute of Computer Science, University of Tartu, Estonia\n\nEngineered datasets by scraping and post-processing 15K+ Facebook and Threads posts and 2M+ comments.\nAnalysed and selected from various spam detection and toxicity quantification models. Conducted in-depth descriptive analysis across 10 key criteria."
  },
  {
    "objectID": "CV.html#key-projects",
    "href": "CV.html#key-projects",
    "title": "Madhav Kadam",
    "section": "Key Projects",
    "text": "Key Projects\nAdobe Behaviour Simulation Challenge Nov – Dec 2023\nRepresenting IIT Indore at Inter-IIT Technical Meet 12 · GitHub\n\nLed a team on predicting tweet engagement and generating targeted content for Adobe.\nDeveloped a Classifier-Regressor approach using BERTweet embeddings, achieving superior tweet popularity prediction across diverse engagement levels (0–1M likes). Detailed Report\nEmployed fine-tuned SOTA language models (LLaMA-2, NeuralHermes-2.5) with RAG for tweet generation.\nProposed LogRMSE as an improved evaluation metric for social media engagement.\n\n\nVitals Extraction Challenge — Cloudphysician Healthcare Jan – Feb 2023\nRepresenting IIT Indore at Inter-IIT Technical Meet 11 · GitHub\n\nDeveloped the fastest and most accurate pipeline (96% accuracy), successfully identifying and digitising vitals and graphs from ICU monitor screen images.\nImplemented SOTA models like YOLOv8, Mask RCNN, Segformer, RetinaNet and DETR on a custom dataset of 2000 self-annotated + 7000 unannotated images. Presentation\nEnhanced accuracy with PaddleOCR, SCAN algorithm, and semi-supervised learning.\n\n\nOther Projects\nIITIHub · Efficient Mixed-Integer Planning for UAVs · Amusement Park in OpenGL · Natural Language to SQL · MIPS 16-bit Processor in VHDL (GitHub) · Finnacio · Song Recommendation from Facial Emotions · RoastIn"
  },
  {
    "objectID": "CV.html#key-courses",
    "href": "CV.html#key-courses",
    "title": "Madhav Kadam",
    "section": "Key Courses",
    "text": "Key Courses\nMathematics — Optimization Algorithms and Techniques, Differential Equations, Calculus, Linear Algebra, Complex Analysis, Numerical Methods\nComputer Science — Distributed Network Algorithms, Parallel Computing, Computational Intelligence, Data Structures and Algorithms, Design and Analysis of Algorithms, Operating Systems, Compiler Techniques, Discrete Mathematical Structures, Automata Theory, Computer Networks, Computer Graphics, Computer Architecture, Logic Design, Database and Information Systems, Software Engineering, Service Oriented Systems, Blockchain for Responsible Computing, Computer Vision, Algebraic Graph Theory, Cryptography and Network Security, Machine Learning\nEconomics — Intermediate Microeconomics, Indian Economy, Development Economics, International Economics, Innovation and Intellectual Property Rights"
  },
  {
    "objectID": "CV.html#leadership-service",
    "href": "CV.html#leadership-service",
    "title": "Madhav Kadam",
    "section": "Leadership & Service",
    "text": "Leadership & Service\nCo-headed Cynaptics (AI/ML) Club, IIT Indore — led a team of 60+ students involved in research, projects and competitions in AI.\nPlacement Coordinator for CS department at Training and Placement Cell, IIT Indore.\nDelivered a talk on Interpretable, Explainable & Trustworthy AI, IIT Indore (Apr 2023).\nCo-head — Debating Society, IIT Indore. Mentored 100+ sophomores and freshers to sharpen their oratory skills.\nMicrosoft Learn Student Ambassador — IIT Indore.\nMember — Academic Council, IIT Indore (A.Y. 2022–23) · Srijan (Poetry) Club · Aquatics Club.\nMentored — IITISoC Projects (1 & 2), Institute Projects (link), Student Counsellor.\nProduced short film Breaking Point · Written and performed a poetic debate for Republic Day.\nVolunteered — Taught in government schools of underprivileged localities (AVANA Club initiative)."
  },
  {
    "objectID": "travel.html",
    "href": "travel.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "✕\n    \n    \n    \n  \n\n  \n  \n    Madhav Kadam\n    // travel.log\n  \n\n  \n  locating cities...",
    "crumbs": [
      "Home",
      "Atlas"
    ]
  },
  {
    "objectID": "Resources/OperatingSystems/OS.html",
    "href": "Resources/OperatingSystems/OS.html",
    "title": "Operating Systems",
    "section": "",
    "text": "Operating Systems\nA program that acts as an intermediary between a user and a computer and its hardware. Computer Hardware → Operating System → System and application programs → Users. Operating system is a resource allocator and a control program. Kernel is the one program that runs at all the times on a computer. A bootstrap program which is stored in ROM/EPROM [Erasable Programmable Read Only Memory]/EEPROM [Electrically EPROM] is loaded by kernel at power-up or reboot. CMOS [Complimentary metal oxide semiconductor] is a small memory in motherboard that stores BIOS [basic Input/output System] settings.\nNow, I/O and CPU can execute concurrently. Direct memory access (DMA) [DMA is direct memory access] There are device controllers which are in-charge of particular device type and each of such device controller have its own local buffer. Each device controller has a device driver to manage I/O. I/O is from device to local buffer of controller where as CPU works between main memory and local buffers. Completion of I/O is informed to CPU causing interrupt. Interrupt transfer control to ISR [Interrupt Service Routine] using interrupt vector [Contains addresses of all the service routines]. A trap/exception [Exceptions are interrupts triggered by the CPU itself wich can further be clsassified as Aborts and Traps. Aborts are things that prevent the interrupted code from continuing. These are things that indicate a major problem - e.g. division by zero, hardware failures, etc. Whereas Traps are things that don’t prevent the interrupted code from continuing. These can be used for debugging, for virtual memory management, etc.] is software generated interrupt. Operating system is said to be interrupt driven. After an interrupt, OS preserves state of CPU by storing registers and Program counter. I/O subsystem is responsible for spooling [Overlapping og output of one job with input of other jobs], buffering and caching. There are two possible I/O structures : After I/O starts control returns to user program can be either be given only upon completion or can be returned without waiting for I/O completion. In the later case, OS uses 1) System calls are basically request to OS to allow user to wait for I/O completion and 2) Device status tables contains entry for each I/O device indicating its type, address and state.\nStorage Structure : 1) RAM : Volatile, random access, storage media that CPU can access directly 2) Secondary storage is non-volatile extension of Main memory. Caching is copying information into a faster storage system temporarily. Magnetic Tapes → Optical Disks → Hard disks → Solid-state disks → Main memory → cache → registers. Cache is smaller than storage that is being cached and its management is a important design problem.\n\n\n\nVon neumann Architecture\n\n\nVon neumann Architecture\nMultiprocessor systems are also known as parallel or tightly coupled systems. These have higher throughput, economy of scale and increase reliability because of graceful degradation and fault tolerance. In Asymmetric Multiprocessing each processor performs specific task whereas in symmetric each processor performs all the tasks. There are clustered systems which share storage via a SAN [Storage Area Network]. Asymmetric clustering is when one machine stays in hot stand-by mode whereas in symmetric all machines look after each other. Clustering can be used for HPC [High Performance Computing] using parallelisation. There is DLM [Distributed Lock Manager] mechanism to avoid conflicting operations. Cache coherency [all the CPUs shall have the most recent values in their cache] is required.\nMultiprogramming and Multitasking : Multiprogramming is batch system where job scheduling is done so that single user cannot keep CPU and I/O busy at all the times and ensures that CPU always has one to execute. For ex, when I/O happens, we have to wait, so for that time OS switches to another Job that is kept in memory. Batch interfaces thus are non-interactive where user specifies all the details in advance for a batch job and receives output when all the processing is done. Multitasking on other hand is timesharing which is logical extension in which CPU switches job so frequently that users can interact with each job while it is running creating pretence of interactive computing. Swapping, virtual memory, CPU scheduling, managing response time are all part of multitasking.\nOperating systems do have dual mode - 1) User Mode 2) Kernel Mode. Mode bit is provided by hardware. Now a days more and more CPUs are also supporting multi-mode operations.\nA process is a program in execution. It is a unit of work within system. Each thread has a program counter specifying the location of next instruction to execute.\nKernals have singly linked list, doubly, circular linked list, binary search tree or even hash maps and bit maps. Computing environments can be traditional, mobile (android), distributed (Network Operating System), Client-Server, Peer-to-peer (Gnutella, Voice over IP, Napster), Virtualized (Citrix XenServer, VMware Elastic Sky X), cloud computing [Loda balancing is essential along with firwalls and cloud custome interface.] (Logical extension of virtualization ex, Amazon Elastic Compute Cloud (EC2) : Now, these can be SaaS (software), PaaS (platform), IaaS (infrastructure) ), Real time embedded systems. There are various open source OSes which counter to copy protection and Digital Rights Management (DRM) systems.\nOperating System Services include a) User interface b) Program execution c) I/O operations d) File system manipulations e) Communications f) Error detection g) Resource allocation h) Accounting i) Protection and Security\n\n\n\nimage.png\n\n\nSystem call is a programming interface to service provided by OS which are typically written in C/C++ and accessed by programs via a high-level API calls rather than direct use. Typically a number is associated with each system call, system call interface maintain a table indexed according to this number.\n\n\n\nAPI working of system calls\n\n\nAPI working of system calls\nSome systems have a program known as registry which is used to store and retrieve configuration information. Policy is what to do and mechanism is how to do?\n\n\n\nTypes of System Calls\n\n\nTypes of System Calls\n\n\n\nAndroid Architecture\n\n\nAndroid Architecture\nDebugging in OS is finding and fixing errors/bugs. Log files are generated containing error info. Core dump [When Application fails] and Crash dump [When OS fails/crashes] and trace listing. Profiling is periodic sampling of instruction pointer to look for statstical trends. Kernighan s Law: Debugging is twice as hard as writing the code in the first place. Therefore, if you write the code as cleverly as possible, you are, by definition, not smart enough to debug it. Dtrace tool allows live instrumentation on production systems by firing probes within a provider and captures state data and sends it to consumers of these probes. SYSGEN program obtains information concerning configs of hardware and compiles system-tuned kernal which is more efficient for that particular set of hardware. GRUB [GNU GRand Unified Bootloader] allows selection of kernal from multiple disks, versions and options.\n\nProcess\nIn batch systems, processes are usually called jobs and in time-shared systems tasks. A process has multiple parts, viz. text section which is program code, program counter, process registers, stack for temporary data and data section with global variables and heap which is dynamically allocated memory during runtime. Thanks to virtualisation, each process has its own address space. Information associated with each process is stored in PCB (Process control block).\n\n\n\nStates of a process\n\n\nStates of a process\nPCB also contains CPU scheduling info, memory management info, accounting info, I/O status information. Process number (PID)\n\n\n\nimage.png\n\n\nContext of a process is stored by virtue of registers and PCB. So here one process have a single program counter i.e single thread. task_struct is used for representing a process in C. Process scheduler have Job Queue, ready queue, device queue and processes migrate among these queues.\n\n\n\nimage.png\n\n\nShort term scheduler is frequently invoked to select next process for CPU allocation. Long term scheduler (Job scheduler) selects which processes shall be brought into ready queue in turn controlling degree of multiprogramming. The long term scheduler strives to maintain a good mix of I/O bound and CPU bound processes. If degree of multiprogramming needs to be decreased, a medium term scheduler can be added which swaps processes in and out of memory.\nParent processes can create children, forming a tree of processes. A child duplicates parent’s address space. System calls associated are,fork()→creation of child process, exec()→replace the process memory with new program exit()→a process calls after execution of last statement wait()→provides the status information and return pid, abort()→can terminate child process. Zombie: Whenever a child process is started an entry is added in process table and that entry is removed only after the child exits and parent gets this status of exit via wait system call which is called reaping, but if that wait call is not invoked, then this entry stays there called as zombie (even if wait is invoked small time period between exit and removing entry, that process is a zombie). Often parent is yet to invoke wait. Zombies are usually transferred to init for dealing. Orphan: If parent terminates without invoking wait the child is called orphan.\nProcesses within a system may be indpendent or cooperating. IPC [Interprocess communication] required. Shared memory and message passing are two models for IPC. Producer-Consumer problem deals shared memory model. Bounded buffer only Buffersize-1 item shared. In message passing send() and recieve() are used on a communication link which can be physical like bus, network, shared memory or logical (direct, indirect, synchronous/asyn, automatic/explicit buffering). Indirect uses mailboxes (ports). Blocking is used in synchronous IPC. If both sender and reciever are blocking we have a rendezvous. Buffering is not required in case of rendezvous. Buffering can be boudned or unbounded capacity. In windows message passing uses advanced local procedure calls (LPC).\nIn client server case, RPCs( remote procedure calls), pipes, sockets and remote method invocation (java) are used. Socket is defined as endpoints of communication. Concat IP and port to get socket. RPC abstracts procedure calls between processes on networked systems using ports for service differentiation. Stubs are used. Client-side stub: locates server, marshals parameters and Server-side stub: receives message, unpacks parameters, performs procedure. Windows: stub code compiled from MIDL (Msft Interface defn lang) specification and Data representation: XDL (external data representation) format to handle different architectures (big-endian, little-endian). OS provides rendezvous (matchmaker) service to connect client and server. Pipes are conduit. They can be ordinary where parent child relation required (unidirectional also called anonymous pipes), or named where it is not required (bidirectional).\n\n\nThreads\nA thread is a lightweight process that runs within the context of a parent process. Threads share the same memory space as the parent process. Threads are lightweight, requiring fewer resources to create and manage. Communication between threads is simpler and more efficient, as they share the same memory space. Ex, A web browser with multiple tabs, where each tab runs in a separate thread.\nAmdahl’s law : \\(speedup\\) ≤ \\(\\frac{1}{S+\\frac{(1-s)}{N}}\\) where S is non-parallelizable portion.\nThreads are of two types, user threads and kernel threads. Many user level threads are mapped to single kernel threads which might result in blocking. One to one mapping provides more concurrency but number of processes per thread is restricted because of overhead. Many to many model allows OS to create a sufficient number of kernel threads. Two level Many to many models allows to bound a user thread to particular kernel thread. POSIX is a library that provides API for managing and creating threads. Runnable in java allows to create threads. Thread pool can be created where threads await for work. Threads can be terminated before it finishes called thread cancellation which can be asynchronous (immediate) or deferred (periodic check for cancellation). A thread can be created with cancellation option disabled as well. Signals are used to notify a process that a event has occurred. Each thread can have TLS (Thread Local Storage) to have own copy of data.\nLightweight Process (LWP): An intermediate data structure between user and kernel threads. LWPs appear as virtual processors, allowing processes to schedule user threads to run.Each LWP is attached to a kernel thread. Scheduler activations provide a communication mechanism from the kernel to the thread library. This mechanism, called an upcall, allows the application to maintain the correct number of kernel threads.\n\n\nProcess Synchronisation\ncounter++ and counter-- concurrent execution enters in a race condition. Such part of the code where we change common variables, update tables, write files, etc are critical sections. General structure of process do { entry section → critical section → exit section → remainder section} while(true); . Solution of critical section problem requires 4 things, 1. Mutual exclusion 2. Progress and 3. Bounded waiting. OS handles CSP [Critical Section Problem] depending on if kernal is preemptive or not.\nPeterson’s Solution : For two processes\n\n\n\nimage.png\n\n\nHardware support for CSP comes with locking and atomic (non-interruptable) hardware instruction.\n\nTest and Set approach : Shared boolean variable lock is initialised to false.\n\n\n\n\nimage.png\n\n\n\n\n\nimage.png\n\n\n\n\n\nBounded wait in test and set\n\n\nBounded wait in test and set\n\nCompare and Swap Approach :\n\n\n\n\nimage.png\n\n\n\n\n\nimage.png\n\n\n\nMutex Locks : software solution implemented using hardware atomic instructions acquire() and release() . It requires busy waiting and thus called spinlock.\nSemaphore : S is an integer variable. Only 2 wait() and signal() atomic operations can use it. Wait call waits till S becomes positive and decrements it. Signal just increments it. Semaphore can be binary or counting. Semaphore with no busy waiting have a value and list or queue. It also has two operations block() and wakeup().\n\n\n\nCPU scheduling\nDispatcher module gives control of the CPU to the process selected by short-term scheduler. It switches context, switches to user mode and jumps to proper location to restart that program. The time taken to do this is called dispatch latency. Gantt Chart is useful depiction of processes.\nScheduling Criteria : 1. CPU Utilisation : Percentage of time the CPU is busy executing processes. 2. Throughput : Number of processes completed per unit of time. 3. Turnaround Time : Time taken to execute a process from start to finish. 4. Waiting Time : Time spent by a process in the ready queue waiting for CPU access. 5. Response Time : Time taken to produce the first response after a request is submitted (for time-sharing environments).\n\nFCFS : Convoy effect : Short processes behind long processes increase waiting time. (Like when a convoy has to pass, traffic stops)\nShortest Job First (SJF) : Processes with shortest length of next CPU burst executed. minimum average waiting time is achieved. Length of next CPU burst can be estimated using exponential averaging with \\(\\alpha\\) set to 0.5 (usually).\nShortest Remaining time first : Preemptive version of SJF.\nPriority Scheduling : An integer associated with each process, smaller is given higher priority. Starvation might be a problem and aging is a solution for the same.\nRound Robin : Time quantum q is given. q should be large enough to respect context switching time. Higher turnaround but lower response time.\nMultilevel Queue : foreground (interactive -RR) and background (batch - fcfs). Time slicing is done for each queue.\n\nThreads are scheduled within PCS (process contention space). Kernel threads are scheduled in SCS (system-contention space).\nIn case of multiple processors, asymmetric scheduling ( one processor does for all) or symmetric (every processor does its own scheduling). Process has a affinity for processor on which it is currently running. Soft affinity comes from baggage of previous instance already in queue. Hard affinity comes when process joins only one specific queue. NUMA is non-uniform memory access. Little’s law : n = λ × W where n is the average queue length λ is the average arrival rate into the queue W is the average waiting time in the queue. In steady state, the number of processes leaving the queue must equal the number of processes arriving.\nFor additional resources and reference :\nhttps://os-book.com/\nhttps://os-book.com/OS10/study-guide/Study-Guide.pdf\nDisclaimer : All the figures I added to my notes are from the “Operating System Concepts by Avi Silberschatz, Peter Baer Galvin, Greg Gagne”. They hold the copyrights."
  },
  {
    "objectID": "Resources/Reliability/index.html#module-1-foundations-of-explainability",
    "href": "Resources/Reliability/index.html#module-1-foundations-of-explainability",
    "title": "TRAI",
    "section": "MODULE 1: FOUNDATIONS OF EXPLAINABILITY",
    "text": "MODULE 1: FOUNDATIONS OF EXPLAINABILITY\n\n1.1 When Is Explainability Needed?\nExplainability is not always required, but certain conditions strongly call for it. Understanding when to apply it is crucial for the assessment.\nHigh-stakes decision contexts — situations where model outputs directly affect people’s lives, rights, or well-being: - Credit scoring and loan approval - Medical diagnosis and treatment recommendations - Criminal recidivism prediction (e.g., COMPAS) - Hiring and promotion decisions - Insurance underwriting - Child welfare risk scoring\nRegulatory and legal requirements — jurisdictions and industries with explicit mandates: - GDPR Article 22 (EU): Right to an explanation for automated decisions - ECOA (US): Adverse action notices for credit denial must list reasons - Healthcare regulations requiring model transparency - FINRA requirements for algorithmic trading explanations\nDebugging and model improvement — when practitioners need to: - Understand why a model is underperforming - Identify spurious correlations learned from data - Detect data leakage (model relying on proxy variables) - Verify the model is using features for the right reasons\nTrust and adoption — when end users or stakeholders need to: - Trust an AI system before acting on its outputs - Override model predictions when domain knowledge conflicts - Build confidence in a new deployed system\nAccountability and contestability — when affected individuals need to: - Challenge or appeal automated decisions - Understand what they can change to receive a different outcome (actionability) - Identify discriminatory patterns affecting protected groups\nWhen explainability is less critical: - Low-stakes automated decisions (e.g., movie recommendations) - Contexts where performance is the only goal and no human is affected - Purely internal research experiments without deployment\n\n\n\n1.2 Matching Explanation Purpose to Appropriate Audiences\nDifferent stakeholders have fundamentally different needs from explanations. A mismatch leads to confusion, misuse, or rejection of the explanation.\n\n\n\n\n\n\n\n\n\nAudience\nPrimary Goal\nPreferred Explanation Type\nKey Concerns\n\n\n\n\nData Scientists / ML Engineers\nDebug, improve model\nFeature importances, SHAP values, partial dependence plots, surrogate models\nTechnical fidelity, statistical validity\n\n\nDomain Experts (Doctors, Lawyers)\nValidate model behavior\nCase-based reasoning, rule extraction, attention maps\nAlignment with domain knowledge\n\n\nBusiness Stakeholders / Managers\nJustify deployment, understand risk\nSummary dashboards, high-level feature contributions\nBusiness impact, ROI, regulatory risk\n\n\nRegulators / Auditors\nEnsure compliance\nModel cards, fairness metrics, global feature importance\nReproducibility, legal compliance\n\n\nEnd Users / Affected Individuals\nUnderstand and contest decisions\nContrastive/counterfactual explanations, natural language\nClarity, actionability, fairness\n\n\nLegal / Compliance Teams\nRegulatory review\nDocumentation, adverse action reasons, audit trails\nLiability, right to explanation\n\n\n\nKey principle — contrastive explanations for end users: People naturally ask “Why was I rejected instead of approved?” rather than “What caused my outcome?” Counterfactual explanations (“If your income were $5,000 higher, you would have been approved”) directly address this need.\nKey principle — global vs. local explanations by audience: - Engineers → both global (model behavior overall) and local (individual predictions) - End users → primarily local explanations for their specific case - Regulators → primarily global explanations for systemic behavior\n\n\n\n1.3 Core Explainability Terms\nMaster these definitions precisely — matching-the-pairs questions frequently test these.\nInterpretability — the degree to which a human can understand the cause of a decision; an intrinsic property of a model (e.g., a linear model is inherently interpretable).\nExplainability — the ability to present model behavior in human-understandable terms, often via post-hoc methods applied to opaque models. Broader than interpretability.\nTransparency — openness about the model’s architecture, training data, and decision process; a property of the overall system, not just the model.\nAccountability — the ability to attribute responsibility for model outcomes to specific actors (developers, deployers, regulators).\nFidelity — how accurately an explanation reflects the true behavior of the model it is explaining.\nGlobal Explanation — describes the overall behavior of a model across all inputs (e.g., which features matter most on average).\nLocal Explanation — describes the model’s behavior for a specific input instance (e.g., why this particular loan was denied).\nModel-agnostic method — an explanation technique that can be applied to any model regardless of its architecture (e.g., LIME, SHAP).\nModel-specific method — an explanation technique that works only for a particular model type (e.g., attention weights for transformers, feature importances for tree models).\nIntrinsic interpretability — a model that is interpretable by design (linear regression, decision trees, rule-based systems).\nPost-hoc explainability — explanations generated after training, applied to an already-trained model (e.g., SHAP applied to a neural network).\nSurrogate model — a simpler, interpretable model trained to approximate the behavior of a complex model.\nContrastive explanation — explains a decision relative to an alternative outcome (“Why A and not B?”).\nCounterfactual explanation — a what-if explanation that identifies the minimum change to an input that would change the prediction.\nSaliency map — a visualization highlighting which parts of an input (e.g., pixels in an image) most influenced the prediction.\nPartial Dependence Plot (PDP) — shows the marginal effect of one or two features on the predicted outcome, averaged over all other features.\nIndividual Conditional Expectation (ICE) plot — like a PDP but shows the effect for each individual instance, revealing heterogeneous effects.\nFeature importance — a score indicating how much a feature contributes to model predictions.\nSHAP value — a value that quantifies the contribution of each feature to a specific prediction, based on Shapley values from game theory.\n\n\n\n1.4 Assessing Explanation Quality: Fidelity, Clarity, Stability, and Usefulness\nThe four pillars of explanation quality. You may be asked to evaluate or rank explanations using these criteria.\nFidelity (Faithfulness) - Does the explanation accurately reflect what the model actually computes? - High fidelity: the explanation would lead you to correctly predict what the model would do in new cases - Measured by: how well the surrogate/explanation model matches the original model’s predictions - Risk of low fidelity: users make decisions based on a misleading explanation - Example failure: a saliency map highlights “dog’s face” in an image, but the model is actually relying on the collar to predict breed\nClarity (Comprehensibility) - Is the explanation understandable to its intended audience? - Measured by: user studies, task completion rates, comprehension tests - Tradeoff: technically accurate explanations can be incomprehensible to lay users - Best practice: tailor the explanation format to the audience (SHAP plots for engineers; natural language for patients)\nStability (Robustness) - Does the explanation remain consistent when the input changes slightly? - High stability: similar inputs produce similar explanations - Measured by: perturbation tests — slightly vary input, check if explanation changes drastically - Unstable explanations undermine trust and suggest the explanation method is unreliable - LIME is known to have stability issues due to its random sampling; SHAP is generally more stable\nUsefulness (Actionability) - Does the explanation enable the recipient to take meaningful action? - For end users: can they understand what to change to get a different outcome? - For engineers: does the explanation help them improve the model? - For regulators: does the explanation help them assess compliance? - Usefulness is audience-dependent — same explanation can be useful for one group and useless for another\nSummary table:\n\n\n\nProperty\nQuestion It Answers\nWho Cares Most\n\n\n\n\nFidelity\nDoes it reflect the model?\nEngineers, auditors\n\n\nClarity\nIs it understandable?\nEnd users, managers\n\n\nStability\nIs it consistent?\nEngineers, regulators\n\n\nUsefulness\nCan you act on it?\nEnd users, domain experts"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-2-applying-explanation-methods",
    "href": "Resources/Reliability/index.html#module-2-applying-explanation-methods",
    "title": "TRAI",
    "section": "MODULE 2: APPLYING EXPLANATION METHODS",
    "text": "MODULE 2: APPLYING EXPLANATION METHODS\n\n2.1 LIME — Local Interpretable Model-agnostic Explanations\nWhat it does: LIME approximates the behavior of any black-box model locally around a specific prediction by training a simple interpretable model (usually linear regression) on perturbed samples near the input of interest.\nHow it works (step by step): 1. Take a specific input instance you want to explain (e.g., a loan application). 2. Generate perturbed versions of that instance (by randomly turning features on/off or adding noise). 3. Query the black-box model to get predictions for all perturbed samples. 4. Weight the perturbed samples by their proximity to the original instance. 5. Fit a simple interpretable model (e.g., linear regression, decision tree) on these weighted samples. 6. The coefficients of this simple model serve as the local explanation.\nKey properties: - Model-agnostic: works with any classifier or regressor - Local: only explains one prediction at a time - Approximate: the explanation is an approximation, not the ground truth - Perturbation strategy varies by data type: - Tabular: randomly setting features to mean value or zero - Text: removing words/tokens - Images: “superpixel” segmentation — turning off coherent image regions\nStrengths: - Easy to apply to any model - Produces human-readable explanations (feature weights) - Can handle text, images, and tabular data\nWeaknesses: - Instability: random sampling means different runs produce different explanations - Kernel width sensitivity: the choice of neighborhood size heavily affects results - Sampling bias: perturbed samples may fall in unrealistic regions of input space - Explanation may not faithfully reflect global model behavior\nLIME for text — key concept: Features are word presence/absence. A positive weight means the word pushes toward the predicted class; negative means it pushes against.\nCode skeleton (tabular):\nfrom lime.lime_tabular import LimeTabularExplainer\n\nexplainer = LimeTabularExplainer(\n    X_train,\n    feature_names=feature_names,\n    class_names=class_names,\n    mode='classification'\n)\n\nexplanation = explainer.explain_instance(\n    X_test[i],          # single instance to explain\n    model.predict_proba, # black-box prediction function\n    num_features=10      # top features to show\n)\n\nexplanation.show_in_notebook()\n\n\n\n2.2 SHAP — SHapley Additive exPlanations\nFoundation — Shapley values from cooperative game theory: Shapley values fairly distribute a “payout” (the model’s prediction) among “players” (features) based on their marginal contributions across all possible feature coalitions.\nThe SHAP value for feature i is the average marginal contribution of feature i across all possible orderings of features being added to the model.\nSHAP additive property: For any instance, the sum of all SHAP values equals the model’s prediction minus the base rate (expected model output):\nf(x) = base_value + SHAP_1 + SHAP_2 + ... + SHAP_n\nThis makes SHAP values directly interpretable as signed contributions.\nSHAP variants for different model types:\n\n\n\n\n\n\n\n\nVariant\nBest For\nSpeed\n\n\n\n\nTreeSHAP\nTree-based models (XGBoost, Random Forest)\nVery fast (exact)\n\n\nDeepSHAP\nNeural networks\nFast\n\n\nKernelSHAP\nAny model (model-agnostic)\nSlow\n\n\nLinearSHAP\nLinear models\nInstant\n\n\n\nSHAP visualizations — know each one:\n\nForce plot: Single prediction; shows features pushing prediction higher (red) or lower (blue) relative to base value.\nSummary plot (beeswarm): All instances; shows distribution of SHAP values per feature, colored by feature value. Reveals global importance and direction of effect.\nDependence plot: SHAP value of one feature vs. its actual value; reveals non-linear effects and interactions (colored by another feature to reveal interactions).\nWaterfall plot: Single prediction; shows step-by-step how each feature’s SHAP value builds up to the final prediction from the base value.\nBar plot: Global feature importance; average absolute SHAP values per feature.\n\nStrengths: - Theoretically grounded (satisfies efficiency, symmetry, dummy, and additivity axioms) - Consistent: if a feature’s true contribution increases, its SHAP value never decreases - Provides both local and global explanations - More stable than LIME\nWeaknesses: - Computationally expensive for KernelSHAP (exponential in features) - Assumes feature independence in some variants (can mislead with correlated features) - Can be hard to interpret for non-technical audiences - SHAP values can be manipulated by adversarial model design\nCode skeleton:\nimport shap\n\nexplainer = shap.TreeExplainer(model)          # or shap.Explainer(model)\nshap_values = explainer.shap_values(X_test)\n\nshap.summary_plot(shap_values, X_test)         # global summary\nshap.force_plot(explainer.expected_value,      # local explanation\n                shap_values[0], X_test.iloc[0])\nshap.dependence_plot(\"age\", shap_values, X_test)  # feature interaction\n\n\n\n2.3 Visualization Techniques for Interpreting Model Behavior\nPartial Dependence Plots (PDP): - Show the marginal effect of a feature on the predicted outcome, averaged over all other features - Assumes feature independence (problematic with correlated features) - Best for: communicating overall direction and shape of feature effect to non-technical audiences - Plot shape interpretation: flat = feature has no effect; monotonic = consistent direction; non-monotonic = complex interaction\nIndividual Conditional Expectation (ICE) Plots: - Each line shows the effect of changing one feature for one specific instance - Reveals heterogeneity: some instances may respond oppositely to others - Centered ICE (c-ICE): all lines start at zero, making differences in slopes clearer - When to use over PDP: when you suspect interactions or subgroup-specific effects\nAccumulated Local Effects (ALE) Plots: - Addresses PDP’s independence assumption by conditioning on neighborhoods - More reliable than PDP for correlated features - Less common but preferred in high-correlation settings\nSaliency Maps (for images and text): - Gradient-based: compute gradient of output w.r.t. input pixels - GradCAM: uses gradients flowing into the last convolutional layer to produce coarse localization maps - Integrated Gradients: accumulates gradients along the path from a baseline to the input - Useful for: diagnosing whether a vision model is looking at the right region\nAttention Visualization (for transformers): - Shows which input tokens a model attends to when producing an output - Caution: attention weights ≠ feature importance; models can attend to irrelevant tokens - Used for: initial inspection, not definitive explanation\nFeature Importance Plots (tree models): - Mean Decrease in Impurity (MDI): fast but biased toward high-cardinality features - Permutation Importance: model-agnostic; measures drop in performance when feature is shuffled; can be computed on test set - SHAP-based importance: most reliable; addresses MDI’s bias\n\n\n\n2.4 Surrogate Models\nDefinition: A surrogate model is a simpler, interpretable model trained to mimic the predictions of a complex black-box model.\nGlobal surrogate: 1. Generate a large dataset of inputs (can be original training data or new samples). 2. Query the black-box model to get predictions for all inputs. 3. Train an interpretable model (linear regression, decision tree) on these input-prediction pairs. 4. Interpret the surrogate as an approximation of the black-box.\nFidelity assessment: Measure R² or agreement rate between the surrogate and the black-box on held-out data. Higher fidelity = more trustworthy surrogate.\nChoice of surrogate model type: - Decision tree: Provides rule-based explanations; easy to visualize; risk of overfitting if too deep - Linear regression: Provides coefficient-based explanations; best when relationship is approximately linear - Rule lists (if-then rules): Highly interpretable; sparse explanations\nLimitations: - A surrogate is an approximation, not the true model — always report fidelity - If the surrogate has low fidelity, its explanation is misleading - Global surrogates may miss local behavior; use local surrogates (LIME) for individual predictions\nWhen to use surrogates: - When the original model is a proprietary black box with no internal access - When regulatory requirements demand an interpretable model for audit - When you want to communicate model logic to non-technical stakeholders\n\n\n\n2.5 Post-Training Model Analysis\nConcept: After training, systematic analysis of model behavior using interpretability techniques to understand what the model has learned.\nSteps in post-training analysis: 1. Global feature importance: Which features drive predictions overall? (SHAP summary plot, permutation importance) 2. Feature effect analysis: How does each feature affect the prediction? (PDP, ALE plots) 3. Subgroup analysis: Does the model behave differently for demographic subgroups? 4. Error analysis: Which types of inputs does the model get wrong? Are errors clustered? 5. Concept probing: Does the model represent human-interpretable concepts internally? (for neural networks) 6. Layer-wise analysis: For deep networks, what is each layer learning? (activation maximization)\nActivation maximization: Synthesizes the input that maximally activates a neuron — reveals what a unit “detects.” Used in computer vision to understand convolutional filters.\nProbing classifiers: Train a linear classifier on internal representations to test if a concept (e.g., sentiment, POS tag) is encoded in a layer.\nChecklist for post-training analysis: - [ ] Feature importance computed and cross-validated - [ ] Effects visualized for top features - [ ] Subgroup performance compared - [ ] Failure cases examined and categorized - [ ] Explanation quality assessed (fidelity, stability)\n\n\n\n2.6 Counterfactual and Randomized Tests for Feature Sensitivity\nCounterfactual explanations: - Answer: “What is the minimum change to this input that would change the prediction?” - Properties of a good counterfactual: - Validity: The counterfactual input actually receives the desired prediction - Proximity: The change from the original input is minimal - Sparsity: As few features changed as possible - Plausibility: The counterfactual is a realistic/realistic input (not out-of-distribution) - Actionability: Only features the user can actually change are modified (e.g., age is immutable)\nWachter counterfactuals: Minimize weighted distance between original and counterfactual subject to the prediction changing. Simple but may generate implausible instances.\nDiCE (Diverse Counterfactual Explanations): Generates multiple diverse counterfactuals — useful for showing multiple paths to a different outcome.\nRandomized feature sensitivity tests: - Feature permutation: Shuffle a feature across all instances; measure drop in performance. Larger drop = more important feature. - Feature ablation: Set a feature to zero or mean; measure performance change. - Input perturbation: Add small random noise to inputs; measure prediction stability (tests model robustness). - Interventional testing: Systematically vary one feature while holding others constant; verify the model responds as expected by domain knowledge.\nSensitivity analysis workflow: 1. Define the feature of interest. 2. Generate perturbed versions of test instances (vary only that feature). 3. Record model predictions for each perturbation level. 4. Plot the sensitivity curve (prediction vs. feature value). 5. Compare to domain expectations — does the model respond appropriately?\nWhy this matters for fairness: If the model is highly sensitive to race or gender (which should be irrelevant), this is detected through feature sensitivity tests."
  },
  {
    "objectID": "Resources/Reliability/index.html#module-3-diagnosis-of-fairness-issues",
    "href": "Resources/Reliability/index.html#module-3-diagnosis-of-fairness-issues",
    "title": "TRAI",
    "section": "MODULE 3: DIAGNOSIS OF FAIRNESS ISSUES",
    "text": "MODULE 3: DIAGNOSIS OF FAIRNESS ISSUES\n\n3.1 Detecting Bias in Data\nTypes of bias in data — know each precisely:\nHistorical bias: The data reflects historical human prejudice. Even if the data collection process was unbiased, the underlying social patterns encode discrimination. Example: historical hiring data reflecting gender discrimination in STEM.\nRepresentation bias (Underrepresentation): Some groups are sampled at lower rates than their true population frequency. The model has less data about these groups, leading to higher error rates. Example: clinical trial data overrepresenting white males; facial recognition datasets underrepresenting darker-skinned faces.\nMeasurement bias: Different groups are measured with different accuracy or instruments. Example: using arrest records as a proxy for crime — arrest rate reflects policing intensity, not actual crime, and policing is historically biased.\nAggregation bias: Combining data from different subgroups and fitting a single model ignores within-group variation. The model fails to capture group-specific patterns. Example: one diabetes prediction model for all ethnicities when the relationship between HbA1c and diabetes differs by ethnicity.\nLabel bias: The labels themselves are biased, even if the features are not. Example: loan default labels reflecting discriminatory lending practices (loans were denied to minorities so we have fewer default/repayment labels for them).\nTemporal bias: Training data from a past time period does not reflect current population distributions or fairness standards.\nStereotype reinforcement: When training data encodes cultural stereotypes, the model learns and perpetuates them. Example: word embeddings placing “homemaker” closer to “woman” than “man.”\nHow to detect data bias: - Compute group-level statistics: mean, variance, missing data rate, label rates by demographic - Visualize distributions using histograms and density plots stratified by group - Check label rates: is the positive label rate similar across groups? - Check feature distributions: are there systematic differences? - Use demographic parity tests on the raw labels - Check for proxy variables: features highly correlated with protected attributes\n\n\n\n3.2 Risks in Labels, Feature Selection, and Representation\nLabel risks:\n\nSubjective labeling: Human annotators introduce bias when labeling subjective tasks (toxicity, sentiment, risk)\nFeedback loops: If labels are derived from system decisions (e.g., hiring outcomes), and the system was biased, labels reflect that bias\nMissing labels: Differential missingness across groups can bias training\n\nFeature selection risks:\n\nProxy features: Features not directly protected but highly correlated with protected attributes (zip code ↔︎ race; credit score ↔︎ SES; name ↔︎ ethnicity/gender). Removing the protected attribute doesn’t help if proxies remain.\nDisparate impact through feature selection: Some features may have unequal predictive power across groups, causing unequal treatment when used\nIrrelevant features that encode group membership: Including name, photo, or other identifying information\n\nRepresentation risks:\n\nClass imbalance: If a protected group is a small minority, the model optimizes for majority-group performance\nIntersection blindness: Looking only at gender OR race separately misses harm to intersectional groups (Black women may face unique discrimination not captured by either margin)\nDistributional shift: Training data doesn’t represent the deployment population\n\n\n\n\n3.3 Formulating Questions to Uncover Hidden Assumptions\nA critical skill for the assessment. Before training, systematically question the model design.\nQuestions about the task definition: - What does the model’s output actually mean? What is being predicted, and does it capture what we care about? - Is the proxy outcome we’re predicting a fair proxy? (e.g., predicting “creditworthiness” using “repayment” — is repayment access affected by systemic factors?) - Who decided what the “correct” outcome is?\nQuestions about the data: - Who is represented in this dataset? Who is missing? - Over what time period was data collected? Does it reflect current reality? - How were labels assigned? By whom? With what incentives? - Are there features that should not be used? Why are they included?\nQuestions about the model: - What does the model optimize? Is accuracy the right metric here? - Does the model perform equally across demographic subgroups? - What are the consequences of false positives vs. false negatives for different groups?\nQuestions about deployment: - Who will use this model? In what contexts? - What happens to people who receive incorrect predictions? - Who can contest a decision? What recourse exists?"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-4-fixing-fairness-in-the-workflow",
    "href": "Resources/Reliability/index.html#module-4-fixing-fairness-in-the-workflow",
    "title": "TRAI",
    "section": "MODULE 4: FIXING FAIRNESS IN THE WORKFLOW",
    "text": "MODULE 4: FIXING FAIRNESS IN THE WORKFLOW\n\n4.1 Pre-processing Methods for Bias Mitigation\nPre-processing interventions modify the training data before model training begins. They are model-agnostic — the corrected dataset can be used with any model.\nResampling: - Oversampling underrepresented groups: Duplicate instances from minority groups (e.g., SMOTE — Synthetic Minority Oversampling Technique). Ensures equal representation. - Undersampling majority groups: Remove instances from overrepresented groups. Risks losing information. - Stratified sampling: Ensure training data proportionally represents all subgroups.\nReweighting: - Assign higher training weights to underrepresented groups or incorrectly labeled instances - The model pays more attention to these examples during training - Reweighting factor: inverse of group probability — groups with lower representation get higher weights\nLabel flipping / Relabeling: - Identify and correct mislabeled instances, especially for groups where labeling was historically biased - Kamiran & Calders (2012): flip labels for borderline instances to achieve parity\nFeature transformation: - Suppression: Remove protected attributes and their proxies - Fair representations: Learn a new feature space that removes group information while preserving predictive information (Zemel et al., Variational Fair Autoencoder) - Data augmentation: Generate synthetic data for underrepresented groups\nDisparate impact remover: - Transforms feature values to remove correlation with the protected attribute while preserving rank ordering within groups - Adjusts features to achieve the “80% rule” (disparate impact threshold)\n\n\n\n4.2 Causal and Adversarial Techniques\nCausal approaches:\nCausal graphs (DAGs): Model the causal relationships between variables, including protected attributes. Identify paths through which protected attributes affect predictions.\n\nDirect discrimination: Protected attribute → prediction (direct causal path)\nIndirect discrimination: Protected attribute → mediator → prediction (causal path through a feature)\nCounterfactual fairness (Kusner et al.): A prediction is counterfactually fair if it would be the same for an individual in a counterfactual world where their protected attribute was different, with all causally downstream variables adjusted accordingly. More rigorous than standard fairness metrics.\n\nPath-specific counterfactual fairness: Block only the illegitimate causal paths (direct discrimination) while allowing legitimate paths (e.g., education level can mediate gender effects if we consider it legitimate).\nAdversarial debiasing:\nArchitecture: Two-component network 1. Predictor: Learns to predict the target label Y from features X 2. Adversary: Attempts to predict the protected attribute A from the predictor’s output\nTraining: The predictor is trained to minimize prediction loss while also maximizing the adversary’s prediction error (minimax game). This forces the predictor to make predictions from which the protected attribute cannot be recovered.\nPredictor loss = prediction_loss - λ * adversary_accuracy\nAdversary loss = adversary_prediction_loss\nThe hyperparameter λ controls the fairness-accuracy tradeoff.\nFairness VAE / Fair representation learning: - Encode data into a latent space where protected attribute information is removed - Decoder can reconstruct data without protected attribute information - Downstream models trained on fair representations inherit fairness properties\n\n\n\n4.3 Fairness-Aware Training Methods\nIn-processing methods incorporate fairness constraints directly into the model training objective.\nFairness constraints as regularization: - Add a fairness penalty term to the loss function - Loss = prediction_loss + λ * fairness_violation - The model minimizes both prediction error and fairness violation simultaneously\nTypes of fairness constraints (know the definitions):\n\n\n\n\n\n\n\n\nFairness Criterion\nDefinition\nFormula\n\n\n\n\nDemographic Parity\nPrediction rate equal across groups\nP(Ŷ=1|A=0) = P(Ŷ=1|A=1)\n\n\nEqualized Odds\nEqual TPR and FPR across groups\nP(Ŷ=1|Y=y, A=0) = P(Ŷ=1|Y=y, A=1) for y ∈ {0,1}\n\n\nEqual Opportunity\nEqual TPR across groups (relaxed equalized odds)\nP(Ŷ=1|Y=1, A=0) = P(Ŷ=1|Y=1, A=1)\n\n\nPredictive Parity\nEqual PPV (precision) across groups\nP(Y=1|Ŷ=1, A=0) = P(Y=1|Ŷ=1, A=1)\n\n\nIndividual Fairness\nSimilar individuals receive similar predictions\nd(x,x’) small → d(f(x),f(x’)) small\n\n\nCalibration\nPredicted probabilities match actual rates within each group\nP(Y=1|Ŷ=p, A=a) = p for all groups a\n\n\n\nThe impossibility theorem (Chouldechova 2017, Kleinberg et al. 2016): When base rates differ between groups, you cannot simultaneously satisfy calibration, equal FPR, and equal FPR. Fairness criteria are mutually exclusive under certain conditions. This means you must choose which criterion to prioritize based on the context and the costs of different types of errors.\nReduction approaches (Agarwal et al., Reductions framework): - Reduce fairness-constrained classification to a sequence of standard cost-sensitive classification problems - Iteratively re-weight training examples to satisfy fairness constraints - Implemented in Fairlearn (Microsoft)\nPrejudice Remover (Kamishima et al.): - Adds a regularizer to logistic regression that penalizes mutual information between predictions and the protected attribute\n\n\n\n4.4 Post-processing: Calibration on Group-Specific Thresholds\nPost-processing methods modify model outputs after training to improve fairness. They don’t require access to model internals.\nThreshold optimization: - Different decision thresholds for different demographic groups - Choose thresholds to equalize a target fairness metric (e.g., equalized odds) - Steps: 1. Get predicted probabilities from the trained model 2. For each group, find the threshold that achieves the fairness target 3. Apply group-specific thresholds at decision time\nReject option classification: - For predictions near the decision boundary, the model is uncertain - Give the favorable outcome to disadvantaged group members in the uncertain zone - Reduces discrimination in borderline cases\nCalibration: - Platt scaling or isotonic regression to align predicted probabilities with actual frequencies - Apply separately per group to achieve within-group calibration - Relevant for COMPAS-style risk scores where calibration was achieved but other fairness criteria were violated\nRoth-Morwitz algorithm (Equalized Odds Post-processing, Hardt et al. 2016): - Find a randomized threshold policy that equalizes TPR and FPR across groups - Operates only on predictions and protected attribute, not original features - Model-agnostic, easy to apply as a final step\n\n\n\n4.5 Multi-Accuracy Auditing Across Subgroups\nMulti-accuracy (Kim et al. 2019): - A predictor is “multi-accurate” if it is accurate simultaneously across all subgroups in a rich class of groups (not just demographic groups) - Extends standard accuracy auditing from top-level groups to intersectional subgroups\nMulti-calibration: - The model’s predicted probabilities are calibrated for all subgroups, not just the overall population - Stronger guarantee than simple calibration: even for the intersection of being female, over 40, and from a specific zip code, the probability estimates are accurate\nAuditing workflow: 1. Define the set of subgroups to audit (demographic, intersectional, behavioral) 2. Compute the target metric (accuracy, calibration, fairness metric) for each subgroup 3. Identify subgroups where the metric falls below the overall level (or a defined threshold) 4. Diagnose the cause: representation gap, label noise, or model error 5. Apply targeted interventions (resampling, reweighting, threshold adjustment) 6. Re-audit to verify improvement\nFairness tools and libraries:\n\n\n\n\n\n\n\n\nTool\nDeveloper\nKey Features\n\n\n\n\nFairlearn\nMicrosoft\nFairness metrics, reduction algorithms, dashboard\n\n\nAI Fairness 360 (AIF360)\nIBM\n70+ fairness metrics, 10+ bias mitigation algorithms\n\n\nWhat-If Tool\nGoogle\nInteractive exploration of model behavior by subgroup\n\n\nAequitas\nUniversity of Chicago\nBias auditing toolkit for machine learning pipelines"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-5-monitoring-fairness-over-time",
    "href": "Resources/Reliability/index.html#module-5-monitoring-fairness-over-time",
    "title": "TRAI",
    "section": "MODULE 5: MONITORING FAIRNESS OVER TIME",
    "text": "MODULE 5: MONITORING FAIRNESS OVER TIME\n\n5.1 Fairness Unit and Smoke Tests\nUnit tests for fairness (individual components): - Test that a data preprocessing function does not introduce bias (e.g., imputation does not use group mean in a way that encodes group differences) - Test that feature encoding does not encode protected attribute information - Test that label assignment is consistent across groups\nSmoke tests (basic sanity checks before deployment): - Does the model’s positive prediction rate differ by more than 20% across groups? (80% rule / four-fifths rule) - Does the model’s accuracy differ significantly across groups? - Does the model produce identical predictions for identical inputs regardless of protected attribute? - Is the model’s error rate stable across demographic subgroups?\nFairness testing frameworks: - Write fairness tests as part of CI/CD pipelines - Fail the pipeline if fairness metrics fall below thresholds - Use tools like Fairlearn’s MetricFrame to compute multiple metrics across multiple groups simultaneously\nCounterfactual consistency test: - Generate pairs of inputs identical except for the protected attribute - Check that the model produces the same prediction for both - If not, the model uses the protected attribute (or a proxy) directly\n\n\n\n5.2 Continuously Evaluating Models Against Fairness Metrics\nKey fairness metrics to monitor continuously:\n\nDemographic parity difference: |P(Ŷ=1|A=0) - P(Ŷ=1|A=1)|; target = 0\nEqualized odds difference: Max difference in TPR and FPR across groups\nDisparate impact ratio: P(Ŷ=1|A=minority) / P(Ŷ=1|A=majority); target ≥ 0.8 (four-fifths rule)\nCalibration error by group: |P(Y=1|Ŷ=p, A=a) - p| for each group a\nAUC-ROC by group: Measure discriminative power within each group\n\nMonitoring pipeline: 1. Log predictions and (where available) outcomes along with group membership 2. Compute fairness metrics on a rolling window or fixed schedule 3. Alert when a metric crosses a predefined threshold 4. Investigate root causes (distribution shift, label drift, model degradation) 5. Retrain or recalibrate as needed\nConcept/data drift and fairness: - Distributional shift affects groups unequally — a model that was fair at deployment may become unfair as the population changes - Monitoring for accuracy by group also detects fairness degradation - Covariate shift: input distribution changes; label shift: outcome rates change\n\n\n\n5.3 Updating Test Sets as User Bases Evolve\nWhy test sets become stale: - User demographics evolve as the product grows - Societal norms change (what was acceptable language may become unacceptable) - Edge cases become common over time - New protected groups emerge (e.g., gender identity categories)\nBest practices: - Rolling test sets: Continuously collect new labeled examples from production; retire old examples - Diverse annotation: Include annotators from underrepresented groups; use annotation guidelines that reflect current norms - Adversarial test sets: Collect examples specifically designed to challenge the model (Dynabench approach) - Red-teaming: Actively probe for failure cases in new subpopulations - Regular refresh schedule: Audit test set demographics quarterly or after significant user growth events - Version control for test sets: Track changes in test set composition alongside model versions\nFeedback loop management: - If the model’s outputs influence future training data (e.g., a content recommendation model creates filter bubbles), the training distribution will drift toward model biases - Detect feedback loops through diversity monitoring and user behavior analysis - Break feedback loops with randomization, exploration, and diverse data collection"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-6-building-robust-models",
    "href": "Resources/Reliability/index.html#module-6-building-robust-models",
    "title": "TRAI",
    "section": "MODULE 6: BUILDING ROBUST MODELS",
    "text": "MODULE 6: BUILDING ROBUST MODELS\n\n6.1 Designing Training Objectives That Reflect Real-World Goals\nThe alignment problem in ML: The objective function optimized during training is rarely identical to the true goal. This gap causes models to find unexpected solutions.\nCommon misalignments:\n\nOptimizing accuracy vs. optimizing decision quality: Accuracy penalizes all errors equally, but false positives and false negatives often have very different costs.\nOptimizing average performance vs. worst-case performance: A model with high average accuracy may fail catastrophically on rare but critical inputs.\nOptimizing training distribution vs. deployment distribution: Training loss doesn’t measure deployment performance.\n\nSolutions and design principles:\nCost-sensitive loss functions: - Assign different weights to different error types - loss = w_FP * FP_loss + w_FN * FN_loss - Set weights based on real-world costs of each error type\nTask-specific metrics as objectives: - For medical screening: optimize recall (sensitivity) subject to a precision constraint - For fraud detection: optimize F-β score with β &gt; 1 to weight recall more - For ranking: optimize NDCG or MAP rather than cross-entropy\nDistributional robustness: - Optimize worst-case performance over a family of distributions (Distributionally Robust Optimization, DRO) - Ensures the model performs well even under distribution shift\nRegularization for generalization: - L1/L2 regularization prevents overfitting to training distribution - Dropout, data augmentation, mixup improve robustness to input variation\nMulti-objective optimization: - Define multiple objectives (accuracy, fairness, robustness) and find Pareto-optimal solutions - Use constrained optimization: maximize accuracy subject to fairness constraints\n\n\n\n6.2 User-Driven Testing for Input Sensitivity\nBehavioral testing (Ribeiro et al., CheckList): A framework for systematic model testing that goes beyond accuracy on a held-out set.\nThree test types in CheckList:\n\nMinimum Functionality Tests (MFT): Test basic expected behaviors. Example: a sentiment model should classify “This is great!” as positive.\nInvariance Tests (INV): Perturbations that should NOT change the prediction. Example: changing a name from “John” to “Jamal” should not change a sentiment prediction.\nDirectional Expectation Tests (DIR): Perturbations where prediction should change in a known direction. Example: adding “not” before a positive word should decrease positive sentiment score.\n\nUser-driven sensitivity testing: - Allow users to drag feature sliders and see how predictions change - Provide “what-if” interfaces (Google’s What-If Tool) - Show feature sensitivity curves for individual predictions - Let domain experts define test cases based on their knowledge\nStress testing: - Test on extreme values: minimum, maximum, boundary conditions - Test on adversarial examples (see Security section) - Test on out-of-distribution inputs (data from a different hospital, country, time period)\n\n\n\n6.3 Identifying and Managing Edge Cases\nDefinition: Edge cases are inputs that occur rarely but for which the model’s failure would be high-cost or high-risk.\nSources of edge cases: - Rare demographic groups or rare feature combinations - Unusual events (weather anomalies, market crashes, pandemics) - Adversarial inputs - Data from new distributions not present during training\nIdentifying edge cases: - Uncertainty quantification: Flag inputs where model confidence is low (high entropy, high predictive variance in ensemble models) - Anomaly detection: Detect out-of-distribution inputs before the model makes a prediction - Error analysis: Cluster misclassified examples to find systematic failure modes - Human-in-the-loop review: Route low-confidence predictions to human reviewers\nManaging edge cases at deployment: - Abstention/rejection option: Allow the model to refuse to make a prediction if uncertainty is too high - Confidence thresholding: Only act on predictions above a minimum confidence level - Escalation policies: Define procedures for routing edge cases to human experts - Monitoring dashboards: Track edge case occurrence rates and model performance on them"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-7-privacy-and-legal-readiness",
    "href": "Resources/Reliability/index.html#module-7-privacy-and-legal-readiness",
    "title": "TRAI",
    "section": "MODULE 7: PRIVACY AND LEGAL READINESS",
    "text": "MODULE 7: PRIVACY AND LEGAL READINESS\n\n7.1 Data Protection Principles During Model Development\nCore principles (aligned with GDPR and general best practice):\nLawfulness, fairness, and transparency: Data must be collected and used lawfully; individuals must be informed.\nPurpose limitation: Data collected for one purpose should not be used for an unrelated purpose without consent. Example: health data collected for treatment should not be used to train a commercial model without explicit consent.\nData minimization: Collect and retain only the data necessary for the specified purpose. Do not store more than needed.\nAccuracy: Keep data up to date; have mechanisms to correct inaccurate data.\nStorage limitation: Retain data only as long as necessary; have data deletion policies.\nIntegrity and confidentiality: Protect data against unauthorized access, loss, or destruction.\nAccountability: Organizations must demonstrate compliance with these principles.\n\n\n\n7.2 Regulatory Standards: GDPR and HIPAA\nGDPR (General Data Protection Regulation — EU):\nKey provisions relevant to ML: - Article 5: Data protection principles (above) - Article 9: Special categories of sensitive data: health, racial/ethnic origin, political opinions, religious beliefs, sexual orientation, biometric data → require explicit consent or other legal basis - Article 13/14: Transparency — inform individuals about automated processing - Article 15: Right of access — individuals can request their data - Article 17: Right to erasure (“right to be forgotten”) — complicates model training (retraining without deleted data) - Article 20: Right to data portability - Article 22: Right not to be subject to solely automated decisions with significant effects; right to human review and to contest decisions - Recital 71: Automated decisions should use appropriate mathematical and statistical procedures; should not concern special categories of data\nHIPAA (Health Insurance Portability and Accountability Act — US):\nKey provisions for ML: - Applies to Protected Health Information (PHI): individually identifiable health data - 18 de-identification identifiers must be removed for data to be considered de-identified (Safe Harbor method) - Expert determination: A statistician certifies that risk of re-identification is very small - Business Associate Agreement (BAA): Any third party (including cloud ML providers) handling PHI must sign a BAA - Minimum necessary rule: Only access the minimum PHI needed for the task - Security Rule: Technical safeguards for PHI (encryption, access controls, audit logs)\nGDPR vs. HIPAA comparison:\n\n\n\n\n\n\n\n\nAspect\nGDPR\nHIPAA\n\n\n\n\nScope\nAny personal data, EU residents\nHealth information, US covered entities\n\n\nGeographic reach\nGlobal (if processing EU data)\nUS-focused\n\n\nRight to explanation\nYes (Article 22)\nLimited\n\n\nPenalties\nUp to 4% global annual revenue\nUp to $1.9M per violation category\n\n\nData subject rights\nBroad\nLimited\n\n\n\n\n\n\n7.3 Privacy-Preserving Practices\nData minimization: - Collect only features needed for the model - Aggregate where possible (use zip code instead of full address) - Use feature selection to remove unnecessary variables early\nAccess control: - Role-based access: only authorized personnel access sensitive data - Audit logs: track who accessed what data and when - Data compartmentalization: sensitive attributes stored separately\nAnonymization and pseudonymization: - Anonymization: True removal of identifying information; irreversible - Pseudonymization: Replace identifiers with pseudonyms; re-identification possible with key - k-anonymity: Every record is indistinguishable from at least k-1 other records on quasi-identifying attributes - l-diversity: Each equivalence class has at least l well-represented sensitive values - t-closeness: Distribution of sensitive attribute in each equivalence class is close to overall distribution\nDifferential Privacy (DP): - Mathematical definition: a mechanism M is ε-differentially private if for any dataset D and D’ differing by one record, and any output S: P(M(D) ∈ S) ≤ e^ε * P(M(D’) ∈ S) - Adding carefully calibrated noise to model training or outputs - DP-SGD: Add Gaussian noise to gradients during training; clip gradients to bound sensitivity - ε (epsilon): Privacy budget — smaller ε = stronger privacy guarantee = more noise = lower accuracy - Used by: Apple, Google for analytics; PyTorch Opacus library for ML\nFederated Learning: - Train models across multiple decentralized devices without centralizing raw data - Each device trains locally on its data; only model updates (gradients) are shared - Aggregated on a central server (FedAvg algorithm) - Privacy benefit: raw data never leaves the device - Residual risk: gradient sharing can still leak information (gradient inversion attacks)\nSecure Multi-Party Computation (MPC): - Multiple parties jointly compute a function over their combined data without revealing their individual data to each other - Example: hospitals jointly train a model without sharing patient records\nHomomorphic Encryption: - Perform computation on encrypted data; decrypt only the final result - Currently too slow for large-scale ML, but improving"
  },
  {
    "objectID": "Resources/Reliability/index.html#module-8-security-and-adversarial-defense",
    "href": "Resources/Reliability/index.html#module-8-security-and-adversarial-defense",
    "title": "TRAI",
    "section": "MODULE 8: SECURITY AND ADVERSARIAL DEFENSE",
    "text": "MODULE 8: SECURITY AND ADVERSARIAL DEFENSE\n\n8.1 Identifying Security Threats\nAdversarial attacks:\nEvasion attacks (inference time): Craft inputs that cause misclassification without being detected. - White-box attacks: Attacker has full knowledge of model architecture and parameters - FGSM (Fast Gradient Sign Method): add perturbation in the direction of gradient of loss w.r.t. input - PGD (Projected Gradient Descent): iterative FGSM with projection back onto ε-ball - C&W attack: optimization-based; more powerful but slower - Black-box attacks: Attacker only has access to model outputs - Transfer attacks: generate adversarial examples on a substitute model; transfer to target - Query-based attacks: use model predictions to estimate gradients\nPoisoning attacks (training time): Inject malicious examples into training data to corrupt the model. - Clean-label poisoning: Inject examples with correct labels but adversarial features - Backdoor/Trojan attacks: Inject examples with a trigger pattern that causes misclassification at inference\nModel extraction / Stealing: Repeatedly query a model to reconstruct its parameters or decision boundary.\nModel inversion: Use model outputs to reconstruct sensitive training data. Example: reconstructing a face from a face recognition model’s confidence scores.\nMembership inference: Determine whether a specific record was in the training data. Enabled by models overfitting to training data.\nPrompt injection (for LLMs): Craft inputs that override the model’s system instructions or cause unintended behavior.\nData poisoning via crowdsourced labeling: Compromise annotators to label data incorrectly.\n\n\n\n8.2 Defense Strategies\nInput validation and preprocessing: - Detect out-of-distribution inputs before they reach the model - Feature squeeze: reduce input precision (e.g., color bit depth) to remove adversarial perturbations - Input transformation: JPEG compression, smoothing, resizing — destroy fine-grained adversarial patterns - Certifiable defenses: guarantee robustness within a defined ε-ball (randomized smoothing)\nAdversarial training: - Include adversarial examples in training data - At each training step, generate adversarial examples for the current batch; train on both clean and adversarial - PGD adversarial training (Madry et al.) is the gold standard for robustness - Cost: significant training time increase; slight accuracy drop on clean data\nRandomized smoothing: - Smooth the model’s predictions by averaging over Gaussian-noised copies of the input - Provides certified ℓ₂ robustness: guarantees that the prediction doesn’t change within a radius - Model-agnostic but requires many forward passes at inference time\nCertified robustness: - Formally verify that no adversarial perturbation within a defined bound can change the prediction - Methods: interval bound propagation, Lipschitz-based bounds, linear programming relaxations - Currently practical only for small networks or tight ε bounds\nBackdoor detection and mitigation: - Neural Cleanse: detect backdoors by finding small universal perturbations that flip predictions - STRIP (STRong Intentional Perturbation): detect backdoored inputs by superimposing random patterns - Fine-pruning: prune neurons that are unusually dormant on clean data but active on poisoned data\nDifferential privacy for defense: - DP training limits the influence of any single training point, making poisoning less effective - Also limits membership inference risk\nAccess controls and rate limiting: - Limit the number of queries a user can make (defends against model extraction and membership inference) - Add noise to predictions or return only top-k classes - Monitor for unusual query patterns\n\n\n\n8.3 Evaluating Tradeoffs Between Security, Interpretability, and Performance\nThe security-accuracy tradeoff: - Adversarially trained models are more robust but less accurate on clean data (typically 1–10% drop) - Defense mechanisms (input smoothing, randomized smoothing) add latency - Stronger privacy guarantees (smaller ε in DP) reduce model utility\nThe interpretability-performance tradeoff: - Complex models (deep nets, ensembles) generally outperform interpretable models - Requiring interpretability may constrain model choice (e.g., using a decision tree instead of a neural network) - Post-hoc explanations partially resolve this by explaining complex models — but with reduced fidelity\nThe interpretability-security tradeoff: - Highly interpretable models are more vulnerable to adversarial attack (attacker can understand the model’s decision boundary) - Obfuscating model internals can be a weak security measure (security through obscurity is not sufficient) - SHAP explanations can be manipulated: a model can produce biased decisions while showing fair explanations on a scrutiny dataset\nThe privacy-utility tradeoff: - Stronger data protection (more noise in DP, more anonymization) → less accurate models - Federated learning preserves privacy but convergence is slower and accuracy may suffer - De-identification reduces dataset richness\nDecision framework for high-risk contexts: 1. Identify the most critical risk: safety failure, privacy breach, discrimination, or adversarial attack 2. Prioritize the mitigation for the highest-risk failure mode 3. Quantify the tradeoff: how much accuracy/interpretability do you sacrifice? 4. Document the decision and the rationale 5. Define monitoring and escalation procedures\nExample high-risk tradeoff (medical AI): - Use an interpretable model (logistic regression) even if a neural network would be more accurate, because clinicians need to verify the model’s reasoning before acting - Accept the accuracy cost in exchange for clinical trust and regulatory compliance - Apply differential privacy even if it slightly reduces model performance, because patient privacy is legally and ethically paramount"
  },
  {
    "objectID": "Resources/jspuzz/jspuzz.html",
    "href": "Resources/jspuzz/jspuzz.html",
    "title": "Subtiles 2",
    "section": "",
    "text": "Puzzle\nFirst of all grid contains \\(169\\) cells, assuming somehow we can fill all the cells, we get \\(n*(n+1)&lt;=169*2\\) where we fill numbers unto \\(n.\\) This gives us value that \\(n\\) has to be \\(17\\) since \\(n\\) must be integer. Thus we know that all the equations in the grid should resolve to a number between \\(1\\) to \\(17.\\) Now we need to solve them for the same.\nFew equations that I primaraily take cue from are square roots, log and powers. Since \\(b^2\\) is a cell, \\(b\\) has to an integer among \\(\\pm{1},\\pm{2},\\pm{3}, \\pm{4}.\\) but \\((b-1)^2\\) removes \\(-4\\) from consideration.\nBecause \\(a-c\\) is denominator \\(a!=c.\\) \\(c-1\\) is denominator so \\(c!=1.\\) Now there is a cell \\(\\sqrt{c-a}\\) which means \\(c&gt;a\\) and in another cell where value is \\(b/(a-c),\\) \\(a-c\\) is denominator which is negative, so to make cell value positive, we need to eliminate positive possibilities of \\(b.\\) So \\(b\\) is now \\(-1/-2/-3.\\)\nNow there is a element \\(c^b\\) and \\(b\\) is negative integer which suggests \\(c\\) has to be of form \\(1/d\\) where \\(d\\) is integer \\(&gt;1.\\) and since we have a cell with \\(\\log_{c}{a}\\) gives away that \\(a=c^k\\) where \\(k\\) is an integer among \\(2\\) to \\(17\\) beacuse \\(c&gt;a.\\) There is a cell \\(\\frac{\\sqrt{a+2}}{a}\\) which means \\(a\\) is positive implying \\(c\\) is also positive.\nThere is a cell with \\(a^b - 12/a\\) where if \\(b=-1,\\) we have \\(-11/a\\) which won’t work. So remove \\(b=-1.\\) Now there is also \\(b/a-1.\\) Which means if we put \\(a\\) as \\(c^k = (1/d)^k\\) we get \\(-b*d^k/(d^k-1).\\) Now, let’s take \\(b=-2\\) so \\(2*d^k/(d^k-1)\\) has to be an integer –&gt; \\(d^k\\) has to be \\(2\\) or \\(3\\) and with current constraints on \\(d\\) and \\(k\\) we cannot have it. So \\(b=-2\\) has to be ruled out. Only \\(b=-3\\) is left. So we have here \\(3*d^k/(d^k-1)\\) so \\(d^k\\) should be either \\(2\\) or \\(4.\\) \\(d^k=2\\) won’t work so \\(d^k=4\\) and deterministicly we can conclude that \\(d=2\\) and \\(k=2.\\) Thus \\(c = 1/2\\) and \\(a = 1/4.\\)\n\\(a= 1/4, b=-3, c=1/2.\\) We can now fill labeled cells.\n\n\n\n\n\n\n\n\nC1\n\n\nC2\n\n\nC3\n\n\nC4\n\n\nC5\n\n\nC6\n\n\nC7\n\n\nC8\n\n\nC9\n\n\nC10\n\n\nC11\n\n\nC12\n\n\nC13\n\n\n\n\n\n\n\n\nFirst apporach that comes to my mind was to just code this. We can precompute all \\(K\\)-ominos and can link them with \\(K+1\\) as well as \\(K-1\\)-ominos as it is just adding or removing one of the cell. Then we know at least one cell allocated to each number, we put all variations of \\(K\\)-ominos in that cell which can be base of \\(K+1\\)-omino we selected, and only those which satisfy conditions like not overlap with any other existing number we pick.\nAnother way I can see this unfolds is deducing a bunch of things. Now we have values at \\(37\\) cells. \\(R4C11\\) has to be \\(12\\) otherwise we will cut \\(11s\\) or \\(6s.\\) Similarly,\\(R11C6\\) has to \\(14.\\) Now just to connect exisiting \\(12\\) cells we must exhaust all remaining \\(12s.\\) There are only \\(2\\) ways to do this. (In 3rd way we cut off \\(6s\\)). But we can surely confirm \\(R5,\\)\\(R6\\) in \\(C10\\) to be \\(12.\\) So now table becomes\n\n\n\n\n\n\n\n\nC1\n\n\nC2\n\n\nC3\n\n\nC4\n\n\nC5\n\n\nC6\n\n\nC7\n\n\nC8\n\n\nC9\n\n\nC10\n\n\nC11\n\n\nC12\n\n\nC13\n\n\n\n\n\n\nR1\n\n\n\n\n\n\n\n\n\n\n15\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR2\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n11\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR3\n\n\n\n\n15\n\n\n\n\n5\n\n\n\n\n\n\n15\n\n\n\n\n11\n\n\n\n\n11\n\n\n\n\n\n\n\n\nR4\n\n\n\n\n\n\n\n\n\n\n15\n\n\n\n\n\n\n8\n\n\n\n\n12\n\n\n12\n\n\n12\n\n\n\n\n\n\nR5\n\n\n\n\n16\n\n\n\n\n\n\n\n\n8\n\n\n\n\n\n\n\n\n12\n\n\n6\n\n\n\n\n\n\n\n\nR6\n\n\n\n\n\n\n\n\n16\n\n\n\n\n\n\n\n\n\n\n\n\n12\n\n\n\n\n\n\n6\n\n\n\n\nR7\n\n\n\n\n\n\n16\n\n\n\n\n3\n\n\n\n\n16\n\n\n\n\n1\n\n\n\n\n12\n\n\n\n\n\n\n\n\nR8\n\n\n13\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n4\n\n\n12\n\n\n\n\n\n\n\n\nR9\n\n\n\n\n\n\n7\n\n\n\n\n\n\n\n\n\n\n12\n\n\n12\n\n\n12\n\n\n12\n\n\n10\n\n\n\n\n\n\nR10\n\n\n\n\n2\n\n\n\n\n13\n\n\n\n\n16\n\n\n\n\n\n\n14\n\n\n\n\n\n\n\n\n\n\n\n\nR11\n\n\n\n\n\n\n13\n\n\n\n\n14\n\n\n14\n\n\n14\n\n\n\n\n\n\n14\n\n\n\n\n10\n\n\n\n\n\n\nR12\n\n\n\n\n\n\n\n\n\n\n\n\n9\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR13\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n9\n\n\n\n\n\n\n\n\n\n\n\n\n\nNow we can choose \\(R9\\) or \\(C8\\) to put three more \\(12s.\\) But choosing \\(C8,\\) will make it impossible for \\(11\\) to make anything like it without isolating other numbers. So we must put them in \\(R9.\\) Now in \\(R8C11\\) also we should put 12.\nNow with help of current structure of \\(12\\)-omino, we know structure of \\(13\\)-omino will be have this structure. We already have \\(3\\) \\(13s\\) given. Now we can use either \\(R8\\) or \\(C1.\\) But using \\(C1\\) will cause issue in every possible scenario.\nAfter this Now, \\(2\\) cannot use \\(R10C1\\) or \\(R9C2\\) as \\(7\\)s will have issue. \\(7\\) has to use at least the below marked cells. So we get the following.\n\n\n\n\n\n\n\nC1\n\n\nC2\n\n\nC3\n\n\nC4\n\n\nC5\n\n\nC6\n\n\nC7\n\n\nC8\n\n\nC9\n\n\nC10\n\n\nC11\n\n\nC12\n\n\nC13\n\n\n\n\n\n\nR1\n\n\n\n\n\n\n\n\n\n\n15\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR2\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n11\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR3\n\n\n\n\n15\n\n\n\n\n5\n\n\n\n\n\n\n15\n\n\n\n\n11\n\n\n\n\n11\n\n\n\n\n\n\n\n\nR4\n\n\n\n\n\n\n\n\n\n\n15\n\n\n\n\n\n\n8\n\n\n\n\n12\n\n\n12\n\n\n12\n\n\n\n\n\n\nR5\n\n\n\n\n16\n\n\n\n\n\n\n\n\n8\n\n\n\n\n\n\n\n\n12\n\n\n6\n\n\n\n\n\n\n\n\nR6\n\n\n\n\n\n\n\n\n16\n\n\n\n\n\n\n\n\n\n\n\n\n12\n\n\n\n\n\n\n6\n\n\n\n\nR7\n\n\n\n\n\n\n16\n\n\n\n\n3\n\n\n\n\n16\n\n\n\n\n1\n\n\n\n\n12\n\n\n\n\n\n\n\n\nR8\n\n\n13\n\n\n13\n\n\n13\n\n\n13\n\n\n\n\n\n\n\n\n\n\n\n\n4\n\n\n12\n\n\n\n\n\n\n\n\nR9\n\n\n7\n\n\n7\n\n\n7\n\n\n13\n\n\n\n\n\n\n\n\n12\n\n\n12\n\n\n12\n\n\n12\n\n\n10\n\n\n\n\n\n\nR10\n\n\n7\n\n\n2\n\n\n\n\n13\n\n\n\n\n16\n\n\n\n\n\n\n14\n\n\n\n\n\n\n\n\n\n\n\n\nR11\n\n\n7\n\n\n\n\n13\n\n\n\n\n14\n\n\n14\n\n\n14\n\n\n\n\n\n\n14\n\n\n\n\n10\n\n\n\n\n\n\nR12\n\n\n\n\n\n\n13\n\n\n\n\n\n\n9\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nR13\n\n\n\n\n\n\n13\n\n\n13\n\n\n13\n\n\n\n\n\n\n\n\n9\n\n\n\n\n\n\n\n\n\n\n\n\n\ncontinuing thinking along these lines gives us the final grid. Please refer Solution. Now, cacluating max and min row sum and multipying gives us the final answer.\nI want you to refer to original Jane Street Website, but in case it is down, please contact me, I will add the original puzzle."
  },
  {
    "objectID": "Resources/Seniors-talk/index.html",
    "href": "Resources/Seniors-talk/index.html",
    "title": "Being Internship Ready",
    "section": "",
    "text": "What did Seniors advise??\n\nAs I sat with my seniors, they shared their unique perspectives on preparing for interviews. I was reminded of the quote, “The secret of getting ahead is getting started.”\nDeepkamal, a seasoned ICPC World Finalist in his 4th year, emphasized the significance of Data Structures and Algorithms (DSA) for job placements. He mentioned that Competitive Programming (CP) isn’t obligatory but stressed the utmost importance of DSA. He advised against any form of cheating and suggested using Leetcode as the best approach to excel. Additionally, he highlighted the importance of a strong grasp of Database Management Systems (DBMS) and recommended Interviewbit as a well-structured preparation platform. He cautioned against falling for the trap of paid courses and suggested that 30 days are adequate for Interviewbit preparation.\nJainil recommends focusing on writing clean and precise code and emphasizes the importance of algorithms for machine learning enthusiasts. He suggests not immediately looking for a solution when you’re a beginner and recommends spending a maximum of an hour on a problem as you progress. He also advises against doing competitive programming just for the sake of it and stresses the importance of being able to explain your solutions in interviews. Jainil also suggests preparing well for database management systems, object-oriented programming, and being able to explain your projects and introductions. He recommends focusing on the “why” factor in projects. Additionally, Jainil advises against copying projects and suggests preparing a questionnaire in case you face tough questions during an interview.\nKushan was really into certificates and preferred paper and research internships over corporate jobs. He had a published paper with Nagendra Sir and a strong GRE score. He recommended focusing on strong SOP and LOR for Masters and having a good CPI.\nSruthi stressed having some dream companies as they keep up your motivation. Satya Bhaiya, who happens to be the placement manager from the CSE department, coordinated this discussion. He also explained how to prepare a good CV and resume. From my year, Mihir Patel had doubts about consulting and was looking at ISB as an option. As the conversation progressed, the importance of mock interviews and solving puzzles in Interview-bit became apparent. Sound basics of C/C++ also seemed to be a crucial skill. All the seniors thoroughly answered my doubts regarding R&D roles. They advised ML enthusiasts, “You can start working in startups or go for a research internship.”\nListening to these bright minds and their journey, the ups and downs they have gone through, I could not help but think of the famous quote - “The only impossible journey is the one you never begin.” These words echoed in my mind as I left the session after a photograph with all the seniors, ready to start preparing for my own interviews.\nSince my notes were very brief, I used ChatGPT to curate this blog. You can also find this blog on medium"
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html",
    "href": "Resources/Core5/Deployment/index.html",
    "title": "VLM Deployment",
    "section": "",
    "text": "Once a VLM is trained, the hard part begins: training it at scale across many GPUs and many nodes, then squeezing it into a phone, a smart-TV, or a kiosk while keeping latency low and accuracy intact. This document covers the full lifecycle: distributed training, communication tuning, edge-export formats, Tizen / mobile deployment, monitoring, and forensic troubleshooting.\n\n\n\n\nThe chapters are ordered the way a deployment engineer actually encounters them.\n\nConfiguring Multi-GPU Training\nManaging Multi-Node Training\nOptimizing Communication Overhead\nConverting Models to Edge-optimized Formats\nOptimizing for Tizen Platform\nImplementing Mobile Inference\nMonitoring Deployment Performance\nTroubleshooting Optimized Models\nReading List & References\n\nEvery section gives mental model + PyTorch code + war-story-tier tips. The references at the end are the canonical sources you should cite in interviews and design reviews.\n\n\n\n\n\n\n\nBefore any code, you must know what you are sharding.\n\n\n\n\n\n\n\n\nParallelism\nWhat is split across devices?\nBest for\n\n\n\n\nData Parallel (DP / DDP)\nMini-batch\nModel fits on one GPU\n\n\nZeRO-1\nOptimizer states\nDP + memory savings\n\n\nZeRO-2\nOptimizer states + grads\nMore memory savings\n\n\nZeRO-3 / FSDP\nOptimizer + grads + params\nModels too big for one GPU\n\n\nTensor Parallel (TP)\nSingle layer’s matrices\nLayers too big for one GPU\n\n\nPipeline Parallel (PP)\nWhole stages of layers\nVery deep models\n\n\nSequence / Context Parallel (SP/CP)\nThe sequence dimension\nVery long contexts\n\n\nExpert Parallel (EP)\nMoE experts\nMoE models\n\n\n\nModern VLMs use a combination — typically FSDP + TP, with SP for long-video models.\n\n\n\nDDP replicates the full model on every GPU and averages gradients via NCCL all-reduce after each backward.\n# launch with:  torchrun --nproc_per_node=8 train.py\nimport os, torch, torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\nfrom torch.utils.data import DataLoader, DistributedSampler\n\ndef setup():\n    dist.init_process_group(backend=\"nccl\")\n    local_rank = int(os.environ[\"LOCAL_RANK\"])\n    torch.cuda.set_device(local_rank)\n    return local_rank\n\nlocal_rank = setup()\nmodel = build_vlm().to(local_rank)\nmodel = DDP(model, device_ids=[local_rank], find_unused_parameters=False,\n            gradient_as_bucket_view=True, broadcast_buffers=False)\n\nsampler = DistributedSampler(dataset, shuffle=True, drop_last=True)\nloader  = DataLoader(dataset, batch_size=8, sampler=sampler,\n                     num_workers=4, pin_memory=True, persistent_workers=True)\n\nopt    = torch.optim.AdamW(model.parameters(), lr=1e-4)\nscaler = torch.amp.GradScaler(\"cuda\")\n\nfor epoch in range(EPOCHS):\n    sampler.set_epoch(epoch)                     # different shuffle every epoch\n    for batch in loader:\n        with torch.autocast(\"cuda\", dtype=torch.bfloat16):\n            loss = model(**batch).loss\n        loss.backward()                          # all-reduce happens here\n        opt.step(); opt.zero_grad(set_to_none=True)\nFive DDP details that always come up:\n\ndevice_ids must contain a single device — the local rank.\nfind_unused_parameters=True is required if any parameter is conditionally skipped in forward (e.g. a frozen LoRA branch). It costs CPU time; flip it off whenever possible.\ngradient_as_bucket_view=True saves memory by giving you a view into the all-reduce bucket buffer instead of allocating new gradient tensors.\nbroadcast_buffers=False turns off the all-reduce of BatchNorm running stats — useless for ViTs/LLMs which use LayerNorm only.\nset_to_none=True in zero_grad() keeps the gradient pointer null instead of a zero tensor, slightly faster.\n\n\n\n\nTo simulate a larger batch when memory is tight:\naccum = 4\nopt.zero_grad(set_to_none=True)\nfor step, batch in enumerate(loader):\n    is_last = (step + 1) % accum == 0\n    ctx = model.no_sync() if not is_last else nullcontext()\n    with ctx:\n        with torch.autocast(\"cuda\", dtype=torch.bfloat16):\n            loss = model(**batch).loss / accum\n        loss.backward()\n    if is_last:\n        opt.step(); opt.zero_grad(set_to_none=True)\nmodel.no_sync() skips the all-reduce during the inner accumulation steps — communication cost goes down by accum. Forget this and you will all-reduce on every micro-batch, paying the full network cost while gaining no math.\n\n\n\nWhen the model does not fit on a single GPU, shard everything: parameters, gradients, optimizer states.\nimport functools\nfrom torch.distributed.fsdp import (\n    FullyShardedDataParallel as FSDP,\n    MixedPrecision, ShardingStrategy, BackwardPrefetch,\n)\nfrom torch.distributed.fsdp.wrap import transformer_auto_wrap_policy\nfrom transformers.models.qwen2.modeling_qwen2 import Qwen2DecoderLayer\n\nmp = MixedPrecision(\n    param_dtype=torch.bfloat16,\n    reduce_dtype=torch.bfloat16,\n    buffer_dtype=torch.bfloat16,\n)\n\nwrap_policy = functools.partial(\n    transformer_auto_wrap_policy,\n    transformer_layer_cls={Qwen2DecoderLayer},   # one shard per LLM block\n)\n\nmodel = FSDP(\n    model,\n    sharding_strategy=ShardingStrategy.FULL_SHARD,         # ZeRO-3\n    auto_wrap_policy=wrap_policy,\n    mixed_precision=mp,\n    backward_prefetch=BackwardPrefetch.BACKWARD_PRE,       # overlap comm\n    limit_all_gathers=True,\n    device_id=torch.cuda.current_device(),\n    use_orig_params=True,                                  # required for ParamGroups\n)\nSharding strategies:\n\nFULL_SHARD — ZeRO-3 (params + grads + opt states sharded).\nSHARD_GRAD_OP — ZeRO-2 (grads + opt states only).\nHYBRID_SHARD — full-shard inside a node, replicate across nodes (best for 2-node setups with fat intra-node NVLink and thin inter-node Ethernet).\nNO_SHARD — equivalent to DDP.\n\nFSDP2 (PyTorch ≥ 2.4) introduces fully_shard() as the preferred API — per-parameter sharding, cleaner ergonomics, full DTensor-native. Same conceptual model, nicer code.\n\n\n\n\n\n\n\n\n\n\n\n\nFormat\nRange\nPrecision\nWhere to use\n\n\n\n\nfp16\n±65 504\n~3 decimal\nInference, V100\n\n\nbf16\n±\\(3.4\\times 10^{38}\\)\n~2 decimal\nDefault training on A100/H100\n\n\nfp8 (e4m3 / e5m2)\n±448 / ±57 344\n~1 decimal\nH100 only, large speedup\n\n\n\nbf16 is the de facto training dtype because it has the same exponent range as fp32 — no GradScaler required, no overflow surprises. fp16 only stays alive on V100s that have no bf16 hardware. fp8 needs the Transformer Engine library (pip install transformer_engine) and explicit scaling factors.\n\n\n\nRecompute activations during backward instead of storing them — trades FLOPs for memory.\nfrom torch.distributed.algorithms._checkpoint.checkpoint_wrapper import (\n    apply_activation_checkpointing,\n    CheckpointWrapper,\n    CheckpointImpl,\n)\n\ndef check_fn(submodule):\n    return isinstance(submodule, Qwen2DecoderLayer)\n\napply_activation_checkpointing(\n    model,\n    checkpoint_wrapper_fn=functools.partial(\n        CheckpointWrapper,\n        checkpoint_impl=CheckpointImpl.NO_REENTRANT,\n    ),\n    check_fn=check_fn,\n)\nNO_REENTRANT is the modern variant and integrates cleanly with FSDP. Saves ~\\(O(L)\\) memory at the cost of one extra forward.\n\n\n\nWhen a single transformer block does not fit on a GPU (e.g. 70 B+), split the matrices themselves:\n\nColumn-wise split on W_q, W_k, W_v and W_up, W_gate of MLP.\nRow-wise split on W_o and W_down.\nAll-gather along the head dimension before the row-parallel layer.\n\nIn PyTorch (≥ 2.3):\nfrom torch.distributed.tensor.parallel import (\n    parallelize_module, ColwiseParallel, RowwiseParallel, SequenceParallel,\n)\nfrom torch.distributed.device_mesh import init_device_mesh\n\nmesh = init_device_mesh(\"cuda\", (DP, TP), mesh_dim_names=(\"dp\",\"tp\"))\ntp_mesh = mesh[\"tp\"]\n\nplan = {\n    \"self_attn.q_proj\": ColwiseParallel(),\n    \"self_attn.k_proj\": ColwiseParallel(),\n    \"self_attn.v_proj\": ColwiseParallel(),\n    \"self_attn.o_proj\": RowwiseParallel(),\n    \"mlp.gate_proj\":  ColwiseParallel(),\n    \"mlp.up_proj\":    ColwiseParallel(),\n    \"mlp.down_proj\":  RowwiseParallel(),\n}\nfor blk in model.model.layers:\n    parallelize_module(blk, tp_mesh, plan)\nSequence parallel additionally splits LayerNorm activations along the sequence axis between TP collective calls — relevant when sequences are very long (video, long-context VLMs).\n\n\n\nPipeline parallel (PP) splits the model into stages and pipelines micro-batches through them. PyTorch’s torch.distributed.pipelining (PiPPy) does this cleanly. PP shines when:\n\nModel is deep (≥ 80 layers).\nInter-node bandwidth is the bottleneck (PP communicates only activations at stage boundaries, not gradients across all params).\n\nFor 7-13 B VLMs, PP is overkill; FSDP + TP is enough.\n\n\n\n\n\n\n\n\n\n\n\nSize\nSingle node 8×H100 80 GB\nMulti-node\n\n\n\n\n1–3 B\nDDP + bf16\nseldom needed\n\n\n7–13 B\nFSDP FULL_SHARD + activation ckpt\nHYBRID_SHARD across 2 nodes\n\n\n30–70 B\nFSDP + TP=2 + activation ckpt\nFSDP + TP=4 across N nodes\n\n\n100 B+\nFSDP + TP=8 + PP=2/4\nscale TP first, then PP\n\n\n\n\n\n\n\n\n\n\n\nInside a node, GPUs talk over NVLink/NVSwitch — 600–900 GB/s on A100/H100 nodes.\nAcross nodes, they go over Ethernet (25–100 Gbit/s) or InfiniBand (200–400 Gbit/s, ~5× faster). The bandwidth difference is 6–30×; the latency difference is even larger. Every architectural decision in multi-node training is a workaround for this fact.\n\n\n\ntorchrun (PyTorch’s own launcher, supersedes torch.distributed.launch):\n# Node 0 (master):\ntorchrun \\\n  --nnodes=4 --nproc_per_node=8 \\\n  --node_rank=0 --rdzv_id=vlm_run01 --rdzv_backend=c10d \\\n  --rdzv_endpoint=node0.cluster:29500 train.py\n\n# Node 1..3: same command with --node_rank=1, 2, 3\ntorchrun is elastic by default — workers can join and leave thanks to the c10d rendezvous backend.\nSLURM (srun) — the dominant academic / national-lab scheduler:\n#!/bin/bash\n#SBATCH --job-name=vlm\n#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=8\n#SBATCH --gpus-per-task=1\n#SBATCH --cpus-per-task=16\n#SBATCH --time=24:00:00\n#SBATCH --partition=h100\n\nexport MASTER_ADDR=$(scontrol show hostnames \"$SLURM_JOB_NODELIST\" | head -n1)\nexport MASTER_PORT=29500\nexport OMP_NUM_THREADS=8\nexport NCCL_DEBUG=WARN\n\nsrun python -u train.py\nInside train.py you read RANK, LOCAL_RANK, WORLD_SIZE from environment variables that SLURM/torchrun set automatically.\n\n\n\nimport os, torch, torch.distributed as dist\n\ndef setup():\n    if \"RANK\" in os.environ and \"WORLD_SIZE\" in os.environ:\n        dist.init_process_group(backend=\"nccl\")\n    else:\n        # SLURM path\n        rank   = int(os.environ[\"SLURM_PROCID\"])\n        world  = int(os.environ[\"SLURM_NTASKS\"])\n        local  = int(os.environ[\"SLURM_LOCALID\"])\n        dist.init_process_group(backend=\"nccl\", rank=rank, world_size=world,\n                                init_method=f\"tcp://{os.environ['MASTER_ADDR']}:{os.environ['MASTER_PORT']}\")\n        os.environ[\"LOCAL_RANK\"] = str(local)\n    torch.cuda.set_device(int(os.environ[\"LOCAL_RANK\"]))\n\n\n\nNCCL is NVIDIA’s collective communication library; PyTorch’s nccl backend is a thin wrapper. The collectives you need to know:\n\nall_reduce — every rank ends up with the sum (or other op) of every input. Used after loss.backward() to average gradients.\nall_gather — every rank collects all inputs concatenated. Used by FSDP to materialise full parameters before forward.\nreduce_scatter — sum, then scatter chunks back. Used by FSDP for gradients.\nbroadcast — one rank’s tensor goes to all others. Used to seed initial weights and to share the schedule.\nbarrier — synchronisation point.\n\nNCCL chooses among several algorithms:\n\n\n\n\n\n\n\n\n\nAlgorithm\nBandwidth-optimal?\nLatency\nNotes\n\n\n\n\nRing\nYes for large messages\n\\(O(P)\\) steps\nclassic\n\n\nTree\nBetter latency\n\\(O(\\log P)\\)\nnewer default for small msgs\n\n\nCollNet (SHARP)\nHardware-accelerated on Mellanox switches\nvery low\nInfiniBand only\n\n\nNVLS (NVLink SHARP)\nIntra-node H100 NVSwitch\nvery low\nDGX H100 only\n\n\n\nYou can profile NCCL with NCCL_DEBUG=INFO; it prints the algorithm chosen for each collective.\n\n\n\nA simple mental picture: an 8×H100 DGX node looks like\n    GPU0—NVLink—GPU1—NVLink—GPU2—…\n       \\        /\n        NVSwitch (intra-node, 900 GB/s pairwise)\n       /        \\\n   GPU4    …   GPU7\n   |                  |\n   NIC0  NIC1  NIC2  NIC3  (InfiniBand HCAs, 1:1 with GPUs)\n       \\    |    |    /\n        switch fabric (200–400 Gbit/s per port)\nFor best inter-node performance you must:\n\nUse GPUDirect RDMA (NICs read GPU memory directly).\nPin each GPU process to the NUMA node owning its HCA via numactl.\nEnable NCCL_IB_HCA to use the right HCA.\nSet NCCL_IB_GID_INDEX=3 for RoCE v2, NCCL_IB_GID_INDEX=0 for IB.\n\n\n\n\nexport NCCL_DEBUG=WARN                  # INFO for first run, WARN afterwards\nexport NCCL_ASYNC_ERROR_HANDLING=1      # crash on collective error\nexport NCCL_SOCKET_IFNAME=^lo,docker0   # which NIC for bootstrap\nexport NCCL_IB_HCA=mlx5_0,mlx5_2        # which HCAs to use\nexport NCCL_IB_TIMEOUT=22               # higher for flaky fabrics\nexport NCCL_IB_RETRY_CNT=7\nexport NCCL_NET_GDR_LEVEL=5             # max GPUDirect RDMA\nexport NCCL_P2P_LEVEL=NVL               # force NVLink intra-node\nexport NCCL_NVLS_ENABLE=1               # H100 NVSwitch SHARP\nexport NCCL_ALGO=Tree,Ring              # let NCCL choose\nexport NCCL_PROTO=Simple,LL,LL128       # protocols, lowest latency to highest BW\nexport OMP_NUM_THREADS=8                # avoid CPU oversubscription\n\n\n\nA 64-GPU job runs for a week and hits a memory error. What do you do?\n\nCheckpoint frequently (every 250–1000 steps) but asynchronously (next chapter).\ntorchrun elastic restarts crashed workers automatically.\ntorch.distributed.elastic monitors and signals workers.\nTorchSnapshot / Distributed Checkpoint (DCP) writes sharded checkpoints in parallel — orders of magnitude faster than gathering everything to rank 0.\n\nimport torch.distributed.checkpoint as dcp\n\nstate = {\"model\": model.state_dict(), \"opt\": opt.state_dict(), \"step\": step}\ndcp.save(state, checkpoint_id=f\"/ckpt/step_{step}\")\nEach rank writes its own shard; loading reverses the process. DCP is the default in PyTorch 2.4+.\n\n\n\nA 64-GPU job demands ~64× the data throughput of a single GPU. Best practices:\n\nSharded data: WebDataset or MosaicML Streaming format — many tar shards on object store, each rank streams a disjoint set.\nnum_workers ≈ 4 × CPU/GPU with pin_memory=True.\npersistent_workers=True to avoid re-spawning every epoch.\nprefetch_factor=2 to overlap CPU and GPU.\nImage decoding on GPU with NVIDIA DALI for VLMs — JPEG → tensor is often the bottleneck.\n\n\n\n\n\n\n\n\n\nFor a parameter \\(P\\), an all-reduce takes time\n\\[\nT_{\\text{ar}}(P) \\approx \\alpha + \\beta \\cdot 2 \\frac{N-1}{N} \\cdot P\n\\]\nwhere \\(\\alpha\\) is latency, \\(\\beta = 1/\\text{bandwidth}\\), and the factor \\(\\frac{2(N-1)}{N}\\) comes from the ring-all-reduce algorithm. For \\(N\\) large, this becomes \\(2P/B\\) — twice the parameter bytes over the network.\nA single bf16 7 B model has \\(2 \\cdot 7\\text{B} = 14\\) GB. On a 100 Gbit/s NIC = 12.5 GB/s, one all-reduce takes 1.1 seconds. Compare to ~0.4 seconds of compute for the backward pass → communication dominates unless we hide it.\n\n\n\nPyTorch DDP automatically buckets gradients and starts all-reducing the bucket as soon as it is filled — overlap with the rest of the backward pass. Two knobs:\n\ngradient_as_bucket_view=True — saves memory.\nbucket_cap_mb (default 25 MB) — larger buckets = fewer collectives but less overlap. For 7-13 B models, 50-100 MB is often optimal.\n\nFor FSDP, set backward_prefetch=BackwardPrefetch.BACKWARD_PRE so the next shard’s parameters are all-gathered while you compute the current backward. The matching forward_prefetch=True does the analogous thing in forward.\n\n\n\nTrade communication for accuracy:\n\nfp16 all-reduce with MixedPrecision(reduce_dtype=torch.float16) — halves bytes.\nPowerSGD (Vogels et al., 2019) — rank-r low-rank approximation. Built into PyTorch DDP.\n\nimport torch.distributed.algorithms.ddp_comm_hooks.powerSGD_hook as ps\nstate = ps.PowerSGDState(process_group=None, matrix_approximation_rank=4,\n                         start_powerSGD_iter=1_000)\nmodel.register_comm_hook(state, ps.powerSGD_hook)\nPowerSGD is essentially free for many models after a warm-up period.\n\n\n\nDDP reduces buckets in the order parameters are registered. To maximise overlap, register parameters in forward order (default if you wrap a nn.Module directly).\nFor FSDP, set the auto-wrap policy at the right granularity — each FSDP unit is a single all-gather. Too many small units = collective overhead; too few large units = no overlap.\n\n\n\nZeRO-3 / FSDP FULL_SHARD has the lowest memory but the most communication: every parameter is gathered and re-discarded once per forward + once per backward. ZeRO-2 (SHARD_GRAD_OP) gathers parameters once (they are kept) but still scatters gradients. Choose:\n\nMemory-bound? → ZeRO-3.\nCommunication-bound? → ZeRO-2 or HYBRID_SHARD.\n\nHYBRID_SHARD is the sweet spot for 2–4 node setups: full-shard inside the fast NVLink island, replicate across the slow Ethernet/IB.\n\n\n\nWhen you use TP and SP, you must overlap the all-gather of activations before each row-parallel matmul with the previous compute. PyTorch’s parallelize_module does this automatically if you pass use_local_output=False.\n\n\n\nA few real-world tunes:\n\nFor a 2-node A100-40 GB setup with 200 Gbit/s IB, NCCL_ALGO=Ring NCCL_PROTO=LL128 gives best throughput for 1 GB messages.\nFor an H100 NVL node, NCCL_NVLS_ENABLE=1 cuts intra-node all-reduce by ~30 %.\nFor noisy fabrics, NCCL_IB_TIMEOUT=22 NCCL_IB_RETRY_CNT=7 avoids spurious crashes; pair with elastic restart.\n\n\n\n\nThree tools, all free:\n\ntorch.profiler — annotate ranges, dump Chrome trace, inspect collective calls.\nNVIDIA Nsight Systems (nsys) — system-wide GPU + NCCL traces.\nNCCL_DEBUG=INFO logs + the nccl-tests benchmark — measure raw all-reduce bandwidth.\n\nfrom torch.profiler import profile, ProfilerActivity, schedule\n\nwith profile(\n    activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],\n    schedule=schedule(wait=1, warmup=1, active=3, repeat=1),\n    on_trace_ready=lambda p: p.export_chrome_trace(f\"trace_{dist.get_rank()}.json\"),\n    record_shapes=True, with_stack=True,\n) as p:\n    for step in range(5):\n        ...\n        p.step()\nOpen the trace in chrome://tracing; look for nccl:all_reduce rows that don’t overlap the compute rows above them. Anything not overlapped is wasted time.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nFormat\nTarget runtime\nStrengths\nNotes\n\n\n\n\nTorchScript\nLibTorch C++\nDrop-in from PyTorch\nLegacy in 2026; mostly replaced by ExecuTorch\n\n\nExecuTorch (.pte)\nExecuTorch runtime (mobile, embedded)\nNative PyTorch path, partial-backend execution\nDefault for new PyTorch mobile in 2024+\n\n\nONNX (.onnx)\nonnxruntime, TensorRT, OpenVINO\nCross-vendor IR\nOperator coverage gaps for new ops\n\n\nTensorRT (.engine)\nNVIDIA Jetson, Orin\nBest NVIDIA performance\nVendor-locked\n\n\nTFLite (.tflite)\nAndroid, Tizen, microcontrollers\nMature mobile ecosystem\nNeeds conversion via ONNX or AI-Edge-Torch\n\n\nCoreML (.mlpackage)\niOS / macOS\nNeural Engine acceleration\nApple-only\n\n\nOpenVINO IR (.xml+.bin)\nIntel CPUs, NPUs\nBest Intel performance\nx86 / NPU only\n\n\nGGUF\nllama.cpp\nQuantised LLM-only\nUsed for LLM backbone of VLMs\n\n\n\n\n\n\nPyTorch ≥ 2.2 introduced torch.export, which captures the model as an ExportedProgram — an FX graph with a sound, stable representation. This is the source format for both ExecuTorch and the modern ONNX exporter.\nimport torch\n\nexample_inputs = (torch.randn(1, 3, 384, 384),)\nep = torch.export.export(vision_model, example_inputs)\nprint(ep)                                  # ExportedProgram with input/output specs\ntorch.export.save(ep, \"vision.pt2\")\nDynamic shapes (variable resolution!) are declared explicitly:\nfrom torch.export import Dim\nH = Dim(\"H\", min=224, max=1024)\nW = Dim(\"W\", min=224, max=1024)\nep = torch.export.export(model, (torch.randn(1,3,384,384),),\n                         dynamic_shapes={\"pixel_values\": {2: H, 3: W}})\n\n\n\nimport torch\ntorch.onnx.export(\n    model, example_inputs, \"vlm.onnx\",\n    input_names=[\"pixel_values\",\"input_ids\"],\n    output_names=[\"logits\"],\n    dynamic_axes={\"pixel_values\": {0: \"B\", 2: \"H\", 3: \"W\"},\n                  \"input_ids\":   {0: \"B\", 1: \"T\"},\n                  \"logits\":      {0: \"B\", 1: \"T\"}},\n    opset_version=18,\n    dynamo=True,                       # the new exporter, FX-based\n)\nThe dynamo=True exporter is the future — it relies on torch.export under the hood and supports control flow and dynamic shapes far better than the legacy TorchScript-based exporter.\nValidate the ONNX model:\nimport onnxruntime as ort, numpy as np\nsess = ort.InferenceSession(\"vlm.onnx\", providers=[\"CUDAExecutionProvider\",\"CPUExecutionProvider\"])\nouts = sess.run(None, {\"pixel_values\": np.random.randn(1,3,384,384).astype(np.float32),\n                       \"input_ids\":   np.array([[1,2,3]], dtype=np.int64)})\nTwo things to verify every time:\n\nOutput deltas between torch_model(x) and onnx_runtime(x) should be &lt; 1e-3 in fp32 (&lt; 1e-2 in fp16). Anything larger is a buggy export.\nAll operators supported by your target runtime. Run onnx.checker.check_model(\"vlm.onnx\") and polygraphy inspect model vlm.onnx --mode=onnx to enumerate ops.\n\n\n\n\nExecuTorch is the successor to PyTorch Mobile. The pipeline:\ntorch.export → quantization → to_edge → backend partitioning → to_executorch → .pte\nfrom executorch.exir import to_edge\nfrom executorch.backends.xnnpack.partition.xnnpack_partitioner import XnnpackPartitioner\n\nep   = torch.export.export(model, example_inputs)\nedge = to_edge(ep)\nedge = edge.to_backend(XnnpackPartitioner())          # delegate ops to XNNPACK\nprog = edge.to_executorch()\nwith open(\"vlm.pte\", \"wb\") as f:\n    f.write(prog.buffer)\nBackends ship out-of-the-box: XNNPACK (CPU, ARM/x86), Vulkan (mobile GPU), CoreML, Qualcomm QNN (Hexagon NPU), MediaTek, Apple MPS.\nThe .pte binary is loaded by ExecuTorch’s C++/Java/Swift runtime; you ship one file per platform.\n\n\n\nPath of least resistance: ONNX → TensorRT via trtexec.\ntrtexec --onnx=vlm.onnx --saveEngine=vlm.engine \\\n        --fp16 --workspace=4096 \\\n        --minShapes=pixel_values:1x3x224x224,input_ids:1x16 \\\n        --optShapes=pixel_values:1x3x384x384,input_ids:1x64 \\\n        --maxShapes=pixel_values:1x3x768x768,input_ids:1x256\nFor Jetson Orin / AGX-Orin you compile the engine on the device (engines are device-specific). For data-center INT8/FP8, supply a calibration cache (see Optimization doc).\n\n\n\nimport coremltools as ct\nmlmodel = ct.convert(\n    \"vlm.onnx\",\n    convert_to=\"mlprogram\",\n    compute_units=ct.ComputeUnit.ALL,     # CPU + GPU + Neural Engine\n    minimum_deployment_target=ct.target.iOS17,\n)\nmlmodel.save(\"VLM.mlpackage\")\niOS 18 supports 8-bit weight quantisation natively and INT4 weight-only quantisation via ct.optimize.coreml.linear_quantize_weights.\n\n\n\nGoogle’s ai-edge-torch is the modern PyTorch → TFLite bridge:\nimport ai_edge_torch\nedge = ai_edge_torch.convert(model.eval(), sample_args)\nedge.export(\"vlm.tflite\")\nIt uses torch.export under the hood and handles dynamic shapes properly. The resulting .tflite runs in any TFLite runtime — Android, Tizen, embedded Linux.\n\n\n\nFor Intel CPUs / iGPUs / NPUs (Meteor Lake, Lunar Lake) you go via ONNX:\novc vlm.onnx --output_dir ovir/         # OpenVINO Model Converter\nInference:\nfrom openvino.runtime import Core\ncore = Core()\nmodel = core.read_model(\"ovir/vlm.xml\")\ncompiled = core.compile_model(model, device_name=\"NPU\")     # or \"GPU\", \"CPU\"\nOpenVINO is particularly strong for VLMs on AI PCs because the NPU has dedicated INT8 matmul hardware.\n\n\n\nBefore shipping any exported model, verify:\n\nNumerical parity vs PyTorch reference within tolerance.\nLatency measured per device (CPU, GPU, NPU) at target batch / shape.\nMemory peak under sustained load (some op kernels temporarily allocate huge buffers).\nCold-start time — engine load + first inference; users care about app launch.\nThermal behaviour — phones throttle after 30 s of sustained compute.\nPrivacy / on-device requirements — no network calls leaking inputs.\n\n\n\n\n\n\n\n\n\nTizen is Samsung’s Linux-based OS for TVs, smartwatches, washing machines, fridges, and Galaxy IoT devices. It has been the dominant Smart-TV OS since 2015 and ships in tens of millions of devices a year. Apps come in two flavours: native (C/C++ using Tizen ML APIs) and Web/Tizen .NET (JavaScript / C#). Tizen Studio is the IDE.\nFor VLM deployment the runtime you target is NNStreamer + TensorFlow Lite (with optional GPU/NPU delegates), surfaced via the Tizen ML API (formerly Inference Tizen API).\n\n\n\n   Application (C/C++, .NET, Web)\n         │\n   Tizen ML API  (high-level)\n         │\n   NNStreamer  (GStreamer plugin, pipeline-style ML)\n         │\n   ML inference backends\n   ├── TensorFlow Lite     (CPU, GPU, Hexagon, EdgeTPU)\n   ├── ONNX Runtime        (CPU, GPU, NPU)\n   ├── Snapdragon NPE\n   ├── Samsung ENN         (Exynos NPU)\n   └── OpenVINO            (Intel-based Tizen devices)\nNNStreamer is Samsung’s GStreamer-based ML pipeline framework. It treats an ML model as a filter (tensor_filter) in a streaming pipeline, with tensor data flowing as GstBuffers. This is perfect for VLM use-cases like TV captioning of live video — you connect a camera/video source to a vision-encoder filter, fan out to a Q-Former filter, fan out to an LLM filter, and write text to a UI sink.\n\n\n\n\nExport the vision encoder (e.g. SigLIP-So400m) to a small TFLite model, quantised INT8.\nExport the projector as a separate TFLite model (a few MB).\nExport the LLM to a quantised GGUF or TFLite-LLM (currently 4-bit AWQ/GPTQ is best).\nWire them with NNStreamer, with the LLM decoder running in a tensor_filter that supports KV-cache.\n\n\n\n\nA minimal C example using Tizen ML API:\n#include &lt;mv_inference.h&gt;\n#include &lt;mv_inference_type.h&gt;\n\nmv_engine_config_h cfg;\nmv_create_engine_config(&cfg);\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_MODEL_WEIGHT_FILE_PATH,\n                                      \"vision_int8.tflite\");\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_BACKEND_TYPE,\n                                      \"tflite\");\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_TARGET_DEVICE_TYPE,\n                                      \"npu\");           // or \"gpu\" / \"cpu\"\n\nmv_inference_h infer;\nmv_inference_create(&infer);\nmv_inference_configure(infer, cfg);\nmv_inference_prepare(infer);\n\nmv_source_h src;\nmv_create_source(&src);\nmv_source_fill_by_buffer(src, frame_buffer, frame_size, width, height, MEDIA_VISION_COLORSPACE_RGB888);\n\nmv_inference_image_classify(src, infer, NULL, callback, NULL);\nFor VLMs you typically need the lower-level inference-engine-tflite API to get the raw tensor outputs (because you fuse them with text), then feed them into the next stage.\n\n\n\ngst-launch-1.0 \\\n  v4l2src ! videoconvert ! videoscale ! \\\n  video/x-raw,format=RGB,width=384,height=384 ! \\\n  tensor_converter ! \\\n  tensor_filter framework=tensorflow-lite model=vision_int8.tflite custom=Delegate:NPU ! \\\n  tensor_filter framework=tensorflow-lite model=projector_int8.tflite ! \\\n  tensor_filter framework=tensorflow-lite model=llm_int4.tflite ! \\\n  tensor_decoder mode=detokenize ! \\\n  appsink\nThis pipeline does end-to-end video → caption entirely on the TV’s NPU.\n\n\n\nFor Galaxy phones and select Tizen devices, the ENN backend offers ~3–10× better performance than TFLite-GPU. You convert via Samsung’s ENN converter:\nenn_converter --input vision.onnx --output vision.nnc --quant int8 \\\n              --target exynos2400\nENN supports per-channel INT8 weight + per-tensor INT8 activation, and FP16 fallback for unsupported ops. The runtime is loaded by NNStreamer with framework=snpe-enn.\n\n\n\nA 2024 4K Tizen TV has ~3–4 GB total RAM, of which ~1 GB is available to apps. Practical VLM budget:\n\n\n\nComponent\nBudget\n\n\n\n\nVision encoder (INT8)\n80–150 MB\n\n\nProjector\n5–10 MB\n\n\nLLM 1–3 B (INT4)\n600–800 MB\n\n\nKV cache (for 1k tokens)\n100–200 MB\n\n\nMisc app\n100 MB\n\n\n\nThis means for TVs you typically pick a small LLM (Phi-3-mini-3.8B INT4 ≈ 1.9 GB → too big; Qwen2.5-1.5B-Instruct INT4 ≈ 800 MB → fits).\n\n\n\nTizen apps are killed aggressively. Strategies:\n\nPre-load models on app launch in a background thread.\nPin tensors in RAM by holding the mv_inference_h handle.\nLazy-load the LLM only when the user actually asks a question (vision encoder always on for ambient features).\n\n\n\n\nTizen Smart-TVs frequently process camera frames and microphone audio — Samsung’s Smart Hub privacy policy mandates that all VLM inference happens on-device unless the user explicitly opts in to cloud. Your deployment must run fully offline by default.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nPlatform\nCompute units\nFrameworks\nNotes\n\n\n\n\nAndroid (high-end)\nCPU (8 cores), GPU (Adreno/Mali), NPU (Hexagon, NPU0)\nExecuTorch, TFLite, ONNX-RT, MediaPipe, QNN\nHexagon HTP is the strongest mobile NPU\n\n\nAndroid (mid-tier)\nCPU + GPU only\nTFLite GPU/XNNPACK\nNPU may be absent\n\n\niOS\nCPU (4-8 cores), GPU, Apple Neural Engine (ANE)\nCoreML, ExecuTorch+CoreML, MLX, MPSGraph\nANE accepts mlprogram only\n\n\nApple Vision Pro\nM2 + R1\nCoreML\nReal-time spatial VLMs\n\n\n\nFor VLMs in 2026, the realistic on-device targets are 3–8 B LLM, 0.4 B vision encoder, INT4 weights, running 5–25 tokens/s.\n\n\n\nThe standard mobile path. Steps:\n\nExport the model (see §4.4).\nAdd the ExecuTorch AAR to your Android app:\nimplementation 'org.pytorch:executorch-android:0.4.0'\nLoad and run from Kotlin:\nimport org.pytorch.executorch.Module\nimport org.pytorch.executorch.EValue\nimport org.pytorch.executorch.Tensor\n\nval module = Module.load(assetFilePath(this, \"vlm.pte\"))\nval pix = Tensor.fromBlob(pixelFloats, longArrayOf(1, 3, 384, 384))\nval ids = Tensor.fromBlob(tokenIds, longArrayOf(1, tokenIds.size.toLong()))\nval out = module.forward(EValue.from(pix), EValue.from(ids))\nval logits = out[0].toTensor()\nTo use the Qualcomm NPU, swap XnnpackPartitioner for QnnPartitioner(QnnExecuTorchHtpBackendOptions(...)) during export.\n\n\n\n\nfrom executorch.backends.apple.coreml.partition.coreml_partitioner import CoreMLPartitioner\nedge = edge.to_backend(CoreMLPartitioner(skip_ops_for_coreml_delegation=[]))\nprog = edge.to_executorch()\nThen in Swift:\nimport ExecuTorch\nlet module = try Module(filePath: pteURL.path)\nlet output = try module.forward([pixelTensor, idTensor])\nIf you prefer pure CoreML (no ExecuTorch), use coremltools (§4.6) and MLModel.\n\n\n\nThe LLM in a VLM is autoregressive; without a KV cache, generating 100 tokens is 100× more expensive than a single forward. ExecuTorch and CoreML both support stateful models as of 2024:\n\nCoreML state: declare states with ct.target.iOS18, ct.StateType. The state lives across predictions() calls.\nExecuTorch: explicit (input, kv_in) → (output, kv_out) signature; the runtime exposes MutableTensor to update in place.\n\nA common mistake is to materialise the full KV cache at max-length. Use a ring buffer of kv_len = current_pos actual length.\n\n\n\nFor real-time camera apps, you cannot afford a full ViT pass per frame at 30 fps. Two tricks:\n\nTemporal sub-sampling: run vision encoder at 5 fps, interpolate features for in-between frames.\nPatch-residual encoders: re-run only patches whose mean intensity has changed (delta encoding).\nFrame-difference gating: a tiny CNN decides whether the scene changed enough to require re-encoding.\n\n\n\n\nMost LLM tokenizers are SentencePiece or BPE — port them with sentencepiece-android, tiktoken-rs, or compile the C library yourself. Never ship a Python tokenizer to a phone.\nFor VLMs, you must also reserve and map the &lt;image&gt; token correctly — the same special-token IDs you trained with.\n\n\n\nDon’t allocate a float32 384×384×3 = 1.7 MB buffer on the JVM heap. Use Android’s ByteBuffer.allocateDirect (zero-copy to native), or MediaPipe Image, or iOS vImage for resize and normalisation. Saving 10 ms per frame here adds up over a long session.\n\n\n\nA phone CPU at 100 % drops to half its clock after ~3 minutes. To stay below the thermal limit:\n\nPin VLM inference to efficiency cores when possible (SCHED_BATCH on Android).\nUse NPU &gt; GPU &gt; CPU. NPUs are 10× more energy-efficient than CPUs.\nDrop the frame rate when the user is idle.\nRead BatteryManager.BATTERY_PROPERTY_CURRENT_NOW and back off when the device is hot.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nMetric\nDefinition\nTarget\n\n\n\n\nLatency p50 / p95 / p99\nTime per request at the 50/95/99 percentile\nDepends; for chat, p95 ≤ 1.5 s TTFT\n\n\nTTFT (Time To First Token)\nLatency to first generated token\nCritical for UX\n\n\nTPOT (Time Per Output Token)\nAverage inter-token latency\nSteady-state generation speed\n\n\nThroughput\nRequests / second or tokens / second\nServer capacity\n\n\nGPU SM utilisation\n% of streaming multiprocessors active\n&gt; 60 % is healthy\n\n\nGPU memory\nBytes allocated\n&lt; 90 % to avoid OOM\n\n\nEnergy per request\nJoules\nMobile: minimise\n\n\nAccuracy drift\nKPI vs validation set\n&lt; 1 % drop after quantisation\n\n\n\n\n\n\nInstrument the inference server with the prometheus-client library:\nfrom prometheus_client import Counter, Histogram, Gauge\n\ninflight  = Gauge(\"vlm_inflight_requests\", \"Currently processing\")\nlatency   = Histogram(\"vlm_request_latency_seconds\", \"End-to-end latency\",\n                      buckets=(.05,.1,.25,.5,1,2,5,10))\ntokens    = Counter(\"vlm_tokens_generated_total\", \"Generated tokens\")\ngpu_util  = Gauge(\"vlm_gpu_util_percent\", \"SM utilization\")\n\n@app.post(\"/generate\")\nasync def generate(req):\n    inflight.inc()\n    t0 = time.time()\n    out = model.generate(**req.payload)\n    latency.observe(time.time() - t0)\n    tokens.inc(out.shape[1])\n    inflight.dec()\n    return out\nScrape with Prometheus, render with Grafana. The dashboard you want:\n\np50/p95/p99 latency line.\nTTFT histogram.\nTokens/sec rate.\nGPU memory bar.\nError rate panel.\nSaturation: queue depth vs in-flight.\n\n\n\n\nNVIDIA’s DCGM exporter exposes per-GPU SM utilisation, memory, temperature, power, NVLink throughput, ECC errors, PCIe replays. Run it as a sidecar; scrape with the same Prometheus.\n\n\n\nFor deep dives:\nimport torch\nfrom torch.profiler import profile, ProfilerActivity\n\nwith profile(\n    activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],\n    record_shapes=True, profile_memory=True, with_stack=True,\n) as prof:\n    out = model.generate(**inputs)\nprint(prof.key_averages().table(sort_by=\"cuda_time_total\", row_limit=20))\nprof.export_chrome_trace(\"trace.json\")\nOpen the trace in chrome://tracing; look for:\n\nLong blue gaps between CUDA kernels — CPU-side bottleneck.\naten::copy_ with large tensor sizes — unwanted CPU↔︎GPU copies.\nMany tiny kernels — fusion opportunity (try torch.compile).\n\n\n\n\nBoth serving stacks expose detailed metrics out of the box:\n\nvLLM: --enable-metrics flag enables /metrics Prometheus endpoint with per-batch info.\nTensorRT-LLM: integrates with Triton Inference Server; Triton has its own Prometheus exporter.\n\nThe two queue-management metrics you must track:\n\nPending requests in the scheduler — if &gt; 0 for long periods, you’re under-provisioned.\nPre-emption count — vLLM’s continuous batching may evict requests when memory is tight.\n\n\n\n\nFor multi-stage pipelines (preprocess → vision → projector → LLM → postprocess), use OpenTelemetry to get end-to-end span traces. Jaeger or Honeycomb work great.\n\n\n\nPhones don’t run Prometheus. Strategies:\n\nFirebase Performance / iOS MetricKit for latency histograms.\nCustom thin telemetry: aggregate p50/p95 locally, batch-upload daily (privacy-friendly).\nCrash reporting: Firebase Crashlytics for native crashes from the inference C++ code.\n\n\n\n\nThe model that was 75 % MMMU yesterday is now 65 % — why? Possible causes:\n\nQuantisation calibration drift (some weights got re-quantised on the latest release).\nTokenizer mismatch (wrong special tokens).\nImage preprocessor mismatch (mean/std flipped, BGR vs RGB).\nResolution mismatch (training was 384², inference is 336²).\nSampling temperature changed.\n\nMitigation: a continuous golden-set evaluation — 200 hand-picked prompts run nightly through every deployed checkpoint; metrics piped into Prometheus. Alert on &gt; 2 σ drops.\n\n\n\nPer-token cost is the deployment KPI for cloud-served VLMs:\n\\[\n\\text{cost / 1k tokens} = \\frac{\\text{GPU hourly price} \\cdot 3600}{\\text{tokens / sec / replica}} / 1000\n\\]\nIf your H100 costs $4/hour and the deployment yields 200 tok/s, then \\(4 \\times 3600 / 200 / 1000 = \\$0.072 / k\\) tokens. Track this in Grafana right next to latency.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSymptom\nLikely cause\n\n\n\n\nAccuracy collapse\nQuantisation calibration / dtype mismatch\n\n\nLatency regression\nOperator fallback to CPU; missing kernel\n\n\nOOM at inference\nKV cache overflow; static-shape graph too large\n\n\nNon-deterministic output\nReduction order in fp16/bf16; sampling temperature\n\n\nHang or crash on device\nUnsupported op; quantisation domain mismatch\n\n\n\n\n\n\nA systematic recipe:\n\nReproduce locally: capture the exact input that fails (image + tokens) and re-run through PyTorch reference.\nCompare layer outputs: instrument both reference and exported model with hooks, dump activations to disk, np.testing.assert_allclose layer-by-layer.\nactivations = {}\ndef hook(name): return lambda m, i, o: activations.setdefault(name, o.detach().cpu().float().numpy())\nfor n, m in model.named_modules():\n    m.register_forward_hook(hook(n))\nFind the first divergent layer. If it is a quantised conv, you have a calibration / scale problem. If it is a softmax, you have an fp16 overflow.\nInspect operators: polygraphy run vlm.onnx --trt --validate --rtol 1e-3 --atol 1e-3 for TRT, executorch.exir.print_program for ExecuTorch.\nBisect by partition: comment out half the model, re-export, see if the bug persists. Classic binary search.\n\n\n\n\n\n\nSome on-device runtimes (CoreML before iOS 17) silently downcast to fp16. The exponent range is smaller; intermediate exp() overflows. Fix: insert softmax in fp32 explicitly, or scale logits before softmax.\n\n\n\nCLIP uses mean (0.48145466, 0.4578275, 0.40821073) and std (0.26862954, 0.26130258, 0.27577711). SigLIP uses mean (0.5, 0.5, 0.5) and std (0.5, 0.5, 0.5). Mix them up and your model will almost work — accuracy drops 5–10 % silently.\n\n\n\nOpenCV reads BGR; PIL reads RGB. Models almost always trained on RGB. Swap the channels and accuracy plummets.\n\n\n\nDecoder-only LLMs require left padding at inference time (so the rightmost token is the most recent). Encoder/BERT models want right padding. Mix them up and the model will generate gibberish for the longest sample of a batch.\n\n\n\nAdding &lt;image&gt; after training without re-training the embedding produces random initial values. The model “works” but is 10 % worse on benchmarks.\n\n\n\nAfter quantisation, some runtimes recompute rotary embeddings with slightly different precision. Result: the cache from step \\(t\\) doesn’t match the embedding at step \\(t+1\\), so generation drifts. Fix: compute rotary positions in fp32 even when weights are int4.\n\n\n\n\n\n\nonnxruntime will gladly run an unsupported op on CPU while the rest runs on GPU — silently. Detect with sess.get_providers() and per-op profiling. Common offenders: ScatterND, dynamic Reshape, Range with int64.\n\n\n\n--workspace=128 is too small for VLMs. Use --workspace=4096 or --memPoolSize=workspace:4096.\n\n\n\nIf scaled_dot_product_attention falls back to the math kernel, you lose 2-3×. Check with torch.backends.cuda.sdp_kernel(enable_flash=True).\n\n\n\nA single Python interpreter is throttled by the GIL. Use multiple worker processes (gunicorn, uvicorn), or pure C++/Rust front-ends, or vLLM/TGI/TensorRT-LLM which release the GIL during compute.\n\n\n\n\n\n\nA 7 B LLM with 32-layer × 32-head × 128-dim KV in bf16 costs \\(32 \\times 2 \\times 32 \\times 128 \\times 2 = 524 288\\) bytes per token. At 8 k context that’s 4 GB; at 32 k it’s 16 GB. Either:\n\nQuantise the KV cache (fp8 / int8) — torch.compile does this with --quantize-kv.\nTruncate the context window.\nUse paged KV cache (vLLM PagedAttention).\nUse grouped-query attention (already standard in LLaMA-3, Qwen-2).\n\n\n\n\nIf you compiled an ONNX graph with max_seq_len=8192, the runtime allocates buffers for 8192 every call. Use dynamic axes or split the engine into multiple seq-length buckets.\n\n\n\n\nbf16 reductions on GPU are non-associative; the order of summation depends on which SMs scheduled which blocks. To get bit-exact reproducibility:\ntorch.use_deterministic_algorithms(True, warn_only=False)\ntorch.backends.cudnn.deterministic = True\ntorch.backends.cudnn.benchmark = False\nimport os; os.environ[\"CUBLAS_WORKSPACE_CONFIG\"] = \":4096:8\"\nSet sampling to greedy (do_sample=False) and seed everything (torch, numpy, Python’s random).\n\n\n\nOn distributed jobs, the most common cause is a single rank exiting while others wait at an all-reduce. Symptoms: training stalls; nvidia-smi shows 0 % util on some GPUs. Cure:\n\nNCCL_ASYNC_ERROR_HANDLING=1 — propagate errors across the world.\nTORCH_NCCL_BLOCKING_WAIT=1 — block on collectives with timeout.\nTORCH_NCCL_ASYNC_ERROR_HANDLING=1 (&gt;= 2.4) — supersedes the above.\nWrap each dist.collective call inside a try/except, log rank-local errors, broadcast termination signal.\n\nFor on-device crashes, attach symbol files (addr2line on Android, dSYM on iOS) to logs so you can resolve native stack frames.\n\n\n\nWhen something breaks after optimisation:\n\nCompare end-to-end outputs in fp32 reference vs target runtime.\nRe-confirm image preprocessing (mean, std, channel order, resize mode).\nRe-confirm tokenizer special tokens and pad direction.\nInspect operator coverage and provider mapping on the target.\nDump activations layer-by-layer; binary-search to first divergence.\nRe-export with debug names and shape inference enabled.\nReproduce in a tiny repro; isolate to one block.\nOpen an upstream issue with that repro.\n\n\n\n\nYou will not understand every line of a 3-million-line runtime stack. What you must build instead is fast iteration — small experiments, layer hooks, numpy assertions, a CI that exports + numerically tests every commit. A deployment engineer’s competitive advantage is not memory; it is the speed at which they can localise a bug.\n\n\n\n\n\n\n\n\n\nSergeev & Del Balso, Horovod: fast and easy distributed deep learning, 2018.\nRajbhandari et al., ZeRO: Memory Optimizations Toward Training Trillion Parameter Models, SC 2020.\nRajbhandari et al., ZeRO-Offload, ATC 2021.\nKorthikanti et al., Reducing Activation Recomputation in Large Transformer Models, 2022.\nSmith et al., Using DeepSpeed and Megatron to Train Megatron-Turing NLG 530B, 2022.\nShoeybi et al., Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism, 2019.\nHuang et al., GPipe: Efficient Training of Giant Neural Networks, NeurIPS 2019.\nNarayanan et al., Pipeline-Parallel Training of Transformers, 2021.\nVogels et al., PowerSGD: Practical Low-Rank Gradient Compression, NeurIPS 2019.\nTouvron et al., LLaMA / LLaMA-2 / LLaMA-3 technical reports.\nPyTorch FSDP design RFCs and the FSDP2 (fully_shard) docs.\n\n\n\n\n\nExecuTorch documentation (pytorch.org/executorch).\nAI-Edge-Torch (Google, 2024) — PyTorch ↔︎ TFLite bridge.\nCoreML Tools User Guide.\nTensorRT Developer Guide; trtexec and polygraphy manuals.\nOpenVINO Toolkit Documentation.\nTizen Native API documentation: ML Inference, NNStreamer, Media Vision.\nLim et al., NNStreamer: Stream Processing Paradigm for Neural Networks, 2019.\nYang et al., MobileBERT, ACL 2020.\n\n\n\n\n\nPatarasuk & Yuan, Bandwidth Optimal All-Reduce Algorithms for Clusters of Workstations, 2009.\nSanders et al., Two-tree algorithms for full bandwidth broadcast, 2009.\nNCCL documentation and the nccl-tests repo (NVIDIA).\nSergeev et al., Horovod Timeline Profiling, 2018.\nCao et al., Pollux, OSDI 2021 — co-adaptive training.\n\n\n\n\n\nSculley et al., Hidden Technical Debt in Machine Learning Systems, NeurIPS 2015.\nBreck et al., The ML Test Score: A Rubric for ML Production Readiness, 2017.\nPolyzotis et al., Data Lifecycle Challenges in Production Machine Learning, SIGMOD Record 2018.\nHuggingFace Text Generation Inference server docs.\nvLLM technical paper and PagedAttention blog post.\n\n\n\n\n\nLilian Weng, “Large Transformer Model Inference Optimization” (Jan 2023).\nLilian Weng, “Scaling neural network: A short summary of GPipe, PipeDream, ZeRO, etc.”\nJay Alammar, “How GPT3 Works”.\nHuggingFace blog: “Efficient Training on Multiple GPUs”.\nPyTorch blog: “PyTorch Distributed Overview”, “FSDP”, “FSDP2”.\nNVIDIA blog: “NCCL 2.x Performance Tuning”.\nMosaic / Databricks blog: “How we trained DBRX”, “How we trained MPT”.\nSebastian Raschka, “Tips for LLM Pretraining and Fine-Tuning”.\n\n\n\n\n\nPass once front to back.\nBuild a 4-GPU FSDP toy with a 1 B model and watch the trace.\nPick a phone you have and run an ExecuTorch demo of a small VLM.\nRe-read the references for the parts that felt thin.\nPair this with the Optimization document — they are two halves of the same problem.\n\nIf you can do all four, you can deploy a VLM anywhere on Earth.\n\nEnd of Vision-Language Model Deployment document."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#how-to-read-this-document",
    "href": "Resources/Core5/Deployment/index.html#how-to-read-this-document",
    "title": "VLM Deployment",
    "section": "",
    "text": "The chapters are ordered the way a deployment engineer actually encounters them.\n\nConfiguring Multi-GPU Training\nManaging Multi-Node Training\nOptimizing Communication Overhead\nConverting Models to Edge-optimized Formats\nOptimizing for Tizen Platform\nImplementing Mobile Inference\nMonitoring Deployment Performance\nTroubleshooting Optimized Models\nReading List & References\n\nEvery section gives mental model + PyTorch code + war-story-tier tips. The references at the end are the canonical sources you should cite in interviews and design reviews."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#configuring-multi-gpu-training",
    "href": "Resources/Core5/Deployment/index.html#configuring-multi-gpu-training",
    "title": "VLM Deployment",
    "section": "",
    "text": "Before any code, you must know what you are sharding.\n\n\n\n\n\n\n\n\nParallelism\nWhat is split across devices?\nBest for\n\n\n\n\nData Parallel (DP / DDP)\nMini-batch\nModel fits on one GPU\n\n\nZeRO-1\nOptimizer states\nDP + memory savings\n\n\nZeRO-2\nOptimizer states + grads\nMore memory savings\n\n\nZeRO-3 / FSDP\nOptimizer + grads + params\nModels too big for one GPU\n\n\nTensor Parallel (TP)\nSingle layer’s matrices\nLayers too big for one GPU\n\n\nPipeline Parallel (PP)\nWhole stages of layers\nVery deep models\n\n\nSequence / Context Parallel (SP/CP)\nThe sequence dimension\nVery long contexts\n\n\nExpert Parallel (EP)\nMoE experts\nMoE models\n\n\n\nModern VLMs use a combination — typically FSDP + TP, with SP for long-video models.\n\n\n\nDDP replicates the full model on every GPU and averages gradients via NCCL all-reduce after each backward.\n# launch with:  torchrun --nproc_per_node=8 train.py\nimport os, torch, torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\nfrom torch.utils.data import DataLoader, DistributedSampler\n\ndef setup():\n    dist.init_process_group(backend=\"nccl\")\n    local_rank = int(os.environ[\"LOCAL_RANK\"])\n    torch.cuda.set_device(local_rank)\n    return local_rank\n\nlocal_rank = setup()\nmodel = build_vlm().to(local_rank)\nmodel = DDP(model, device_ids=[local_rank], find_unused_parameters=False,\n            gradient_as_bucket_view=True, broadcast_buffers=False)\n\nsampler = DistributedSampler(dataset, shuffle=True, drop_last=True)\nloader  = DataLoader(dataset, batch_size=8, sampler=sampler,\n                     num_workers=4, pin_memory=True, persistent_workers=True)\n\nopt    = torch.optim.AdamW(model.parameters(), lr=1e-4)\nscaler = torch.amp.GradScaler(\"cuda\")\n\nfor epoch in range(EPOCHS):\n    sampler.set_epoch(epoch)                     # different shuffle every epoch\n    for batch in loader:\n        with torch.autocast(\"cuda\", dtype=torch.bfloat16):\n            loss = model(**batch).loss\n        loss.backward()                          # all-reduce happens here\n        opt.step(); opt.zero_grad(set_to_none=True)\nFive DDP details that always come up:\n\ndevice_ids must contain a single device — the local rank.\nfind_unused_parameters=True is required if any parameter is conditionally skipped in forward (e.g. a frozen LoRA branch). It costs CPU time; flip it off whenever possible.\ngradient_as_bucket_view=True saves memory by giving you a view into the all-reduce bucket buffer instead of allocating new gradient tensors.\nbroadcast_buffers=False turns off the all-reduce of BatchNorm running stats — useless for ViTs/LLMs which use LayerNorm only.\nset_to_none=True in zero_grad() keeps the gradient pointer null instead of a zero tensor, slightly faster.\n\n\n\n\nTo simulate a larger batch when memory is tight:\naccum = 4\nopt.zero_grad(set_to_none=True)\nfor step, batch in enumerate(loader):\n    is_last = (step + 1) % accum == 0\n    ctx = model.no_sync() if not is_last else nullcontext()\n    with ctx:\n        with torch.autocast(\"cuda\", dtype=torch.bfloat16):\n            loss = model(**batch).loss / accum\n        loss.backward()\n    if is_last:\n        opt.step(); opt.zero_grad(set_to_none=True)\nmodel.no_sync() skips the all-reduce during the inner accumulation steps — communication cost goes down by accum. Forget this and you will all-reduce on every micro-batch, paying the full network cost while gaining no math.\n\n\n\nWhen the model does not fit on a single GPU, shard everything: parameters, gradients, optimizer states.\nimport functools\nfrom torch.distributed.fsdp import (\n    FullyShardedDataParallel as FSDP,\n    MixedPrecision, ShardingStrategy, BackwardPrefetch,\n)\nfrom torch.distributed.fsdp.wrap import transformer_auto_wrap_policy\nfrom transformers.models.qwen2.modeling_qwen2 import Qwen2DecoderLayer\n\nmp = MixedPrecision(\n    param_dtype=torch.bfloat16,\n    reduce_dtype=torch.bfloat16,\n    buffer_dtype=torch.bfloat16,\n)\n\nwrap_policy = functools.partial(\n    transformer_auto_wrap_policy,\n    transformer_layer_cls={Qwen2DecoderLayer},   # one shard per LLM block\n)\n\nmodel = FSDP(\n    model,\n    sharding_strategy=ShardingStrategy.FULL_SHARD,         # ZeRO-3\n    auto_wrap_policy=wrap_policy,\n    mixed_precision=mp,\n    backward_prefetch=BackwardPrefetch.BACKWARD_PRE,       # overlap comm\n    limit_all_gathers=True,\n    device_id=torch.cuda.current_device(),\n    use_orig_params=True,                                  # required for ParamGroups\n)\nSharding strategies:\n\nFULL_SHARD — ZeRO-3 (params + grads + opt states sharded).\nSHARD_GRAD_OP — ZeRO-2 (grads + opt states only).\nHYBRID_SHARD — full-shard inside a node, replicate across nodes (best for 2-node setups with fat intra-node NVLink and thin inter-node Ethernet).\nNO_SHARD — equivalent to DDP.\n\nFSDP2 (PyTorch ≥ 2.4) introduces fully_shard() as the preferred API — per-parameter sharding, cleaner ergonomics, full DTensor-native. Same conceptual model, nicer code.\n\n\n\n\n\n\n\n\n\n\n\n\nFormat\nRange\nPrecision\nWhere to use\n\n\n\n\nfp16\n±65 504\n~3 decimal\nInference, V100\n\n\nbf16\n±\\(3.4\\times 10^{38}\\)\n~2 decimal\nDefault training on A100/H100\n\n\nfp8 (e4m3 / e5m2)\n±448 / ±57 344\n~1 decimal\nH100 only, large speedup\n\n\n\nbf16 is the de facto training dtype because it has the same exponent range as fp32 — no GradScaler required, no overflow surprises. fp16 only stays alive on V100s that have no bf16 hardware. fp8 needs the Transformer Engine library (pip install transformer_engine) and explicit scaling factors.\n\n\n\nRecompute activations during backward instead of storing them — trades FLOPs for memory.\nfrom torch.distributed.algorithms._checkpoint.checkpoint_wrapper import (\n    apply_activation_checkpointing,\n    CheckpointWrapper,\n    CheckpointImpl,\n)\n\ndef check_fn(submodule):\n    return isinstance(submodule, Qwen2DecoderLayer)\n\napply_activation_checkpointing(\n    model,\n    checkpoint_wrapper_fn=functools.partial(\n        CheckpointWrapper,\n        checkpoint_impl=CheckpointImpl.NO_REENTRANT,\n    ),\n    check_fn=check_fn,\n)\nNO_REENTRANT is the modern variant and integrates cleanly with FSDP. Saves ~\\(O(L)\\) memory at the cost of one extra forward.\n\n\n\nWhen a single transformer block does not fit on a GPU (e.g. 70 B+), split the matrices themselves:\n\nColumn-wise split on W_q, W_k, W_v and W_up, W_gate of MLP.\nRow-wise split on W_o and W_down.\nAll-gather along the head dimension before the row-parallel layer.\n\nIn PyTorch (≥ 2.3):\nfrom torch.distributed.tensor.parallel import (\n    parallelize_module, ColwiseParallel, RowwiseParallel, SequenceParallel,\n)\nfrom torch.distributed.device_mesh import init_device_mesh\n\nmesh = init_device_mesh(\"cuda\", (DP, TP), mesh_dim_names=(\"dp\",\"tp\"))\ntp_mesh = mesh[\"tp\"]\n\nplan = {\n    \"self_attn.q_proj\": ColwiseParallel(),\n    \"self_attn.k_proj\": ColwiseParallel(),\n    \"self_attn.v_proj\": ColwiseParallel(),\n    \"self_attn.o_proj\": RowwiseParallel(),\n    \"mlp.gate_proj\":  ColwiseParallel(),\n    \"mlp.up_proj\":    ColwiseParallel(),\n    \"mlp.down_proj\":  RowwiseParallel(),\n}\nfor blk in model.model.layers:\n    parallelize_module(blk, tp_mesh, plan)\nSequence parallel additionally splits LayerNorm activations along the sequence axis between TP collective calls — relevant when sequences are very long (video, long-context VLMs).\n\n\n\nPipeline parallel (PP) splits the model into stages and pipelines micro-batches through them. PyTorch’s torch.distributed.pipelining (PiPPy) does this cleanly. PP shines when:\n\nModel is deep (≥ 80 layers).\nInter-node bandwidth is the bottleneck (PP communicates only activations at stage boundaries, not gradients across all params).\n\nFor 7-13 B VLMs, PP is overkill; FSDP + TP is enough.\n\n\n\n\n\n\n\n\n\n\n\nSize\nSingle node 8×H100 80 GB\nMulti-node\n\n\n\n\n1–3 B\nDDP + bf16\nseldom needed\n\n\n7–13 B\nFSDP FULL_SHARD + activation ckpt\nHYBRID_SHARD across 2 nodes\n\n\n30–70 B\nFSDP + TP=2 + activation ckpt\nFSDP + TP=4 across N nodes\n\n\n100 B+\nFSDP + TP=8 + PP=2/4\nscale TP first, then PP"
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#managing-multi-node-training",
    "href": "Resources/Core5/Deployment/index.html#managing-multi-node-training",
    "title": "VLM Deployment",
    "section": "",
    "text": "Inside a node, GPUs talk over NVLink/NVSwitch — 600–900 GB/s on A100/H100 nodes.\nAcross nodes, they go over Ethernet (25–100 Gbit/s) or InfiniBand (200–400 Gbit/s, ~5× faster). The bandwidth difference is 6–30×; the latency difference is even larger. Every architectural decision in multi-node training is a workaround for this fact.\n\n\n\ntorchrun (PyTorch’s own launcher, supersedes torch.distributed.launch):\n# Node 0 (master):\ntorchrun \\\n  --nnodes=4 --nproc_per_node=8 \\\n  --node_rank=0 --rdzv_id=vlm_run01 --rdzv_backend=c10d \\\n  --rdzv_endpoint=node0.cluster:29500 train.py\n\n# Node 1..3: same command with --node_rank=1, 2, 3\ntorchrun is elastic by default — workers can join and leave thanks to the c10d rendezvous backend.\nSLURM (srun) — the dominant academic / national-lab scheduler:\n#!/bin/bash\n#SBATCH --job-name=vlm\n#SBATCH --nodes=4\n#SBATCH --ntasks-per-node=8\n#SBATCH --gpus-per-task=1\n#SBATCH --cpus-per-task=16\n#SBATCH --time=24:00:00\n#SBATCH --partition=h100\n\nexport MASTER_ADDR=$(scontrol show hostnames \"$SLURM_JOB_NODELIST\" | head -n1)\nexport MASTER_PORT=29500\nexport OMP_NUM_THREADS=8\nexport NCCL_DEBUG=WARN\n\nsrun python -u train.py\nInside train.py you read RANK, LOCAL_RANK, WORLD_SIZE from environment variables that SLURM/torchrun set automatically.\n\n\n\nimport os, torch, torch.distributed as dist\n\ndef setup():\n    if \"RANK\" in os.environ and \"WORLD_SIZE\" in os.environ:\n        dist.init_process_group(backend=\"nccl\")\n    else:\n        # SLURM path\n        rank   = int(os.environ[\"SLURM_PROCID\"])\n        world  = int(os.environ[\"SLURM_NTASKS\"])\n        local  = int(os.environ[\"SLURM_LOCALID\"])\n        dist.init_process_group(backend=\"nccl\", rank=rank, world_size=world,\n                                init_method=f\"tcp://{os.environ['MASTER_ADDR']}:{os.environ['MASTER_PORT']}\")\n        os.environ[\"LOCAL_RANK\"] = str(local)\n    torch.cuda.set_device(int(os.environ[\"LOCAL_RANK\"]))\n\n\n\nNCCL is NVIDIA’s collective communication library; PyTorch’s nccl backend is a thin wrapper. The collectives you need to know:\n\nall_reduce — every rank ends up with the sum (or other op) of every input. Used after loss.backward() to average gradients.\nall_gather — every rank collects all inputs concatenated. Used by FSDP to materialise full parameters before forward.\nreduce_scatter — sum, then scatter chunks back. Used by FSDP for gradients.\nbroadcast — one rank’s tensor goes to all others. Used to seed initial weights and to share the schedule.\nbarrier — synchronisation point.\n\nNCCL chooses among several algorithms:\n\n\n\n\n\n\n\n\n\nAlgorithm\nBandwidth-optimal?\nLatency\nNotes\n\n\n\n\nRing\nYes for large messages\n\\(O(P)\\) steps\nclassic\n\n\nTree\nBetter latency\n\\(O(\\log P)\\)\nnewer default for small msgs\n\n\nCollNet (SHARP)\nHardware-accelerated on Mellanox switches\nvery low\nInfiniBand only\n\n\nNVLS (NVLink SHARP)\nIntra-node H100 NVSwitch\nvery low\nDGX H100 only\n\n\n\nYou can profile NCCL with NCCL_DEBUG=INFO; it prints the algorithm chosen for each collective.\n\n\n\nA simple mental picture: an 8×H100 DGX node looks like\n    GPU0—NVLink—GPU1—NVLink—GPU2—…\n       \\        /\n        NVSwitch (intra-node, 900 GB/s pairwise)\n       /        \\\n   GPU4    …   GPU7\n   |                  |\n   NIC0  NIC1  NIC2  NIC3  (InfiniBand HCAs, 1:1 with GPUs)\n       \\    |    |    /\n        switch fabric (200–400 Gbit/s per port)\nFor best inter-node performance you must:\n\nUse GPUDirect RDMA (NICs read GPU memory directly).\nPin each GPU process to the NUMA node owning its HCA via numactl.\nEnable NCCL_IB_HCA to use the right HCA.\nSet NCCL_IB_GID_INDEX=3 for RoCE v2, NCCL_IB_GID_INDEX=0 for IB.\n\n\n\n\nexport NCCL_DEBUG=WARN                  # INFO for first run, WARN afterwards\nexport NCCL_ASYNC_ERROR_HANDLING=1      # crash on collective error\nexport NCCL_SOCKET_IFNAME=^lo,docker0   # which NIC for bootstrap\nexport NCCL_IB_HCA=mlx5_0,mlx5_2        # which HCAs to use\nexport NCCL_IB_TIMEOUT=22               # higher for flaky fabrics\nexport NCCL_IB_RETRY_CNT=7\nexport NCCL_NET_GDR_LEVEL=5             # max GPUDirect RDMA\nexport NCCL_P2P_LEVEL=NVL               # force NVLink intra-node\nexport NCCL_NVLS_ENABLE=1               # H100 NVSwitch SHARP\nexport NCCL_ALGO=Tree,Ring              # let NCCL choose\nexport NCCL_PROTO=Simple,LL,LL128       # protocols, lowest latency to highest BW\nexport OMP_NUM_THREADS=8                # avoid CPU oversubscription\n\n\n\nA 64-GPU job runs for a week and hits a memory error. What do you do?\n\nCheckpoint frequently (every 250–1000 steps) but asynchronously (next chapter).\ntorchrun elastic restarts crashed workers automatically.\ntorch.distributed.elastic monitors and signals workers.\nTorchSnapshot / Distributed Checkpoint (DCP) writes sharded checkpoints in parallel — orders of magnitude faster than gathering everything to rank 0.\n\nimport torch.distributed.checkpoint as dcp\n\nstate = {\"model\": model.state_dict(), \"opt\": opt.state_dict(), \"step\": step}\ndcp.save(state, checkpoint_id=f\"/ckpt/step_{step}\")\nEach rank writes its own shard; loading reverses the process. DCP is the default in PyTorch 2.4+.\n\n\n\nA 64-GPU job demands ~64× the data throughput of a single GPU. Best practices:\n\nSharded data: WebDataset or MosaicML Streaming format — many tar shards on object store, each rank streams a disjoint set.\nnum_workers ≈ 4 × CPU/GPU with pin_memory=True.\npersistent_workers=True to avoid re-spawning every epoch.\nprefetch_factor=2 to overlap CPU and GPU.\nImage decoding on GPU with NVIDIA DALI for VLMs — JPEG → tensor is often the bottleneck."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#optimizing-communication-overhead",
    "href": "Resources/Core5/Deployment/index.html#optimizing-communication-overhead",
    "title": "VLM Deployment",
    "section": "",
    "text": "For a parameter \\(P\\), an all-reduce takes time\n\\[\nT_{\\text{ar}}(P) \\approx \\alpha + \\beta \\cdot 2 \\frac{N-1}{N} \\cdot P\n\\]\nwhere \\(\\alpha\\) is latency, \\(\\beta = 1/\\text{bandwidth}\\), and the factor \\(\\frac{2(N-1)}{N}\\) comes from the ring-all-reduce algorithm. For \\(N\\) large, this becomes \\(2P/B\\) — twice the parameter bytes over the network.\nA single bf16 7 B model has \\(2 \\cdot 7\\text{B} = 14\\) GB. On a 100 Gbit/s NIC = 12.5 GB/s, one all-reduce takes 1.1 seconds. Compare to ~0.4 seconds of compute for the backward pass → communication dominates unless we hide it.\n\n\n\nPyTorch DDP automatically buckets gradients and starts all-reducing the bucket as soon as it is filled — overlap with the rest of the backward pass. Two knobs:\n\ngradient_as_bucket_view=True — saves memory.\nbucket_cap_mb (default 25 MB) — larger buckets = fewer collectives but less overlap. For 7-13 B models, 50-100 MB is often optimal.\n\nFor FSDP, set backward_prefetch=BackwardPrefetch.BACKWARD_PRE so the next shard’s parameters are all-gathered while you compute the current backward. The matching forward_prefetch=True does the analogous thing in forward.\n\n\n\nTrade communication for accuracy:\n\nfp16 all-reduce with MixedPrecision(reduce_dtype=torch.float16) — halves bytes.\nPowerSGD (Vogels et al., 2019) — rank-r low-rank approximation. Built into PyTorch DDP.\n\nimport torch.distributed.algorithms.ddp_comm_hooks.powerSGD_hook as ps\nstate = ps.PowerSGDState(process_group=None, matrix_approximation_rank=4,\n                         start_powerSGD_iter=1_000)\nmodel.register_comm_hook(state, ps.powerSGD_hook)\nPowerSGD is essentially free for many models after a warm-up period.\n\n\n\nDDP reduces buckets in the order parameters are registered. To maximise overlap, register parameters in forward order (default if you wrap a nn.Module directly).\nFor FSDP, set the auto-wrap policy at the right granularity — each FSDP unit is a single all-gather. Too many small units = collective overhead; too few large units = no overlap.\n\n\n\nZeRO-3 / FSDP FULL_SHARD has the lowest memory but the most communication: every parameter is gathered and re-discarded once per forward + once per backward. ZeRO-2 (SHARD_GRAD_OP) gathers parameters once (they are kept) but still scatters gradients. Choose:\n\nMemory-bound? → ZeRO-3.\nCommunication-bound? → ZeRO-2 or HYBRID_SHARD.\n\nHYBRID_SHARD is the sweet spot for 2–4 node setups: full-shard inside the fast NVLink island, replicate across the slow Ethernet/IB.\n\n\n\nWhen you use TP and SP, you must overlap the all-gather of activations before each row-parallel matmul with the previous compute. PyTorch’s parallelize_module does this automatically if you pass use_local_output=False.\n\n\n\nA few real-world tunes:\n\nFor a 2-node A100-40 GB setup with 200 Gbit/s IB, NCCL_ALGO=Ring NCCL_PROTO=LL128 gives best throughput for 1 GB messages.\nFor an H100 NVL node, NCCL_NVLS_ENABLE=1 cuts intra-node all-reduce by ~30 %.\nFor noisy fabrics, NCCL_IB_TIMEOUT=22 NCCL_IB_RETRY_CNT=7 avoids spurious crashes; pair with elastic restart.\n\n\n\n\nThree tools, all free:\n\ntorch.profiler — annotate ranges, dump Chrome trace, inspect collective calls.\nNVIDIA Nsight Systems (nsys) — system-wide GPU + NCCL traces.\nNCCL_DEBUG=INFO logs + the nccl-tests benchmark — measure raw all-reduce bandwidth.\n\nfrom torch.profiler import profile, ProfilerActivity, schedule\n\nwith profile(\n    activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],\n    schedule=schedule(wait=1, warmup=1, active=3, repeat=1),\n    on_trace_ready=lambda p: p.export_chrome_trace(f\"trace_{dist.get_rank()}.json\"),\n    record_shapes=True, with_stack=True,\n) as p:\n    for step in range(5):\n        ...\n        p.step()\nOpen the trace in chrome://tracing; look for nccl:all_reduce rows that don’t overlap the compute rows above them. Anything not overlapped is wasted time."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#converting-models-to-edge-optimized-formats",
    "href": "Resources/Core5/Deployment/index.html#converting-models-to-edge-optimized-formats",
    "title": "VLM Deployment",
    "section": "",
    "text": "Format\nTarget runtime\nStrengths\nNotes\n\n\n\n\nTorchScript\nLibTorch C++\nDrop-in from PyTorch\nLegacy in 2026; mostly replaced by ExecuTorch\n\n\nExecuTorch (.pte)\nExecuTorch runtime (mobile, embedded)\nNative PyTorch path, partial-backend execution\nDefault for new PyTorch mobile in 2024+\n\n\nONNX (.onnx)\nonnxruntime, TensorRT, OpenVINO\nCross-vendor IR\nOperator coverage gaps for new ops\n\n\nTensorRT (.engine)\nNVIDIA Jetson, Orin\nBest NVIDIA performance\nVendor-locked\n\n\nTFLite (.tflite)\nAndroid, Tizen, microcontrollers\nMature mobile ecosystem\nNeeds conversion via ONNX or AI-Edge-Torch\n\n\nCoreML (.mlpackage)\niOS / macOS\nNeural Engine acceleration\nApple-only\n\n\nOpenVINO IR (.xml+.bin)\nIntel CPUs, NPUs\nBest Intel performance\nx86 / NPU only\n\n\nGGUF\nllama.cpp\nQuantised LLM-only\nUsed for LLM backbone of VLMs\n\n\n\n\n\n\nPyTorch ≥ 2.2 introduced torch.export, which captures the model as an ExportedProgram — an FX graph with a sound, stable representation. This is the source format for both ExecuTorch and the modern ONNX exporter.\nimport torch\n\nexample_inputs = (torch.randn(1, 3, 384, 384),)\nep = torch.export.export(vision_model, example_inputs)\nprint(ep)                                  # ExportedProgram with input/output specs\ntorch.export.save(ep, \"vision.pt2\")\nDynamic shapes (variable resolution!) are declared explicitly:\nfrom torch.export import Dim\nH = Dim(\"H\", min=224, max=1024)\nW = Dim(\"W\", min=224, max=1024)\nep = torch.export.export(model, (torch.randn(1,3,384,384),),\n                         dynamic_shapes={\"pixel_values\": {2: H, 3: W}})\n\n\n\nimport torch\ntorch.onnx.export(\n    model, example_inputs, \"vlm.onnx\",\n    input_names=[\"pixel_values\",\"input_ids\"],\n    output_names=[\"logits\"],\n    dynamic_axes={\"pixel_values\": {0: \"B\", 2: \"H\", 3: \"W\"},\n                  \"input_ids\":   {0: \"B\", 1: \"T\"},\n                  \"logits\":      {0: \"B\", 1: \"T\"}},\n    opset_version=18,\n    dynamo=True,                       # the new exporter, FX-based\n)\nThe dynamo=True exporter is the future — it relies on torch.export under the hood and supports control flow and dynamic shapes far better than the legacy TorchScript-based exporter.\nValidate the ONNX model:\nimport onnxruntime as ort, numpy as np\nsess = ort.InferenceSession(\"vlm.onnx\", providers=[\"CUDAExecutionProvider\",\"CPUExecutionProvider\"])\nouts = sess.run(None, {\"pixel_values\": np.random.randn(1,3,384,384).astype(np.float32),\n                       \"input_ids\":   np.array([[1,2,3]], dtype=np.int64)})\nTwo things to verify every time:\n\nOutput deltas between torch_model(x) and onnx_runtime(x) should be &lt; 1e-3 in fp32 (&lt; 1e-2 in fp16). Anything larger is a buggy export.\nAll operators supported by your target runtime. Run onnx.checker.check_model(\"vlm.onnx\") and polygraphy inspect model vlm.onnx --mode=onnx to enumerate ops.\n\n\n\n\nExecuTorch is the successor to PyTorch Mobile. The pipeline:\ntorch.export → quantization → to_edge → backend partitioning → to_executorch → .pte\nfrom executorch.exir import to_edge\nfrom executorch.backends.xnnpack.partition.xnnpack_partitioner import XnnpackPartitioner\n\nep   = torch.export.export(model, example_inputs)\nedge = to_edge(ep)\nedge = edge.to_backend(XnnpackPartitioner())          # delegate ops to XNNPACK\nprog = edge.to_executorch()\nwith open(\"vlm.pte\", \"wb\") as f:\n    f.write(prog.buffer)\nBackends ship out-of-the-box: XNNPACK (CPU, ARM/x86), Vulkan (mobile GPU), CoreML, Qualcomm QNN (Hexagon NPU), MediaTek, Apple MPS.\nThe .pte binary is loaded by ExecuTorch’s C++/Java/Swift runtime; you ship one file per platform.\n\n\n\nPath of least resistance: ONNX → TensorRT via trtexec.\ntrtexec --onnx=vlm.onnx --saveEngine=vlm.engine \\\n        --fp16 --workspace=4096 \\\n        --minShapes=pixel_values:1x3x224x224,input_ids:1x16 \\\n        --optShapes=pixel_values:1x3x384x384,input_ids:1x64 \\\n        --maxShapes=pixel_values:1x3x768x768,input_ids:1x256\nFor Jetson Orin / AGX-Orin you compile the engine on the device (engines are device-specific). For data-center INT8/FP8, supply a calibration cache (see Optimization doc).\n\n\n\nimport coremltools as ct\nmlmodel = ct.convert(\n    \"vlm.onnx\",\n    convert_to=\"mlprogram\",\n    compute_units=ct.ComputeUnit.ALL,     # CPU + GPU + Neural Engine\n    minimum_deployment_target=ct.target.iOS17,\n)\nmlmodel.save(\"VLM.mlpackage\")\niOS 18 supports 8-bit weight quantisation natively and INT4 weight-only quantisation via ct.optimize.coreml.linear_quantize_weights.\n\n\n\nGoogle’s ai-edge-torch is the modern PyTorch → TFLite bridge:\nimport ai_edge_torch\nedge = ai_edge_torch.convert(model.eval(), sample_args)\nedge.export(\"vlm.tflite\")\nIt uses torch.export under the hood and handles dynamic shapes properly. The resulting .tflite runs in any TFLite runtime — Android, Tizen, embedded Linux.\n\n\n\nFor Intel CPUs / iGPUs / NPUs (Meteor Lake, Lunar Lake) you go via ONNX:\novc vlm.onnx --output_dir ovir/         # OpenVINO Model Converter\nInference:\nfrom openvino.runtime import Core\ncore = Core()\nmodel = core.read_model(\"ovir/vlm.xml\")\ncompiled = core.compile_model(model, device_name=\"NPU\")     # or \"GPU\", \"CPU\"\nOpenVINO is particularly strong for VLMs on AI PCs because the NPU has dedicated INT8 matmul hardware.\n\n\n\nBefore shipping any exported model, verify:\n\nNumerical parity vs PyTorch reference within tolerance.\nLatency measured per device (CPU, GPU, NPU) at target batch / shape.\nMemory peak under sustained load (some op kernels temporarily allocate huge buffers).\nCold-start time — engine load + first inference; users care about app launch.\nThermal behaviour — phones throttle after 30 s of sustained compute.\nPrivacy / on-device requirements — no network calls leaking inputs."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#optimizing-for-tizen-platform",
    "href": "Resources/Core5/Deployment/index.html#optimizing-for-tizen-platform",
    "title": "VLM Deployment",
    "section": "",
    "text": "Tizen is Samsung’s Linux-based OS for TVs, smartwatches, washing machines, fridges, and Galaxy IoT devices. It has been the dominant Smart-TV OS since 2015 and ships in tens of millions of devices a year. Apps come in two flavours: native (C/C++ using Tizen ML APIs) and Web/Tizen .NET (JavaScript / C#). Tizen Studio is the IDE.\nFor VLM deployment the runtime you target is NNStreamer + TensorFlow Lite (with optional GPU/NPU delegates), surfaced via the Tizen ML API (formerly Inference Tizen API).\n\n\n\n   Application (C/C++, .NET, Web)\n         │\n   Tizen ML API  (high-level)\n         │\n   NNStreamer  (GStreamer plugin, pipeline-style ML)\n         │\n   ML inference backends\n   ├── TensorFlow Lite     (CPU, GPU, Hexagon, EdgeTPU)\n   ├── ONNX Runtime        (CPU, GPU, NPU)\n   ├── Snapdragon NPE\n   ├── Samsung ENN         (Exynos NPU)\n   └── OpenVINO            (Intel-based Tizen devices)\nNNStreamer is Samsung’s GStreamer-based ML pipeline framework. It treats an ML model as a filter (tensor_filter) in a streaming pipeline, with tensor data flowing as GstBuffers. This is perfect for VLM use-cases like TV captioning of live video — you connect a camera/video source to a vision-encoder filter, fan out to a Q-Former filter, fan out to an LLM filter, and write text to a UI sink.\n\n\n\n\nExport the vision encoder (e.g. SigLIP-So400m) to a small TFLite model, quantised INT8.\nExport the projector as a separate TFLite model (a few MB).\nExport the LLM to a quantised GGUF or TFLite-LLM (currently 4-bit AWQ/GPTQ is best).\nWire them with NNStreamer, with the LLM decoder running in a tensor_filter that supports KV-cache.\n\n\n\n\nA minimal C example using Tizen ML API:\n#include &lt;mv_inference.h&gt;\n#include &lt;mv_inference_type.h&gt;\n\nmv_engine_config_h cfg;\nmv_create_engine_config(&cfg);\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_MODEL_WEIGHT_FILE_PATH,\n                                      \"vision_int8.tflite\");\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_BACKEND_TYPE,\n                                      \"tflite\");\nmv_engine_config_set_string_attribute(cfg, MV_INFERENCE_TARGET_DEVICE_TYPE,\n                                      \"npu\");           // or \"gpu\" / \"cpu\"\n\nmv_inference_h infer;\nmv_inference_create(&infer);\nmv_inference_configure(infer, cfg);\nmv_inference_prepare(infer);\n\nmv_source_h src;\nmv_create_source(&src);\nmv_source_fill_by_buffer(src, frame_buffer, frame_size, width, height, MEDIA_VISION_COLORSPACE_RGB888);\n\nmv_inference_image_classify(src, infer, NULL, callback, NULL);\nFor VLMs you typically need the lower-level inference-engine-tflite API to get the raw tensor outputs (because you fuse them with text), then feed them into the next stage.\n\n\n\ngst-launch-1.0 \\\n  v4l2src ! videoconvert ! videoscale ! \\\n  video/x-raw,format=RGB,width=384,height=384 ! \\\n  tensor_converter ! \\\n  tensor_filter framework=tensorflow-lite model=vision_int8.tflite custom=Delegate:NPU ! \\\n  tensor_filter framework=tensorflow-lite model=projector_int8.tflite ! \\\n  tensor_filter framework=tensorflow-lite model=llm_int4.tflite ! \\\n  tensor_decoder mode=detokenize ! \\\n  appsink\nThis pipeline does end-to-end video → caption entirely on the TV’s NPU.\n\n\n\nFor Galaxy phones and select Tizen devices, the ENN backend offers ~3–10× better performance than TFLite-GPU. You convert via Samsung’s ENN converter:\nenn_converter --input vision.onnx --output vision.nnc --quant int8 \\\n              --target exynos2400\nENN supports per-channel INT8 weight + per-tensor INT8 activation, and FP16 fallback for unsupported ops. The runtime is loaded by NNStreamer with framework=snpe-enn.\n\n\n\nA 2024 4K Tizen TV has ~3–4 GB total RAM, of which ~1 GB is available to apps. Practical VLM budget:\n\n\n\nComponent\nBudget\n\n\n\n\nVision encoder (INT8)\n80–150 MB\n\n\nProjector\n5–10 MB\n\n\nLLM 1–3 B (INT4)\n600–800 MB\n\n\nKV cache (for 1k tokens)\n100–200 MB\n\n\nMisc app\n100 MB\n\n\n\nThis means for TVs you typically pick a small LLM (Phi-3-mini-3.8B INT4 ≈ 1.9 GB → too big; Qwen2.5-1.5B-Instruct INT4 ≈ 800 MB → fits).\n\n\n\nTizen apps are killed aggressively. Strategies:\n\nPre-load models on app launch in a background thread.\nPin tensors in RAM by holding the mv_inference_h handle.\nLazy-load the LLM only when the user actually asks a question (vision encoder always on for ambient features).\n\n\n\n\nTizen Smart-TVs frequently process camera frames and microphone audio — Samsung’s Smart Hub privacy policy mandates that all VLM inference happens on-device unless the user explicitly opts in to cloud. Your deployment must run fully offline by default."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#implementing-mobile-inference",
    "href": "Resources/Core5/Deployment/index.html#implementing-mobile-inference",
    "title": "VLM Deployment",
    "section": "",
    "text": "Platform\nCompute units\nFrameworks\nNotes\n\n\n\n\nAndroid (high-end)\nCPU (8 cores), GPU (Adreno/Mali), NPU (Hexagon, NPU0)\nExecuTorch, TFLite, ONNX-RT, MediaPipe, QNN\nHexagon HTP is the strongest mobile NPU\n\n\nAndroid (mid-tier)\nCPU + GPU only\nTFLite GPU/XNNPACK\nNPU may be absent\n\n\niOS\nCPU (4-8 cores), GPU, Apple Neural Engine (ANE)\nCoreML, ExecuTorch+CoreML, MLX, MPSGraph\nANE accepts mlprogram only\n\n\nApple Vision Pro\nM2 + R1\nCoreML\nReal-time spatial VLMs\n\n\n\nFor VLMs in 2026, the realistic on-device targets are 3–8 B LLM, 0.4 B vision encoder, INT4 weights, running 5–25 tokens/s.\n\n\n\nThe standard mobile path. Steps:\n\nExport the model (see §4.4).\nAdd the ExecuTorch AAR to your Android app:\nimplementation 'org.pytorch:executorch-android:0.4.0'\nLoad and run from Kotlin:\nimport org.pytorch.executorch.Module\nimport org.pytorch.executorch.EValue\nimport org.pytorch.executorch.Tensor\n\nval module = Module.load(assetFilePath(this, \"vlm.pte\"))\nval pix = Tensor.fromBlob(pixelFloats, longArrayOf(1, 3, 384, 384))\nval ids = Tensor.fromBlob(tokenIds, longArrayOf(1, tokenIds.size.toLong()))\nval out = module.forward(EValue.from(pix), EValue.from(ids))\nval logits = out[0].toTensor()\nTo use the Qualcomm NPU, swap XnnpackPartitioner for QnnPartitioner(QnnExecuTorchHtpBackendOptions(...)) during export.\n\n\n\n\nfrom executorch.backends.apple.coreml.partition.coreml_partitioner import CoreMLPartitioner\nedge = edge.to_backend(CoreMLPartitioner(skip_ops_for_coreml_delegation=[]))\nprog = edge.to_executorch()\nThen in Swift:\nimport ExecuTorch\nlet module = try Module(filePath: pteURL.path)\nlet output = try module.forward([pixelTensor, idTensor])\nIf you prefer pure CoreML (no ExecuTorch), use coremltools (§4.6) and MLModel.\n\n\n\nThe LLM in a VLM is autoregressive; without a KV cache, generating 100 tokens is 100× more expensive than a single forward. ExecuTorch and CoreML both support stateful models as of 2024:\n\nCoreML state: declare states with ct.target.iOS18, ct.StateType. The state lives across predictions() calls.\nExecuTorch: explicit (input, kv_in) → (output, kv_out) signature; the runtime exposes MutableTensor to update in place.\n\nA common mistake is to materialise the full KV cache at max-length. Use a ring buffer of kv_len = current_pos actual length.\n\n\n\nFor real-time camera apps, you cannot afford a full ViT pass per frame at 30 fps. Two tricks:\n\nTemporal sub-sampling: run vision encoder at 5 fps, interpolate features for in-between frames.\nPatch-residual encoders: re-run only patches whose mean intensity has changed (delta encoding).\nFrame-difference gating: a tiny CNN decides whether the scene changed enough to require re-encoding.\n\n\n\n\nMost LLM tokenizers are SentencePiece or BPE — port them with sentencepiece-android, tiktoken-rs, or compile the C library yourself. Never ship a Python tokenizer to a phone.\nFor VLMs, you must also reserve and map the &lt;image&gt; token correctly — the same special-token IDs you trained with.\n\n\n\nDon’t allocate a float32 384×384×3 = 1.7 MB buffer on the JVM heap. Use Android’s ByteBuffer.allocateDirect (zero-copy to native), or MediaPipe Image, or iOS vImage for resize and normalisation. Saving 10 ms per frame here adds up over a long session.\n\n\n\nA phone CPU at 100 % drops to half its clock after ~3 minutes. To stay below the thermal limit:\n\nPin VLM inference to efficiency cores when possible (SCHED_BATCH on Android).\nUse NPU &gt; GPU &gt; CPU. NPUs are 10× more energy-efficient than CPUs.\nDrop the frame rate when the user is idle.\nRead BatteryManager.BATTERY_PROPERTY_CURRENT_NOW and back off when the device is hot."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#monitoring-deployment-performance",
    "href": "Resources/Core5/Deployment/index.html#monitoring-deployment-performance",
    "title": "VLM Deployment",
    "section": "",
    "text": "Metric\nDefinition\nTarget\n\n\n\n\nLatency p50 / p95 / p99\nTime per request at the 50/95/99 percentile\nDepends; for chat, p95 ≤ 1.5 s TTFT\n\n\nTTFT (Time To First Token)\nLatency to first generated token\nCritical for UX\n\n\nTPOT (Time Per Output Token)\nAverage inter-token latency\nSteady-state generation speed\n\n\nThroughput\nRequests / second or tokens / second\nServer capacity\n\n\nGPU SM utilisation\n% of streaming multiprocessors active\n&gt; 60 % is healthy\n\n\nGPU memory\nBytes allocated\n&lt; 90 % to avoid OOM\n\n\nEnergy per request\nJoules\nMobile: minimise\n\n\nAccuracy drift\nKPI vs validation set\n&lt; 1 % drop after quantisation\n\n\n\n\n\n\nInstrument the inference server with the prometheus-client library:\nfrom prometheus_client import Counter, Histogram, Gauge\n\ninflight  = Gauge(\"vlm_inflight_requests\", \"Currently processing\")\nlatency   = Histogram(\"vlm_request_latency_seconds\", \"End-to-end latency\",\n                      buckets=(.05,.1,.25,.5,1,2,5,10))\ntokens    = Counter(\"vlm_tokens_generated_total\", \"Generated tokens\")\ngpu_util  = Gauge(\"vlm_gpu_util_percent\", \"SM utilization\")\n\n@app.post(\"/generate\")\nasync def generate(req):\n    inflight.inc()\n    t0 = time.time()\n    out = model.generate(**req.payload)\n    latency.observe(time.time() - t0)\n    tokens.inc(out.shape[1])\n    inflight.dec()\n    return out\nScrape with Prometheus, render with Grafana. The dashboard you want:\n\np50/p95/p99 latency line.\nTTFT histogram.\nTokens/sec rate.\nGPU memory bar.\nError rate panel.\nSaturation: queue depth vs in-flight.\n\n\n\n\nNVIDIA’s DCGM exporter exposes per-GPU SM utilisation, memory, temperature, power, NVLink throughput, ECC errors, PCIe replays. Run it as a sidecar; scrape with the same Prometheus.\n\n\n\nFor deep dives:\nimport torch\nfrom torch.profiler import profile, ProfilerActivity\n\nwith profile(\n    activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],\n    record_shapes=True, profile_memory=True, with_stack=True,\n) as prof:\n    out = model.generate(**inputs)\nprint(prof.key_averages().table(sort_by=\"cuda_time_total\", row_limit=20))\nprof.export_chrome_trace(\"trace.json\")\nOpen the trace in chrome://tracing; look for:\n\nLong blue gaps between CUDA kernels — CPU-side bottleneck.\naten::copy_ with large tensor sizes — unwanted CPU↔︎GPU copies.\nMany tiny kernels — fusion opportunity (try torch.compile).\n\n\n\n\nBoth serving stacks expose detailed metrics out of the box:\n\nvLLM: --enable-metrics flag enables /metrics Prometheus endpoint with per-batch info.\nTensorRT-LLM: integrates with Triton Inference Server; Triton has its own Prometheus exporter.\n\nThe two queue-management metrics you must track:\n\nPending requests in the scheduler — if &gt; 0 for long periods, you’re under-provisioned.\nPre-emption count — vLLM’s continuous batching may evict requests when memory is tight.\n\n\n\n\nFor multi-stage pipelines (preprocess → vision → projector → LLM → postprocess), use OpenTelemetry to get end-to-end span traces. Jaeger or Honeycomb work great.\n\n\n\nPhones don’t run Prometheus. Strategies:\n\nFirebase Performance / iOS MetricKit for latency histograms.\nCustom thin telemetry: aggregate p50/p95 locally, batch-upload daily (privacy-friendly).\nCrash reporting: Firebase Crashlytics for native crashes from the inference C++ code.\n\n\n\n\nThe model that was 75 % MMMU yesterday is now 65 % — why? Possible causes:\n\nQuantisation calibration drift (some weights got re-quantised on the latest release).\nTokenizer mismatch (wrong special tokens).\nImage preprocessor mismatch (mean/std flipped, BGR vs RGB).\nResolution mismatch (training was 384², inference is 336²).\nSampling temperature changed.\n\nMitigation: a continuous golden-set evaluation — 200 hand-picked prompts run nightly through every deployed checkpoint; metrics piped into Prometheus. Alert on &gt; 2 σ drops.\n\n\n\nPer-token cost is the deployment KPI for cloud-served VLMs:\n\\[\n\\text{cost / 1k tokens} = \\frac{\\text{GPU hourly price} \\cdot 3600}{\\text{tokens / sec / replica}} / 1000\n\\]\nIf your H100 costs $4/hour and the deployment yields 200 tok/s, then \\(4 \\times 3600 / 200 / 1000 = \\$0.072 / k\\) tokens. Track this in Grafana right next to latency."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#troubleshooting-optimized-models",
    "href": "Resources/Core5/Deployment/index.html#troubleshooting-optimized-models",
    "title": "VLM Deployment",
    "section": "",
    "text": "Symptom\nLikely cause\n\n\n\n\nAccuracy collapse\nQuantisation calibration / dtype mismatch\n\n\nLatency regression\nOperator fallback to CPU; missing kernel\n\n\nOOM at inference\nKV cache overflow; static-shape graph too large\n\n\nNon-deterministic output\nReduction order in fp16/bf16; sampling temperature\n\n\nHang or crash on device\nUnsupported op; quantisation domain mismatch\n\n\n\n\n\n\nA systematic recipe:\n\nReproduce locally: capture the exact input that fails (image + tokens) and re-run through PyTorch reference.\nCompare layer outputs: instrument both reference and exported model with hooks, dump activations to disk, np.testing.assert_allclose layer-by-layer.\nactivations = {}\ndef hook(name): return lambda m, i, o: activations.setdefault(name, o.detach().cpu().float().numpy())\nfor n, m in model.named_modules():\n    m.register_forward_hook(hook(n))\nFind the first divergent layer. If it is a quantised conv, you have a calibration / scale problem. If it is a softmax, you have an fp16 overflow.\nInspect operators: polygraphy run vlm.onnx --trt --validate --rtol 1e-3 --atol 1e-3 for TRT, executorch.exir.print_program for ExecuTorch.\nBisect by partition: comment out half the model, re-export, see if the bug persists. Classic binary search.\n\n\n\n\n\n\nSome on-device runtimes (CoreML before iOS 17) silently downcast to fp16. The exponent range is smaller; intermediate exp() overflows. Fix: insert softmax in fp32 explicitly, or scale logits before softmax.\n\n\n\nCLIP uses mean (0.48145466, 0.4578275, 0.40821073) and std (0.26862954, 0.26130258, 0.27577711). SigLIP uses mean (0.5, 0.5, 0.5) and std (0.5, 0.5, 0.5). Mix them up and your model will almost work — accuracy drops 5–10 % silently.\n\n\n\nOpenCV reads BGR; PIL reads RGB. Models almost always trained on RGB. Swap the channels and accuracy plummets.\n\n\n\nDecoder-only LLMs require left padding at inference time (so the rightmost token is the most recent). Encoder/BERT models want right padding. Mix them up and the model will generate gibberish for the longest sample of a batch.\n\n\n\nAdding &lt;image&gt; after training without re-training the embedding produces random initial values. The model “works” but is 10 % worse on benchmarks.\n\n\n\nAfter quantisation, some runtimes recompute rotary embeddings with slightly different precision. Result: the cache from step \\(t\\) doesn’t match the embedding at step \\(t+1\\), so generation drifts. Fix: compute rotary positions in fp32 even when weights are int4.\n\n\n\n\n\n\nonnxruntime will gladly run an unsupported op on CPU while the rest runs on GPU — silently. Detect with sess.get_providers() and per-op profiling. Common offenders: ScatterND, dynamic Reshape, Range with int64.\n\n\n\n--workspace=128 is too small for VLMs. Use --workspace=4096 or --memPoolSize=workspace:4096.\n\n\n\nIf scaled_dot_product_attention falls back to the math kernel, you lose 2-3×. Check with torch.backends.cuda.sdp_kernel(enable_flash=True).\n\n\n\nA single Python interpreter is throttled by the GIL. Use multiple worker processes (gunicorn, uvicorn), or pure C++/Rust front-ends, or vLLM/TGI/TensorRT-LLM which release the GIL during compute.\n\n\n\n\n\n\nA 7 B LLM with 32-layer × 32-head × 128-dim KV in bf16 costs \\(32 \\times 2 \\times 32 \\times 128 \\times 2 = 524 288\\) bytes per token. At 8 k context that’s 4 GB; at 32 k it’s 16 GB. Either:\n\nQuantise the KV cache (fp8 / int8) — torch.compile does this with --quantize-kv.\nTruncate the context window.\nUse paged KV cache (vLLM PagedAttention).\nUse grouped-query attention (already standard in LLaMA-3, Qwen-2).\n\n\n\n\nIf you compiled an ONNX graph with max_seq_len=8192, the runtime allocates buffers for 8192 every call. Use dynamic axes or split the engine into multiple seq-length buckets.\n\n\n\n\nbf16 reductions on GPU are non-associative; the order of summation depends on which SMs scheduled which blocks. To get bit-exact reproducibility:\ntorch.use_deterministic_algorithms(True, warn_only=False)\ntorch.backends.cudnn.deterministic = True\ntorch.backends.cudnn.benchmark = False\nimport os; os.environ[\"CUBLAS_WORKSPACE_CONFIG\"] = \":4096:8\"\nSet sampling to greedy (do_sample=False) and seed everything (torch, numpy, Python’s random).\n\n\n\nOn distributed jobs, the most common cause is a single rank exiting while others wait at an all-reduce. Symptoms: training stalls; nvidia-smi shows 0 % util on some GPUs. Cure:\n\nNCCL_ASYNC_ERROR_HANDLING=1 — propagate errors across the world.\nTORCH_NCCL_BLOCKING_WAIT=1 — block on collectives with timeout.\nTORCH_NCCL_ASYNC_ERROR_HANDLING=1 (&gt;= 2.4) — supersedes the above.\nWrap each dist.collective call inside a try/except, log rank-local errors, broadcast termination signal.\n\nFor on-device crashes, attach symbol files (addr2line on Android, dSYM on iOS) to logs so you can resolve native stack frames.\n\n\n\nWhen something breaks after optimisation:\n\nCompare end-to-end outputs in fp32 reference vs target runtime.\nRe-confirm image preprocessing (mean, std, channel order, resize mode).\nRe-confirm tokenizer special tokens and pad direction.\nInspect operator coverage and provider mapping on the target.\nDump activations layer-by-layer; binary-search to first divergence.\nRe-export with debug names and shape inference enabled.\nReproduce in a tiny repro; isolate to one block.\nOpen an upstream issue with that repro.\n\n\n\n\nYou will not understand every line of a 3-million-line runtime stack. What you must build instead is fast iteration — small experiments, layer hooks, numpy assertions, a CI that exports + numerically tests every commit. A deployment engineer’s competitive advantage is not memory; it is the speed at which they can localise a bug."
  },
  {
    "objectID": "Resources/Core5/Deployment/index.html#reading-list-references",
    "href": "Resources/Core5/Deployment/index.html#reading-list-references",
    "title": "VLM Deployment",
    "section": "",
    "text": "Sergeev & Del Balso, Horovod: fast and easy distributed deep learning, 2018.\nRajbhandari et al., ZeRO: Memory Optimizations Toward Training Trillion Parameter Models, SC 2020.\nRajbhandari et al., ZeRO-Offload, ATC 2021.\nKorthikanti et al., Reducing Activation Recomputation in Large Transformer Models, 2022.\nSmith et al., Using DeepSpeed and Megatron to Train Megatron-Turing NLG 530B, 2022.\nShoeybi et al., Megatron-LM: Training Multi-Billion Parameter Language Models Using Model Parallelism, 2019.\nHuang et al., GPipe: Efficient Training of Giant Neural Networks, NeurIPS 2019.\nNarayanan et al., Pipeline-Parallel Training of Transformers, 2021.\nVogels et al., PowerSGD: Practical Low-Rank Gradient Compression, NeurIPS 2019.\nTouvron et al., LLaMA / LLaMA-2 / LLaMA-3 technical reports.\nPyTorch FSDP design RFCs and the FSDP2 (fully_shard) docs.\n\n\n\n\n\nExecuTorch documentation (pytorch.org/executorch).\nAI-Edge-Torch (Google, 2024) — PyTorch ↔︎ TFLite bridge.\nCoreML Tools User Guide.\nTensorRT Developer Guide; trtexec and polygraphy manuals.\nOpenVINO Toolkit Documentation.\nTizen Native API documentation: ML Inference, NNStreamer, Media Vision.\nLim et al., NNStreamer: Stream Processing Paradigm for Neural Networks, 2019.\nYang et al., MobileBERT, ACL 2020.\n\n\n\n\n\nPatarasuk & Yuan, Bandwidth Optimal All-Reduce Algorithms for Clusters of Workstations, 2009.\nSanders et al., Two-tree algorithms for full bandwidth broadcast, 2009.\nNCCL documentation and the nccl-tests repo (NVIDIA).\nSergeev et al., Horovod Timeline Profiling, 2018.\nCao et al., Pollux, OSDI 2021 — co-adaptive training.\n\n\n\n\n\nSculley et al., Hidden Technical Debt in Machine Learning Systems, NeurIPS 2015.\nBreck et al., The ML Test Score: A Rubric for ML Production Readiness, 2017.\nPolyzotis et al., Data Lifecycle Challenges in Production Machine Learning, SIGMOD Record 2018.\nHuggingFace Text Generation Inference server docs.\nvLLM technical paper and PagedAttention blog post.\n\n\n\n\n\nLilian Weng, “Large Transformer Model Inference Optimization” (Jan 2023).\nLilian Weng, “Scaling neural network: A short summary of GPipe, PipeDream, ZeRO, etc.”\nJay Alammar, “How GPT3 Works”.\nHuggingFace blog: “Efficient Training on Multiple GPUs”.\nPyTorch blog: “PyTorch Distributed Overview”, “FSDP”, “FSDP2”.\nNVIDIA blog: “NCCL 2.x Performance Tuning”.\nMosaic / Databricks blog: “How we trained DBRX”, “How we trained MPT”.\nSebastian Raschka, “Tips for LLM Pretraining and Fine-Tuning”.\n\n\n\n\n\nPass once front to back.\nBuild a 4-GPU FSDP toy with a 1 B model and watch the trace.\nPick a phone you have and run an ExecuTorch demo of a small VLM.\nRe-read the references for the parts that felt thin.\nPair this with the Optimization document — they are two halves of the same problem.\n\nIf you can do all four, you can deploy a VLM anywhere on Earth.\n\nEnd of Vision-Language Model Deployment document."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html",
    "href": "Resources/Core5/Optimization/index.html",
    "title": "VLM Optimization",
    "section": "",
    "text": "Optimization is where models go from “works on the demo notebook” to “wins on the leaderboard, runs on a phone, and recovers gracefully from a midnight crash”. This document covers the eight competencies you need to call yourself a VLM optimization expert: checkpointing, learning-rate scheduling, pre-training failure recovery, adaptation strategies, parameter-efficient fine-tuning, loss design, quantization, and knowledge distillation. Every section has both first-principles theory and runnable PyTorch.\n\n\n\n\n\nManaging Checkpoint Strategies\nTuning Learning Rate Schedules\nHandling Pre-training Failures\nAdapting Pretrained Models\nImplementing Parameter-Efficient Methods\nDeveloping Loss Functions\nEngineering Quantization Techniques\nBuilding Knowledge Distillation Systems\nReading List & References\n\n\n\n\n\n\n\n\nA naïve torch.save(model.state_dict(), \"ckpt.pt\") works for a 100 M model on one GPU. For a 70 B model on 64 GPUs it (a) takes 8 minutes, (b) requires rank-0 to hold the whole model in CPU RAM, (c) corrupts on crash, and (d) leaves your optimizer states orphaned across half the cluster. A real checkpoint strategy answers six questions:\n\nWhat do we save? (weights, optimizer, LR scheduler, RNG state, dataloader cursor, scaler, EMA copy)\nWhen do we save? (every N steps, every K minutes, best validation metric, at exit)\nWhere do we save? (local SSD, network FS, object store, sharded vs single file)\nHow do we save? (sync vs async, sharded vs gathered, atomic vs partial)\nHow do we resume? (bit-exact vs warm-start; same world size vs reshape)\nHow do we retire? (rolling window, by metric, manual pinning)\n\nGet any of these wrong and a week of training disappears in seconds.\n\n\n\nstate = {\n    \"model\":          model.state_dict(),       # weights\n    \"optimizer\":      optimizer.state_dict(),   # Adam m, v, step\n    \"scheduler\":      scheduler.state_dict(),   # current step\n    \"scaler\":         scaler.state_dict(),      # if using GradScaler\n    \"ema\":            ema.state_dict(),         # exponential moving average\n    \"step\":           global_step,\n    \"epoch\":          epoch,\n    \"rng\": {\n        \"python\":      random.getstate(),\n        \"numpy\":       np.random.get_state(),\n        \"torch\":       torch.get_rng_state(),\n        \"cuda\":        torch.cuda.get_rng_state_all(),\n    },\n    \"sampler\":        sampler.state_dict() if hasattr(sampler, \"state_dict\") else None,\n    \"config\":         dataclasses.asdict(config),\n    \"git_sha\":        get_git_sha(),\n    \"version\":        \"ckpt_v3\",\n}\nForgetting any of these creates a subtle resume bug. Forgetting the RNG state means a different shuffle order; forgetting the sampler state means re-doing seen samples; forgetting scaler means a different fp16 loss-scale.\n\n\n\nPyTorch 2.4+ ships torch.distributed.checkpoint (DCP). Every rank writes its own shard in parallel; resume gathers them back. Sharded checkpoints survive world-size changes (resharding-on-load) — invaluable for cluster failures.\nimport torch.distributed.checkpoint as dcp\nfrom torch.distributed.checkpoint.state_dict import get_state_dict, set_state_dict\n\nstate_dict, optim_state = get_state_dict(model, optimizer)\nckpt = {\"model\": state_dict, \"optim\": optim_state, \"step\": step}\ndcp.save(ckpt, checkpoint_id=f\"s3://bucket/ckpts/step_{step}\")\nResume:\nstate_dict, optim_state = get_state_dict(model, optimizer)\nckpt = {\"model\": state_dict, \"optim\": optim_state, \"step\": 0}\ndcp.load(ckpt, checkpoint_id=\"s3://bucket/ckpts/step_5000\")\nset_state_dict(model, optimizer, model_state_dict=ckpt[\"model\"], optim_state_dict=ckpt[\"optim\"])\nDCP integrates with FSDP, TP, PP. Don’t roll your own.\n\n\n\nA 30-second dcp.save once per 1000 steps wastes 3 % of training time. Asynchronously snapshot the state in pinned host memory, then offload the write to a background thread:\nfrom torch.distributed.checkpoint.state_dict_saver import async_save\n\nfuture = async_save(ckpt, checkpoint_id=f\"...{step}\")   # returns immediately\n# training continues\nfuture.result()                                          # wait only at exit / next save\nThis makes checkpointing essentially free for any save period &gt; 200 steps.\n\n\n\nThe checkpoint must be readable after a crash. Two strategies:\n\nWrite to a temp directory, then rename: dcp.save(..., \"ckpt_TMP_{step}\") then os.rename to ckpt_{step}. POSIX rename is atomic.\nTwo-phase commit: write all shards then a DONE marker; resume only loads if DONE exists.\n\n\n\n\nDisk fills fast. A sensible default for a 7-day run:\n\nKeep the last \\(K=3\\) recent checkpoints (rolling).\nKeep the best by validation metric (pinned).\nKeep one per epoch (sparse history).\n\ndef retain_recent(folder, k=3):\n    ckpts = sorted(p for p in os.listdir(folder) if p.startswith(\"step_\"),\n                   key=lambda p: int(p.split(\"_\")[1]))\n    for old in ckpts[:-k]:\n        shutil.rmtree(os.path.join(folder, old))\n\n\n\nEMA produces a parameter average that is usually 1–3 points better than the raw model on validation. The update rule, applied every step:\n\\[\n\\theta^{\\text{EMA}}_t = \\beta \\cdot \\theta^{\\text{EMA}}_{t-1} + (1-\\beta) \\cdot \\theta_t, \\qquad \\beta \\in [0.999, 0.9999].\n\\]\nA common variant warms up \\(\\beta\\) from 0 over the first few thousand steps. Save both the live and EMA states in your checkpoint; serve from EMA.\nclass EMA:\n    def __init__(self, model, decay=0.9999):\n        self.decay = decay\n        self.shadow = {n: p.detach().clone() for n, p in model.named_parameters() if p.requires_grad}\n\n    @torch.no_grad()\n    def update(self, model):\n        for n, p in model.named_parameters():\n            if p.requires_grad:\n                self.shadow[n].mul_(self.decay).add_(p.detach(), alpha=1 - self.decay)\n\n    def state_dict(self):  return self.shadow\n    def load_state_dict(self, sd): self.shadow = {k: v.clone() for k, v in sd.items()}\n\n\n\nbest_metric = float(\"-inf\")\ndef maybe_save_best(val_metric, step):\n    nonlocal best_metric\n    if val_metric &gt; best_metric:\n        best_metric = val_metric\n        dcp.save(ckpt, checkpoint_id=f\"{folder}/best\")\n\n\n\nFor very large runs, save directly to S3 / GCS / Azure Blob. dcp.save accepts a custom FsspecWriter:\nfrom torch.distributed.checkpoint.fsspec import FsspecWriter\ndcp.save(ckpt, storage_writer=FsspecWriter(\"s3://bucket/ckpts/step_5000\"))\nFor Samsung / on-prem clusters, Lustre or WekaIO is typical; DCP works against any POSIX FS.\n\n\n\n\n\n\n\n\n\n\n\nDecision\nDefault\nReason\n\n\n\n\nSave period\n250 steps async, 2000 steps sync\noverlap vs reliability\n\n\nFormat\nDCP sharded\nresharding & speed\n\n\nRetention\nlast 3 + best + per-epoch\ndisk hygiene\n\n\nEMA\nβ = 0.9999 from step 5000\n+ ~1–3 pts\n\n\nResume mode\nbit-exact (same world size) → DCP\nsample-perfect\n\n\nBackend\nlocal SSD ⊕ object store\nspeed + durability\n\n\n\n\n\n\n\n\n\n\n\nFor transformers, learning-rate (LR) schedule and warmup are single most-tuned hyperparameters. The right schedule recovers 1–3 points; the wrong one diverges. The four pillars:\n\nInitial LR — base learning rate at peak.\nWarmup — linear ramp from 0 to the peak.\nDecay — cosine, linear, inverse-square-root, or constant.\nFinal LR — value at the end of training (often 10 % of peak).\n\n\n\n\nIn the first few thousand steps, gradients are noisy and weights are far from a good solution. A large LR amplifies noise; a small LR makes effective progress impossible. Warmup linearly increases LR from 0 to peak over the first \\(W\\) steps:\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\min(1, t/W).\n\\]\nTypical \\(W = 500\\) to 5000 steps; longer for tiny batch sizes; can be in number of samples rather than steps for stability across hardware.\n\n\n\n\n\n\\[\n\\eta_t = \\eta_{\\min} + \\tfrac{1}{2}(\\eta_{\\max} - \\eta_{\\min})\\left(1 + \\cos\\!\\left(\\pi \\cdot \\frac{t-W}{T-W}\\right)\\right).\n\\]\nThe default for LLMs, ViTs, CLIP, BLIP-2, LLaVA. Smooth and forgiving.\n\n\n\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\left(1 - \\frac{t-W}{T-W}\\right).\n\\]\nFaster decay early on; used by RoBERTa, T5.\n\n\n\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\sqrt{W/t}\\quad (t &gt; W).\n\\]\nThe original Transformer schedule (Vaswani et al., 2017). Allows training to continue indefinitely; useful for never-ending pre-training.\n\n\n\nKeep LR constant during the bulk of training, then cool down at the end. Defazio & Mishchenko’s Schedule-Free (2024) eliminates the schedule entirely with a momentum-style rule:\n\\[\n\\theta_t \\leftarrow \\theta_{t-1} - \\eta \\nabla L(\\bar{\\theta}_t), \\quad \\bar{\\theta}_t = (1-c_t)\\bar{\\theta}_{t-1} + c_t \\theta_t.\n\\]\nEmpirically matches cosine without needing to specify \\(T\\).\n\n\n\n\nfrom torch.optim.lr_scheduler import LambdaLR\nimport math\n\ndef cosine_with_warmup(optimizer, num_warmup, num_total, min_ratio=0.1):\n    def lr_lambda(step):\n        if step &lt; num_warmup:\n            return step / max(1, num_warmup)\n        progress = (step - num_warmup) / max(1, num_total - num_warmup)\n        return max(min_ratio, 0.5 * (1 + math.cos(math.pi * progress)))\n    return LambdaLR(optimizer, lr_lambda)\nHuggingFace transformers.get_cosine_schedule_with_warmup does the same; optimum exposes more exotic schedules.\n\n\n\nVLMs often fine-tune a frozen ViT at \\(10^{-6}\\), a fresh projector at \\(10^{-3}\\), and an LLM with LoRA at \\(10^{-4}\\). Build parameter groups:\noptimizer = torch.optim.AdamW(\n    [\n        {\"params\": vision.parameters(),  \"lr\": 1e-6},\n        {\"params\": projector.parameters(), \"lr\": 1e-3},\n        {\"params\": [p for n,p in llm.named_parameters() if \"lora_\" in n], \"lr\": 1e-4},\n    ],\n    betas=(0.9, 0.95), weight_decay=0.05, eps=1e-8,\n)\nULMFit (Howard & Ruder, 2018) introduced discriminative fine-tuning — lower LR for earlier layers, higher for later. For ViTs/LLMs this is implemented as a layer-wise LR decay factor \\(\\gamma\\) (commonly 0.65–0.85):\n\\[\n\\eta_{\\ell} = \\eta_{\\max} \\cdot \\gamma^{L - \\ell}\n\\]\nwhere \\(\\ell\\) is the layer index and \\(L\\) is the total layers.\n\n\n\n\n\n\nSetting\nSuggested peak LR\n\n\n\n\nViT pre-training from scratch\n\\(1\\times 10^{-3}\\) (AdamW, β2=0.95)\n\n\nCLIP-style contrastive\n\\(5\\times 10^{-4}\\) (linear)\n\n\nLLM pre-training (decoder, 7 B)\n\\(3\\times 10^{-4}\\)\n\n\nLLM continued pre-training\n\\(1\\times 10^{-4}\\)\n\n\nVLM Stage-1 (projector only)\n\\(1\\times 10^{-3}\\)\n\n\nVLM Stage-2 (full instruction tune)\n\\(2\\times 10^{-5}\\)\n\n\nLoRA on a 7 B LLM\n\\(1\\times 10^{-4}\\) to \\(5\\times 10^{-4}\\)\n\n\nQLoRA\n\\(2\\times 10^{-4}\\)\n\n\n\nThese are starting points. A 30-minute LR-finder sweep (Smith, 2017) almost always pays for itself.\n\n\n\nFor SGD-style optimisers, the linear-scaling rule (Goyal et al., 2017): when batch size goes \\(k \\times\\), multiply LR by \\(k\\) and use linear warmup. For AdamW, the rule is sub-linear (often \\(\\sqrt{k}\\) or even less). For contrastive losses (CLIP, SigLIP) where larger batch directly improves the objective, the optimal LR rises slower than \\(\\sqrt{k}\\).\n\n\n\nTwo newer optimisers worth knowing:\n\nLion (Chen et al., 2023) — uses only the sign of the momentum; needs LR ≈ 1/10 of AdamW; memory-efficient.\nSchedule-Free AdamW — eliminates the LR schedule; benchmark-competitive.\n\nBoth have first-class PyTorch implementations (from lion_pytorch import Lion, from schedulefree import AdamWScheduleFree).\n\n\n\nAlways clip gradients during VLM training:\ntorch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\nThe 1.0 norm is standard for LLMs; tighter (0.5) for ViTs pre-training; looser (5.0) for SGD-from-scratch. Clipping prevents the rare spike from blowing up Adam’s moments.\n\n\n\n\nLoss plateaus immediately: warmup too short or LR too low.\nLoss spikes after warmup: peak LR too high; reduce or extend warmup.\nLoss diverges late: weight decay too low; learning rate too high.\nBest validation arrives early: training too long; reduce \\(T\\).\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nSymptom\nLikely cause\n\n\n\n\nLoss = NaN suddenly\nfp16 overflow; exploding gradient; bad sample\n\n\nLoss diverges monotonically\nLR too high; weight init bad; missing LN\n\n\nLoss plateaus at high value\nLR too low; loss bug; label mismatch\n\n\nLoss spikes then recovers\nbad batch; well-behaved transformer thing\n\n\nGrad norm explodes\ndead ReLU / very deep block; precision issue\n\n\nGPU OOM partway\ndataloader memory leak; KV cache overflow\n\n\nThroughput collapses\nfilesystem hot-spot; sync stragglers\n\n\nValidation diverges from train\noverfitting; eval data drift\n\n\n\n\n\n\nMost failures appear in the first day. Hit each item:\n\nVerify the first batch: shapes, dtypes, image normalisation, tokens make sense; loss for one sample is reasonable.\nOverfit a tiny subset (e.g., 64 samples) — loss should hit ~0 in a few hundred steps. If it doesn’t, your model is broken before scaling matters.\nInitial loss == log(vocab) for an untrained LM (e.g., ≈ 10.5 for 32k vocab).\nWarmup curve is smooth (no sudden drops or spikes).\nGrad-norm is in [0.1, 10] range; if it is \\(10^4\\), you have a NaN-cascade in waiting.\nGPU utilisation &gt; 60 % on every rank.\nPer-rank loss differs by &lt; 5 % — bigger gaps mean data sharding bugs.\nEMA loss drifts only slowly — large EMA-loss vs raw-loss gap means too-fast LR.\n\n\n\n\nThe most common cause is fp16 overflow. Possible loci:\n\nSoftmax denominator — exponentiated logits &gt; 65k overflow. Cast softmax to fp32 explicitly.\nLoss scale too high in GradScaler — the scaler should auto-adjust; watch its history.\nAdam \\(\\epsilon\\) too small — bump to \\(1\\times 10^{-6}\\) for fp16, \\(1\\times 10^{-8}\\) for bf16.\nLayer-norm divide-by-zero — bf16 helps; ensure \\(\\epsilon \\geq 10^{-5}\\).\n\nSwitch to bf16 in any 2025+ training run. It has the same exponent range as fp32 and is supported on every GPU since A100. fp16 is a legacy choice you should only use on V100.\nIf you must keep fp16, instrument:\ndef hook(module, input, output):\n    if torch.is_tensor(output) and not torch.isfinite(output).all():\n        print(f\"non-finite at {module}; min/max={output.min().item()}/{output.max().item()}\")\nfor name, m in model.named_modules(): m.register_forward_hook(hook)\n\n\n\nWhen a run NaNs at step 87 421:\n\nRoll back to the most recent checkpoint (say step 86 000).\nSkip the bad batch: advance the dataloader past the offending sample(s). If you don’t track samples per rank, you cannot do this — another reason to log sampler state.\nLower LR by 2× temporarily and resume; usually fine after 1k steps.\nClip more aggressively: e.g., reduce max_norm from 1.0 to 0.5.\nIf it NaNs again at the same step: investigate the data. Often a corrupted JPEG, a single sample with insane CLIP score, or a near-empty text.\n\n\n\n\nTransformers, especially LLM backbones at &gt;1 B params, exhibit recoverable loss spikes during pre-training (Chowdhery et al., PaLM, 2022). The principled response is:\n\nDon’t restart unless the spike is &gt; 4 × the recent EMA loss and doesn’t recover within 1k steps.\nLog gradient skew (kurtosis) — large skew predicts spikes.\nAdd embed-norm regularisation (Dehghani et al., ViT-22B) for huge models.\n\n\n\n\nTrain throughput halving overnight is usually:\n\nStorage filling up — checkpoint retention failed; SSD at 99 %.\nStragglers — one rank slow because of a hot CPU or NIC.\nPower throttling — GPU drops from 700 W to 400 W when DC AC fails.\nNCCL congestion — a noisy neighbour on the IB switch.\n\nAlways log time_per_step per rank and alert on a 30 % deviation.\n\n\n\nA subtle but common bug: a single corrupt shard that gets sampled by every rank eventually. Symptoms:\n\nAll ranks NaN around the same global step.\nReplaying from checkpoint reproduces the NaN at the same step.\n\nFix: hash every sample, fingerprint shards, and skip on read errors:\nimport io, hashlib\ndef safe_decode(jpeg_bytes):\n    try:\n        img = Image.open(io.BytesIO(jpeg_bytes)).convert(\"RGB\")\n        img.load()\n        return img\n    except Exception:\n        return None\n\n\n\nWhen investigating a failure, set torch.use_deterministic_algorithms(True), fixed seeds, and run on a single GPU. The aim isn’t reproducible production — it’s a reliable repro of the bug.\n\n\n\nYou cannot diagnose what you didn’t log. Mandatory logs per step:\n\nstep, epoch, sample count, wall time, throughput.\nraw loss, EMA loss, validation loss every \\(V\\) steps.\ngradient norm (pre-clip), parameter norm.\nLR per group, optimizer step count.\nGPU memory, util, power.\non-rank zero only: image-grids of a fixed sample, a generated caption, attention map.\n\nUse Weights & Biases, MLflow, or plain TensorBoard.\n\n\n\nWhenever a run crashes, write a one-page post-mortem with:\n\nSymptom — what failed, when, on which rank.\nTrigger — root cause if known; hypothesis if not.\nDiagnosis steps — what you ran, what you saw.\nResolution — code, config, or hardware change.\nPrevention — alert, test, or runbook entry.\n\nA bug fixed and undocumented will recur.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nStrategy\nWhat is updated?\nCompute\nUse case\n\n\n\n\nLinear probe\nA single linear classifier on frozen features\ntiny\nDiagnostic, retrieval\n\n\nFull fine-tune\nAll parameters\nhuge\nDomain shift, max accuracy\n\n\nLast-N-layers\nThe top N layers only\nmedium\nWhen data is scarce\n\n\nPEFT (LoRA / Adapters / Prompts)\n&lt; 1 % of parameters\nsmall\nPersonalisation, multi-tenant\n\n\nContinued pre-training\nAll params, same objective\nhuge\nNew language / domain corpus\n\n\n\nFor VLMs, the typical adaptation pipeline is:\n\nStage 1: train projector only (alignment).\nStage 2: instruction-tune projector + LLM (often LoRA on LLM, projector full).\nStage 3: domain-tune on small specialised data (LoRA on a copy of LLM; switchable at inference).\n\n\n\n\nWhen fine-tuning, the model’s original abilities decay. Mitigation:\n\nLower LR for layers carrying broad knowledge (early ViT layers, base LLM blocks).\nMix the original pre-training data into the fine-tune mix.\nEWC (Kirkpatrick et al., 2017) — penalise changes weighted by Fisher information.\nLoRA / Adapters — base weights frozen, so original abilities are mathematically preserved.\n\n\n\n\nFor a ViT encoder, unfreezing the last 4 blocks typically wins; for an LLM, the top 8 blocks or all attention layers. A defensible heuristic:\n\nProbe by layer index — train a linear classifier on each layer’s features. The “knee” of the curve identifies where task-specific information starts to dominate.\nUnfreeze from the knee upward.\n\n\n\n\nThe recipe:\n\nContinued pre-training on a few B unlabelled images + captions (LM loss).\nInstruction tuning on tens of thousands of domain instructions.\nEvaluation on domain benchmarks (PathVQA, ChartQA, GeoChat, RoboVQA).\n\nFor medical VLMs, look at LLaVA-Med, RadFM, PathChat. For documents, LLaVA-NeXT-Doc, mPLUG-DocOwl, InternVL2-Doc.\n\n\n\ndef stage1_alignment(vlm, loader, steps=5000):\n    vlm.freeze_for_alignment()                  # vision + LLM frozen\n    opt = torch.optim.AdamW(vlm.projector.parameters(), lr=1e-3)\n    schd = cosine_with_warmup(opt, 500, steps)\n    train(vlm, loader, opt, schd, steps)\n\ndef stage2_instruction(vlm, loader, steps=20000):\n    add_lora(vlm.llm, r=64, alpha=16, target_modules=(\"q_proj\",\"v_proj\"))\n    for p in vlm.vision.parameters(): p.requires_grad = False\n    params = list(vlm.projector.parameters()) + [p for p in vlm.llm.parameters() if p.requires_grad]\n    opt = torch.optim.AdamW(params, lr=2e-5)\n    schd = cosine_with_warmup(opt, 1000, steps)\n    train(vlm, loader, opt, schd, steps)\n\ndef stage3_domain(vlm, loader, steps=5000):\n    # Train a new LoRA adapter named \"medical\" while keeping stage2 LoRA frozen\n    add_lora(vlm.llm, r=8, alpha=16, name=\"medical\")\n    opt = torch.optim.AdamW(get_lora_params(vlm.llm, name=\"medical\"), lr=1e-4)\n    schd = cosine_with_warmup(opt, 200, steps)\n    train(vlm, loader, opt, schd, steps)\n\n\n\nAlways run three suites before declaring success:\n\nTarget benchmark — domain-specific.\nGeneral VLM benchmarks — MMMU, MMBench, MathVista. Drop &gt; 2 pts = regression.\nSmoke tests — your own held-out prompts. Sanity-check qualitatively.\n\nA common mistake is to celebrate a domain win that masks a 10-point general drop.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nMethod\nTrainable params\nMemory\nNotes\n\n\n\n\nLoRA\n\\(r(d_{\\text{in}} + d_{\\text{out}})\\) per matrix\nlow\nDefault; merges at inference\n\n\nQLoRA\nSame as LoRA\n4-bit base\nFine-tune 33 B on a single 24 GB GPU\n\n\nDoRA\nLoRA + magnitude vector\nslightly higher\nOften beats LoRA\n\n\nVeRA\nTwo shared random matrices + scaling\ntiny\nMulti-task heavy\n\n\n(IA)\\(^3\\)\nThree scaling vectors per block\ntiny\nFast inference\n\n\nBitFit\nAll biases\nvery low\nStrong baseline\n\n\nPrompt tuning\nA small embedding matrix prepended\nvery low\nBrittle for long context\n\n\nPrefix tuning\nPer-layer trainable prefixes\nlow\nPowerful, harder to tune\n\n\nAdapters (Houlsby/Pfeiffer)\nBottleneck MLPs inside blocks\nlow\nMature; serial vs parallel variants\n\n\nLoRA-FA\nLoRA + frozen down-projection\nhalf of LoRA\nUsed in DoRA\n\n\n\n\n\n\nFor a frozen linear layer \\(W \\in \\mathbb{R}^{d \\times k}\\), learn a low-rank update:\n\\[\nW' = W + \\Delta W, \\qquad \\Delta W = \\tfrac{\\alpha}{r} \\cdot B A, \\quad A \\in \\mathbb{R}^{r \\times k}, \\; B \\in \\mathbb{R}^{d \\times r}.\n\\]\nTypical \\(r \\in \\{4, 8, 16, 32, 64\\}\\), \\(\\alpha = 2r\\). At inference, merge: \\(W \\leftarrow W + \\tfrac{\\alpha}{r} BA\\) — zero overhead.\nPure-PyTorch implementation:\nclass LoRALinear(nn.Module):\n    def __init__(self, base: nn.Linear, r=8, alpha=16, dropout=0.0):\n        super().__init__()\n        self.base = base\n        for p in self.base.parameters(): p.requires_grad = False\n        d_in, d_out = base.in_features, base.out_features\n        self.A = nn.Parameter(torch.empty(r, d_in))\n        self.B = nn.Parameter(torch.zeros(d_out, r))\n        nn.init.kaiming_uniform_(self.A, a=math.sqrt(5))   # Kaiming for A\n        nn.init.zeros_(self.B)                              # zero for B -&gt; ΔW=0 at init\n        self.scale = alpha / r\n        self.drop = nn.Dropout(dropout) if dropout &gt; 0 else nn.Identity()\n\n    def forward(self, x):\n        return self.base(x) + self.drop(x) @ self.A.T @ self.B.T * self.scale\nThe standard target modules in a transformer block are the four attention projections (q_proj, k_proj, v_proj, o_proj); for LLaMA-style models add the MLP projections (gate_proj, up_proj, down_proj).\n\n\n\nThe breakthrough: quantise the base model to 4-bit NormalFloat (NF4), keep LoRA in bf16, double-quantise the quantisation constants. Memory drops 4× while accuracy is preserved within 0.1 pts.\nfrom transformers import BitsAndBytesConfig, AutoModelForCausalLM\nfrom peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training\n\nbnb = BitsAndBytesConfig(\n    load_in_4bit=True,\n    bnb_4bit_quant_type=\"nf4\",\n    bnb_4bit_compute_dtype=torch.bfloat16,\n    bnb_4bit_use_double_quant=True,\n)\n\nllm = AutoModelForCausalLM.from_pretrained(\n    \"Qwen/Qwen2.5-7B-Instruct\", quantization_config=bnb,\n    attn_implementation=\"flash_attention_2\",\n)\nllm = prepare_model_for_kbit_training(llm, use_gradient_checkpointing=True)\n\nlora_cfg = LoraConfig(\n    r=64, lora_alpha=16, lora_dropout=0.05, bias=\"none\",\n    target_modules=[\"q_proj\",\"k_proj\",\"v_proj\",\"o_proj\",\n                    \"gate_proj\",\"up_proj\",\"down_proj\"],\n    task_type=\"CAUSAL_LM\",\n)\nllm = get_peft_model(llm, lora_cfg)\nllm.print_trainable_parameters()\nWhy NF4? The empirical distribution of pre-trained LLM weights is roughly Gaussian; NF4 is the quantile-based 4-bit code-book that is optimal for a Gaussian under squared error. Standard INT4 / FP4 codebooks distribute levels uniformly, which is sub-optimal.\nQLoRA also introduces paged optimisers — Adam states overflow into CPU memory in pages, eliminating the OOM that used to plague long-context fine-tuning.\n\n\n\nDoRA decomposes the weight matrix into magnitude and direction:\n\\[\nW = m \\cdot \\frac{V}{\\|V\\|_c}, \\qquad m \\in \\mathbb{R}^{1 \\times k}, \\; V \\in \\mathbb{R}^{d \\times k}.\n\\]\nLoRA updates direction; DoRA additionally learns the per-column magnitude \\(m\\). Typically +0.5 to +1.5 points over LoRA at similar parameter count. Supported in HuggingFace PEFT: LoraConfig(use_dora=True).\n\n\n\nA bottleneck MLP inserted after each attention or FFN block:\n\\[\nh \\leftarrow h + W_{\\text{up}} \\cdot \\sigma(W_{\\text{down}} \\cdot h), \\quad W_{\\text{down}} \\in \\mathbb{R}^{r \\times d}, W_{\\text{up}} \\in \\mathbb{R}^{d \\times r}.\n\\]\nThe Pfeiffer variant keeps only one adapter per block (after FFN); Houlsby has two (after attention and FFN). Adapters are slightly slower than LoRA at inference because they’re not mergeable; but they handle tasks where LoRA’s low-rank assumption is too restrictive (multi-task switching).\n\n\n\nThree learnable vectors per block: \\(l_k, l_v, l_{ff}\\). They re-scale keys, values, and FFN activations:\n\\[\n\\text{Attn}(Q, K, V) = \\text{softmax}\\!\\left(\\frac{Q (l_k \\odot K)^\\top}{\\sqrt d}\\right)(l_v \\odot V), \\quad \\text{FFN}(x) = W_2(l_{ff} \\odot \\text{GELU}(W_1 x)).\n\\]\nOften 100× fewer parameters than LoRA. Competitive on many benchmarks.\n\n\n\nPrompt tuning (Lester et al., 2021): prepend \\(K\\) learnable token embeddings to the input. Only those embeddings train.\nPrefix tuning (Li & Liang, 2021): per layer, prepend \\(K\\) learnable key/value vectors to the attention. More expressive than prompt tuning.\nBoth work surprisingly well at very large model scales (≥ 11 B); at smaller scales they underperform LoRA.\n\n\n\nYou can LoRA the ViT too. Target the attention projections plus the patch-embedding conv:\ntarget = [\"q_proj\",\"k_proj\",\"v_proj\",\"out_proj\",\"fc1\",\"fc2\"]   # CLIP ViT\nFor best accuracy, also unfreeze the last LayerNorm and the CLS token / positional embeddings — those are tiny and frequently bottleneck adaptation.\n\n\n\nA production VLM may host dozens of LoRA adapters (one per customer, per language, per domain). S-LoRA and vLLM Multi-LoRA schedule a single base model and swap adapter weights per request. Memory cost is proportional to total adapter size, not to base × adapters.\n\n\n\nPEFT generally matches full fine-tune within 0.5–1 pt on most benchmarks. It loses when:\n\nThe base model lacks the capability being fine-tuned (LoRA can’t add what isn’t there).\nThe domain shift is huge (medical scans vs natural images) — consider continued pre-training first.\nThe rank is too low for the task complexity — raise \\(r\\) from 8 to 64 or 128.\n\n\n\n\n\n\n\n\n\nTwo models with identical architecture but different losses behave entirely differently. A loss expresses what counts as success. For VLMs, success means a combination of contrastive alignment, image-text matching, language generation, masking-prediction, and (sometimes) explicit reward signals. We will walk every loss with formula + PyTorch.\n\n\n\nGiven a sequence \\(w_1, \\ldots, w_T\\) and conditional images \\(\\mathbf{V}\\):\n\\[\n\\mathcal{L}_{\\text{LM}} = -\\sum_{t=1}^T \\log p_\\theta(w_t \\mid w_{&lt;t}, \\mathbf{V}).\n\\]\nImplementation detail: the LM loss is only computed over assistant tokens, never over system/user instruction tokens. Set labels[mask_user_tokens] = -100 so PyTorch’s CrossEntropyLoss(ignore_index=-100) skips them.\ndef shift_and_mask(input_ids, labels):\n    # next-token prediction\n    return input_ids[:, :-1], labels[:, 1:]\n\nlogits = llm(input_ids).logits\nshift_logits, shift_labels = shift_and_mask(logits, labels)\nloss = F.cross_entropy(shift_logits.reshape(-1, shift_logits.size(-1)),\n                       shift_labels.reshape(-1), ignore_index=-100)\n\n\n\nAlready introduced in the Architecture doc. The PyTorch one-liner:\ndef clip_loss(image_features, text_features, logit_scale):\n    image_features = F.normalize(image_features, dim=-1)\n    text_features  = F.normalize(text_features,  dim=-1)\n    logits_per_image = logit_scale.exp() * image_features @ text_features.T\n    labels = torch.arange(image_features.size(0), device=image_features.device)\n    return 0.5 * (F.cross_entropy(logits_per_image, labels) +\n                  F.cross_entropy(logits_per_image.T, labels))\nThe temperature \\(\\tau = 1/\\text{logit\\_scale}\\). Clip it to [0.01, 100] to avoid blow-ups:\nwith torch.no_grad():\n    logit_scale.clamp_(0, math.log(100))\n\n\n\n\\[\n\\mathcal{L}_{\\text{SigLIP}} = -\\frac{1}{B}\\sum_{i,j} \\log \\sigma\\!\\big( z_{ij}(\\tau s_{ij} + b) \\big).\n\\]\nPyTorch:\ndef siglip_loss(img, txt, logit_scale, logit_bias):\n    img = F.normalize(img, dim=-1); txt = F.normalize(txt, dim=-1)\n    logits = logit_scale.exp() * img @ txt.T + logit_bias\n    labels = 2 * torch.eye(img.size(0), device=img.device) - 1   # +1 on diag, -1 off-diag\n    return -F.logsigmoid(labels * logits).mean()\n\n\n\nA binary classifier on the fused [CLS] token:\ndef itm_loss(model, img, txt, hard_negatives_idx):\n    pos = model.fuse(img, txt).cls       # positive pair\n    neg = model.fuse(img, txt[hard_negatives_idx]).cls\n    logits = model.itm_head(torch.cat([pos, neg], dim=0))   # [2B, 2]\n    targets = torch.cat([torch.ones(pos.size(0)),\n                         torch.zeros(neg.size(0))]).long().to(logits.device)\n    return F.cross_entropy(logits, targets)\nHard negatives are sampled with weights proportional to current cosine similarity — see ALBEF eq. (10).\n\n\n\nMAE (He et al., 2022) reconstructs masked patches in pixel space:\n\\[\n\\mathcal{L}_{\\text{MAE}} = \\frac{1}{|\\mathcal{M}|} \\sum_{i \\in \\mathcal{M}} \\| p_\\theta(x_i) - x_i \\|_2^2\n\\]\nwith per-patch L2 on the normalised target patches (mean-std per patch).\nBEiT reconstructs discrete visual tokens from a frozen dVAE:\n\\[\n\\mathcal{L}_{\\text{BEiT}} = \\mathbb{E}_{x, \\mathcal{M}}\\sum_{i\\in\\mathcal{M}} -\\log p_\\theta(z_i \\mid x_{\\mathcal{M}^c})\n\\]\nwhere \\(z_i\\) are the dVAE’s tokens.\nEVA (Fang et al., 2023) predicts CLIP features at masked positions — feature distillation + masked modelling.\n\n\n\nMulti-task training combines several losses:\n\\[\n\\mathcal{L} = \\lambda_1 \\mathcal{L}_{\\text{ITC}} + \\lambda_2 \\mathcal{L}_{\\text{ITM}} + \\lambda_3 \\mathcal{L}_{\\text{LM}} + \\lambda_4 \\mathcal{L}_{\\text{MIM}}.\n\\]\nDefault weights: \\(1, 1, 1, 1\\) for BLIP-2 representation stage; \\(0, 0, 1, 0\\) for BLIP-2 generative stage; varied for FLAVA / BEiT-3.\n\n\n\nFor RLHF-style alignment of VLMs (LLaVA-RLHF, RLAIF-V):\n\\[\n\\mathcal{L}_{\\text{DPO}} = -\\mathbb{E}_{(x, y_w, y_l)} \\log \\sigma\\!\\Big(\\beta \\log \\frac{\\pi_\\theta(y_w|x)}{\\pi_{\\text{ref}}(y_w|x)} - \\beta \\log \\frac{\\pi_\\theta(y_l|x)}{\\pi_{\\text{ref}}(y_l|x)} \\Big).\n\\]\nwhere \\((y_w, y_l)\\) are chosen/rejected responses, \\(\\pi_{\\text{ref}}\\) is the frozen reference model, \\(\\beta \\approx 0.1\\). KTO (Ethayarajh et al., 2024) variant works with unpaired labels.\ndef dpo_loss(pi_logp_chosen, pi_logp_rejected, ref_logp_chosen, ref_logp_rejected, beta=0.1):\n    pi_diff  = pi_logp_chosen  - pi_logp_rejected\n    ref_diff = ref_logp_chosen - ref_logp_rejected\n    return -F.logsigmoid(beta * (pi_diff - ref_diff)).mean()\n\n\n\n\nZ-loss (Chowdhery et al., PaLM): penalise the log-sum-exp of the LM head logits — \\(\\lambda_z \\cdot \\log^2 Z\\) where \\(Z = \\sum \\exp(\\text{logits})\\). Keeps logits well-scaled, prevents NaN.\nRouter-z loss for MoE (DeepSeek-MoE): balances expert load.\nSparsity / contrastive auxiliary for image-text retrieval to encourage uniformity.\n\n\n\n\n\nIs the loss bounded below? If not, expect divergence.\nAre gradients well-scaled when inputs are large? Add log-sum-exp tricks.\nDoes the loss reduce to a known good loss when constants degenerate? Sanity.\nIs the loss invariant to your symmetry? (translation, scale, etc.) If yes, good — if not, your data needs more augmentation.\nDoes it scale linearly with batch size, or quadratically? Contrastive scales \\(O(B^2)\\); ITM scales \\(O(B)\\).\nWhat does ChatGPT-as-a-judge think of the model trained on this loss? A surprising sanity check.\n\n\n\n\nclass VLMLoss(nn.Module):\n    def __init__(self, lambda_itc=1.0, lambda_itm=1.0, lambda_lm=1.0):\n        super().__init__()\n        self.lambda_itc = lambda_itc\n        self.lambda_itm = lambda_itm\n        self.lambda_lm  = lambda_lm\n        self.itm_head = nn.Linear(768, 2)\n\n    def forward(self, vision_emb, text_emb, fused_cls, lm_logits, labels, hard_neg_idx, logit_scale):\n        l_itc = clip_loss(vision_emb, text_emb, logit_scale)\n        l_itm = F.cross_entropy(self.itm_head(fused_cls), labels)\n        l_lm  = F.cross_entropy(lm_logits.reshape(-1, lm_logits.size(-1)),\n                                labels.reshape(-1), ignore_index=-100)\n        return self.lambda_itc * l_itc + self.lambda_itm * l_itm + self.lambda_lm * l_lm, {\n            \"itc\": l_itc.detach(), \"itm\": l_itm.detach(), \"lm\": l_lm.detach(),\n        }\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nTerm\nMeaning\n\n\n\n\nPTQ\nPost-training quantisation; no re-training\n\n\nQAT\nQuantisation-aware training; simulate quantisation during fine-tuning\n\n\nWeight-only\nActivations stay in fp16/bf16, weights are int4/int8\n\n\nW4A16\n4-bit weights, fp16 activations (e.g., AWQ, GPTQ)\n\n\nW8A8\n8-bit weights and activations (e.g., SmoothQuant)\n\n\nActivation outliers\nThe tiny number of activation channels with huge magnitudes that ruin naive INT8 quantisation\n\n\nPer-tensor\nOne scale for the whole tensor\n\n\nPer-channel\nOne scale per output channel\n\n\nPer-group\nOne scale per group of \\(g\\) contiguous channels (typical \\(g=64\\) or \\(128\\))\n\n\nStatic\nActivation scales precomputed from calibration data\n\n\nDynamic\nActivation scales computed at runtime per batch\n\n\nMixed-precision\nDifferent layers / channels use different bit widths\n\n\n\n\n\n\nSymmetric uniform quantisation of a tensor \\(x\\) to \\(b\\)-bit signed integers:\n\\[\ns = \\frac{\\max(|x|)}{2^{b-1} - 1}, \\quad q = \\mathrm{clip}\\!\\left(\\mathrm{round}(x/s),\\; -2^{b-1},\\; 2^{b-1}-1\\right), \\quad \\hat{x} = s \\cdot q.\n\\]\nAsymmetric:\n\\[\ns = \\frac{x_{\\max} - x_{\\min}}{2^b - 1}, \\quad z = \\mathrm{round}(-x_{\\min}/s), \\quad q = \\mathrm{clip}(\\mathrm{round}(x/s) + z, 0, 2^b - 1).\n\\]\nQuantisation error \\(\\| x - \\hat x \\|\\) is essentially uniform with variance \\(s^2/12\\) → smaller \\(s\\) means smaller error → group quantisation (one \\(s\\) per small group) reduces error dramatically over per-tensor.\n\n\n\nDettmers (LLM.int8(), 2022) discovered: in transformers &gt; 6.7 B parameters, a small number of activation channels have magnitudes 50–500× larger than the rest. A per-tensor scale chosen for the outliers would round the typical channels to zero. Solutions:\n\nLLM.int8(): mixed precision — outlier channels in fp16, the rest in int8.\nSmoothQuant (Xiao et al., 2023): migrate the difficulty from activations to weights: \\[\nY = X \\cdot W = (X \\cdot \\mathrm{diag}(s)^{-1})(\\mathrm{diag}(s) \\cdot W).\n\\] Pick \\(s_j = \\max(|X_{:,j}|)^\\alpha / \\max(|W_{j,:}|)^{1-\\alpha}\\) with \\(\\alpha \\approx 0.5\\). Now both sides are well-conditioned.\nAWQ (Lin et al., 2023): identify the 1 % “salient” weight channels by activation magnitude and scale them up before INT4 rounding — preserves their representation. Pure weight-only.\n\n\n\n\nGPTQ uses second-order information: layer-wise, it picks weights to quantise one at a time, updating the remaining weights to compensate, using the OBS (Optimal Brain Surgeon) approximation with a small calibration set (128 examples). Produces 3–4 bit per-channel quantised matrices with &lt; 1 % perplexity loss on most LLMs.\nThe competing landscape (early-2026 picture):\n\n\n\n\n\n\n\n\nMethod\nBits\nSalient features\n\n\n\n\nAWQ\n4 W / 16 A\nBest for serving; ~3× speedup\n\n\nGPTQ\n3-4 W / 16 A\nBest for low-bit\n\n\nGGUF (llama.cpp)\n2-8 W / 16 A\nMature CPU/GPU ecosystem\n\n\nBitsAndBytes NF4\n4 W / 16 A\nUsed in QLoRA; double quantisation\n\n\nSmoothQuant\n8 W / 8 A\nINT8 with no accuracy loss\n\n\nFP8 (Transformer Engine)\n8 W / 8 A\nH100 tensor cores\n\n\nHQQ\n2-8 W / 16 A\nCalibration-free\n\n\nSpQR\n3 W / 16 A\nSparse-quantised\n\n\nOmniQuant\n4-8 W / 16 A\nLearnable equivalent transformations\n\n\n\n\n\n\nfrom awq import AutoAWQForCausalLM\nfrom transformers import AutoTokenizer\n\ntok = AutoTokenizer.from_pretrained(\"Qwen/Qwen2.5-7B-Instruct\")\nmodel = AutoAWQForCausalLM.from_pretrained(\n    \"Qwen/Qwen2.5-7B-Instruct\", device_map=\"auto\", torch_dtype=\"auto\",\n)\nquant_config = {\"zero_point\": True, \"q_group_size\": 128, \"w_bit\": 4, \"version\": \"GEMM\"}\nmodel.quantize(tok, quant_config=quant_config,\n               calib_data=\"pileval\", max_calib_samples=512, max_calib_seq_len=512)\nmodel.save_quantized(\"./qwen2.5-7B-awq\")\nFor VLMs, you must:\n\nQuantise the LLM backbone (vision + projector remain in bf16).\nCalibrate with multimodal inputs — pass image-text pairs through the model, not just text. Otherwise the activation statistics are wrong on visual-token positions.\nRe-evaluate on multimodal benchmarks; sometimes you must rescale specific layers.\n\n\n\n\nQAT inserts fake-quant nodes (FakeQuantize) into the model and trains end-to-end with the quantisation simulation in the loss path. Used when post-training methods don’t recover enough accuracy.\nimport torch.ao.quantization as taq\nqconfig = taq.QConfig(\n    activation=taq.observer.MovingAverageMinMaxObserver.with_args(dtype=torch.qint8),\n    weight=taq.observer.PerChannelMinMaxObserver.with_args(dtype=torch.qint8, qscheme=torch.per_channel_symmetric),\n)\nmodel.qconfig = qconfig\ntaq.prepare_qat(model, inplace=True)\n# fine-tune for a few epochs\ntaq.convert(model, inplace=True)\nFor modern PyTorch (≥ 2.2) the torch.ao.quantization.quantize_pt2e API is the recommended path; it integrates with torch.export.\n\n\n\nFor inference, the KV cache often dwarfs the model weights. Quantising it to fp8 or int8 can halve memory without measurable accuracy loss:\nmodel.generate(..., cache_implementation=\"quantized\",\n               cache_config={\"backend\": \"HQQ\", \"nbits\": 4, \"axis_key\": -1, \"axis_value\": -1})\nvLLM, TensorRT-LLM, and HuggingFace all support fp8 KV cache.\n\n\n\nFP8 (e4m3 for activations, e5m2 for gradients) is hardware-accelerated on H100. Use the Transformer Engine library:\nimport transformer_engine.pytorch as te\nfrom transformer_engine.common.recipe import Format, DelayedScaling\n\nfp8_recipe = DelayedScaling(fp8_format=Format.HYBRID, amax_history_len=32)\n\nwith te.fp8_autocast(enabled=True, fp8_recipe=fp8_recipe):\n    out = model(x)\nSpeed-ups: 1.5–2× over bf16 with no accuracy loss in most LLM pre-training runs.\n\n\n\nThe calibration dataset must mirror inference traffic:\n\nFor a chat VLM, calibrate with chat-formatted multimodal data.\nFor a video VLM, calibrate with video clips.\nFor a multilingual VLM, calibrate with samples from every language.\n\nA 128–512 sample calibration set is usually enough; &gt; 2000 brings diminishing returns and risks overfitting the quantisation parameters.\n\n\n\nThree layers of validation:\n\nPer-layer activation MSE vs reference — flag layers &gt; 1 % MSE.\nEnd-to-end perplexity / loss on a held-out set.\nDownstream benchmarks (MMMU, MMBench). Anything worse than 1 % below the bf16 reference is a regression.\n\nThe dangerous failure: end-to-end perplexity looks fine but one specific capability (OCR? Chinese?) is broken. Always evaluate multiple axes.\n\n\n\n\n\n\n\n\nA 7 B-parameter teacher VLM running at 25 tok/s on an A100 is unusable on a phone. Knowledge distillation transfers the teacher’s competence into a 0.5–2 B-parameter student that runs at 50 tok/s on a Snapdragon 8 Gen 3.\n\n\n\n\n\n\n\n\n\n\n\nFlavour\nWhat is transferred\nLoss\n\n\n\n\nLogit distillation (Hinton)\nSoft probabilities over vocab\nKL divergence on softmaxed logits with temperature \\(T\\)\n\n\nFeature distillation\nIntermediate hidden states\nMSE on student-projected features\n\n\nAttention transfer\nAttention maps\nMSE / KL on attention weights\n\n\n\nFor VLMs you typically combine all three.\n\n\n\n\\[\n\\mathcal{L}_{\\text{KD}} = \\alpha \\cdot T^2 \\cdot \\mathrm{KL}\\!\\big( p_\\text{student}^{(T)} \\,\\|\\, p_\\text{teacher}^{(T)} \\big) + (1-\\alpha) \\cdot \\mathcal{L}_{\\text{CE}}(p_\\text{student}, y)\n\\]\nwhere \\(p^{(T)}_i = \\mathrm{softmax}(z_i / T)\\) and \\(T \\in [1, 10]\\) smooths the teacher’s distribution to reveal “dark knowledge” — relative probabilities among non-target classes that carry information beyond a one-hot label.\ndef kd_loss(student_logits, teacher_logits, labels, T=4.0, alpha=0.5):\n    soft_s = F.log_softmax(student_logits / T, dim=-1)\n    soft_t = F.softmax(teacher_logits / T, dim=-1)\n    l_kl = F.kl_div(soft_s, soft_t, reduction=\"batchmean\") * (T * T)\n    l_ce = F.cross_entropy(student_logits, labels)\n    return alpha * l_kl + (1 - alpha) * l_ce\nFor LLM distillation on a sequence:\ndef seq_kd(student_logits, teacher_logits, labels, T=2.0, alpha=0.7, ignore=-100):\n    s = student_logits[:, :-1].reshape(-1, student_logits.size(-1))\n    t = teacher_logits[:, :-1].reshape(-1, teacher_logits.size(-1))\n    y = labels[:, 1:].reshape(-1)\n    mask = (y != ignore)\n    soft_s = F.log_softmax(s[mask] / T, dim=-1)\n    soft_t = F.softmax(t[mask] / T, dim=-1)\n    l_kl = F.kl_div(soft_s, soft_t, reduction=\"batchmean\") * (T * T)\n    l_ce = F.cross_entropy(s, y, ignore_index=ignore)\n    return alpha * l_kl + (1 - alpha) * l_ce\n\n\n\nMatch the student’s hidden state at layer \\(l_s\\) to the teacher’s at layer \\(l_t\\) via a learned projector \\(g\\):\n\\[\n\\mathcal{L}_{\\text{feat}} = \\frac{1}{N} \\sum_i \\| g(h^{(l_s)}_{\\text{stu},i}) - h^{(l_t)}_{\\text{tea},i} \\|_2^2.\n\\]\nFor VLM vision distillation, this is essentially what EVA does (CLIP teacher → masked-prediction student).\n\n\n\nFollowing Zagoruyko & Komodakis (2017), match a normalised attention map:\n\\[\n\\mathcal{L}_{\\text{att}} = \\sum_l \\left\\| \\frac{A_{\\text{stu}}^{(l)}}{\\|A_{\\text{stu}}^{(l)}\\|_F} - \\frac{A_{\\text{tea}}^{(l)}}{\\|A_{\\text{tea}}^{(l)}\\|_F} \\right\\|_F^2.\n\\]\nFor transformers, attention maps have shape [B, H, T, T]; align heads either via a Hungarian assignment or by pooling over heads.\n\n\n\nHinton’s KL is \\(\\mathrm{KL}(p_\\text{stu} \\| p_\\text{tea})\\) — forward KL. Reverse KL \\(\\mathrm{KL}(p_\\text{tea} \\| p_\\text{stu})\\) is mode-seeking: the student focuses on the highest-probability modes of the teacher and ignores low-probability “long tails”. For long-form generation, reverse KL produces more decisive students. MiniLLM (Gu et al., 2023) implements policy-gradient optimisation of reverse KL for generative distillation.\n\n\n\n\nOff-policy: teacher generates targets, student trains on them — fast but the distribution mismatch (teacher and student generate different sequences) hurts.\nOn-policy: at each step, student generates a token, teacher predicts the distribution at that exact context, loss matches them. Slower but better.\n\nA typical recipe: off-policy for the first 10 k steps to bootstrap, then on-policy.\n\n\n\nclass DistillVLM(nn.Module):\n    def __init__(self, teacher, student, T=2.0, alpha=0.7, feature_layers=(8, 16)):\n        super().__init__()\n        self.teacher = teacher.eval()\n        for p in self.teacher.parameters(): p.requires_grad = False\n        self.student = student\n        self.T, self.alpha = T, alpha\n        self.feat_proj = nn.ModuleList([\n            nn.Linear(student.cfg.d, teacher.cfg.d) for _ in feature_layers\n        ])\n        self.feat_layers = feature_layers\n\n    def forward(self, pixel_values, input_ids, labels):\n        with torch.no_grad():\n            t_out = self.teacher(pixel_values=pixel_values, input_ids=input_ids,\n                                 output_hidden_states=True)\n        s_out = self.student(pixel_values=pixel_values, input_ids=input_ids,\n                             output_hidden_states=True)\n        l_kd = seq_kd(s_out.logits, t_out.logits, labels, self.T, self.alpha)\n        l_feat = 0\n        for proj, l in zip(self.feat_proj, self.feat_layers):\n            l_feat = l_feat + F.mse_loss(proj(s_out.hidden_states[l]),\n                                         t_out.hidden_states[l])\n        return l_kd + 0.1 * l_feat\n\n\n\nThe distillation data matters at least as much as the loss. Best practice:\n\nTeacher-generated captions for raw images (LLaVA-1.5’s “CapFilt” trick).\nDiverse instruction mix: VQA, OCR, charts, math, reasoning, dialog.\nSelf-distillation (student becomes the teacher of an even smaller student).\n\nVLM-specific distillation papers: MiniLM-V (Wang et al., 2024), TinyLLaVA (2024), LLaVA-PoolDist, MobileVLM (Chu et al., 2023).\n\n\n\n\nMatch teacher’s behaviour on a held-out agreement set (% same prediction).\nMatch teacher’s quality on standard benchmarks — gap should be &lt; 5 pts for a 2× compression.\nVerify what was lost — usually long-tail knowledge or rare languages. Re-fine-tune on those if necessary.\n\n\n\n\n\\[\n\\mathcal{L} = \\alpha \\cdot \\mathcal{L}_{\\text{KD-logit}}^{T} + \\beta \\cdot \\mathcal{L}_{\\text{feat}} + \\gamma \\cdot \\mathcal{L}_{\\text{att}} + (1-\\alpha-\\beta-\\gamma) \\cdot \\mathcal{L}_{\\text{LM}}\n\\]\nwith \\(\\alpha=0.5,\\, \\beta=0.1,\\, \\gamma=0.05\\) as a sensible starting point. Anneal \\(\\beta, \\gamma \\to 0\\) over training so the student isn’t forever tied to the teacher’s intermediate representations.\n\n\n\n\n\n\n\n\n\nPyTorch documentation: torch.distributed.checkpoint, torch.distributed.fsdp.FSDP, torch.optim.swa_utils.AveragedModel.\nTorchSnapshot / DCP design RFCs.\nMosaicML Composer docs — best-practice production training loop.\n\n\n\n\n\nVaswani et al., Attention Is All You Need — original inverse-square-root warmup, 2017.\nLoshchilov & Hutter, Decoupled Weight Decay Regularization (AdamW), 2017.\nSmith, Cyclical Learning Rates for Training Neural Networks, 2017.\nGoyal et al., Accurate, Large Minibatch SGD: Training ImageNet in 1 Hour, 2017.\nHoward & Ruder, Universal Language Model Fine-tuning for Text Classification (ULMFiT), 2018.\nChen et al., Symbolic Discovery of Optimization Algorithms (Lion), 2023.\nDefazio & Mishchenko, Schedule-Free, 2024.\n\n\n\n\n\nChowdhery et al., PaLM: Scaling Language Modeling with Pathways, 2022 — loss spikes.\nBrown et al., Language Models are Few-Shot Learners (GPT-3), 2020 — training-curve commentary.\nDehghani et al., Scaling Vision Transformers to 22 Billion Parameters, 2023.\nYang et al., Tensor Programs V (μP), 2022 — width-stable initialisation.\n\n\n\n\n\nHoulsby et al., Parameter-Efficient Transfer Learning for NLP, ICML 2019.\nPfeiffer et al., AdapterFusion, EACL 2021.\nHu et al., LoRA: Low-Rank Adaptation of Large Language Models, ICLR 2022.\nDettmers et al., QLoRA: Efficient Finetuning of Quantized LLMs, NeurIPS 2023.\nLiu et al., DoRA: Weight-Decomposed Low-Rank Adaptation, ICML 2024.\nLiu et al., Few-Shot Parameter-Efficient Fine-Tuning is Better and Cheaper than In-Context Learning (T-Few, (IA)\\(^3\\)), NeurIPS 2022.\nLi & Liang, Prefix-Tuning, ACL 2021.\nLester et al., The Power of Scale for Parameter-Efficient Prompt Tuning, EMNLP 2021.\nKopiczko et al., VeRA, 2023.\nHuggingFace PEFT library docs and examples.\n\n\n\n\n\nvan den Oord et al., Representation Learning with Contrastive Predictive Coding (InfoNCE), 2018.\nChen et al., A Simple Framework for Contrastive Learning of Visual Representations (SimCLR), 2020.\nHe et al., Momentum Contrast for Unsupervised Visual Representation Learning (MoCo), 2020.\nCaron et al., Unsupervised Learning of Visual Features by Contrasting Cluster Assignments (SwAV), 2020.\nZhai et al., Sigmoid Loss for Language Image Pre-Training (SigLIP), 2023.\nRafailov et al., Direct Preference Optimization, NeurIPS 2023.\nEthayarajh et al., KTO: Model Alignment as Prospect Theoretic Optimization, 2024.\nWang & Isola, Understanding Contrastive Representation Learning through Alignment and Uniformity on the Hypersphere, ICML 2020.\n\n\n\n\n\nJacob et al., Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference, CVPR 2018.\nDettmers et al., LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale, 2022.\nFrantar et al., GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers, ICLR 2023.\nXiao et al., SmoothQuant, ICML 2023.\nLin et al., AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration, MLSys 2024.\nShao et al., OmniQuant, ICLR 2024.\nEgiazarian et al., Extreme Compression of Large Language Models via Additive Quantization (AQLM), 2024.\nNVIDIA Transformer Engine docs and Using FP8 with Transformer Engine blog.\n\n\n\n\n\nHinton et al., Distilling the Knowledge in a Neural Network, 2015.\nRomero et al., FitNets: Hints for Thin Deep Nets, 2015.\nZagoruyko & Komodakis, Paying More Attention to Attention, ICLR 2017.\nSanh et al., DistilBERT, 2019.\nWang et al., MiniLM and MiniLMv2, 2020 / 2021.\nGu et al., MiniLLM: Knowledge Distillation of Large Language Models, 2023.\nTunstall et al., Zephyr: Direct Distillation of LM Alignment, 2023.\nChu et al., MobileVLM, 2023.\nZhou et al., TinyLLaVA, 2024.\n\n\n\n\n\nLilian Weng, “Large Language Model Inference” (2023).\nLilian Weng, “From Autoencoder to Beta-VAE” (general background).\nLilian Weng, “Policy Gradient Algorithms” (for DPO/PPO/RLHF context).\nJay Alammar, “The Illustrated Stable Diffusion”, “The Illustrated GPT-2”.\nHuggingFace blog: “Making LLMs lighter with quantization”, “QLoRA Explained”.\nSebastian Raschka, “Practical Tips for Finetuning LLMs Using LoRA” (2023), “Understanding Reasoning LLMs” (2025).\nTim Dettmers, “8-bit Methods for Efficient Deep Learning” (personal blog).\nMosaicML / Databricks blog series on optimisation tricks.\n\n\n\n\n\nOne pass through the entire document.\nImplement LoRA from scratch on a 1 B LLM and verify against PEFT.\nQuantise a 7 B VLM to AWQ-W4A16 and run MMMU evaluation.\nDistil a 7 B VLM into a 1.5 B student using logit + feature distillation, evaluate the gap.\nRun a tiny multi-GPU pre-training with FSDP + cosine schedule + DCP checkpoints; deliberately crash mid-run and resume.\nRead three papers per topic from the lists above.\n\nIf you can complete the five exercises, you have the optimisation skill-set of a senior VLM engineer.\n\nEnd of Vision-Language Model Optimization document."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#roadmap",
    "href": "Resources/Core5/Optimization/index.html#roadmap",
    "title": "VLM Optimization",
    "section": "",
    "text": "Managing Checkpoint Strategies\nTuning Learning Rate Schedules\nHandling Pre-training Failures\nAdapting Pretrained Models\nImplementing Parameter-Efficient Methods\nDeveloping Loss Functions\nEngineering Quantization Techniques\nBuilding Knowledge Distillation Systems\nReading List & References"
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#managing-checkpoint-strategies",
    "href": "Resources/Core5/Optimization/index.html#managing-checkpoint-strategies",
    "title": "VLM Optimization",
    "section": "",
    "text": "A naïve torch.save(model.state_dict(), \"ckpt.pt\") works for a 100 M model on one GPU. For a 70 B model on 64 GPUs it (a) takes 8 minutes, (b) requires rank-0 to hold the whole model in CPU RAM, (c) corrupts on crash, and (d) leaves your optimizer states orphaned across half the cluster. A real checkpoint strategy answers six questions:\n\nWhat do we save? (weights, optimizer, LR scheduler, RNG state, dataloader cursor, scaler, EMA copy)\nWhen do we save? (every N steps, every K minutes, best validation metric, at exit)\nWhere do we save? (local SSD, network FS, object store, sharded vs single file)\nHow do we save? (sync vs async, sharded vs gathered, atomic vs partial)\nHow do we resume? (bit-exact vs warm-start; same world size vs reshape)\nHow do we retire? (rolling window, by metric, manual pinning)\n\nGet any of these wrong and a week of training disappears in seconds.\n\n\n\nstate = {\n    \"model\":          model.state_dict(),       # weights\n    \"optimizer\":      optimizer.state_dict(),   # Adam m, v, step\n    \"scheduler\":      scheduler.state_dict(),   # current step\n    \"scaler\":         scaler.state_dict(),      # if using GradScaler\n    \"ema\":            ema.state_dict(),         # exponential moving average\n    \"step\":           global_step,\n    \"epoch\":          epoch,\n    \"rng\": {\n        \"python\":      random.getstate(),\n        \"numpy\":       np.random.get_state(),\n        \"torch\":       torch.get_rng_state(),\n        \"cuda\":        torch.cuda.get_rng_state_all(),\n    },\n    \"sampler\":        sampler.state_dict() if hasattr(sampler, \"state_dict\") else None,\n    \"config\":         dataclasses.asdict(config),\n    \"git_sha\":        get_git_sha(),\n    \"version\":        \"ckpt_v3\",\n}\nForgetting any of these creates a subtle resume bug. Forgetting the RNG state means a different shuffle order; forgetting the sampler state means re-doing seen samples; forgetting scaler means a different fp16 loss-scale.\n\n\n\nPyTorch 2.4+ ships torch.distributed.checkpoint (DCP). Every rank writes its own shard in parallel; resume gathers them back. Sharded checkpoints survive world-size changes (resharding-on-load) — invaluable for cluster failures.\nimport torch.distributed.checkpoint as dcp\nfrom torch.distributed.checkpoint.state_dict import get_state_dict, set_state_dict\n\nstate_dict, optim_state = get_state_dict(model, optimizer)\nckpt = {\"model\": state_dict, \"optim\": optim_state, \"step\": step}\ndcp.save(ckpt, checkpoint_id=f\"s3://bucket/ckpts/step_{step}\")\nResume:\nstate_dict, optim_state = get_state_dict(model, optimizer)\nckpt = {\"model\": state_dict, \"optim\": optim_state, \"step\": 0}\ndcp.load(ckpt, checkpoint_id=\"s3://bucket/ckpts/step_5000\")\nset_state_dict(model, optimizer, model_state_dict=ckpt[\"model\"], optim_state_dict=ckpt[\"optim\"])\nDCP integrates with FSDP, TP, PP. Don’t roll your own.\n\n\n\nA 30-second dcp.save once per 1000 steps wastes 3 % of training time. Asynchronously snapshot the state in pinned host memory, then offload the write to a background thread:\nfrom torch.distributed.checkpoint.state_dict_saver import async_save\n\nfuture = async_save(ckpt, checkpoint_id=f\"...{step}\")   # returns immediately\n# training continues\nfuture.result()                                          # wait only at exit / next save\nThis makes checkpointing essentially free for any save period &gt; 200 steps.\n\n\n\nThe checkpoint must be readable after a crash. Two strategies:\n\nWrite to a temp directory, then rename: dcp.save(..., \"ckpt_TMP_{step}\") then os.rename to ckpt_{step}. POSIX rename is atomic.\nTwo-phase commit: write all shards then a DONE marker; resume only loads if DONE exists.\n\n\n\n\nDisk fills fast. A sensible default for a 7-day run:\n\nKeep the last \\(K=3\\) recent checkpoints (rolling).\nKeep the best by validation metric (pinned).\nKeep one per epoch (sparse history).\n\ndef retain_recent(folder, k=3):\n    ckpts = sorted(p for p in os.listdir(folder) if p.startswith(\"step_\"),\n                   key=lambda p: int(p.split(\"_\")[1]))\n    for old in ckpts[:-k]:\n        shutil.rmtree(os.path.join(folder, old))\n\n\n\nEMA produces a parameter average that is usually 1–3 points better than the raw model on validation. The update rule, applied every step:\n\\[\n\\theta^{\\text{EMA}}_t = \\beta \\cdot \\theta^{\\text{EMA}}_{t-1} + (1-\\beta) \\cdot \\theta_t, \\qquad \\beta \\in [0.999, 0.9999].\n\\]\nA common variant warms up \\(\\beta\\) from 0 over the first few thousand steps. Save both the live and EMA states in your checkpoint; serve from EMA.\nclass EMA:\n    def __init__(self, model, decay=0.9999):\n        self.decay = decay\n        self.shadow = {n: p.detach().clone() for n, p in model.named_parameters() if p.requires_grad}\n\n    @torch.no_grad()\n    def update(self, model):\n        for n, p in model.named_parameters():\n            if p.requires_grad:\n                self.shadow[n].mul_(self.decay).add_(p.detach(), alpha=1 - self.decay)\n\n    def state_dict(self):  return self.shadow\n    def load_state_dict(self, sd): self.shadow = {k: v.clone() for k, v in sd.items()}\n\n\n\nbest_metric = float(\"-inf\")\ndef maybe_save_best(val_metric, step):\n    nonlocal best_metric\n    if val_metric &gt; best_metric:\n        best_metric = val_metric\n        dcp.save(ckpt, checkpoint_id=f\"{folder}/best\")\n\n\n\nFor very large runs, save directly to S3 / GCS / Azure Blob. dcp.save accepts a custom FsspecWriter:\nfrom torch.distributed.checkpoint.fsspec import FsspecWriter\ndcp.save(ckpt, storage_writer=FsspecWriter(\"s3://bucket/ckpts/step_5000\"))\nFor Samsung / on-prem clusters, Lustre or WekaIO is typical; DCP works against any POSIX FS.\n\n\n\n\n\n\n\n\n\n\n\nDecision\nDefault\nReason\n\n\n\n\nSave period\n250 steps async, 2000 steps sync\noverlap vs reliability\n\n\nFormat\nDCP sharded\nresharding & speed\n\n\nRetention\nlast 3 + best + per-epoch\ndisk hygiene\n\n\nEMA\nβ = 0.9999 from step 5000\n+ ~1–3 pts\n\n\nResume mode\nbit-exact (same world size) → DCP\nsample-perfect\n\n\nBackend\nlocal SSD ⊕ object store\nspeed + durability"
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#tuning-learning-rate-schedules",
    "href": "Resources/Core5/Optimization/index.html#tuning-learning-rate-schedules",
    "title": "VLM Optimization",
    "section": "",
    "text": "For transformers, learning-rate (LR) schedule and warmup are single most-tuned hyperparameters. The right schedule recovers 1–3 points; the wrong one diverges. The four pillars:\n\nInitial LR — base learning rate at peak.\nWarmup — linear ramp from 0 to the peak.\nDecay — cosine, linear, inverse-square-root, or constant.\nFinal LR — value at the end of training (often 10 % of peak).\n\n\n\n\nIn the first few thousand steps, gradients are noisy and weights are far from a good solution. A large LR amplifies noise; a small LR makes effective progress impossible. Warmup linearly increases LR from 0 to peak over the first \\(W\\) steps:\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\min(1, t/W).\n\\]\nTypical \\(W = 500\\) to 5000 steps; longer for tiny batch sizes; can be in number of samples rather than steps for stability across hardware.\n\n\n\n\n\n\\[\n\\eta_t = \\eta_{\\min} + \\tfrac{1}{2}(\\eta_{\\max} - \\eta_{\\min})\\left(1 + \\cos\\!\\left(\\pi \\cdot \\frac{t-W}{T-W}\\right)\\right).\n\\]\nThe default for LLMs, ViTs, CLIP, BLIP-2, LLaVA. Smooth and forgiving.\n\n\n\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\left(1 - \\frac{t-W}{T-W}\\right).\n\\]\nFaster decay early on; used by RoBERTa, T5.\n\n\n\n\\[\n\\eta_t = \\eta_{\\max} \\cdot \\sqrt{W/t}\\quad (t &gt; W).\n\\]\nThe original Transformer schedule (Vaswani et al., 2017). Allows training to continue indefinitely; useful for never-ending pre-training.\n\n\n\nKeep LR constant during the bulk of training, then cool down at the end. Defazio & Mishchenko’s Schedule-Free (2024) eliminates the schedule entirely with a momentum-style rule:\n\\[\n\\theta_t \\leftarrow \\theta_{t-1} - \\eta \\nabla L(\\bar{\\theta}_t), \\quad \\bar{\\theta}_t = (1-c_t)\\bar{\\theta}_{t-1} + c_t \\theta_t.\n\\]\nEmpirically matches cosine without needing to specify \\(T\\).\n\n\n\n\nfrom torch.optim.lr_scheduler import LambdaLR\nimport math\n\ndef cosine_with_warmup(optimizer, num_warmup, num_total, min_ratio=0.1):\n    def lr_lambda(step):\n        if step &lt; num_warmup:\n            return step / max(1, num_warmup)\n        progress = (step - num_warmup) / max(1, num_total - num_warmup)\n        return max(min_ratio, 0.5 * (1 + math.cos(math.pi * progress)))\n    return LambdaLR(optimizer, lr_lambda)\nHuggingFace transformers.get_cosine_schedule_with_warmup does the same; optimum exposes more exotic schedules.\n\n\n\nVLMs often fine-tune a frozen ViT at \\(10^{-6}\\), a fresh projector at \\(10^{-3}\\), and an LLM with LoRA at \\(10^{-4}\\). Build parameter groups:\noptimizer = torch.optim.AdamW(\n    [\n        {\"params\": vision.parameters(),  \"lr\": 1e-6},\n        {\"params\": projector.parameters(), \"lr\": 1e-3},\n        {\"params\": [p for n,p in llm.named_parameters() if \"lora_\" in n], \"lr\": 1e-4},\n    ],\n    betas=(0.9, 0.95), weight_decay=0.05, eps=1e-8,\n)\nULMFit (Howard & Ruder, 2018) introduced discriminative fine-tuning — lower LR for earlier layers, higher for later. For ViTs/LLMs this is implemented as a layer-wise LR decay factor \\(\\gamma\\) (commonly 0.65–0.85):\n\\[\n\\eta_{\\ell} = \\eta_{\\max} \\cdot \\gamma^{L - \\ell}\n\\]\nwhere \\(\\ell\\) is the layer index and \\(L\\) is the total layers.\n\n\n\n\n\n\nSetting\nSuggested peak LR\n\n\n\n\nViT pre-training from scratch\n\\(1\\times 10^{-3}\\) (AdamW, β2=0.95)\n\n\nCLIP-style contrastive\n\\(5\\times 10^{-4}\\) (linear)\n\n\nLLM pre-training (decoder, 7 B)\n\\(3\\times 10^{-4}\\)\n\n\nLLM continued pre-training\n\\(1\\times 10^{-4}\\)\n\n\nVLM Stage-1 (projector only)\n\\(1\\times 10^{-3}\\)\n\n\nVLM Stage-2 (full instruction tune)\n\\(2\\times 10^{-5}\\)\n\n\nLoRA on a 7 B LLM\n\\(1\\times 10^{-4}\\) to \\(5\\times 10^{-4}\\)\n\n\nQLoRA\n\\(2\\times 10^{-4}\\)\n\n\n\nThese are starting points. A 30-minute LR-finder sweep (Smith, 2017) almost always pays for itself.\n\n\n\nFor SGD-style optimisers, the linear-scaling rule (Goyal et al., 2017): when batch size goes \\(k \\times\\), multiply LR by \\(k\\) and use linear warmup. For AdamW, the rule is sub-linear (often \\(\\sqrt{k}\\) or even less). For contrastive losses (CLIP, SigLIP) where larger batch directly improves the objective, the optimal LR rises slower than \\(\\sqrt{k}\\).\n\n\n\nTwo newer optimisers worth knowing:\n\nLion (Chen et al., 2023) — uses only the sign of the momentum; needs LR ≈ 1/10 of AdamW; memory-efficient.\nSchedule-Free AdamW — eliminates the LR schedule; benchmark-competitive.\n\nBoth have first-class PyTorch implementations (from lion_pytorch import Lion, from schedulefree import AdamWScheduleFree).\n\n\n\nAlways clip gradients during VLM training:\ntorch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\nThe 1.0 norm is standard for LLMs; tighter (0.5) for ViTs pre-training; looser (5.0) for SGD-from-scratch. Clipping prevents the rare spike from blowing up Adam’s moments.\n\n\n\n\nLoss plateaus immediately: warmup too short or LR too low.\nLoss spikes after warmup: peak LR too high; reduce or extend warmup.\nLoss diverges late: weight decay too low; learning rate too high.\nBest validation arrives early: training too long; reduce \\(T\\)."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#handling-pre-training-failures",
    "href": "Resources/Core5/Optimization/index.html#handling-pre-training-failures",
    "title": "VLM Optimization",
    "section": "",
    "text": "Symptom\nLikely cause\n\n\n\n\nLoss = NaN suddenly\nfp16 overflow; exploding gradient; bad sample\n\n\nLoss diverges monotonically\nLR too high; weight init bad; missing LN\n\n\nLoss plateaus at high value\nLR too low; loss bug; label mismatch\n\n\nLoss spikes then recovers\nbad batch; well-behaved transformer thing\n\n\nGrad norm explodes\ndead ReLU / very deep block; precision issue\n\n\nGPU OOM partway\ndataloader memory leak; KV cache overflow\n\n\nThroughput collapses\nfilesystem hot-spot; sync stragglers\n\n\nValidation diverges from train\noverfitting; eval data drift\n\n\n\n\n\n\nMost failures appear in the first day. Hit each item:\n\nVerify the first batch: shapes, dtypes, image normalisation, tokens make sense; loss for one sample is reasonable.\nOverfit a tiny subset (e.g., 64 samples) — loss should hit ~0 in a few hundred steps. If it doesn’t, your model is broken before scaling matters.\nInitial loss == log(vocab) for an untrained LM (e.g., ≈ 10.5 for 32k vocab).\nWarmup curve is smooth (no sudden drops or spikes).\nGrad-norm is in [0.1, 10] range; if it is \\(10^4\\), you have a NaN-cascade in waiting.\nGPU utilisation &gt; 60 % on every rank.\nPer-rank loss differs by &lt; 5 % — bigger gaps mean data sharding bugs.\nEMA loss drifts only slowly — large EMA-loss vs raw-loss gap means too-fast LR.\n\n\n\n\nThe most common cause is fp16 overflow. Possible loci:\n\nSoftmax denominator — exponentiated logits &gt; 65k overflow. Cast softmax to fp32 explicitly.\nLoss scale too high in GradScaler — the scaler should auto-adjust; watch its history.\nAdam \\(\\epsilon\\) too small — bump to \\(1\\times 10^{-6}\\) for fp16, \\(1\\times 10^{-8}\\) for bf16.\nLayer-norm divide-by-zero — bf16 helps; ensure \\(\\epsilon \\geq 10^{-5}\\).\n\nSwitch to bf16 in any 2025+ training run. It has the same exponent range as fp32 and is supported on every GPU since A100. fp16 is a legacy choice you should only use on V100.\nIf you must keep fp16, instrument:\ndef hook(module, input, output):\n    if torch.is_tensor(output) and not torch.isfinite(output).all():\n        print(f\"non-finite at {module}; min/max={output.min().item()}/{output.max().item()}\")\nfor name, m in model.named_modules(): m.register_forward_hook(hook)\n\n\n\nWhen a run NaNs at step 87 421:\n\nRoll back to the most recent checkpoint (say step 86 000).\nSkip the bad batch: advance the dataloader past the offending sample(s). If you don’t track samples per rank, you cannot do this — another reason to log sampler state.\nLower LR by 2× temporarily and resume; usually fine after 1k steps.\nClip more aggressively: e.g., reduce max_norm from 1.0 to 0.5.\nIf it NaNs again at the same step: investigate the data. Often a corrupted JPEG, a single sample with insane CLIP score, or a near-empty text.\n\n\n\n\nTransformers, especially LLM backbones at &gt;1 B params, exhibit recoverable loss spikes during pre-training (Chowdhery et al., PaLM, 2022). The principled response is:\n\nDon’t restart unless the spike is &gt; 4 × the recent EMA loss and doesn’t recover within 1k steps.\nLog gradient skew (kurtosis) — large skew predicts spikes.\nAdd embed-norm regularisation (Dehghani et al., ViT-22B) for huge models.\n\n\n\n\nTrain throughput halving overnight is usually:\n\nStorage filling up — checkpoint retention failed; SSD at 99 %.\nStragglers — one rank slow because of a hot CPU or NIC.\nPower throttling — GPU drops from 700 W to 400 W when DC AC fails.\nNCCL congestion — a noisy neighbour on the IB switch.\n\nAlways log time_per_step per rank and alert on a 30 % deviation.\n\n\n\nA subtle but common bug: a single corrupt shard that gets sampled by every rank eventually. Symptoms:\n\nAll ranks NaN around the same global step.\nReplaying from checkpoint reproduces the NaN at the same step.\n\nFix: hash every sample, fingerprint shards, and skip on read errors:\nimport io, hashlib\ndef safe_decode(jpeg_bytes):\n    try:\n        img = Image.open(io.BytesIO(jpeg_bytes)).convert(\"RGB\")\n        img.load()\n        return img\n    except Exception:\n        return None\n\n\n\nWhen investigating a failure, set torch.use_deterministic_algorithms(True), fixed seeds, and run on a single GPU. The aim isn’t reproducible production — it’s a reliable repro of the bug.\n\n\n\nYou cannot diagnose what you didn’t log. Mandatory logs per step:\n\nstep, epoch, sample count, wall time, throughput.\nraw loss, EMA loss, validation loss every \\(V\\) steps.\ngradient norm (pre-clip), parameter norm.\nLR per group, optimizer step count.\nGPU memory, util, power.\non-rank zero only: image-grids of a fixed sample, a generated caption, attention map.\n\nUse Weights & Biases, MLflow, or plain TensorBoard.\n\n\n\nWhenever a run crashes, write a one-page post-mortem with:\n\nSymptom — what failed, when, on which rank.\nTrigger — root cause if known; hypothesis if not.\nDiagnosis steps — what you ran, what you saw.\nResolution — code, config, or hardware change.\nPrevention — alert, test, or runbook entry.\n\nA bug fixed and undocumented will recur."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#adapting-pretrained-models",
    "href": "Resources/Core5/Optimization/index.html#adapting-pretrained-models",
    "title": "VLM Optimization",
    "section": "",
    "text": "Strategy\nWhat is updated?\nCompute\nUse case\n\n\n\n\nLinear probe\nA single linear classifier on frozen features\ntiny\nDiagnostic, retrieval\n\n\nFull fine-tune\nAll parameters\nhuge\nDomain shift, max accuracy\n\n\nLast-N-layers\nThe top N layers only\nmedium\nWhen data is scarce\n\n\nPEFT (LoRA / Adapters / Prompts)\n&lt; 1 % of parameters\nsmall\nPersonalisation, multi-tenant\n\n\nContinued pre-training\nAll params, same objective\nhuge\nNew language / domain corpus\n\n\n\nFor VLMs, the typical adaptation pipeline is:\n\nStage 1: train projector only (alignment).\nStage 2: instruction-tune projector + LLM (often LoRA on LLM, projector full).\nStage 3: domain-tune on small specialised data (LoRA on a copy of LLM; switchable at inference).\n\n\n\n\nWhen fine-tuning, the model’s original abilities decay. Mitigation:\n\nLower LR for layers carrying broad knowledge (early ViT layers, base LLM blocks).\nMix the original pre-training data into the fine-tune mix.\nEWC (Kirkpatrick et al., 2017) — penalise changes weighted by Fisher information.\nLoRA / Adapters — base weights frozen, so original abilities are mathematically preserved.\n\n\n\n\nFor a ViT encoder, unfreezing the last 4 blocks typically wins; for an LLM, the top 8 blocks or all attention layers. A defensible heuristic:\n\nProbe by layer index — train a linear classifier on each layer’s features. The “knee” of the curve identifies where task-specific information starts to dominate.\nUnfreeze from the knee upward.\n\n\n\n\nThe recipe:\n\nContinued pre-training on a few B unlabelled images + captions (LM loss).\nInstruction tuning on tens of thousands of domain instructions.\nEvaluation on domain benchmarks (PathVQA, ChartQA, GeoChat, RoboVQA).\n\nFor medical VLMs, look at LLaVA-Med, RadFM, PathChat. For documents, LLaVA-NeXT-Doc, mPLUG-DocOwl, InternVL2-Doc.\n\n\n\ndef stage1_alignment(vlm, loader, steps=5000):\n    vlm.freeze_for_alignment()                  # vision + LLM frozen\n    opt = torch.optim.AdamW(vlm.projector.parameters(), lr=1e-3)\n    schd = cosine_with_warmup(opt, 500, steps)\n    train(vlm, loader, opt, schd, steps)\n\ndef stage2_instruction(vlm, loader, steps=20000):\n    add_lora(vlm.llm, r=64, alpha=16, target_modules=(\"q_proj\",\"v_proj\"))\n    for p in vlm.vision.parameters(): p.requires_grad = False\n    params = list(vlm.projector.parameters()) + [p for p in vlm.llm.parameters() if p.requires_grad]\n    opt = torch.optim.AdamW(params, lr=2e-5)\n    schd = cosine_with_warmup(opt, 1000, steps)\n    train(vlm, loader, opt, schd, steps)\n\ndef stage3_domain(vlm, loader, steps=5000):\n    # Train a new LoRA adapter named \"medical\" while keeping stage2 LoRA frozen\n    add_lora(vlm.llm, r=8, alpha=16, name=\"medical\")\n    opt = torch.optim.AdamW(get_lora_params(vlm.llm, name=\"medical\"), lr=1e-4)\n    schd = cosine_with_warmup(opt, 200, steps)\n    train(vlm, loader, opt, schd, steps)\n\n\n\nAlways run three suites before declaring success:\n\nTarget benchmark — domain-specific.\nGeneral VLM benchmarks — MMMU, MMBench, MathVista. Drop &gt; 2 pts = regression.\nSmoke tests — your own held-out prompts. Sanity-check qualitatively.\n\nA common mistake is to celebrate a domain win that masks a 10-point general drop."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#implementing-parameter-efficient-methods",
    "href": "Resources/Core5/Optimization/index.html#implementing-parameter-efficient-methods",
    "title": "VLM Optimization",
    "section": "",
    "text": "Method\nTrainable params\nMemory\nNotes\n\n\n\n\nLoRA\n\\(r(d_{\\text{in}} + d_{\\text{out}})\\) per matrix\nlow\nDefault; merges at inference\n\n\nQLoRA\nSame as LoRA\n4-bit base\nFine-tune 33 B on a single 24 GB GPU\n\n\nDoRA\nLoRA + magnitude vector\nslightly higher\nOften beats LoRA\n\n\nVeRA\nTwo shared random matrices + scaling\ntiny\nMulti-task heavy\n\n\n(IA)\\(^3\\)\nThree scaling vectors per block\ntiny\nFast inference\n\n\nBitFit\nAll biases\nvery low\nStrong baseline\n\n\nPrompt tuning\nA small embedding matrix prepended\nvery low\nBrittle for long context\n\n\nPrefix tuning\nPer-layer trainable prefixes\nlow\nPowerful, harder to tune\n\n\nAdapters (Houlsby/Pfeiffer)\nBottleneck MLPs inside blocks\nlow\nMature; serial vs parallel variants\n\n\nLoRA-FA\nLoRA + frozen down-projection\nhalf of LoRA\nUsed in DoRA\n\n\n\n\n\n\nFor a frozen linear layer \\(W \\in \\mathbb{R}^{d \\times k}\\), learn a low-rank update:\n\\[\nW' = W + \\Delta W, \\qquad \\Delta W = \\tfrac{\\alpha}{r} \\cdot B A, \\quad A \\in \\mathbb{R}^{r \\times k}, \\; B \\in \\mathbb{R}^{d \\times r}.\n\\]\nTypical \\(r \\in \\{4, 8, 16, 32, 64\\}\\), \\(\\alpha = 2r\\). At inference, merge: \\(W \\leftarrow W + \\tfrac{\\alpha}{r} BA\\) — zero overhead.\nPure-PyTorch implementation:\nclass LoRALinear(nn.Module):\n    def __init__(self, base: nn.Linear, r=8, alpha=16, dropout=0.0):\n        super().__init__()\n        self.base = base\n        for p in self.base.parameters(): p.requires_grad = False\n        d_in, d_out = base.in_features, base.out_features\n        self.A = nn.Parameter(torch.empty(r, d_in))\n        self.B = nn.Parameter(torch.zeros(d_out, r))\n        nn.init.kaiming_uniform_(self.A, a=math.sqrt(5))   # Kaiming for A\n        nn.init.zeros_(self.B)                              # zero for B -&gt; ΔW=0 at init\n        self.scale = alpha / r\n        self.drop = nn.Dropout(dropout) if dropout &gt; 0 else nn.Identity()\n\n    def forward(self, x):\n        return self.base(x) + self.drop(x) @ self.A.T @ self.B.T * self.scale\nThe standard target modules in a transformer block are the four attention projections (q_proj, k_proj, v_proj, o_proj); for LLaMA-style models add the MLP projections (gate_proj, up_proj, down_proj).\n\n\n\nThe breakthrough: quantise the base model to 4-bit NormalFloat (NF4), keep LoRA in bf16, double-quantise the quantisation constants. Memory drops 4× while accuracy is preserved within 0.1 pts.\nfrom transformers import BitsAndBytesConfig, AutoModelForCausalLM\nfrom peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training\n\nbnb = BitsAndBytesConfig(\n    load_in_4bit=True,\n    bnb_4bit_quant_type=\"nf4\",\n    bnb_4bit_compute_dtype=torch.bfloat16,\n    bnb_4bit_use_double_quant=True,\n)\n\nllm = AutoModelForCausalLM.from_pretrained(\n    \"Qwen/Qwen2.5-7B-Instruct\", quantization_config=bnb,\n    attn_implementation=\"flash_attention_2\",\n)\nllm = prepare_model_for_kbit_training(llm, use_gradient_checkpointing=True)\n\nlora_cfg = LoraConfig(\n    r=64, lora_alpha=16, lora_dropout=0.05, bias=\"none\",\n    target_modules=[\"q_proj\",\"k_proj\",\"v_proj\",\"o_proj\",\n                    \"gate_proj\",\"up_proj\",\"down_proj\"],\n    task_type=\"CAUSAL_LM\",\n)\nllm = get_peft_model(llm, lora_cfg)\nllm.print_trainable_parameters()\nWhy NF4? The empirical distribution of pre-trained LLM weights is roughly Gaussian; NF4 is the quantile-based 4-bit code-book that is optimal for a Gaussian under squared error. Standard INT4 / FP4 codebooks distribute levels uniformly, which is sub-optimal.\nQLoRA also introduces paged optimisers — Adam states overflow into CPU memory in pages, eliminating the OOM that used to plague long-context fine-tuning.\n\n\n\nDoRA decomposes the weight matrix into magnitude and direction:\n\\[\nW = m \\cdot \\frac{V}{\\|V\\|_c}, \\qquad m \\in \\mathbb{R}^{1 \\times k}, \\; V \\in \\mathbb{R}^{d \\times k}.\n\\]\nLoRA updates direction; DoRA additionally learns the per-column magnitude \\(m\\). Typically +0.5 to +1.5 points over LoRA at similar parameter count. Supported in HuggingFace PEFT: LoraConfig(use_dora=True).\n\n\n\nA bottleneck MLP inserted after each attention or FFN block:\n\\[\nh \\leftarrow h + W_{\\text{up}} \\cdot \\sigma(W_{\\text{down}} \\cdot h), \\quad W_{\\text{down}} \\in \\mathbb{R}^{r \\times d}, W_{\\text{up}} \\in \\mathbb{R}^{d \\times r}.\n\\]\nThe Pfeiffer variant keeps only one adapter per block (after FFN); Houlsby has two (after attention and FFN). Adapters are slightly slower than LoRA at inference because they’re not mergeable; but they handle tasks where LoRA’s low-rank assumption is too restrictive (multi-task switching).\n\n\n\nThree learnable vectors per block: \\(l_k, l_v, l_{ff}\\). They re-scale keys, values, and FFN activations:\n\\[\n\\text{Attn}(Q, K, V) = \\text{softmax}\\!\\left(\\frac{Q (l_k \\odot K)^\\top}{\\sqrt d}\\right)(l_v \\odot V), \\quad \\text{FFN}(x) = W_2(l_{ff} \\odot \\text{GELU}(W_1 x)).\n\\]\nOften 100× fewer parameters than LoRA. Competitive on many benchmarks.\n\n\n\nPrompt tuning (Lester et al., 2021): prepend \\(K\\) learnable token embeddings to the input. Only those embeddings train.\nPrefix tuning (Li & Liang, 2021): per layer, prepend \\(K\\) learnable key/value vectors to the attention. More expressive than prompt tuning.\nBoth work surprisingly well at very large model scales (≥ 11 B); at smaller scales they underperform LoRA.\n\n\n\nYou can LoRA the ViT too. Target the attention projections plus the patch-embedding conv:\ntarget = [\"q_proj\",\"k_proj\",\"v_proj\",\"out_proj\",\"fc1\",\"fc2\"]   # CLIP ViT\nFor best accuracy, also unfreeze the last LayerNorm and the CLS token / positional embeddings — those are tiny and frequently bottleneck adaptation.\n\n\n\nA production VLM may host dozens of LoRA adapters (one per customer, per language, per domain). S-LoRA and vLLM Multi-LoRA schedule a single base model and swap adapter weights per request. Memory cost is proportional to total adapter size, not to base × adapters.\n\n\n\nPEFT generally matches full fine-tune within 0.5–1 pt on most benchmarks. It loses when:\n\nThe base model lacks the capability being fine-tuned (LoRA can’t add what isn’t there).\nThe domain shift is huge (medical scans vs natural images) — consider continued pre-training first.\nThe rank is too low for the task complexity — raise \\(r\\) from 8 to 64 or 128."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#developing-loss-functions",
    "href": "Resources/Core5/Optimization/index.html#developing-loss-functions",
    "title": "VLM Optimization",
    "section": "",
    "text": "Two models with identical architecture but different losses behave entirely differently. A loss expresses what counts as success. For VLMs, success means a combination of contrastive alignment, image-text matching, language generation, masking-prediction, and (sometimes) explicit reward signals. We will walk every loss with formula + PyTorch.\n\n\n\nGiven a sequence \\(w_1, \\ldots, w_T\\) and conditional images \\(\\mathbf{V}\\):\n\\[\n\\mathcal{L}_{\\text{LM}} = -\\sum_{t=1}^T \\log p_\\theta(w_t \\mid w_{&lt;t}, \\mathbf{V}).\n\\]\nImplementation detail: the LM loss is only computed over assistant tokens, never over system/user instruction tokens. Set labels[mask_user_tokens] = -100 so PyTorch’s CrossEntropyLoss(ignore_index=-100) skips them.\ndef shift_and_mask(input_ids, labels):\n    # next-token prediction\n    return input_ids[:, :-1], labels[:, 1:]\n\nlogits = llm(input_ids).logits\nshift_logits, shift_labels = shift_and_mask(logits, labels)\nloss = F.cross_entropy(shift_logits.reshape(-1, shift_logits.size(-1)),\n                       shift_labels.reshape(-1), ignore_index=-100)\n\n\n\nAlready introduced in the Architecture doc. The PyTorch one-liner:\ndef clip_loss(image_features, text_features, logit_scale):\n    image_features = F.normalize(image_features, dim=-1)\n    text_features  = F.normalize(text_features,  dim=-1)\n    logits_per_image = logit_scale.exp() * image_features @ text_features.T\n    labels = torch.arange(image_features.size(0), device=image_features.device)\n    return 0.5 * (F.cross_entropy(logits_per_image, labels) +\n                  F.cross_entropy(logits_per_image.T, labels))\nThe temperature \\(\\tau = 1/\\text{logit\\_scale}\\). Clip it to [0.01, 100] to avoid blow-ups:\nwith torch.no_grad():\n    logit_scale.clamp_(0, math.log(100))\n\n\n\n\\[\n\\mathcal{L}_{\\text{SigLIP}} = -\\frac{1}{B}\\sum_{i,j} \\log \\sigma\\!\\big( z_{ij}(\\tau s_{ij} + b) \\big).\n\\]\nPyTorch:\ndef siglip_loss(img, txt, logit_scale, logit_bias):\n    img = F.normalize(img, dim=-1); txt = F.normalize(txt, dim=-1)\n    logits = logit_scale.exp() * img @ txt.T + logit_bias\n    labels = 2 * torch.eye(img.size(0), device=img.device) - 1   # +1 on diag, -1 off-diag\n    return -F.logsigmoid(labels * logits).mean()\n\n\n\nA binary classifier on the fused [CLS] token:\ndef itm_loss(model, img, txt, hard_negatives_idx):\n    pos = model.fuse(img, txt).cls       # positive pair\n    neg = model.fuse(img, txt[hard_negatives_idx]).cls\n    logits = model.itm_head(torch.cat([pos, neg], dim=0))   # [2B, 2]\n    targets = torch.cat([torch.ones(pos.size(0)),\n                         torch.zeros(neg.size(0))]).long().to(logits.device)\n    return F.cross_entropy(logits, targets)\nHard negatives are sampled with weights proportional to current cosine similarity — see ALBEF eq. (10).\n\n\n\nMAE (He et al., 2022) reconstructs masked patches in pixel space:\n\\[\n\\mathcal{L}_{\\text{MAE}} = \\frac{1}{|\\mathcal{M}|} \\sum_{i \\in \\mathcal{M}} \\| p_\\theta(x_i) - x_i \\|_2^2\n\\]\nwith per-patch L2 on the normalised target patches (mean-std per patch).\nBEiT reconstructs discrete visual tokens from a frozen dVAE:\n\\[\n\\mathcal{L}_{\\text{BEiT}} = \\mathbb{E}_{x, \\mathcal{M}}\\sum_{i\\in\\mathcal{M}} -\\log p_\\theta(z_i \\mid x_{\\mathcal{M}^c})\n\\]\nwhere \\(z_i\\) are the dVAE’s tokens.\nEVA (Fang et al., 2023) predicts CLIP features at masked positions — feature distillation + masked modelling.\n\n\n\nMulti-task training combines several losses:\n\\[\n\\mathcal{L} = \\lambda_1 \\mathcal{L}_{\\text{ITC}} + \\lambda_2 \\mathcal{L}_{\\text{ITM}} + \\lambda_3 \\mathcal{L}_{\\text{LM}} + \\lambda_4 \\mathcal{L}_{\\text{MIM}}.\n\\]\nDefault weights: \\(1, 1, 1, 1\\) for BLIP-2 representation stage; \\(0, 0, 1, 0\\) for BLIP-2 generative stage; varied for FLAVA / BEiT-3.\n\n\n\nFor RLHF-style alignment of VLMs (LLaVA-RLHF, RLAIF-V):\n\\[\n\\mathcal{L}_{\\text{DPO}} = -\\mathbb{E}_{(x, y_w, y_l)} \\log \\sigma\\!\\Big(\\beta \\log \\frac{\\pi_\\theta(y_w|x)}{\\pi_{\\text{ref}}(y_w|x)} - \\beta \\log \\frac{\\pi_\\theta(y_l|x)}{\\pi_{\\text{ref}}(y_l|x)} \\Big).\n\\]\nwhere \\((y_w, y_l)\\) are chosen/rejected responses, \\(\\pi_{\\text{ref}}\\) is the frozen reference model, \\(\\beta \\approx 0.1\\). KTO (Ethayarajh et al., 2024) variant works with unpaired labels.\ndef dpo_loss(pi_logp_chosen, pi_logp_rejected, ref_logp_chosen, ref_logp_rejected, beta=0.1):\n    pi_diff  = pi_logp_chosen  - pi_logp_rejected\n    ref_diff = ref_logp_chosen - ref_logp_rejected\n    return -F.logsigmoid(beta * (pi_diff - ref_diff)).mean()\n\n\n\n\nZ-loss (Chowdhery et al., PaLM): penalise the log-sum-exp of the LM head logits — \\(\\lambda_z \\cdot \\log^2 Z\\) where \\(Z = \\sum \\exp(\\text{logits})\\). Keeps logits well-scaled, prevents NaN.\nRouter-z loss for MoE (DeepSeek-MoE): balances expert load.\nSparsity / contrastive auxiliary for image-text retrieval to encourage uniformity.\n\n\n\n\n\nIs the loss bounded below? If not, expect divergence.\nAre gradients well-scaled when inputs are large? Add log-sum-exp tricks.\nDoes the loss reduce to a known good loss when constants degenerate? Sanity.\nIs the loss invariant to your symmetry? (translation, scale, etc.) If yes, good — if not, your data needs more augmentation.\nDoes it scale linearly with batch size, or quadratically? Contrastive scales \\(O(B^2)\\); ITM scales \\(O(B)\\).\nWhat does ChatGPT-as-a-judge think of the model trained on this loss? A surprising sanity check.\n\n\n\n\nclass VLMLoss(nn.Module):\n    def __init__(self, lambda_itc=1.0, lambda_itm=1.0, lambda_lm=1.0):\n        super().__init__()\n        self.lambda_itc = lambda_itc\n        self.lambda_itm = lambda_itm\n        self.lambda_lm  = lambda_lm\n        self.itm_head = nn.Linear(768, 2)\n\n    def forward(self, vision_emb, text_emb, fused_cls, lm_logits, labels, hard_neg_idx, logit_scale):\n        l_itc = clip_loss(vision_emb, text_emb, logit_scale)\n        l_itm = F.cross_entropy(self.itm_head(fused_cls), labels)\n        l_lm  = F.cross_entropy(lm_logits.reshape(-1, lm_logits.size(-1)),\n                                labels.reshape(-1), ignore_index=-100)\n        return self.lambda_itc * l_itc + self.lambda_itm * l_itm + self.lambda_lm * l_lm, {\n            \"itc\": l_itc.detach(), \"itm\": l_itm.detach(), \"lm\": l_lm.detach(),\n        }"
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#engineering-quantization-techniques",
    "href": "Resources/Core5/Optimization/index.html#engineering-quantization-techniques",
    "title": "VLM Optimization",
    "section": "",
    "text": "Term\nMeaning\n\n\n\n\nPTQ\nPost-training quantisation; no re-training\n\n\nQAT\nQuantisation-aware training; simulate quantisation during fine-tuning\n\n\nWeight-only\nActivations stay in fp16/bf16, weights are int4/int8\n\n\nW4A16\n4-bit weights, fp16 activations (e.g., AWQ, GPTQ)\n\n\nW8A8\n8-bit weights and activations (e.g., SmoothQuant)\n\n\nActivation outliers\nThe tiny number of activation channels with huge magnitudes that ruin naive INT8 quantisation\n\n\nPer-tensor\nOne scale for the whole tensor\n\n\nPer-channel\nOne scale per output channel\n\n\nPer-group\nOne scale per group of \\(g\\) contiguous channels (typical \\(g=64\\) or \\(128\\))\n\n\nStatic\nActivation scales precomputed from calibration data\n\n\nDynamic\nActivation scales computed at runtime per batch\n\n\nMixed-precision\nDifferent layers / channels use different bit widths\n\n\n\n\n\n\nSymmetric uniform quantisation of a tensor \\(x\\) to \\(b\\)-bit signed integers:\n\\[\ns = \\frac{\\max(|x|)}{2^{b-1} - 1}, \\quad q = \\mathrm{clip}\\!\\left(\\mathrm{round}(x/s),\\; -2^{b-1},\\; 2^{b-1}-1\\right), \\quad \\hat{x} = s \\cdot q.\n\\]\nAsymmetric:\n\\[\ns = \\frac{x_{\\max} - x_{\\min}}{2^b - 1}, \\quad z = \\mathrm{round}(-x_{\\min}/s), \\quad q = \\mathrm{clip}(\\mathrm{round}(x/s) + z, 0, 2^b - 1).\n\\]\nQuantisation error \\(\\| x - \\hat x \\|\\) is essentially uniform with variance \\(s^2/12\\) → smaller \\(s\\) means smaller error → group quantisation (one \\(s\\) per small group) reduces error dramatically over per-tensor.\n\n\n\nDettmers (LLM.int8(), 2022) discovered: in transformers &gt; 6.7 B parameters, a small number of activation channels have magnitudes 50–500× larger than the rest. A per-tensor scale chosen for the outliers would round the typical channels to zero. Solutions:\n\nLLM.int8(): mixed precision — outlier channels in fp16, the rest in int8.\nSmoothQuant (Xiao et al., 2023): migrate the difficulty from activations to weights: \\[\nY = X \\cdot W = (X \\cdot \\mathrm{diag}(s)^{-1})(\\mathrm{diag}(s) \\cdot W).\n\\] Pick \\(s_j = \\max(|X_{:,j}|)^\\alpha / \\max(|W_{j,:}|)^{1-\\alpha}\\) with \\(\\alpha \\approx 0.5\\). Now both sides are well-conditioned.\nAWQ (Lin et al., 2023): identify the 1 % “salient” weight channels by activation magnitude and scale them up before INT4 rounding — preserves their representation. Pure weight-only.\n\n\n\n\nGPTQ uses second-order information: layer-wise, it picks weights to quantise one at a time, updating the remaining weights to compensate, using the OBS (Optimal Brain Surgeon) approximation with a small calibration set (128 examples). Produces 3–4 bit per-channel quantised matrices with &lt; 1 % perplexity loss on most LLMs.\nThe competing landscape (early-2026 picture):\n\n\n\n\n\n\n\n\nMethod\nBits\nSalient features\n\n\n\n\nAWQ\n4 W / 16 A\nBest for serving; ~3× speedup\n\n\nGPTQ\n3-4 W / 16 A\nBest for low-bit\n\n\nGGUF (llama.cpp)\n2-8 W / 16 A\nMature CPU/GPU ecosystem\n\n\nBitsAndBytes NF4\n4 W / 16 A\nUsed in QLoRA; double quantisation\n\n\nSmoothQuant\n8 W / 8 A\nINT8 with no accuracy loss\n\n\nFP8 (Transformer Engine)\n8 W / 8 A\nH100 tensor cores\n\n\nHQQ\n2-8 W / 16 A\nCalibration-free\n\n\nSpQR\n3 W / 16 A\nSparse-quantised\n\n\nOmniQuant\n4-8 W / 16 A\nLearnable equivalent transformations\n\n\n\n\n\n\nfrom awq import AutoAWQForCausalLM\nfrom transformers import AutoTokenizer\n\ntok = AutoTokenizer.from_pretrained(\"Qwen/Qwen2.5-7B-Instruct\")\nmodel = AutoAWQForCausalLM.from_pretrained(\n    \"Qwen/Qwen2.5-7B-Instruct\", device_map=\"auto\", torch_dtype=\"auto\",\n)\nquant_config = {\"zero_point\": True, \"q_group_size\": 128, \"w_bit\": 4, \"version\": \"GEMM\"}\nmodel.quantize(tok, quant_config=quant_config,\n               calib_data=\"pileval\", max_calib_samples=512, max_calib_seq_len=512)\nmodel.save_quantized(\"./qwen2.5-7B-awq\")\nFor VLMs, you must:\n\nQuantise the LLM backbone (vision + projector remain in bf16).\nCalibrate with multimodal inputs — pass image-text pairs through the model, not just text. Otherwise the activation statistics are wrong on visual-token positions.\nRe-evaluate on multimodal benchmarks; sometimes you must rescale specific layers.\n\n\n\n\nQAT inserts fake-quant nodes (FakeQuantize) into the model and trains end-to-end with the quantisation simulation in the loss path. Used when post-training methods don’t recover enough accuracy.\nimport torch.ao.quantization as taq\nqconfig = taq.QConfig(\n    activation=taq.observer.MovingAverageMinMaxObserver.with_args(dtype=torch.qint8),\n    weight=taq.observer.PerChannelMinMaxObserver.with_args(dtype=torch.qint8, qscheme=torch.per_channel_symmetric),\n)\nmodel.qconfig = qconfig\ntaq.prepare_qat(model, inplace=True)\n# fine-tune for a few epochs\ntaq.convert(model, inplace=True)\nFor modern PyTorch (≥ 2.2) the torch.ao.quantization.quantize_pt2e API is the recommended path; it integrates with torch.export.\n\n\n\nFor inference, the KV cache often dwarfs the model weights. Quantising it to fp8 or int8 can halve memory without measurable accuracy loss:\nmodel.generate(..., cache_implementation=\"quantized\",\n               cache_config={\"backend\": \"HQQ\", \"nbits\": 4, \"axis_key\": -1, \"axis_value\": -1})\nvLLM, TensorRT-LLM, and HuggingFace all support fp8 KV cache.\n\n\n\nFP8 (e4m3 for activations, e5m2 for gradients) is hardware-accelerated on H100. Use the Transformer Engine library:\nimport transformer_engine.pytorch as te\nfrom transformer_engine.common.recipe import Format, DelayedScaling\n\nfp8_recipe = DelayedScaling(fp8_format=Format.HYBRID, amax_history_len=32)\n\nwith te.fp8_autocast(enabled=True, fp8_recipe=fp8_recipe):\n    out = model(x)\nSpeed-ups: 1.5–2× over bf16 with no accuracy loss in most LLM pre-training runs.\n\n\n\nThe calibration dataset must mirror inference traffic:\n\nFor a chat VLM, calibrate with chat-formatted multimodal data.\nFor a video VLM, calibrate with video clips.\nFor a multilingual VLM, calibrate with samples from every language.\n\nA 128–512 sample calibration set is usually enough; &gt; 2000 brings diminishing returns and risks overfitting the quantisation parameters.\n\n\n\nThree layers of validation:\n\nPer-layer activation MSE vs reference — flag layers &gt; 1 % MSE.\nEnd-to-end perplexity / loss on a held-out set.\nDownstream benchmarks (MMMU, MMBench). Anything worse than 1 % below the bf16 reference is a regression.\n\nThe dangerous failure: end-to-end perplexity looks fine but one specific capability (OCR? Chinese?) is broken. Always evaluate multiple axes."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#building-knowledge-distillation-systems",
    "href": "Resources/Core5/Optimization/index.html#building-knowledge-distillation-systems",
    "title": "VLM Optimization",
    "section": "",
    "text": "A 7 B-parameter teacher VLM running at 25 tok/s on an A100 is unusable on a phone. Knowledge distillation transfers the teacher’s competence into a 0.5–2 B-parameter student that runs at 50 tok/s on a Snapdragon 8 Gen 3.\n\n\n\n\n\n\n\n\n\n\n\nFlavour\nWhat is transferred\nLoss\n\n\n\n\nLogit distillation (Hinton)\nSoft probabilities over vocab\nKL divergence on softmaxed logits with temperature \\(T\\)\n\n\nFeature distillation\nIntermediate hidden states\nMSE on student-projected features\n\n\nAttention transfer\nAttention maps\nMSE / KL on attention weights\n\n\n\nFor VLMs you typically combine all three.\n\n\n\n\\[\n\\mathcal{L}_{\\text{KD}} = \\alpha \\cdot T^2 \\cdot \\mathrm{KL}\\!\\big( p_\\text{student}^{(T)} \\,\\|\\, p_\\text{teacher}^{(T)} \\big) + (1-\\alpha) \\cdot \\mathcal{L}_{\\text{CE}}(p_\\text{student}, y)\n\\]\nwhere \\(p^{(T)}_i = \\mathrm{softmax}(z_i / T)\\) and \\(T \\in [1, 10]\\) smooths the teacher’s distribution to reveal “dark knowledge” — relative probabilities among non-target classes that carry information beyond a one-hot label.\ndef kd_loss(student_logits, teacher_logits, labels, T=4.0, alpha=0.5):\n    soft_s = F.log_softmax(student_logits / T, dim=-1)\n    soft_t = F.softmax(teacher_logits / T, dim=-1)\n    l_kl = F.kl_div(soft_s, soft_t, reduction=\"batchmean\") * (T * T)\n    l_ce = F.cross_entropy(student_logits, labels)\n    return alpha * l_kl + (1 - alpha) * l_ce\nFor LLM distillation on a sequence:\ndef seq_kd(student_logits, teacher_logits, labels, T=2.0, alpha=0.7, ignore=-100):\n    s = student_logits[:, :-1].reshape(-1, student_logits.size(-1))\n    t = teacher_logits[:, :-1].reshape(-1, teacher_logits.size(-1))\n    y = labels[:, 1:].reshape(-1)\n    mask = (y != ignore)\n    soft_s = F.log_softmax(s[mask] / T, dim=-1)\n    soft_t = F.softmax(t[mask] / T, dim=-1)\n    l_kl = F.kl_div(soft_s, soft_t, reduction=\"batchmean\") * (T * T)\n    l_ce = F.cross_entropy(s, y, ignore_index=ignore)\n    return alpha * l_kl + (1 - alpha) * l_ce\n\n\n\nMatch the student’s hidden state at layer \\(l_s\\) to the teacher’s at layer \\(l_t\\) via a learned projector \\(g\\):\n\\[\n\\mathcal{L}_{\\text{feat}} = \\frac{1}{N} \\sum_i \\| g(h^{(l_s)}_{\\text{stu},i}) - h^{(l_t)}_{\\text{tea},i} \\|_2^2.\n\\]\nFor VLM vision distillation, this is essentially what EVA does (CLIP teacher → masked-prediction student).\n\n\n\nFollowing Zagoruyko & Komodakis (2017), match a normalised attention map:\n\\[\n\\mathcal{L}_{\\text{att}} = \\sum_l \\left\\| \\frac{A_{\\text{stu}}^{(l)}}{\\|A_{\\text{stu}}^{(l)}\\|_F} - \\frac{A_{\\text{tea}}^{(l)}}{\\|A_{\\text{tea}}^{(l)}\\|_F} \\right\\|_F^2.\n\\]\nFor transformers, attention maps have shape [B, H, T, T]; align heads either via a Hungarian assignment or by pooling over heads.\n\n\n\nHinton’s KL is \\(\\mathrm{KL}(p_\\text{stu} \\| p_\\text{tea})\\) — forward KL. Reverse KL \\(\\mathrm{KL}(p_\\text{tea} \\| p_\\text{stu})\\) is mode-seeking: the student focuses on the highest-probability modes of the teacher and ignores low-probability “long tails”. For long-form generation, reverse KL produces more decisive students. MiniLLM (Gu et al., 2023) implements policy-gradient optimisation of reverse KL for generative distillation.\n\n\n\n\nOff-policy: teacher generates targets, student trains on them — fast but the distribution mismatch (teacher and student generate different sequences) hurts.\nOn-policy: at each step, student generates a token, teacher predicts the distribution at that exact context, loss matches them. Slower but better.\n\nA typical recipe: off-policy for the first 10 k steps to bootstrap, then on-policy.\n\n\n\nclass DistillVLM(nn.Module):\n    def __init__(self, teacher, student, T=2.0, alpha=0.7, feature_layers=(8, 16)):\n        super().__init__()\n        self.teacher = teacher.eval()\n        for p in self.teacher.parameters(): p.requires_grad = False\n        self.student = student\n        self.T, self.alpha = T, alpha\n        self.feat_proj = nn.ModuleList([\n            nn.Linear(student.cfg.d, teacher.cfg.d) for _ in feature_layers\n        ])\n        self.feat_layers = feature_layers\n\n    def forward(self, pixel_values, input_ids, labels):\n        with torch.no_grad():\n            t_out = self.teacher(pixel_values=pixel_values, input_ids=input_ids,\n                                 output_hidden_states=True)\n        s_out = self.student(pixel_values=pixel_values, input_ids=input_ids,\n                             output_hidden_states=True)\n        l_kd = seq_kd(s_out.logits, t_out.logits, labels, self.T, self.alpha)\n        l_feat = 0\n        for proj, l in zip(self.feat_proj, self.feat_layers):\n            l_feat = l_feat + F.mse_loss(proj(s_out.hidden_states[l]),\n                                         t_out.hidden_states[l])\n        return l_kd + 0.1 * l_feat\n\n\n\nThe distillation data matters at least as much as the loss. Best practice:\n\nTeacher-generated captions for raw images (LLaVA-1.5’s “CapFilt” trick).\nDiverse instruction mix: VQA, OCR, charts, math, reasoning, dialog.\nSelf-distillation (student becomes the teacher of an even smaller student).\n\nVLM-specific distillation papers: MiniLM-V (Wang et al., 2024), TinyLLaVA (2024), LLaVA-PoolDist, MobileVLM (Chu et al., 2023).\n\n\n\n\nMatch teacher’s behaviour on a held-out agreement set (% same prediction).\nMatch teacher’s quality on standard benchmarks — gap should be &lt; 5 pts for a 2× compression.\nVerify what was lost — usually long-tail knowledge or rare languages. Re-fine-tune on those if necessary.\n\n\n\n\n\\[\n\\mathcal{L} = \\alpha \\cdot \\mathcal{L}_{\\text{KD-logit}}^{T} + \\beta \\cdot \\mathcal{L}_{\\text{feat}} + \\gamma \\cdot \\mathcal{L}_{\\text{att}} + (1-\\alpha-\\beta-\\gamma) \\cdot \\mathcal{L}_{\\text{LM}}\n\\]\nwith \\(\\alpha=0.5,\\, \\beta=0.1,\\, \\gamma=0.05\\) as a sensible starting point. Anneal \\(\\beta, \\gamma \\to 0\\) over training so the student isn’t forever tied to the teacher’s intermediate representations."
  },
  {
    "objectID": "Resources/Core5/Optimization/index.html#reading-list-references",
    "href": "Resources/Core5/Optimization/index.html#reading-list-references",
    "title": "VLM Optimization",
    "section": "",
    "text": "PyTorch documentation: torch.distributed.checkpoint, torch.distributed.fsdp.FSDP, torch.optim.swa_utils.AveragedModel.\nTorchSnapshot / DCP design RFCs.\nMosaicML Composer docs — best-practice production training loop.\n\n\n\n\n\nVaswani et al., Attention Is All You Need — original inverse-square-root warmup, 2017.\nLoshchilov & Hutter, Decoupled Weight Decay Regularization (AdamW), 2017.\nSmith, Cyclical Learning Rates for Training Neural Networks, 2017.\nGoyal et al., Accurate, Large Minibatch SGD: Training ImageNet in 1 Hour, 2017.\nHoward & Ruder, Universal Language Model Fine-tuning for Text Classification (ULMFiT), 2018.\nChen et al., Symbolic Discovery of Optimization Algorithms (Lion), 2023.\nDefazio & Mishchenko, Schedule-Free, 2024.\n\n\n\n\n\nChowdhery et al., PaLM: Scaling Language Modeling with Pathways, 2022 — loss spikes.\nBrown et al., Language Models are Few-Shot Learners (GPT-3), 2020 — training-curve commentary.\nDehghani et al., Scaling Vision Transformers to 22 Billion Parameters, 2023.\nYang et al., Tensor Programs V (μP), 2022 — width-stable initialisation.\n\n\n\n\n\nHoulsby et al., Parameter-Efficient Transfer Learning for NLP, ICML 2019.\nPfeiffer et al., AdapterFusion, EACL 2021.\nHu et al., LoRA: Low-Rank Adaptation of Large Language Models, ICLR 2022.\nDettmers et al., QLoRA: Efficient Finetuning of Quantized LLMs, NeurIPS 2023.\nLiu et al., DoRA: Weight-Decomposed Low-Rank Adaptation, ICML 2024.\nLiu et al., Few-Shot Parameter-Efficient Fine-Tuning is Better and Cheaper than In-Context Learning (T-Few, (IA)\\(^3\\)), NeurIPS 2022.\nLi & Liang, Prefix-Tuning, ACL 2021.\nLester et al., The Power of Scale for Parameter-Efficient Prompt Tuning, EMNLP 2021.\nKopiczko et al., VeRA, 2023.\nHuggingFace PEFT library docs and examples.\n\n\n\n\n\nvan den Oord et al., Representation Learning with Contrastive Predictive Coding (InfoNCE), 2018.\nChen et al., A Simple Framework for Contrastive Learning of Visual Representations (SimCLR), 2020.\nHe et al., Momentum Contrast for Unsupervised Visual Representation Learning (MoCo), 2020.\nCaron et al., Unsupervised Learning of Visual Features by Contrasting Cluster Assignments (SwAV), 2020.\nZhai et al., Sigmoid Loss for Language Image Pre-Training (SigLIP), 2023.\nRafailov et al., Direct Preference Optimization, NeurIPS 2023.\nEthayarajh et al., KTO: Model Alignment as Prospect Theoretic Optimization, 2024.\nWang & Isola, Understanding Contrastive Representation Learning through Alignment and Uniformity on the Hypersphere, ICML 2020.\n\n\n\n\n\nJacob et al., Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference, CVPR 2018.\nDettmers et al., LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale, 2022.\nFrantar et al., GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers, ICLR 2023.\nXiao et al., SmoothQuant, ICML 2023.\nLin et al., AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration, MLSys 2024.\nShao et al., OmniQuant, ICLR 2024.\nEgiazarian et al., Extreme Compression of Large Language Models via Additive Quantization (AQLM), 2024.\nNVIDIA Transformer Engine docs and Using FP8 with Transformer Engine blog.\n\n\n\n\n\nHinton et al., Distilling the Knowledge in a Neural Network, 2015.\nRomero et al., FitNets: Hints for Thin Deep Nets, 2015.\nZagoruyko & Komodakis, Paying More Attention to Attention, ICLR 2017.\nSanh et al., DistilBERT, 2019.\nWang et al., MiniLM and MiniLMv2, 2020 / 2021.\nGu et al., MiniLLM: Knowledge Distillation of Large Language Models, 2023.\nTunstall et al., Zephyr: Direct Distillation of LM Alignment, 2023.\nChu et al., MobileVLM, 2023.\nZhou et al., TinyLLaVA, 2024.\n\n\n\n\n\nLilian Weng, “Large Language Model Inference” (2023).\nLilian Weng, “From Autoencoder to Beta-VAE” (general background).\nLilian Weng, “Policy Gradient Algorithms” (for DPO/PPO/RLHF context).\nJay Alammar, “The Illustrated Stable Diffusion”, “The Illustrated GPT-2”.\nHuggingFace blog: “Making LLMs lighter with quantization”, “QLoRA Explained”.\nSebastian Raschka, “Practical Tips for Finetuning LLMs Using LoRA” (2023), “Understanding Reasoning LLMs” (2025).\nTim Dettmers, “8-bit Methods for Efficient Deep Learning” (personal blog).\nMosaicML / Databricks blog series on optimisation tricks.\n\n\n\n\n\nOne pass through the entire document.\nImplement LoRA from scratch on a 1 B LLM and verify against PEFT.\nQuantise a 7 B VLM to AWQ-W4A16 and run MMMU evaluation.\nDistil a 7 B VLM into a 1.5 B student using logit + feature distillation, evaluate the gap.\nRun a tiny multi-GPU pre-training with FSDP + cosine schedule + DCP checkpoints; deliberately crash mid-run and resume.\nRead three papers per topic from the lists above.\n\nIf you can complete the five exercises, you have the optimisation skill-set of a senior VLM engineer.\n\nEnd of Vision-Language Model Optimization document."
  },
  {
    "objectID": "Resources/Projects/index.html",
    "href": "Resources/Projects/index.html",
    "title": "Projects",
    "section": "",
    "text": "Behaviour Simulation on Social Networks by Adobe\n\n\n\nMultimodal\n\nInterIIT\n\nMachine Learning\n\nSocial Media Analytics\n\n\n\n\n\n\nJan 4, 2024\n\n\nMadhav Kadam\n\n\n\n\n\n\n\nVitals extraction from ICU monitors\n\n\n\nComputer Vision\n\nInterIIT\n\nMachine Learning\n\n\n\n\n\n\nJan 3, 2023\n\n\nMadhav Kadam\n\n\n\n\n\nNo matching items\nReuseCC BY-NC-ND 4.0"
  },
  {
    "objectID": "Resources/Projects/Vitals/index.html",
    "href": "Resources/Projects/Vitals/index.html",
    "title": "Vitals extraction from ICU monitors",
    "section": "",
    "text": "As a member of the IIT Indore team that clinched gold at the Inter IIT Tech Meet 11.0, I’m excited to share our journey through the Vital Extraction Challenge. This competition, sponsored by Cloudphysician, pushed us to develop an innovative solution for extracting patient vitals from ICU monitor images. Our team’s dedication and ingenuity not only secured us the top spot but also resulted in a solution that was nearly five times faster than the runner-up, with a 2% edge in accuracy!"
  },
  {
    "objectID": "Resources/Projects/Vitals/index.html#the-challenge-bridging-the-gap-in-icu-monitoring",
    "href": "Resources/Projects/Vitals/index.html#the-challenge-bridging-the-gap-in-icu-monitoring",
    "title": "Vitals extraction from ICU monitors",
    "section": "The Challenge: Bridging the Gap in ICU Monitoring",
    "text": "The Challenge: Bridging the Gap in ICU Monitoring\nIn an ideal world, ICUs would have a 1:6 nurse-to-patient ratio. However, the reality often falls short. Our challenge was to create a system that could extract vital signs from images of ICU monitors, effectively augmenting existing setups without requiring a complete overhaul of infrastructure."
  },
  {
    "objectID": "Resources/Projects/Vitals/index.html#our-approach-a-three-pronged-attack",
    "href": "Resources/Projects/Vitals/index.html#our-approach-a-three-pronged-attack",
    "title": "Vitals extraction from ICU monitors",
    "section": "Our Approach: A Three-Pronged Attack",
    "text": "Our Approach: A Three-Pronged Attack\nWe tackled this complex problem with a three-step pipeline:\n\nPreprocessing\nVital Detection\nOptical Character Recognition (OCR)\n\nLet’s dive into each stage and explore the techniques that set our solution apart.\n\nPreprocessing: Setting the Stage\nThe first hurdle was isolating the monitor screen from its surroundings. We employed the YOLO v8-n model for this task, achieving impressive results.After segmentation, we applied a perspective transformation to standardize the monitor’s orientation and size, crucial for the subsequent stages.\n\n\nVital Detection: The Heart of the Matter\nThis stage in the pipeline aims to detect the appropriate vitals in the cropped and transformed monitor screen. However, there was a lot of unlabelled data, and manually annotating all 9000 images was not an option. And it will never be a good solution in any real-world scenario. We identified two methods to deal with the vast amount of unlabelled data.\nOne approach was to use a customized Semi-Supervised Learning Model, which would learn from the small amount of labelled data and use it to generate pseudo labels and then true labels for the unlabelled data.\nAnother approach was to intelligently pick and manually annotate a small amount of unlabelled data and then add it to the training dataset in order to bring maximum diversity and representation to the training data without much effort. This approach was chosen because of its better results and lesser computational requirement.\nFor vital detection, we utilized the YOLO v8-s model, which delivered outstanding performance.\n\n\nOCR: Deciphering the Digits\nThe final step was extracting the actual vital sign values. We opted for PaddleOCR, known for its accuracy and speed. However, this stage wasn’t without its challenges. We encountered issues like:\n\nMisreading similar characters (e.g., “0” as “O”)\nDetecting extraneous characters (like brackets)\nLosing digits due to segmentation errors\n\nTo address these, we implemented a series of logic-based checks and corrections, ensuring the most probable correct value was returned.\n##HR Graph Digitization Initially, we converted the HR graph segmented image into binary. In order to selectively obtain only the graph, the longest connected pixels row-wise were saved, and the rest discarded. The 2-D binary image was then projected into a 1-D Time Series. The plot was further rescaled in the x and y variables."
  },
  {
    "objectID": "Resources/Projects/Vitals/index.html#conclusion-lessons-learned-and-future-directions",
    "href": "Resources/Projects/Vitals/index.html#conclusion-lessons-learned-and-future-directions",
    "title": "Vitals extraction from ICU monitors",
    "section": "Conclusion: Lessons Learned and Future Directions",
    "text": "Conclusion: Lessons Learned and Future Directions\nOur journey through the Vital Extraction Challenge was as enlightening as it was rewarding. We learned the importance of balancing innovation with practicality, and how sometimes the most straightforward approach, executed exceptionally well, can outperform more complex solutions. The potential impact of this technology extends far beyond a competition. By enabling more efficient monitoring of ICU patients, solutions like ours could help bridge the gap in healthcare resources, potentially saving lives in the process.\nFind more about this project at : Github"
  },
  {
    "objectID": "books.html",
    "href": "books.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "Madhav Kadam\n        // books.log\n      \n    \n    \n      \n        —\n        Books Read\n      \n      \n        —\n        Five Stars\n      \n      \n        —\n        Four Stars\n      \n      \n        —\n        Authors\n      \n    \n  \n\n  \n\n  \n    These are the ones I'd lend to a stranger.\n  \n\n\n\n\n  \n    ✕\n    \n    \n      \n      \n      \n      \n      Review\n      \n      \n        \n        Goodreads Description",
    "crumbs": [
      "Home",
      "Books"
    ]
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "",
    "text": "As the team leader of Team 22 at the Inter IIT Tech Meet 12.0, I can’t help but feel a surge of pride as I reflect on our journey through the Adobe Behaviour Simulation Challenge. In today’s digital marketing landscape, understanding and predicting user engagement is the holy grail. Adobe threw down the gauntlet, challenging us to not only predict the likes a tweet might get based on its content but also to create tweets that could hit specific engagement targets."
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#the-challenge-that-sparked-our-curiosity",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#the-challenge-that-sparked-our-curiosity",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "",
    "text": "As the team leader of Team 22 at the Inter IIT Tech Meet 12.0, I can’t help but feel a surge of pride as I reflect on our journey through the Adobe Behaviour Simulation Challenge. In today’s digital marketing landscape, understanding and predicting user engagement is the holy grail. Adobe threw down the gauntlet, challenging us to not only predict the likes a tweet might get based on its content but also to create tweets that could hit specific engagement targets."
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#our-two-pronged-attack",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#our-two-pronged-attack",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "Our Two-Pronged Attack",
    "text": "Our Two-Pronged Attack\nWe divided our approach into two main tasks:\n\nPredicting the number of likes for a given tweet\nGenerating tweets to achieve targeted engagement levels"
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#task-1-cracking-the-code-of-tweet-popularity",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#task-1-cracking-the-code-of-tweet-popularity",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "Task 1: Cracking the Code of Tweet Popularity",
    "text": "Task 1: Cracking the Code of Tweet Popularity\n\nThe Data Dilemma\nOur first hurdle? The dataset was as skewed as a politician’s promise. We knew we needed more data to train a robust model. So, we went on a data hunt, integrating tweets from the top 20 Twitter accounts and a massive dataset from Hugging Face. We set our sights high, filtering for tweets with over 5,000 likes in English.\n\n\nEmbedding Our Way to Success\nEnter BERTweet, our knight in shining armor. We used it to extract embeddings from the tweet content, considering everything from the inferred company to the username and date. It was like giving our model a pair of super-powered glasses to see the intricate details of each tweet.\n\n\nThe Classifier-Regressor Dream Team\nWe trained a classifier to categorize tweets into engagement classes, ranging from the humble “0-100 likes” to the viral “100,000-1,000,000 likes”. But we didn’t stop there. We crafted six specialized regressors, one for each engagement class, using an Artificial Neural Network (ANN) architecture. It was like having a team of experts, each specializing in a different level of tweet popularity.\n\n\nA Novel Approach to Evaluation\nHere’s where we got creative. Instead of just looking at RMSE, we focused on predicting the order of likes. Why? Because in the real world, knowing if your tweet will be a big hit or a small ripple is often more valuable than knowing the exact number of likes."
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#task-2-the-art-of-tweet-generation",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#task-2-the-art-of-tweet-generation",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "Task 2: The Art of Tweet Generation",
    "text": "Task 2: The Art of Tweet Generation\n\nA Picture is Worth a Thousand Likes\nWe realized that tweets aren’t just about text. Images and videos play a huge role in engagement. So, we built a two-stage pipeline:\n\nCaption Generation: We deployed Visual Language Models (VLMs) like LLaVa and CLIP to generate detailed captions for images.\nLLM Fine-tuning: We then fine-tuned Large Language Models (LLMs) with these textual representations.\n\n\n\nThe Audio Twist\nFor videos, we took an innovative approach. Instead of traditional video captioning, we used audio captioning. It was faster and compute-efficient.\n\n\nThe LLM Showdown\nAfter an exhaustive evaluation of various LLMs, we settled on LLaMA. We fine-tuned it with our dataset, turning it into a tweet-generating powerhouse."
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#challenges-and-insights",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#challenges-and-insights",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "Challenges and Insights",
    "text": "Challenges and Insights\nOur journey wasn’t without its bumps. We grappled with:\n\nAudience Dynamics: Why do some memes get more likes than carefully crafted corporate posts?\nContent Bias: News posts, especially about local disasters, seemed to garner more engagement.\nEvaluation Metrics: Traditional metrics like BLEU and ROUGE struggled with the informal nature of tweets."
  },
  {
    "objectID": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#the-road-ahead",
    "href": "Resources/Projects/Adobe Behaviour Simulation Challenge/index.html#the-road-ahead",
    "title": "Behaviour Simulation on Social Networks by Adobe",
    "section": "The Road Ahead",
    "text": "The Road Ahead\nAs we wrap up this challenge, I’m filled with excitement about the possibilities we’ve uncovered. We’ve not just built models; we’ve gained insights into the very nature of social media engagement.The digital landscape is ever-changing, and so too must our approaches evolve.To all the marketers, content creators, and social media enthusiasts out there – watch this space. The future of digital engagement is being written, one tweet at a time."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html",
    "href": "Resources/DeepLearningAdv/index.html",
    "title": "DLA",
    "section": "",
    "text": "This article assumes you are comfortable with basic Python and high-school-level math. By the end, you will have a working expert-level understanding of Transformers, GNNs, CNNs, RNNs, and GANs — the five pillars of modern deep learning.\n\n\n\n\n\n\nFoundations You Must Know First\nTransformer Models & Attention Mechanisms\nGraph Neural Networks (GNNs)\nConvolutional Neural Networks (CNNs)\nRecurrent Neural Networks (RNNs)\nGenerative Adversarial Networks (GANs)\n\n\n ## Chapter 0: Foundations You Must Know First\nBefore we can climb the mountain, we need to understand the ground beneath our feet.\n\n\nA neural network is a mathematical function that maps inputs to outputs by composing many simpler functions called neurons. Each neuron computes a weighted sum of its inputs and passes the result through a non-linear function called an activation function.\nMathematically, a single neuron computes:\noutput = activation( w₁x₁ + w₂x₂ + ... + wₙxₙ + b )\nWhere: - x₁, x₂, ..., xₙ are input features - w₁, w₂, ..., wₙ are learnable weights - b is a learnable bias - activation(·) is a non-linear function like ReLU, sigmoid, or tanh\n\n\n\nWithout non-linearity, stacking multiple layers reduces to a single linear transformation (matrix multiplication collapses into one matrix), making the network no more expressive than logistic regression. Non-linearities allow networks to approximate arbitrarily complex functions — this is the Universal Approximation Theorem.\n\n\n\nTraining means adjusting weights w to minimize a loss function — a measure of how wrong our predictions are. We use gradient descent: compute the derivative of the loss with respect to every weight, then nudge weights in the direction that reduces loss.\nw ← w - η · ∂L/∂w\nWhere η (eta) is the learning rate — a small number like 0.001 that controls step size.\nBackpropagation is the algorithm that efficiently computes all these derivatives using the chain rule of calculus, propagating error gradients backward through the network from output to input.\n\n\n\nAll data in deep learning lives in tensors — multi-dimensional arrays. A scalar is a 0-D tensor, a vector is 1-D, a matrix is 2-D, and so on. PyTorch and TensorFlow both operate on tensors and automatically track gradients through them.\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]], requires_grad=True)\ny = (x ** 2).sum()\ny.backward()\nprint(x.grad)  # dy/dx = 2x → [[2., 4.], [6., 8.]]\nWith that foundation, let us begin.\n\n ## Chapter 1: Transformer Models — The Architecture That Changed Everything\n\n\n\nBefore 2017, sequence modeling was dominated by RNNs and LSTMs. These architectures process sequences token by token, left to right. This creates two crushing limitations:\n\nSequential bottleneck: You cannot process token 10 until you have processed tokens 1–9. This kills parallelism during training.\nVanishing long-range dependencies: Information from token 1 must travel through hundreds of hidden states to influence token 500. Gradients vanish; the network forgets.\n\nThe paper “Attention Is All You Need” (Vaswani et al., 2017) proposed a radical idea: throw away recurrence entirely. Replace it with a mechanism called self-attention that allows every token to directly attend to every other token in the sequence simultaneously.\nThis was not an incremental improvement — it was a paradigm shift. Every modern large language model (GPT, BERT, T5, Claude, Gemini) is built on this architecture.\n\n\n\n\n\nImagine you are at a library. You have a query (what you’re looking for), the library has shelves of keys (catalog labels), and each shelf contains values (actual books). You match your query against every key, find the best matches, and retrieve the corresponding values — possibly a weighted blend of multiple books.\nAttention does exactly this with vectors:\n\nQuery (Q): What am I looking for?\nKey (K): What does each token advertise about itself?\nValue (V): What information does each token actually carry?\n\nFor a sequence of n tokens, each token produces a query vector, a key vector, and a value vector. The output for each token is a weighted sum of all value vectors, where the weights are determined by how well that token’s query matches each other token’s key.\n\n\n\nGiven matrices Q (queries), K (keys), and V (values), attention is computed as:\nAttention(Q, K, V) = softmax( Q·Kᵀ / √dₖ ) · V\nLet us break this down step by step.\nStep 1 — Compute similarity scores:\nscores = Q · Kᵀ     # shape: [n_tokens × n_tokens]\nscores[i][j] measures how much token i should attend to token j. This is just a dot product between the query of token i and the key of token j.\nStep 2 — Scale by √dₖ:\nscores = scores / √dₖ\nWhere dₖ is the dimensionality of the key vectors. Why scale? When dₖ is large, dot products grow large in magnitude, pushing softmax into regions with very small gradients (saturation). Dividing by √dₖ stabilizes training.\nStep 3 — Apply softmax to get attention weights:\nweights = softmax(scores, dim=-1)   # shape: [n_tokens × n_tokens]\nEach row of weights sums to 1. weights[i][j] is the fraction of attention token i pays to token j.\nStep 4 — Weighted sum of values:\noutput = weights · V   # shape: [n_tokens × d_v]\nEach output token is a weighted blend of all value vectors.\n\n\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport math\n\ndef scaled_dot_product_attention(Q, K, V, mask=None):\n    \"\"\"\n    Args:\n        Q: Queries,  shape [batch, heads, seq_len, d_k]\n        K: Keys,     shape [batch, heads, seq_len, d_k]\n        V: Values,   shape [batch, heads, seq_len, d_v]\n        mask: Optional boolean mask, shape [batch, 1, seq_len, seq_len]\n              True = position is masked (ignored)\n    Returns:\n        output:  shape [batch, heads, seq_len, d_v]\n        weights: attention weights [batch, heads, seq_len, seq_len]\n    \"\"\"\n    d_k = Q.size(-1)\n\n    # Step 1 & 2: Scaled similarity scores\n    # Q·Kᵀ → [batch, heads, seq_len_q, seq_len_k]\n    scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(d_k)\n\n    # Step 3: Apply mask (used in decoder to prevent attending to future tokens)\n    if mask is not None:\n        scores = scores.masked_fill(mask, float('-inf'))\n        # -inf → 0 after softmax, effectively ignoring masked positions\n\n    # Step 4: Softmax over the key dimension\n    weights = F.softmax(scores, dim=-1)\n\n    # Step 5: Weighted sum of values\n    output = torch.matmul(weights, V)\n\n    return output, weights\n\n\n# --- Quick test ---\nbatch, heads, seq_len, d_k = 2, 4, 10, 64\nQ = torch.randn(batch, heads, seq_len, d_k)\nK = torch.randn(batch, heads, seq_len, d_k)\nV = torch.randn(batch, heads, seq_len, d_k)\n\nout, w = scaled_dot_product_attention(Q, K, V)\nprint(f\"Output shape: {out.shape}\")    # [2, 4, 10, 64]\nprint(f\"Weights shape: {w.shape}\")     # [2, 4, 10, 10]\nprint(f\"Weights sum (should be 1): {w[0,0,0].sum():.4f}\")\n\n\n\n\nA single attention operation computes one type of relationship between tokens. But language is rich — “bank” relates to “river” in one context and “money” in another. We want the model to capture multiple types of relationships simultaneously.\nMulti-Head Attention runs h independent attention operations (called heads) in parallel, each with its own learned Q, K, V projection matrices, then concatenates and re-projects the results.\nMultiHead(Q, K, V) = Concat(head₁, head₂, ..., headₕ) · Wᴼ\n\nwhere headᵢ = Attention(Q·Wᵢᴼ, K·Wᵢᴷ, V·Wᵢᵛ)\nThe key insight: instead of using d_model-dimensional keys, we project to d_model / h per head. This keeps the total computation roughly constant while multiplying the representational capacity.\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, num_heads):\n        \"\"\"\n        d_model:    Total model dimension (e.g., 512)\n        num_heads:  Number of parallel attention heads (e.g., 8)\n        d_k = d_v = d_model / num_heads = 64 in this example\n        \"\"\"\n        super().__init__()\n        assert d_model % num_heads == 0, \"d_model must be divisible by num_heads\"\n\n        self.d_model = d_model\n        self.num_heads = num_heads\n        self.d_k = d_model // num_heads   # dimension per head\n\n        # Single projection matrices for all heads (efficient)\n        # Instead of h separate W matrices, we use one large W and split\n        self.W_q = nn.Linear(d_model, d_model)   # projects to [batch, seq, d_model]\n        self.W_k = nn.Linear(d_model, d_model)\n        self.W_v = nn.Linear(d_model, d_model)\n        self.W_o = nn.Linear(d_model, d_model)   # output projection\n\n    def split_heads(self, x):\n        \"\"\"\n        Reshape from [batch, seq_len, d_model]\n        to           [batch, num_heads, seq_len, d_k]\n        \"\"\"\n        batch_size, seq_len, _ = x.size()\n        # Reshape: [batch, seq, d_model] → [batch, seq, heads, d_k]\n        x = x.view(batch_size, seq_len, self.num_heads, self.d_k)\n        # Transpose: [batch, seq, heads, d_k] → [batch, heads, seq, d_k]\n        return x.transpose(1, 2)\n\n    def forward(self, query, key, value, mask=None):\n        \"\"\"\n        query, key, value: [batch, seq_len, d_model]\n        mask: optional [batch, 1, seq_len, seq_len]\n        \"\"\"\n        batch_size = query.size(0)\n\n        # 1. Linear projections\n        Q = self.W_q(query)  # [batch, seq, d_model]\n        K = self.W_k(key)\n        V = self.W_v(value)\n\n        # 2. Split into multiple heads\n        Q = self.split_heads(Q)  # [batch, heads, seq, d_k]\n        K = self.split_heads(K)\n        V = self.split_heads(V)\n\n        # 3. Scaled dot-product attention on each head in parallel\n        attn_output, attn_weights = scaled_dot_product_attention(Q, K, V, mask)\n        # attn_output: [batch, heads, seq, d_k]\n\n        # 4. Concatenate heads\n        # Transpose back: [batch, heads, seq, d_k] → [batch, seq, heads, d_k]\n        attn_output = attn_output.transpose(1, 2).contiguous()\n        # Merge heads: [batch, seq, heads, d_k] → [batch, seq, d_model]\n        attn_output = attn_output.view(batch_size, -1, self.d_model)\n\n        # 5. Final output projection\n        output = self.W_o(attn_output)  # [batch, seq, d_model]\n\n        return output, attn_weights\n\n\n# --- Test ---\nd_model, num_heads, seq_len = 512, 8, 20\nmha = MultiHeadAttention(d_model, num_heads)\nx = torch.randn(2, seq_len, d_model)\nout, weights = mha(x, x, x)   # self-attention: Q=K=V=x\nprint(f\"MHA output: {out.shape}\")     # [2, 20, 512]\nprint(f\"Attn weights: {weights.shape}\")  # [2, 8, 20, 20]\n\n\n\nAttention has no built-in notion of order — it treats the input as a set of tokens, not a sequence. We must inject positional information explicitly.\nThe original Transformer used fixed sinusoidal encodings:\nPE(pos, 2i)   = sin(pos / 10000^(2i/d_model))\nPE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))\nWhere pos is the position index and i is the dimension index. Different dimensions encode different frequency sine/cosine waves, allowing the model to distinguish positions and even extrapolate to unseen lengths.\nclass PositionalEncoding(nn.Module):\n    def __init__(self, d_model, max_seq_len=5000, dropout=0.1):\n        super().__init__()\n        self.dropout = nn.Dropout(dropout)\n\n        # Create a matrix of shape [max_seq_len, d_model]\n        pe = torch.zeros(max_seq_len, d_model)\n\n        # Position indices: [0, 1, 2, ..., max_seq_len-1]\n        position = torch.arange(0, max_seq_len).unsqueeze(1).float()\n\n        # Division term: 10000^(2i/d_model), computed in log space for stability\n        div_term = torch.exp(\n            torch.arange(0, d_model, 2).float() * (-math.log(10000.0) / d_model)\n        )\n\n        # Even dimensions: sine, odd dimensions: cosine\n        pe[:, 0::2] = torch.sin(position * div_term)   # 0, 2, 4, ...\n        pe[:, 1::2] = torch.cos(position * div_term)   # 1, 3, 5, ...\n\n        # Add batch dimension and register as non-trainable buffer\n        pe = pe.unsqueeze(0)  # [1, max_seq_len, d_model]\n        self.register_buffer('pe', pe)\n\n    def forward(self, x):\n        \"\"\"\n        x: [batch, seq_len, d_model]\n        Add positional encoding to token embeddings\n        \"\"\"\n        x = x + self.pe[:, :x.size(1), :]\n        return self.dropout(x)\n\n\n\nAfter attention, each position is independently processed by a small 2-layer MLP:\nFFN(x) = max(0, x·W₁ + b₁)·W₂ + b₂\nThe inner dimension is typically 4× the model dimension (e.g., 2048 for d_model=512). This is where most of the “knowledge storage” happens — research has shown that these layers act as key-value memories.\nclass FeedForward(nn.Module):\n    def __init__(self, d_model, d_ff, dropout=0.1):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(d_model, d_ff),\n            nn.ReLU(),\n            nn.Dropout(dropout),\n            nn.Linear(d_ff, d_model),\n            nn.Dropout(dropout),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n\n\n\nEach sublayer (attention and FFN) is wrapped in a residual connection and layer normalization:\noutput = LayerNorm(x + Sublayer(x))\nWhy residuals? They create gradient highways — during backprop, gradients can flow directly from output to input without passing through every layer, preventing vanishing gradients in deep networks.\nWhy layer norm? It normalizes the activations across the feature dimension for each token independently, stabilizing training without the batch-size dependency of batch normalization.\n\n\n\nclass TransformerEncoderLayer(nn.Module):\n    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):\n        super().__init__()\n        self.self_attn = MultiHeadAttention(d_model, num_heads)\n        self.ffn = FeedForward(d_model, d_ff, dropout)\n        self.norm1 = nn.LayerNorm(d_model)\n        self.norm2 = nn.LayerNorm(d_model)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, x, mask=None):\n        # --- Sub-layer 1: Self-Attention ---\n        attn_out, _ = self.self_attn(x, x, x, mask)   # Q=K=V=x\n        x = self.norm1(x + self.dropout(attn_out))     # Residual + Norm\n\n        # --- Sub-layer 2: Feed-Forward ---\n        ffn_out = self.ffn(x)\n        x = self.norm2(x + ffn_out)                    # Residual + Norm\n\n        return x\n\n\n\nThe original Transformer was designed for sequence-to-sequence tasks (e.g., translation). It has two components:\n\n\nThe encoder reads the input sequence and produces rich contextual representations. It stacks N encoder layers (typically N=6). Each encoder layer applies self-attention (where every input token can attend to every other input token) and a feed-forward network.\nInput → Embedding + Positional Encoding\n      → [EncoderLayer₁ → EncoderLayer₂ → ... → EncoderLayerₙ]\n      → Encoder Output (contextual representations)\n\n\n\nThe decoder generates the output sequence token by token. Each decoder layer has three sublayers:\n\nMasked Self-Attention: The decoder attends to its own previously generated tokens. The “masking” prevents attending to future positions (causal masking), since at inference time, future tokens don’t exist yet.\nCross-Attention: This is the encoder-decoder bridge. The decoder’s queries attend to the encoder’s keys and values. This is how the decoder “reads” the input — the decoder asks questions (queries) and the encoder provides answers (keys + values).\nFeed-Forward Network: Same as encoder.\n\nclass TransformerDecoderLayer(nn.Module):\n    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):\n        super().__init__()\n        # Sub-layer 1: Masked self-attention (causal)\n        self.self_attn = MultiHeadAttention(d_model, num_heads)\n        # Sub-layer 2: Cross-attention over encoder output\n        self.cross_attn = MultiHeadAttention(d_model, num_heads)\n        # Sub-layer 3: Feed-forward\n        self.ffn = FeedForward(d_model, d_ff, dropout)\n\n        self.norm1 = nn.LayerNorm(d_model)\n        self.norm2 = nn.LayerNorm(d_model)\n        self.norm3 = nn.LayerNorm(d_model)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, x, encoder_output, src_mask=None, tgt_mask=None):\n        \"\"\"\n        x:              Decoder input, [batch, tgt_len, d_model]\n        encoder_output: [batch, src_len, d_model]\n        src_mask:       Padding mask for encoder output\n        tgt_mask:       Causal mask for decoder input (upper-triangular)\n        \"\"\"\n        # --- Sub-layer 1: Masked self-attention ---\n        # Q, K, V all come from decoder input; mask prevents future-peeking\n        attn1, _ = self.self_attn(x, x, x, tgt_mask)\n        x = self.norm1(x + self.dropout(attn1))\n\n        # --- Sub-layer 2: Cross-attention ---\n        # Queries from decoder, Keys & Values from encoder output\n        attn2, cross_weights = self.cross_attn(\n            query=x,\n            key=encoder_output,\n            value=encoder_output,\n            mask=src_mask\n        )\n        x = self.norm2(x + self.dropout(attn2))\n\n        # --- Sub-layer 3: Feed-forward ---\n        ffn_out = self.ffn(x)\n        x = self.norm3(x + ffn_out)\n\n        return x, cross_weights\n\n\ndef make_causal_mask(seq_len, device):\n    \"\"\"\n    Creates an upper-triangular mask to prevent attending to future positions.\n    mask[i][j] = True means token i should NOT attend to token j.\n    \"\"\"\n    mask = torch.triu(torch.ones(seq_len, seq_len, device=device), diagonal=1).bool()\n    return mask.unsqueeze(0).unsqueeze(0)  # [1, 1, seq, seq]\n\n\n\nclass Transformer(nn.Module):\n    def __init__(self, src_vocab, tgt_vocab, d_model=512, num_heads=8,\n                 num_layers=6, d_ff=2048, max_seq_len=512, dropout=0.1):\n        super().__init__()\n\n        # Embeddings (shared or separate for src/tgt)\n        self.src_embedding = nn.Embedding(src_vocab, d_model)\n        self.tgt_embedding = nn.Embedding(tgt_vocab, d_model)\n        self.pos_encoding = PositionalEncoding(d_model, max_seq_len, dropout)\n\n        # Encoder stack\n        self.encoder_layers = nn.ModuleList([\n            TransformerEncoderLayer(d_model, num_heads, d_ff, dropout)\n            for _ in range(num_layers)\n        ])\n\n        # Decoder stack\n        self.decoder_layers = nn.ModuleList([\n            TransformerDecoderLayer(d_model, num_heads, d_ff, dropout)\n            for _ in range(num_layers)\n        ])\n\n        # Final projection to vocabulary\n        self.fc_out = nn.Linear(d_model, tgt_vocab)\n        self.d_model = d_model\n\n    def encode(self, src, src_mask=None):\n        x = self.pos_encoding(self.src_embedding(src) * math.sqrt(self.d_model))\n        for layer in self.encoder_layers:\n            x = layer(x, src_mask)\n        return x  # [batch, src_len, d_model]\n\n    def decode(self, tgt, encoder_output, src_mask=None, tgt_mask=None):\n        x = self.pos_encoding(self.tgt_embedding(tgt) * math.sqrt(self.d_model))\n        for layer in self.decoder_layers:\n            x, _ = layer(x, encoder_output, src_mask, tgt_mask)\n        return x  # [batch, tgt_len, d_model]\n\n    def forward(self, src, tgt, src_mask=None):\n        tgt_mask = make_causal_mask(tgt.size(1), tgt.device)\n        encoder_output = self.encode(src, src_mask)\n        decoder_output = self.decode(tgt, encoder_output, src_mask, tgt_mask)\n        logits = self.fc_out(decoder_output)\n        return logits  # [batch, tgt_len, tgt_vocab]\n\n\n\n\n\nEncoder-only (BERT-style): No decoder. Bidirectional attention — each token sees all others. Used for understanding tasks: classification, NER, question answering. Pretrained with Masked Language Modeling.\nDecoder-only (GPT-style): No encoder. Causal attention only. Used for generation. Pretrained with next-token prediction. This is the architecture behind ChatGPT, Claude, and LLaMA.\nEncoder-decoder (T5, BART): Full architecture as above. Used for translation, summarization, any seq2seq task.\n\n\n\n\nThe power of attention can be understood from three angles:\n\nArbitrary range: Any two tokens can directly influence each other in a single layer. Path length between any pair is O(1), versus O(n) for RNNs.\nDynamic computation: Attention weights are input-dependent. The same word computes different relationships in different contexts. “Bank” in “river bank” has different keys/queries than “bank” in “central bank.”\nInterpretability: Attention weights provide a window into what the model “looks at,” making Transformers more interpretable than RNNs (though this is a simplification — the relationship between attention and model behavior is complex).\n\n\n ## Chapter 2: Graph Neural Networks — Learning on Structured Data\n\n\n\nMost real-world data is not a sequence or a grid — it is a graph:\n\nSocial networks (users = nodes, friendships = edges)\nMolecules (atoms = nodes, bonds = edges)\nKnowledge graphs (entities = nodes, relations = edges)\nRoad networks (intersections = nodes, roads = edges)\nCitation networks (papers = nodes, citations = edges)\nProtein interaction networks\n\nCNNs exploit the grid structure of images. RNNs exploit the sequential structure of language. GNNs are the equivalent framework for arbitrary graph structures.\n\n\n\nA graph G = (V, E) consists of: - V: a set of n nodes (also called vertices) - E: a set of edges between pairs of nodes - X ∈ ℝⁿˣᶠ: a feature matrix where row i is the feature vector of node i - A ∈ ℝⁿˣⁿ: an adjacency matrix where A[i][j] = 1 if there’s an edge from i to j\nA graph can be directed (edges have direction) or undirected (edges are symmetric). Edges can also have features.\n\n\n\nThe central operation in virtually all GNNs is message passing. The intuition: a node should update its representation based on information gathered from its neighbors. This is analogous to asking: “Given who I am and who I am connected to, how should I update my understanding of myself?”\nThe message-passing framework is:\nStep 1 — Message: Each neighbor sends a message to the central node.\nmᵢⱼ = MESSAGE(hᵢᵗ, hⱼᵗ, eᵢⱼ)\nStep 2 — Aggregate: Collect messages from all neighbors into a single summary.\nMᵢᵗ = AGGREGATE({mᵢⱼ : j ∈ N(i)})\nStep 3 — Update: Update the node’s own representation.\nhᵢᵗ⁺¹ = UPDATE(hᵢᵗ, Mᵢᵗ)\nAfter k rounds of message passing, each node’s representation encodes information from its k-hop neighborhood.\n\n\n\nThe seminal GCN paper (Kipf & Welling, 2017) introduced a particularly elegant form of message passing.\nThe Spectral Motivation: Graph convolutions can be derived from spectral graph theory as a localized approximation of graph signal filtering. But the practical result is beautifully simple.\nFor a single GCN layer:\nH^(l+1) = σ( D̃^(-1/2) Ã D̃^(-1/2) H^(l) W^(l) )\nWhere: - Ã = A + I (adjacency matrix with added self-loops) - D̃[i][i] = Σⱼ Ã[i][j] (degree matrix of Ã) - D̃^(-1/2) Ã D̃^(-1/2) is the symmetric normalized adjacency - W^(l) is the learnable weight matrix for layer l - σ is an activation function (e.g., ReLU)\nThe D̃^(-1/2) Ã D̃^(-1/2) term normalizes by node degree, preventing high-degree nodes from drowning out low-degree nodes. The Ã = A + I self-loops ensure each node also retains its own features.\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass GCNLayer(nn.Module):\n    def __init__(self, in_features, out_features):\n        super().__init__()\n        self.linear = nn.Linear(in_features, out_features, bias=False)\n        # Initialize weights following Glorot uniform\n        nn.init.xavier_uniform_(self.linear.weight)\n\n    def forward(self, H, A_hat):\n        \"\"\"\n        H:     Node feature matrix [n_nodes, in_features]\n        A_hat: Pre-normalized adjacency D̃^(-1/2) Ã D̃^(-1/2) [n_nodes, n_nodes]\n        \"\"\"\n        # Aggregate neighbor features: A_hat @ H\n        aggregated = torch.mm(A_hat, H)           # [n_nodes, in_features]\n        # Linear transformation + activation\n        return self.linear(aggregated)            # [n_nodes, out_features]\n\n\ndef normalize_adjacency(A):\n    \"\"\"\n    Compute D̃^(-1/2) Ã D̃^(-1/2) for GCN.\n    A: adjacency matrix [n, n] (can be torch.Tensor or scipy sparse)\n    \"\"\"\n    n = A.size(0)\n    # Add self-loops: Ã = A + I\n    A_tilde = A + torch.eye(n, device=A.device)\n    # Compute degree matrix D̃\n    D_tilde = A_tilde.sum(dim=1)  # [n] — row sums\n    # D̃^(-1/2) as a vector (we'll apply it as element-wise scaling)\n    D_inv_sqrt = D_tilde.pow(-0.5)\n    D_inv_sqrt[D_inv_sqrt == float('inf')] = 0  # handle isolated nodes\n    # Symmetric normalization: D̃^(-1/2) Ã D̃^(-1/2)\n    # Equivalent to: scale rows and columns by D_inv_sqrt\n    A_hat = D_inv_sqrt.unsqueeze(1) * A_tilde * D_inv_sqrt.unsqueeze(0)\n    return A_hat\n\n\nclass GCN(nn.Module):\n    def __init__(self, in_features, hidden_dim, num_classes, dropout=0.5):\n        super().__init__()\n        self.gcn1 = GCNLayer(in_features, hidden_dim)\n        self.gcn2 = GCNLayer(hidden_dim, num_classes)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, X, A):\n        A_hat = normalize_adjacency(A)\n        # Layer 1: GCN + ReLU + Dropout\n        h = F.relu(self.gcn1(X, A_hat))\n        h = self.dropout(h)\n        # Layer 2: GCN (no activation — logits for classification)\n        logits = self.gcn2(h, A_hat)\n        return logits  # [n_nodes, num_classes]\n\n\n# --- Test ---\nn_nodes, in_features, hidden, n_classes = 100, 16, 64, 7\nX = torch.randn(n_nodes, in_features)\n# Random graph (Erdős–Rényi)\nA = (torch.rand(n_nodes, n_nodes) &gt; 0.9).float()\nA = (A + A.T).clamp(0, 1)  # make symmetric (undirected)\n\nmodel = GCN(in_features, hidden, n_classes)\nlogits = model(X, A)\nprint(f\"Node logits shape: {logits.shape}\")  # [100, 7]\n\n\n\nGCN uses fixed structural weights (based on degree). But not all neighbors are equally important. GAT (Veličković et al., 2018) learns to weight neighbors dynamically — just like Transformer attention.\nFor each edge (i, j), GAT computes an attention coefficient:\neᵢⱼ = LeakyReLU( aᵀ [Whᵢ || Whⱼ] )\nαᵢⱼ = softmax_j(eᵢⱼ) = exp(eᵢⱼ) / Σ_{k∈N(i)} exp(eᵢₖ)\nThen aggregates:\nhᵢ' = σ( Σ_{j∈N(i)} αᵢⱼ · W·hⱼ )\nclass GATLayer(nn.Module):\n    def __init__(self, in_features, out_features, num_heads=1,\n                 dropout=0.6, alpha=0.2, concat=True):\n        \"\"\"\n        in_features:  Input feature dimension\n        out_features: Output feature dimension per head\n        num_heads:    Number of attention heads (multi-head GAT)\n        alpha:        Negative slope for LeakyReLU\n        concat:       If True, concatenate heads; if False, average them\n        \"\"\"\n        super().__init__()\n        self.num_heads = num_heads\n        self.out_features = out_features\n        self.concat = concat\n\n        # One weight matrix per head\n        self.W = nn.Parameter(torch.FloatTensor(num_heads, in_features, out_features))\n        # Attention vector per head: [2 * out_features] → scalar\n        self.a = nn.Parameter(torch.FloatTensor(num_heads, 2 * out_features))\n\n        self.leaky_relu = nn.LeakyReLU(alpha)\n        self.dropout = nn.Dropout(dropout)\n\n        # Initialize\n        nn.init.xavier_uniform_(self.W)\n        nn.init.xavier_uniform_(self.a.unsqueeze(0))\n\n    def forward(self, H, adj):\n        \"\"\"\n        H:   [n_nodes, in_features]\n        adj: [n_nodes, n_nodes] adjacency matrix (binary)\n        \"\"\"\n        n = H.size(0)\n        outputs = []\n\n        for k in range(self.num_heads):\n            # Linear transformation: [n, in_features] @ [in_features, out_features]\n            Wh = H @ self.W[k]  # [n, out_features]\n\n            # Compute attention scores for all pairs (i, j)\n            # Expand for broadcasting: Wh_i for source, Wh_j for target\n            Wh_repeat_i = Wh.unsqueeze(1).expand(n, n, -1)  # [n, n, out]\n            Wh_repeat_j = Wh.unsqueeze(0).expand(n, n, -1)  # [n, n, out]\n\n            # Concatenate along feature dim: [n, n, 2*out]\n            concat_features = torch.cat([Wh_repeat_i, Wh_repeat_j], dim=-1)\n\n            # Apply attention vector: [n, n, 2*out] @ [2*out] → [n, n]\n            e = self.leaky_relu((concat_features * self.a[k]).sum(dim=-1))\n\n            # Mask non-edges with -inf (so they get 0 after softmax)\n            mask = (adj == 0)\n            e = e.masked_fill(mask, float('-inf'))\n\n            # Softmax over neighbors\n            alpha = F.softmax(e, dim=1)    # [n, n]\n            alpha = self.dropout(alpha)\n\n            # Weighted aggregation\n            h_prime = alpha @ Wh           # [n, out_features]\n            outputs.append(h_prime)\n\n        if self.concat:\n            return F.elu(torch.cat(outputs, dim=-1))      # [n, heads*out]\n        else:\n            return F.elu(torch.stack(outputs, dim=0).mean(0))  # [n, out]\n\n\n\nPyTorch Geometric (PyG) provides an elegant base class for message passing. Let’s implement it from scratch to understand the mechanics:\n# Using PyTorch Geometric's MessagePassing base class\nfrom torch_geometric.nn import MessagePassing\nfrom torch_geometric.utils import add_self_loops, degree\n\nclass CustomGCNConv(MessagePassing):\n    \"\"\"\n    A from-scratch GCN convolution using PyG's MessagePassing framework.\n    aggr='add' means we sum incoming messages (GCN uses sum, not mean)\n    \"\"\"\n    def __init__(self, in_channels, out_channels):\n        super().__init__(aggr='add')\n        self.lin = nn.Linear(in_channels, out_channels, bias=False)\n\n    def forward(self, x, edge_index):\n        \"\"\"\n        x:          [n_nodes, in_channels]\n        edge_index: [2, n_edges] — COO format edges (source, target)\n        \"\"\"\n        # Step 1: Add self-loops\n        edge_index, _ = add_self_loops(edge_index, num_nodes=x.size(0))\n\n        # Step 2: Linear transform of node features\n        x = self.lin(x)   # [n_nodes, out_channels]\n\n        # Step 3: Compute degree-based normalization\n        row, col = edge_index\n        deg = degree(col, x.size(0), dtype=x.dtype)  # [n_nodes]\n        deg_inv_sqrt = deg.pow(-0.5)\n        norm = deg_inv_sqrt[row] * deg_inv_sqrt[col]  # per-edge norm factor\n\n        # Step 4: Propagate messages\n        # This calls message() → aggregate() → update() internally\n        return self.propagate(edge_index, x=x, norm=norm)\n\n    def message(self, x_j, norm):\n        \"\"\"\n        Called for each edge. x_j is the source node feature.\n        norm: per-edge normalization factor.\n        Returns the message from source node j to target node i.\n        \"\"\"\n        return norm.view(-1, 1) * x_j   # scale source features by norm\n\n    def update(self, aggr_out):\n        \"\"\"\n        Called after aggregation. aggr_out is the aggregated message.\n        We just return it here; in more complex models, apply an MLP.\n        \"\"\"\n        return aggr_out\n\n\n\nReal graphs change over time: friendships form and dissolve, transactions occur at specific times, proteins interact dynamically. Static GNNs cannot capture this temporal dimension. Two main paradigms handle it:\n\n\nThe graph is observed as a sequence of snapshots at discrete time steps: G₁, G₂, …, Gₜ. We apply a GNN to each snapshot and use an RNN/LSTM to model temporal evolution.\nimport torch\nimport torch.nn as nn\n\nclass TemporalGCN(nn.Module):\n    \"\"\"\n    For each time step, applies a GCN, then uses GRU to propagate temporal info.\n    Architecture: GCN(t) → GRU → node embeddings at time t\n    \"\"\"\n    def __init__(self, node_features, gcn_out, gru_hidden, num_classes):\n        super().__init__()\n        # Shared GCN applied at each time step\n        self.gcn = GCN(node_features, gcn_out, gcn_out)\n        # GRU takes GCN output at each step and updates temporal state\n        self.gru = nn.GRU(gcn_out, gru_hidden, batch_first=True)\n        # Classification head\n        self.classifier = nn.Linear(gru_hidden, num_classes)\n\n    def forward(self, X_sequence, A_sequence):\n        \"\"\"\n        X_sequence: List of T feature matrices, each [n_nodes, node_features]\n        A_sequence: List of T adjacency matrices, each [n_nodes, n_nodes]\n        Returns:    Node predictions at the final time step\n        \"\"\"\n        T = len(X_sequence)\n        n_nodes = X_sequence[0].size(0)\n\n        # Apply GCN at each snapshot → collect temporal node embeddings\n        gcn_outputs = []\n        for t in range(T):\n            h_t = self.gcn(X_sequence[t], A_sequence[t])   # [n_nodes, gcn_out]\n            gcn_outputs.append(h_t)\n\n        # Stack into sequence: [n_nodes, T, gcn_out]\n        # Each node has a sequence of GCN embeddings through time\n        node_sequences = torch.stack(gcn_outputs, dim=1)\n\n        # GRU across time for each node\n        gru_out, _ = self.gru(node_sequences)     # [n_nodes, T, gru_hidden]\n        final_embeddings = gru_out[:, -1, :]      # [n_nodes, gru_hidden]\n\n        return self.classifier(final_embeddings)  # [n_nodes, num_classes]\n\n\n\nInteractions happen at arbitrary continuous timestamps, not discrete snapshots. The graph evolves as a stream of (source, target, time, features) events. This calls for more sophisticated methods like:\nTemporal Point Processes: Model the time between events as a stochastic process.\nTGNN (Temporal Graph Neural Network): Maintain per-node memory states that are updated upon each interaction. The seminal model here is TGN (Temporal Graph Networks, Rossi et al., 2020).\nclass TGNMemory(nn.Module):\n    \"\"\"\n    Simplified TGN memory module.\n    Each node has a memory vector that gets updated when it interacts.\n    \"\"\"\n    def __init__(self, num_nodes, memory_dim, message_dim, time_dim=1):\n        super().__init__()\n        self.memory_dim = memory_dim\n        # Per-node memory: initialized to zeros\n        self.register_buffer('memory', torch.zeros(num_nodes, memory_dim))\n        # Time encoder: maps scalar time delta to a vector\n        self.time_encoder = nn.Linear(1, time_dim)\n        # Message function: [memory_src || memory_dst || time_enc || edge_feat] → message\n        self.message_fn = nn.Linear(2 * memory_dim + time_dim + message_dim, memory_dim)\n        # GRU cell to update memory\n        self.memory_updater = nn.GRUCell(memory_dim, memory_dim)\n\n    def get_memory(self, node_ids):\n        return self.memory[node_ids]\n\n    def update_memory(self, src_ids, dst_ids, timestamps, edge_features):\n        \"\"\"\n        Update memory for source and destination nodes after an interaction.\n        \"\"\"\n        src_mem = self.memory[src_ids]      # [batch, memory_dim]\n        dst_mem = self.memory[dst_ids]\n        time_enc = self.time_encoder(timestamps.unsqueeze(-1))  # [batch, time_dim]\n\n        # Compute messages for src and dst\n        msg_src = self.message_fn(\n            torch.cat([src_mem, dst_mem, time_enc, edge_features], dim=-1)\n        )\n        msg_dst = self.message_fn(\n            torch.cat([dst_mem, src_mem, time_enc, edge_features], dim=-1)\n        )\n\n        # Update memories with GRU\n        self.memory[src_ids] = self.memory_updater(msg_src, src_mem)\n        self.memory[dst_ids] = self.memory_updater(msg_dst, dst_mem)\n\n\n\n\nNode-level tasks output per-node predictions. For graph-level tasks (e.g., “Is this molecule toxic?”), we need to pool all node representations into a single graph embedding.\nGlobal Mean/Max/Sum Pooling: Simple aggregation.\nfrom torch_geometric.nn import global_mean_pool, global_max_pool\n\n# After computing node embeddings h:\ngraph_embedding = global_mean_pool(h, batch)  \n# batch: [n_nodes] tensor indicating which graph each node belongs to\nHierarchical Pooling (DiffPool): Learn to coarsen the graph level-by-level, similar to how CNNs downsample images.\n\n ## Chapter 3: Convolutional Neural Networks — Vision from First Principles\n\n\n\nConsider a 224×224 RGB image. As a flat vector, this has 224 × 224 × 3 = 150,528 dimensions. A single fully-connected layer mapping to 1000 hidden units would need 150,528 × 1,000 = 150 million parameters — and we’d have multiple such layers. This is computationally intractable and prone to catastrophic overfitting.\nWorse, fully-connected layers are not equivariant to translation: if a cat moves 10 pixels to the right, the network sees a completely different input and must learn the same cat detector all over again.\nCNNs solve both problems through: 1. Local connectivity: Each neuron only connects to a small spatial region (the receptive field) 2. Weight sharing: The same filter (set of weights) is applied at every position 3. Hierarchical features: Early layers detect edges, middle layers detect textures, deep layers detect objects\n\n\n\n\n\nA convolution slides a small filter (kernel) across the input, computing a dot product at each position. The result is a feature map indicating where and how strongly that filter’s pattern is present.\nFor a 2D input I and kernel K of size f×f, the output at position (i, j) is:\nOutput[i][j] = Σₘ Σₙ I[i+m][j+n] × K[m][n]\nIn CNNs, we have multiple input channels (e.g., 3 for RGB) and multiple output filters (e.g., 64). For input X of shape [C_in, H, W] and kernel K of shape [C_out, C_in, f, f]:\nOutput[c_out, i, j] = Σ_{c_in} Σₘ Σₙ X[c_in, i+m, j+n] × K[c_out, c_in, m, n]\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\n\ndef manual_conv2d(input, kernel, stride=1, padding=0):\n    \"\"\"\n    Manual implementation of 2D convolution to understand internals.\n    input:  [batch, C_in, H, W]\n    kernel: [C_out, C_in, kH, kW]\n    Returns: [batch, C_out, H_out, W_out]\n    \"\"\"\n    batch, C_in, H, W = input.shape\n    C_out, _, kH, kW = kernel.shape\n\n    # Apply padding\n    if padding &gt; 0:\n        input = F.pad(input, [padding]*4)  # pad all 4 sides\n\n    # Output spatial dimensions\n    H_out = (H + 2*padding - kH) // stride + 1\n    W_out = (W + 2*padding - kW) // stride + 1\n\n    output = torch.zeros(batch, C_out, H_out, W_out)\n\n    for b in range(batch):\n        for c_out in range(C_out):\n            for i in range(H_out):\n                for j in range(W_out):\n                    h_start = i * stride\n                    w_start = j * stride\n                    # Extract patch\n                    patch = input[b, :, h_start:h_start+kH, w_start:w_start+kW]\n                    # Element-wise multiply and sum (dot product)\n                    output[b, c_out, i, j] = (patch * kernel[c_out]).sum()\n\n    return output\n\n\n# Verify against PyTorch's F.conv2d\nx = torch.randn(1, 1, 5, 5)\nk = torch.randn(1, 1, 3, 3)\nour_result = manual_conv2d(x, k)\npytorch_result = F.conv2d(x, k)\nprint(f\"Max difference: {(our_result - pytorch_result).abs().max():.6f}\")  # ~0\n\n\n\nWithout padding, convolutions shrink the spatial dimensions: an n×n input with an f×f kernel produces (n-f+1)×(n-f+1) output. Deep networks would rapidly collapse to size 1.\nValid padding (padding=0): No padding, output is smaller than input. Same padding (padding=(f-1)/2): Pad so output is same size as input.\nWith f=3, padding=1: H_out = H_in\nWith f=5, padding=2: H_out = H_in\nGeneral rule: padding = (f-1) / 2 for odd kernel sizes\n\n\n\nStride controls how many pixels the filter jumps at each step. stride=1 moves one pixel at a time (default). stride=2 moves two pixels, halving the output size.\nH_out = floor((H_in + 2*padding - f) / stride) + 1\nStride &gt; 1 is an alternative to pooling for downsampling — it’s differentiable and can be learned.\n\n\n\nDilation inserts spaces between kernel elements, effectively increasing the receptive field without adding parameters and without losing spatial resolution.\nA 3×3 kernel with dilation=2 has the same number of parameters but sees a 5×5 area (with gaps). Dilation=1 is standard convolution.\nEffective kernel size = f + (f-1)(d-1) = d*(f-1) + 1\nFor f=3, d=2: effective size = 5\nFor f=3, d=4: effective size = 9\nDilated convolutions are widely used in semantic segmentation (DeepLab) and audio generation (WaveNet).\nclass ConvBlock(nn.Module):\n    \"\"\"\n    A comprehensive conv block showcasing padding, stride, and dilation.\n    \"\"\"\n    def __init__(self, in_channels, out_channels, kernel_size=3,\n                 stride=1, padding=1, dilation=1):\n        super().__init__()\n        # When dilation &gt; 1, adjust padding to keep output size stable:\n        # effective_padding = dilation * (kernel_size - 1) / 2\n        self.conv = nn.Conv2d(\n            in_channels, out_channels,\n            kernel_size=kernel_size,\n            stride=stride,\n            padding=padding,\n            dilation=dilation,\n            bias=False   # Usually no bias when using BatchNorm\n        )\n        self.bn = nn.BatchNorm2d(out_channels)\n        self.relu = nn.ReLU(inplace=True)\n\n    def forward(self, x):\n        return self.relu(self.bn(self.conv(x)))\n\n\n# Demonstrate different convolution configurations\nx = torch.randn(1, 32, 56, 56)\n\n# Standard conv: 56 → 56 (same padding)\nstd_conv = ConvBlock(32, 64, kernel_size=3, stride=1, padding=1, dilation=1)\nprint(std_conv(x).shape)    # [1, 64, 56, 56]\n\n# Strided conv: 56 → 28 (downsampling by 2)\nstrided = ConvBlock(32, 64, kernel_size=3, stride=2, padding=1, dilation=1)\nprint(strided(x).shape)     # [1, 64, 28, 28]\n\n# Dilated conv: 56 → 56 (same size, larger receptive field)\ndilated = ConvBlock(32, 64, kernel_size=3, stride=1, padding=2, dilation=2)\nprint(dilated(x).shape)     # [1, 64, 56, 56]\n\n\n\n\nPooling reduces spatial dimensions, making representations more compact and somewhat translation-invariant.\nMax Pooling: Takes the maximum value in each pooling window. Picks the most active feature. Most commonly used.\nAverage Pooling: Takes the mean. Smoother but less sharp.\nGlobal Average Pooling (GAP): Averages across all spatial positions, producing a [batch, channels] tensor from [batch, channels, H, W]. Used at the end of modern CNNs instead of fully-connected layers — drastically reduces parameters and acts as regularization.\nclass PoolingDemo(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.max_pool = nn.MaxPool2d(kernel_size=2, stride=2)     # H,W → H/2, W/2\n        self.avg_pool = nn.AvgPool2d(kernel_size=2, stride=2)\n        self.global_avg = nn.AdaptiveAvgPool2d((1, 1))           # H,W → 1, 1\n\n    def forward(self, x):\n        print(f\"Input:      {x.shape}\")\n        print(f\"MaxPool:    {self.max_pool(x).shape}\")\n        print(f\"AvgPool:    {self.avg_pool(x).shape}\")\n        g = self.global_avg(x)\n        print(f\"GlobalAvg:  {g.shape}\")\n        print(f\"Flattened:  {g.flatten(1).shape}\")\n        return g.flatten(1)\n\ndemo = PoolingDemo()\nx = torch.randn(4, 64, 28, 28)\nout = demo(x)\n\n\n\nLet us build a modern, fully-functional CNN for image classification, following the design principles of ResNet.\n\n\nBefore ResNet (2015), training very deep networks (&gt;20 layers) was notoriously difficult. Gradients exponentially shrink as they propagate back through many layers, making early layers receive near-zero gradients and thus barely learning.\n\n\n\nHe et al. (2015) introduced the residual block:\noutput = F(x, {Wᵢ}) + x\nInstead of learning the desired mapping H(x), the network learns the residual F(x) = H(x) - x. The identity shortcut + x creates a gradient highway: during backprop, the gradient can flow directly through the identity connection, bypassing the conv layers.\nclass ResidualBlock(nn.Module):\n    \"\"\"\n    Standard ResNet residual block.\n    If in_channels != out_channels, uses a 1×1 conv as shortcut projection.\n    \"\"\"\n    expansion = 1  # Basic block doesn't expand channels\n\n    def __init__(self, in_channels, out_channels, stride=1):\n        super().__init__()\n\n        # Main path: 3×3 conv → BN → ReLU → 3×3 conv → BN\n        self.conv1 = nn.Conv2d(in_channels, out_channels,\n                               kernel_size=3, stride=stride, padding=1, bias=False)\n        self.bn1 = nn.BatchNorm2d(out_channels)\n\n        self.conv2 = nn.Conv2d(out_channels, out_channels,\n                               kernel_size=3, stride=1, padding=1, bias=False)\n        self.bn2 = nn.BatchNorm2d(out_channels)\n\n        self.relu = nn.ReLU(inplace=True)\n\n        # Shortcut: if dimensions change, project to match\n        self.shortcut = nn.Sequential()\n        if stride != 1 or in_channels != out_channels:\n            self.shortcut = nn.Sequential(\n                nn.Conv2d(in_channels, out_channels,\n                          kernel_size=1, stride=stride, bias=False),\n                nn.BatchNorm2d(out_channels)\n            )\n\n    def forward(self, x):\n        # Main path\n        residual = self.shortcut(x)    # Save for skip connection (may be projected)\n\n        out = self.relu(self.bn1(self.conv1(x)))\n        out = self.bn2(self.conv2(out))  # No ReLU yet — add residual first\n\n        out += residual                # Residual addition\n        out = self.relu(out)           # ReLU after addition\n\n        return out\n\n\nclass ResNet(nn.Module):\n    \"\"\"\n    Simplified ResNet for CIFAR-10 (32×32 images, 10 classes).\n    Adapted from ResNet architecture, scaled for small images.\n    \"\"\"\n    def __init__(self, num_classes=10):\n        super().__init__()\n\n        # Initial feature extraction\n        self.stem = nn.Sequential(\n            nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False),\n            nn.BatchNorm2d(64),\n            nn.ReLU(inplace=True)\n        )\n        # Note: No max pooling here (images are 32×32, not 224×224)\n\n        # Residual stages\n        self.stage1 = self._make_stage(64, 64, num_blocks=2, stride=1)\n        self.stage2 = self._make_stage(64, 128, num_blocks=2, stride=2)\n        self.stage3 = self._make_stage(128, 256, num_blocks=2, stride=2)\n        self.stage4 = self._make_stage(256, 512, num_blocks=2, stride=2)\n\n        # Global average pooling + classifier\n        self.global_pool = nn.AdaptiveAvgPool2d((1, 1))\n        self.classifier = nn.Linear(512, num_classes)\n\n        # Weight initialization\n        self._init_weights()\n\n    def _make_stage(self, in_channels, out_channels, num_blocks, stride):\n        \"\"\"Create a stage: first block may downsample, rest maintain size.\"\"\"\n        blocks = [ResidualBlock(in_channels, out_channels, stride=stride)]\n        for _ in range(1, num_blocks):\n            blocks.append(ResidualBlock(out_channels, out_channels, stride=1))\n        return nn.Sequential(*blocks)\n\n    def _init_weights(self):\n        for m in self.modules():\n            if isinstance(m, nn.Conv2d):\n                # He initialization: accounts for ReLU non-linearity\n                nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')\n            elif isinstance(m, nn.BatchNorm2d):\n                nn.init.constant_(m.weight, 1)\n                nn.init.constant_(m.bias, 0)\n\n    def forward(self, x):\n        # x: [batch, 3, 32, 32]\n        x = self.stem(x)           # [batch, 64, 32, 32]\n        x = self.stage1(x)         # [batch, 64, 32, 32]\n        x = self.stage2(x)         # [batch, 128, 16, 16]\n        x = self.stage3(x)         # [batch, 256, 8, 8]\n        x = self.stage4(x)         # [batch, 512, 4, 4]\n        x = self.global_pool(x)    # [batch, 512, 1, 1]\n        x = x.flatten(1)           # [batch, 512]\n        return self.classifier(x)  # [batch, num_classes]\n\n\n# --- Full training loop ---\ndef train_cnn(model, train_loader, num_epochs=10, lr=0.1):\n    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\n    model = model.to(device)\n\n    # SGD with momentum and weight decay — the classic combo for CNNs\n    optimizer = torch.optim.SGD(\n        model.parameters(), lr=lr,\n        momentum=0.9, weight_decay=1e-4, nesterov=True\n    )\n    # Cosine annealing: smoothly decay LR to 0\n    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=num_epochs)\n    criterion = nn.CrossEntropyLoss(label_smoothing=0.1)  # Label smoothing regularization\n\n    for epoch in range(num_epochs):\n        model.train()\n        total_loss, correct, total = 0, 0, 0\n\n        for images, labels in train_loader:\n            images, labels = images.to(device), labels.to(device)\n            optimizer.zero_grad()\n            logits = model(images)\n            loss = criterion(logits, labels)\n            loss.backward()\n            # Gradient clipping: prevents exploding gradients\n            torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n            optimizer.step()\n\n            total_loss += loss.item()\n            correct += (logits.argmax(1) == labels).sum().item()\n            total += labels.size(0)\n\n        scheduler.step()\n        acc = 100 * correct / total\n        print(f\"Epoch {epoch+1}/{num_epochs} | Loss: {total_loss/len(train_loader):.3f} | Acc: {acc:.2f}%\")\n\n    return model\n\n\n# Quick architecture check\nmodel = ResNet(num_classes=10)\nx = torch.randn(4, 3, 32, 32)\nout = model(x)\nprint(f\"Output shape: {out.shape}\")   # [4, 10]\nparam_count = sum(p.numel() for p in model.parameters())\nprint(f\"Parameters: {param_count:,}\")\n\n\n\n\nBatch Normalization (Ioffe & Szegedy, 2015) normalizes the activations within a mini-batch:\nx̂ᵢ = (xᵢ - μ_B) / √(σ²_B + ε)\nyᵢ = γ·x̂ᵢ + β\nWhere μ_B and σ²_B are the batch mean and variance, ε is a small constant for numerical stability, and γ, β are learnable scale and shift parameters.\nWhy it helps: 1. Reduces internal covariate shift — the distribution of activations shifts as earlier layers update, forcing later layers to constantly re-adapt. BN stabilizes these distributions. 2. Acts as regularization (reduces need for dropout) 3. Allows higher learning rates 4. Reduces sensitivity to initialization\nDuring inference, batch statistics are replaced by running averages computed during training (model.eval() mode in PyTorch).\n\n\n\n\n\n\n\n\n\n\n\nPrinciple\nImplementation\nWhy\n\n\n\n\nResidual connections\nout = F(x) + x\nSolves vanishing gradients\n\n\nBatchNorm before activation\nBN → ReLU\nStabilizes training\n\n\nGlobal average pooling\nAdaptiveAvgPool2d(1,1)\nReduces parameters, regularizes\n\n\nNo bias with BatchNorm\nbias=False\nBN subsumes bias\n\n\nHe initialization\nkaiming_normal_\nCorrect variance for ReLU\n\n\nLabel smoothing\nCrossEntropyLoss(label_smoothing=0.1)\nPrevents overconfident predictions\n\n\nWeight decay\nSGD with weight_decay=1e-4\nL2 regularization\n\n\n\n\n ## Chapter 4: Recurrent Neural Networks — Sequential Memory\n\n\n\nMany problems are inherently sequential: speech recognition (audio frames over time), text generation (tokens in order), time-series forecasting (measurements at timesteps). Unlike CNNs processing fixed-size windows, RNNs maintain a hidden state — a summary of everything they’ve seen so far.\n\n\n\nThe simplest RNN updates its hidden state at each step:\nhₜ = tanh(Wₕₕ·hₜ₋₁ + Wₓₕ·xₜ + b)\nyₜ = Wₕᵧ·hₜ + bᵧ\nThe same weight matrices Wₕₕ, Wₓₕ, Wₕᵧ are applied at every timestep — this is weight sharing across time.\nThe Problem: Vanilla RNNs cannot learn long-range dependencies. Backpropagation through time (BPTT) requires multiplying gradients through hundreds of timesteps. If |Wₕₕ| &lt; 1, gradients vanish exponentially. If |Wₕₕ| &gt; 1, gradients explode.\n\n\n\nThe LSTM (Hochreiter & Schmidhuber, 1997) solves the vanishing gradient problem with a revolutionary idea: a cell state cₜ that can carry information over long distances with minimal transformation.\nThe cell state is modified by three gates — learned sigmoid-activated filters that decide what to forget, write, and output.\n\n\nForget Gate: How much of the old cell state to keep?\nfₜ = σ(Wf·[hₜ₋₁, xₜ] + bf)\nInput Gate: What new information to write to the cell state?\niₜ = σ(Wᵢ·[hₜ₋₁, xₜ] + bᵢ)\nc̃ₜ = tanh(Wc·[hₜ₋₁, xₜ] + bc)   # Candidate cell values\nCell State Update:\ncₜ = fₜ ⊙ cₜ₋₁ + iₜ ⊙ c̃ₜ\n⊙ is element-wise multiplication. The forget gate erases old info; the input gate writes new info.\nOutput Gate: What to expose as hidden state?\noₜ = σ(Wo·[hₜ₋₁, xₜ] + bo)\nhₜ = oₜ ⊙ tanh(cₜ)\nThe key insight: gradients can flow through cₜ via the addition operation (gradient of addition is 1), not multiplication — so they don’t vanish! The forget gate can learn to be near 1 for important information, creating near-perfect gradient highways across time.\n\n\n\nclass LSTMCell(nn.Module):\n    \"\"\"\n    Single LSTM cell — processes one timestep.\n    Implementing from scratch to understand all gate mechanics.\n    \"\"\"\n    def __init__(self, input_size, hidden_size):\n        super().__init__()\n        self.hidden_size = hidden_size\n\n        # All four gates computed in one efficient matrix operation\n        # Output size is 4 * hidden_size: [f_gate | i_gate | c_candidate | o_gate]\n        self.W_ih = nn.Linear(input_size, 4 * hidden_size)   # Input → gates\n        self.W_hh = nn.Linear(hidden_size, 4 * hidden_size)  # Hidden → gates\n\n        self._init_weights()\n\n    def _init_weights(self):\n        # Orthogonal initialization for hidden-to-hidden weights\n        nn.init.orthogonal_(self.W_hh.weight)\n        # Forget gate bias initialized to 1 — makes it remember by default\n        nn.init.constant_(self.W_ih.bias[self.hidden_size:2*self.hidden_size], 1.0)\n        nn.init.constant_(self.W_hh.bias[self.hidden_size:2*self.hidden_size], 1.0)\n\n    def forward(self, x, h_prev, c_prev):\n        \"\"\"\n        x:      [batch, input_size]   — current input\n        h_prev: [batch, hidden_size]  — previous hidden state\n        c_prev: [batch, hidden_size]  — previous cell state\n\n        Returns:\n        h_next: [batch, hidden_size]\n        c_next: [batch, hidden_size]\n        \"\"\"\n        # Compute all gates in one pass (4x efficient vs. separate matrices)\n        gates = self.W_ih(x) + self.W_hh(h_prev)  # [batch, 4*hidden]\n\n        # Split into 4 equal parts\n        f_gate, i_gate, c_cand, o_gate = gates.chunk(4, dim=1)\n        # Each: [batch, hidden_size]\n\n        # Apply gate activations\n        f = torch.sigmoid(f_gate)    # Forget: 0=forget, 1=keep\n        i = torch.sigmoid(i_gate)    # Input: 0=ignore, 1=write\n        g = torch.tanh(c_cand)       # Candidate values: [-1, 1]\n        o = torch.sigmoid(o_gate)    # Output: 0=hide, 1=expose\n\n        # Update cell state: forget old + write new\n        c_next = f * c_prev + i * g\n\n        # Compute hidden state: gated tanh of cell state\n        h_next = o * torch.tanh(c_next)\n\n        return h_next, c_next\n\n\nclass LSTM(nn.Module):\n    \"\"\"\n    Multi-layer LSTM that processes full sequences.\n    \"\"\"\n    def __init__(self, input_size, hidden_size, num_layers=1,\n                 dropout=0.0, bidirectional=False):\n        super().__init__()\n        self.hidden_size = hidden_size\n        self.num_layers = num_layers\n        self.bidirectional = bidirectional\n        self.num_directions = 2 if bidirectional else 1\n\n        # Create a cell for each layer and direction\n        self.cells = nn.ModuleList()\n        for layer in range(num_layers):\n            for direction in range(self.num_directions):\n                in_size = input_size if layer == 0 else hidden_size * self.num_directions\n                self.cells.append(LSTMCell(in_size, hidden_size))\n\n        self.dropout = nn.Dropout(dropout) if dropout &gt; 0 else nn.Identity()\n\n    def forward(self, x, initial_state=None):\n        \"\"\"\n        x: [batch, seq_len, input_size]\n        Returns:\n            output: [batch, seq_len, hidden_size * num_directions]\n            (h_n, c_n): final states [num_layers*directions, batch, hidden_size]\n        \"\"\"\n        batch_size, seq_len, _ = x.shape\n        device = x.device\n\n        # Initialize hidden and cell states to zeros\n        if initial_state is None:\n            h = torch.zeros(self.num_layers * self.num_directions,\n                            batch_size, self.hidden_size, device=device)\n            c = torch.zeros_like(h)\n        else:\n            h, c = initial_state\n\n        layer_input = x  # [batch, seq, input_size]\n\n        h_list, c_list = [], []\n\n        for layer_idx in range(self.num_layers):\n            # Forward direction\n            fwd_cell = self.cells[layer_idx * self.num_directions]\n            fwd_h = h[layer_idx * self.num_directions]\n            fwd_c = c[layer_idx * self.num_directions]\n\n            fwd_outputs = []\n            for t in range(seq_len):\n                fwd_h, fwd_c = fwd_cell(layer_input[:, t, :], fwd_h, fwd_c)\n                fwd_outputs.append(fwd_h)\n\n            fwd_seq = torch.stack(fwd_outputs, dim=1)  # [batch, seq, hidden]\n\n            if self.bidirectional:\n                # Backward direction: process sequence in reverse\n                bwd_cell = self.cells[layer_idx * 2 + 1]\n                bwd_h = h[layer_idx * 2 + 1]\n                bwd_c = c[layer_idx * 2 + 1]\n\n                bwd_outputs = []\n                for t in reversed(range(seq_len)):\n                    bwd_h, bwd_c = bwd_cell(layer_input[:, t, :], bwd_h, bwd_c)\n                    bwd_outputs.append(bwd_h)\n                bwd_outputs.reverse()  # Reorder to match forward direction\n                bwd_seq = torch.stack(bwd_outputs, dim=1)\n\n                layer_output = torch.cat([fwd_seq, bwd_seq], dim=-1)\n                h_list.extend([fwd_h, bwd_h])\n                c_list.extend([fwd_c, bwd_c])\n            else:\n                layer_output = fwd_seq\n                h_list.append(fwd_h)\n                c_list.append(fwd_c)\n\n            # Apply dropout between layers (not on final layer)\n            if layer_idx &lt; self.num_layers - 1:\n                layer_input = self.dropout(layer_output)\n            else:\n                layer_input = layer_output\n\n        h_n = torch.stack(h_list, dim=0)   # [num_layers*directions, batch, hidden]\n        c_n = torch.stack(c_list, dim=0)\n\n        return layer_output, (h_n, c_n)\n\n\n# --- Test ---\nlstm = LSTM(input_size=10, hidden_size=32, num_layers=2, dropout=0.2, bidirectional=True)\nx = torch.randn(4, 20, 10)   # [batch=4, seq=20, input=10]\noutput, (h_n, c_n) = lstm(x)\nprint(f\"Output shape:   {output.shape}\")   # [4, 20, 64] (32*2 bidirectional)\nprint(f\"Final h shape:  {h_n.shape}\")      # [4, 4, 32]  (2 layers * 2 directions)\n\n\n\nLSTMs shine on problems requiring long-range memory. Key applications:\nLanguage Modeling: Predict the next token given all previous tokens.\nclass LSTMLanguageModel(nn.Module):\n    def __init__(self, vocab_size, embed_dim, hidden_size, num_layers, dropout=0.5):\n        super().__init__()\n        self.embedding = nn.Embedding(vocab_size, embed_dim)\n        self.lstm = nn.LSTM(embed_dim, hidden_size, num_layers,\n                            dropout=dropout, batch_first=True)\n        self.drop = nn.Dropout(dropout)\n        self.decoder = nn.Linear(hidden_size, vocab_size)\n        # Tie weights: embedding and decoder share parameters\n        # This is a critical regularization technique for language models\n        self.decoder.weight = self.embedding.weight\n\n    def forward(self, x, hidden=None):\n        \"\"\"\n        x:      [batch, seq_len] — token indices\n        Returns logits [batch, seq_len, vocab_size] and new hidden state\n        \"\"\"\n        emb = self.drop(self.embedding(x))           # [batch, seq, embed_dim]\n        lstm_out, hidden = self.lstm(emb, hidden)    # [batch, seq, hidden]\n        lstm_out = self.drop(lstm_out)\n        logits = self.decoder(lstm_out)              # [batch, seq, vocab_size]\n        return logits, hidden\n\n    def detach_hidden(self, hidden):\n        \"\"\"Detach hidden state to prevent backprop through entire history.\"\"\"\n        return tuple(h.detach() for h in hidden)\n\n\n\n\nThe GRU (Cho et al., 2014) is a simplification of the LSTM. It merges the forget and input gates into a single update gate and eliminates the separate cell state.\nUpdate Gate: Controls how much of the previous hidden state to keep.\nzₜ = σ(Wz·[hₜ₋₁, xₜ])\nReset Gate: Controls how much of the previous hidden state influences the candidate.\nrₜ = σ(Wr·[hₜ₋₁, xₜ])\nCandidate Hidden State:\nh̃ₜ = tanh(W·[rₜ⊙hₜ₋₁, xₜ])\nFinal Update:\nhₜ = (1 - zₜ) ⊙ hₜ₋₁ + zₜ ⊙ h̃ₜ\nWhen zₜ ≈ 0: the hidden state is almost unchanged (the unit forgets nothing — pure memory). When zₜ ≈ 1: the hidden state is completely replaced by the new candidate.\nclass GRUCell(nn.Module):\n    \"\"\"\n    Single GRU cell. Fewer parameters than LSTM, often comparable performance.\n    \"\"\"\n    def __init__(self, input_size, hidden_size):\n        super().__init__()\n        self.hidden_size = hidden_size\n\n        # Update and reset gates\n        self.W_z = nn.Linear(input_size + hidden_size, hidden_size)\n        self.W_r = nn.Linear(input_size + hidden_size, hidden_size)\n        # Candidate hidden state\n        self.W_h = nn.Linear(input_size + hidden_size, hidden_size)\n\n    def forward(self, x, h_prev):\n        \"\"\"\n        x:      [batch, input_size]\n        h_prev: [batch, hidden_size]\n        Returns: h_next [batch, hidden_size]\n        \"\"\"\n        combined = torch.cat([x, h_prev], dim=1)   # [batch, input+hidden]\n\n        # Compute gates\n        z = torch.sigmoid(self.W_z(combined))      # Update gate\n        r = torch.sigmoid(self.W_r(combined))      # Reset gate\n\n        # Candidate: reset gate modulates how much past state to use\n        combined_r = torch.cat([x, r * h_prev], dim=1)\n        h_candidate = torch.tanh(self.W_h(combined_r))\n\n        # Final hidden state: interpolate between old and new\n        h_next = (1 - z) * h_prev + z * h_candidate\n\n        return h_next\n\n\nclass GRUSequenceModel(nn.Module):\n    \"\"\"\n    GRU-based model for sequence classification (e.g., sentiment analysis).\n    \"\"\"\n    def __init__(self, vocab_size, embed_dim, hidden_size,\n                 num_layers, num_classes, dropout=0.3):\n        super().__init__()\n        self.embedding = nn.Embedding(vocab_size, embed_dim, padding_idx=0)\n        self.gru = nn.GRU(embed_dim, hidden_size, num_layers,\n                          batch_first=True, dropout=dropout,\n                          bidirectional=True)\n        # Attention over all hidden states (instead of just final state)\n        self.attention = nn.Linear(hidden_size * 2, 1)\n        self.classifier = nn.Sequential(\n            nn.Dropout(dropout),\n            nn.Linear(hidden_size * 2, hidden_size),\n            nn.ReLU(),\n            nn.Dropout(dropout),\n            nn.Linear(hidden_size, num_classes)\n        )\n\n    def forward(self, x, lengths=None):\n        \"\"\"\n        x:       [batch, seq_len] — padded token ids\n        lengths: [batch] — actual sequence lengths (for packing)\n        \"\"\"\n        emb = self.embedding(x)           # [batch, seq, embed]\n\n        # Pack padded sequences for efficiency (skip padding in GRU)\n        if lengths is not None:\n            emb = nn.utils.rnn.pack_padded_sequence(\n                emb, lengths.cpu(), batch_first=True, enforce_sorted=False\n            )\n\n        gru_out, _ = self.gru(emb)        # [batch, seq, 2*hidden]\n\n        if lengths is not None:\n            gru_out, _ = nn.utils.rnn.pad_packed_sequence(gru_out, batch_first=True)\n\n        # Attention mechanism over sequence dimension\n        attn_scores = self.attention(gru_out).squeeze(-1)  # [batch, seq]\n        if lengths is not None:\n            # Mask padding positions\n            for i, l in enumerate(lengths):\n                attn_scores[i, l:] = float('-inf')\n        attn_weights = F.softmax(attn_scores, dim=1).unsqueeze(-1)  # [batch, seq, 1]\n        context = (gru_out * attn_weights).sum(dim=1)               # [batch, 2*hidden]\n\n        return self.classifier(context)   # [batch, num_classes]\n\n\n\n\n\n\n\n\n\n\n\nFactor\nLSTM\nGRU\n\n\n\n\nParameters\nMore (4 gate matrices)\nFewer (3 gate matrices)\n\n\nLong sequences\nSlightly better (separate cell state)\nClose, often comparable\n\n\nSmall datasets\nMay overfit more\nBetter generalization\n\n\nTraining speed\nSlower\n~30% faster\n\n\nMemory\nHigher\nLower\n\n\nBest for\nLanguage modeling, complex sequences\nShorter sequences, resource-constrained settings\n\n\n\n\n ## Chapter 5: Generative Adversarial Networks — Learning to Create\n\n\n\nA GAN (Goodfellow et al., 2014) involves two networks locked in a competitive game:\n\nThe Generator (G): A forger who creates fake artwork. It takes random noise z as input and outputs synthetic data (images, audio, text).\nThe Discriminator (D): An art critic who tries to distinguish real artwork from the forger’s fakes. It takes an input sample and outputs a probability that it’s real.\n\nThey are trained adversarially: - G tries to fool D by generating more realistic fakes - D tries to get better at detecting fakes\nThe remarkable result: through this adversarial pressure, G learns to generate incredibly realistic data — without ever directly seeing the training data distribution. G only knows whether D was fooled.\n\n\n\nThe GAN minimax game is formalized as:\nmin_G max_D V(D, G) = E_{x~p_data}[log D(x)] + E_{z~p_z}[log(1 - D(G(z)))]\nBreaking this down: - E_{x~p_data}[log D(x)]: Discriminator should output high probability (close to 1) for real data x. log D(x) is maximized when D(x) → 1. - E_{z~p_z}[log(1 - D(G(z)))]: Discriminator should output low probability for fake data G(z). log(1 - D(G(z))) is maximized when D(G(z)) → 0.\nDiscriminator wants to maximize this (correctly classify real and fake). Generator wants to minimize this (fool the discriminator into classifying fakes as real).\nThe non-saturating trick: In practice, instead of minimizing log(1 - D(G(z))), the generator minimizes -log(D(G(z))) (equivalent to maximizing log(D(G(z)))). This provides stronger gradients early in training when the generator is terrible and the discriminator easily rejects its outputs.\n\n\n\n\n\nDCGAN (Radford et al., 2015) established the architectural guidelines that made GANs stable to train:\nGenerator (Decoder-like): - Takes latent vector z and upsamples to image resolution - Uses transposed convolutions (fractionally strided convolutions) for upsampling - BatchNorm on all layers except output - ReLU activations, tanh on output layer\nDiscriminator (Encoder-like): - Takes image and downsamples to scalar - Uses strided convolutions instead of pooling - BatchNorm on all layers except input - LeakyReLU activations, sigmoid on output\nclass DCGANGenerator(nn.Module):\n    \"\"\"\n    Generator for 64×64 images.\n    Architecture: z [100] → linear → reshape → 4 transposed conv blocks → 64×64 RGB\n    \"\"\"\n    def __init__(self, latent_dim=100, img_channels=3, feature_maps=64):\n        super().__init__()\n        self.latent_dim = latent_dim\n        ngf = feature_maps  # Base number of feature maps\n\n        # Project and reshape noise vector\n        self.project = nn.Sequential(\n            nn.Linear(latent_dim, ngf * 8 * 4 * 4),\n            nn.Unflatten(1, (ngf * 8, 4, 4))\n        )\n        # Note: Unflatten reshapes to [batch, ngf*8, 4, 4]\n\n        # Transposed convolution blocks: each doubles spatial size\n        # 4×4 → 8×8 → 16×16 → 32×32 → 64×64\n        self.blocks = nn.Sequential(\n            # Block 1: 4×4 → 8×8\n            self._tconv_block(ngf * 8, ngf * 4),\n            # Block 2: 8×8 → 16×16\n            self._tconv_block(ngf * 4, ngf * 2),\n            # Block 3: 16×16 → 32×32\n            self._tconv_block(ngf * 2, ngf),\n            # Block 4: 32×32 → 64×64 (output layer — no BN, tanh activation)\n            nn.ConvTranspose2d(ngf, img_channels,\n                               kernel_size=4, stride=2, padding=1, bias=False),\n            nn.Tanh()  # Output in [-1, 1] (match normalized images)\n        )\n\n    def _tconv_block(self, in_ch, out_ch):\n        \"\"\"Transposed conv block: upsample by 2×.\"\"\"\n        return nn.Sequential(\n            nn.ConvTranspose2d(in_ch, out_ch,\n                               kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(out_ch),\n            nn.ReLU(inplace=True)\n        )\n\n    def forward(self, z):\n        \"\"\"z: [batch, latent_dim]\"\"\"\n        x = self.project(z)    # [batch, ngf*8, 4, 4]\n        return self.blocks(x)  # [batch, 3, 64, 64]\n\n\nclass DCGANDiscriminator(nn.Module):\n    \"\"\"\n    Discriminator for 64×64 images.\n    Architecture: 64×64 RGB → 4 conv blocks → linear → scalar\n    \"\"\"\n    def __init__(self, img_channels=3, feature_maps=64):\n        super().__init__()\n        ndf = feature_maps\n        # No BatchNorm on first layer (input is raw pixels)\n        self.model = nn.Sequential(\n            # Block 1: 64×64 → 32×32\n            nn.Conv2d(img_channels, ndf, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.LeakyReLU(0.2, inplace=True),  # LeakyReLU prevents dead neurons\n\n            # Block 2: 32×32 → 16×16\n            nn.Conv2d(ndf, ndf * 2, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 2),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Block 3: 16×16 → 8×8\n            nn.Conv2d(ndf * 2, ndf * 4, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 4),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Block 4: 8×8 → 4×4\n            nn.Conv2d(ndf * 4, ndf * 8, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 8),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Output: 4×4 → 1×1 → scalar\n            nn.Conv2d(ndf * 8, 1, kernel_size=4, stride=1, padding=0, bias=False),\n            nn.Flatten(),\n            nn.Sigmoid()  # Output probability: real (1) or fake (0)\n        )\n\n    def forward(self, x):\n        \"\"\"x: [batch, 3, 64, 64]\"\"\"\n        return self.model(x).squeeze()  # [batch]\n\n\n\n\nGAN training is a two-player game — we alternate between optimizing the discriminator and the generator.\ndef weights_init(m):\n    \"\"\"DCGAN weight initialization: Conv weights from N(0, 0.02)\"\"\"\n    classname = m.__class__.__name__\n    if classname.find('Conv') != -1:\n        nn.init.normal_(m.weight.data, 0.0, 0.02)\n    elif classname.find('BatchNorm') != -1:\n        nn.init.normal_(m.weight.data, 1.0, 0.02)\n        nn.init.constant_(m.bias.data, 0)\n\n\ndef train_gan(\n    generator, discriminator, dataloader,\n    latent_dim=100, num_epochs=200, lr=0.0002,\n    beta1=0.5,  # Adam beta1: lower than default for GANs\n    device='cuda'\n):\n    \"\"\"\n    Full DCGAN training loop with all the stability tricks.\n    \"\"\"\n    generator = generator.to(device)\n    discriminator = discriminator.to(device)\n\n    # Apply DCGAN weight initialization\n    generator.apply(weights_init)\n    discriminator.apply(weights_init)\n\n    # Binary cross-entropy loss for adversarial training\n    criterion = nn.BCELoss()\n\n    # Separate optimizers for G and D\n    # Key: lower beta1=0.5 (instead of default 0.9) reduces momentum, helps stability\n    opt_G = torch.optim.Adam(generator.parameters(),\n                             lr=lr, betas=(beta1, 0.999))\n    opt_D = torch.optim.Adam(discriminator.parameters(),\n                             lr=lr, betas=(beta1, 0.999))\n\n    # Fixed noise for monitoring training progress\n    fixed_noise = torch.randn(64, latent_dim, device=device)\n\n    # Label conventions\n    real_label = 1.0\n    fake_label = 0.0\n\n    # Tracking metrics\n    G_losses, D_losses = [], []\n\n    print(\"Starting GAN Training...\")\n    for epoch in range(num_epochs):\n        for batch_idx, (real_images, _) in enumerate(dataloader):\n            batch_size = real_images.size(0)\n            real_images = real_images.to(device)\n\n            # ==========================================\n            # STEP 1: Train the Discriminator\n            # Goal: maximize log(D(x)) + log(1 - D(G(z)))\n            # ==========================================\n            discriminator.zero_grad()\n\n            # --- Train on real batch ---\n            real_labels = torch.full((batch_size,), real_label, device=device)\n            # Forward pass real images through D\n            pred_real = discriminator(real_images)\n            # Loss: D should output 1 for real images\n            loss_D_real = criterion(pred_real, real_labels)\n            loss_D_real.backward()\n\n            # --- Train on fake batch ---\n            # Generate fake images\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            fake_labels = torch.full((batch_size,), fake_label, device=device)\n            # Note: .detach() — don't backprop through G when training D\n            pred_fake = discriminator(fake_images.detach())\n            # Loss: D should output 0 for fake images\n            loss_D_fake = criterion(pred_fake, fake_labels)\n            loss_D_fake.backward()\n\n            # Total discriminator loss\n            loss_D = loss_D_real + loss_D_fake\n            opt_D.step()\n\n            # ==========================================\n            # STEP 2: Train the Generator\n            # Goal: maximize log(D(G(z))) [non-saturating]\n            # ==========================================\n            generator.zero_grad()\n\n            # Generate new fake images (or reuse — but new is more stable)\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            # For generator training, we WANT D to think these are real\n            # So we use real_label=1 as target — generator succeeds when D(G(z))=1\n            pred_G = discriminator(fake_images)  # No .detach() — we want gradients through G\n            loss_G = criterion(pred_G, real_labels)  # \"Fool D into saying these are real\"\n            loss_G.backward()\n            opt_G.step()\n\n            # Logging\n            G_losses.append(loss_G.item())\n            D_losses.append(loss_D.item())\n\n        if (epoch + 1) % 10 == 0:\n            print(f\"Epoch [{epoch+1}/{num_epochs}] \"\n                  f\"Loss_D: {loss_D.item():.4f}  \"\n                  f\"Loss_G: {loss_G.item():.4f}  \"\n                  f\"D(x): {pred_real.mean().item():.3f}  \"\n                  f\"D(G(z)): {pred_fake.mean().item():.3f}/{pred_G.mean().item():.3f}\")\n\n    return G_losses, D_losses\n\n\n\nGANs are notoriously hard to train. Three fundamental problems arise:\n\n\nThe generator learns to produce a small variety of samples (or even just one) that consistently fool the discriminator, ignoring the full data distribution.\nSymptoms: All generated faces look similar; generated digits all look like “1”.\nSolutions: - Minibatch discrimination: Discriminator compares samples within a mini-batch, penalizing when all fake images are similar. - Unrolled GANs: Generator optimizes against future discriminator states. - Wasserstein GAN (WGAN): Replaces JS divergence with Wasserstein distance — more stable gradient signal.\n\n\n\nWhen the discriminator becomes too good, it outputs near-0 for all fakes, and log(1 - D(G(z))) ≈ log(1) = 0 — zero gradient for the generator.\nSolution: WGAN, non-saturating loss, gradient penalty.\n\n\n\nIf D or G is much stronger than the other, training breaks down.\nSolution: Train D multiple times per G update, or vice versa. Two-timescale update rules (TTUR): different learning rates.\n\n\n\n\nWGAN replaces the discriminator with a critic that outputs an unrestricted real number (not a probability). The loss becomes:\nLoss_Critic = E[Critic(fake)] - E[Critic(real)]   ← Critic minimizes\nLoss_G      = -E[Critic(G(z))]                    ← Generator maximizes critic score\nThe 1-Lipschitz constraint on the critic (required by Wasserstein theory) is enforced via gradient penalty:\nGP = E[(||∇_x̂ Critic(x̂)||₂ - 1)²]\nWhere x̂ is a random interpolation between real and fake: x̂ = ε·x_real + (1-ε)·x_fake.\nclass WGANGPCritic(nn.Module):\n    \"\"\"\n    WGAN-GP Critic (discriminator without sigmoid — outputs raw score).\n    No BatchNorm! (incompatible with gradient penalty). Use LayerNorm or SpectralNorm.\n    \"\"\"\n    def __init__(self, img_channels=3, feature_maps=64):\n        super().__init__()\n        ndf = feature_maps\n        self.model = nn.Sequential(\n            # No BN in critic for WGAN-GP\n            nn.Conv2d(img_channels, ndf, 4, 2, 1),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf, ndf*2, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*2, affine=True),  # InstanceNorm instead of BatchNorm\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*2, ndf*4, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*4, affine=True),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*4, ndf*8, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*8, affine=True),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*8, 1, 4, 1, 0),\n            nn.Flatten()\n            # NO sigmoid! Critic outputs raw real number\n        )\n\n    def forward(self, x):\n        return self.model(x).squeeze()\n\n\ndef gradient_penalty(critic, real_images, fake_images, device):\n    \"\"\"\n    Compute WGAN-GP gradient penalty.\n    Enforces 1-Lipschitz constraint by penalizing gradient norm.\n    \"\"\"\n    batch_size = real_images.size(0)\n\n    # Random interpolation coefficient: ε ~ Uniform[0,1]\n    epsilon = torch.rand(batch_size, 1, 1, 1, device=device)\n\n    # Interpolated samples: x̂ = ε·real + (1-ε)·fake\n    interpolated = epsilon * real_images + (1 - epsilon) * fake_images.detach()\n    interpolated.requires_grad_(True)\n\n    # Critic score for interpolated samples\n    critic_score = critic(interpolated)\n\n    # Compute gradients of critic scores w.r.t. interpolated samples\n    gradients = torch.autograd.grad(\n        outputs=critic_score,\n        inputs=interpolated,\n        grad_outputs=torch.ones_like(critic_score),\n        create_graph=True,   # Need second-order gradients for optimization\n        retain_graph=True,\n    )[0]\n\n    # Gradient norm: flatten spatial dims, compute L2 norm per sample\n    gradients = gradients.view(batch_size, -1)\n    gradient_norm = gradients.norm(2, dim=1)           # [batch]\n\n    # Penalty: (||∇||₂ - 1)² — penalize deviations from unit gradient norm\n    penalty = ((gradient_norm - 1) ** 2).mean()\n\n    return penalty\n\n\ndef train_wgan_gp(generator, critic, dataloader,\n                  latent_dim=100, num_epochs=100,\n                  n_critic=5,    # Train critic n_critic times per generator update\n                  lambda_gp=10,  # GP weight\n                  lr=1e-4, device='cuda'):\n    \"\"\"\n    WGAN-GP training loop.\n    \"\"\"\n    generator, critic = generator.to(device), critic.to(device)\n\n    # Note: WGAN uses Adam with lower learning rate and beta1=0\n    opt_G = torch.optim.Adam(generator.parameters(), lr=lr, betas=(0.0, 0.9))\n    opt_C = torch.optim.Adam(critic.parameters(),    lr=lr, betas=(0.0, 0.9))\n\n    for epoch in range(num_epochs):\n        for real_images, _ in dataloader:\n            batch_size = real_images.size(0)\n            real_images = real_images.to(device)\n\n            # === Train Critic (n_critic times per G update) ===\n            for _ in range(n_critic):\n                noise = torch.randn(batch_size, latent_dim, device=device)\n                fake_images = generator(noise).detach()\n\n                # Wasserstein loss: maximize E[C(real)] - E[C(fake)]\n                loss_C = (critic(fake_images).mean()   # C wants fake scores LOW\n                         - critic(real_images).mean()  # C wants real scores HIGH\n                         + lambda_gp * gradient_penalty(critic, real_images, fake_images, device))\n\n                opt_C.zero_grad()\n                loss_C.backward()\n                opt_C.step()\n\n            # === Train Generator ===\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            # G wants critic to score its fakes as HIGH as possible\n            loss_G = -critic(fake_images).mean()\n\n            opt_G.zero_grad()\n            loss_G.backward()\n            opt_G.step()\n\n        if (epoch + 1) % 10 == 0:\n            print(f\"Epoch {epoch+1}: Loss_C={loss_C.item():.4f} | Loss_G={loss_G.item():.4f}\")\n\n\n\nStandard GANs generate random samples — we can’t control what class or attribute is generated. Conditional GANs (Mirza & Osindero, 2014) condition both G and D on additional information y (a class label, text description, or reference image).\nclass ConditionalGenerator(nn.Module):\n    \"\"\"Generator conditioned on a class label.\"\"\"\n    def __init__(self, latent_dim, num_classes, embed_dim, img_size):\n        super().__init__()\n        self.label_emb = nn.Embedding(num_classes, embed_dim)\n        in_dim = latent_dim + embed_dim\n\n        self.model = nn.Sequential(\n            nn.Linear(in_dim, 256),\n            nn.LeakyReLU(0.2),\n            nn.BatchNorm1d(256),\n            nn.Linear(256, 512),\n            nn.LeakyReLU(0.2),\n            nn.BatchNorm1d(512),\n            nn.Linear(512, img_size * img_size),\n            nn.Tanh()\n        )\n        self.img_size = img_size\n\n    def forward(self, noise, labels):\n        \"\"\"\n        noise:  [batch, latent_dim]\n        labels: [batch] — class indices\n        \"\"\"\n        label_embed = self.label_emb(labels)           # [batch, embed_dim]\n        x = torch.cat([noise, label_embed], dim=1)    # [batch, latent+embed]\n        img = self.model(x)\n        return img.view(img.size(0), 1, self.img_size, self.img_size)\n\n\n\n\n\n\n\n\n\n\n\nGAN Variant\nKey Innovation\nApplication\n\n\n\n\nDCGAN\nConv architecture guidelines\nImage generation\n\n\nWGAN-GP\nWasserstein loss + gradient penalty\nStable training\n\n\ncGAN\nConditioning on labels/text\nControlled generation\n\n\nPix2Pix\nImage-to-image with paired data\nStyle transfer, colorization\n\n\nCycleGAN\nCycle consistency loss, no paired data\nDomain adaptation\n\n\nStyleGAN2\nStyle-based generator, W-space\nHigh-fidelity face synthesis\n\n\nBigGAN\nLarge-scale, class-conditional\nImageNet generation\n\n\nGauGAN\nSPADE normalization\nPhoto-realistic scene synthesis\n\n\n\n\n\n\nUnlike supervised learning, we can’t use simple accuracy. GAN evaluation is an open research problem. Key metrics:\nFréchet Inception Distance (FID): The most widely used metric. Computes the Fréchet distance between the distributions of Inception features from real and generated images. Lower is better.\n# Conceptual FID calculation (uses scipy in practice)\ndef compute_fid(mu_real, sigma_real, mu_fake, sigma_fake):\n    \"\"\"\n    FID = ||μ_r - μ_g||² + Tr(Σ_r + Σ_g - 2(Σ_r·Σ_g)^(1/2))\n    \"\"\"\n    from scipy.linalg import sqrtm\n    import numpy as np\n    diff = mu_real - mu_fake\n    covmean = sqrtm(sigma_real @ sigma_fake)\n    if np.iscomplexobj(covmean):\n        covmean = covmean.real\n    fid = (diff @ diff + np.trace(sigma_real + sigma_fake - 2 * covmean))\n    return fid\nInception Score (IS): Measures both quality (sharp predictions for single class) and diversity (uniform marginal distribution across classes). Higher is better.\nPrecision and Recall: Precision measures sample quality; recall measures coverage of the real data manifold.\n\n\n\n\n\n\n\nAfter diving deep into five paradigms, we can identify universal principles:\n\nResiduals everywhere: Skip connections solve vanishing gradients and ease optimization. Used in Transformers (residual connections), ResNets, and modern RNNs.\nNormalization is essential: Layer Norm (Transformer), Batch Norm (CNNs), Instance Norm (GANs) — each architecture uses the right normalization for its structure.\nAttention is general aggregation: Multi-head attention in Transformers, graph attention in GATs, temporal attention in GRUs — all are forms of learned weighted aggregation.\nScale matters: More parameters, more data, and more compute consistently improve performance across all architectures (the “bitter lesson”).\nInductive biases: Good architectures encode assumptions about data structure. CNNs assume spatial locality. RNNs assume temporal order. GNNs assume relational structure. Transformers make fewer assumptions (more general, but need more data).\n\n\n\n\n\nTransformer = Fully-connected GNN: Self-attention is message passing where every node can attend to every other node. Transformers are GNNs on a complete graph with learned edge weights.\nGNN on grid = CNN: When the graph is a regular 2D grid with local connections, graph convolution reduces to standard convolution.\nAttention in RNNs: The Bahdanau attention mechanism (precursor to Transformers) is attention added on top of encoder-decoder RNNs.\nGAN generator = Decoder: The Transformer decoder, CNN decoder (transposed convolutions), and GAN generator all take compact representations and expand them into rich outputs.\n\n\n\n\n# Universal training best practices\n\n# 1. Learning rate scheduling — never use constant LR\nscheduler = torch.optim.lr_scheduler.OneCycleLR(\n    optimizer, max_lr=0.01,\n    steps_per_epoch=len(dataloader),\n    epochs=num_epochs\n)\n\n# 2. Gradient clipping — essential for RNNs, useful everywhere\ntorch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n\n# 3. Mixed precision training — 2x speed, half memory\nfrom torch.cuda.amp import autocast, GradScaler\nscaler = GradScaler()\nwith autocast():\n    output = model(input)\n    loss = criterion(output, target)\nscaler.scale(loss).backward()\nscaler.step(optimizer)\nscaler.update()\n\n# 4. Exponential Moving Average of weights — better generalization\nfrom torch.optim.swa_utils import AveragedModel\nema_model = AveragedModel(model, multi_avg_fn=torch.optim.swa_utils.get_ema_multi_avg_fn(0.999))\n\n# 5. Data augmentation (for vision tasks)\nimport torchvision.transforms as T\naugment = T.Compose([\n    T.RandomHorizontalFlip(),\n    T.RandomCrop(32, padding=4),\n    T.ColorJitter(brightness=0.4, contrast=0.4, saturation=0.4),\n    T.ToTensor(),\n    T.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])\n])\n\n\n\n\n\nYou now have expert-level knowledge of the five fundamental pillars of modern deep learning. Here is a structured roadmap for further mastery:\nImmediate Next Steps:\n\nImplement and train on real data: Theory becomes knowledge through practice. Train a GCN on Cora (citation network), a CNN on CIFAR-10, a Transformer on a translation task.\nRead the original papers: “Attention Is All You Need”, “Deep Residual Learning for Image Recognition”, “Semi-supervised Classification with GCN”, “Generative Adversarial Nets”, “LSTM (1997)”. Reading primaries deepens understanding enormously.\nExplore PyTorch Geometric for GNNs and HuggingFace Transformers for production-grade Transformer implementations.\n\nAdvanced Topics to Explore:\n\nDiffusion Models: The current state-of-the-art for image generation, surpassing GANs in quality and stability. Based on gradually denoising Gaussian noise.\nGraph Transformers: Combining attention mechanisms with graph structure — the current frontier of GNN research.\nState Space Models (Mamba): A new paradigm for sequence modeling, potentially replacing Transformers for long-context tasks.\nNeural Architecture Search (NAS): Automating the design of neural network architectures.\nFoundation Models: Pretraining massive models on internet-scale data and fine-tuning for downstream tasks (BERT, GPT, SAM, etc.).\n\nThe field moves fast. The architectures in this article have already transformed industries — autonomous driving, drug discovery, natural language processing, computer vision, and beyond. Master these foundations, and you will be equipped not just to use these tools, but to push them forward.\n\nThis article covered: Scaled Dot-Product Attention, Multi-Head Attention, Positional Encoding, Transformer Encoder-Decoder, GCN, GAT, Message Passing Neural Networks, Temporal GNNs, TGN Memory, Convolution operations (padding, stride, dilation), ResNets, LSTM from scratch, GRU from scratch, DCGAN, WGAN-GP, Conditional GAN — all with complete, runnable PyTorch implementations."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#advanced-neural-network-architectures-a-ground-up-masterclass",
    "href": "Resources/DeepLearningAdv/index.html#advanced-neural-network-architectures-a-ground-up-masterclass",
    "title": "DLA",
    "section": "",
    "text": "This article assumes you are comfortable with basic Python and high-school-level math. By the end, you will have a working expert-level understanding of Transformers, GNNs, CNNs, RNNs, and GANs — the five pillars of modern deep learning."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#table-of-contents",
    "href": "Resources/DeepLearningAdv/index.html#table-of-contents",
    "title": "DLA",
    "section": "",
    "text": "Foundations You Must Know First\nTransformer Models & Attention Mechanisms\nGraph Neural Networks (GNNs)\nConvolutional Neural Networks (CNNs)\nRecurrent Neural Networks (RNNs)\nGenerative Adversarial Networks (GANs)\n\n\n ## Chapter 0: Foundations You Must Know First\nBefore we can climb the mountain, we need to understand the ground beneath our feet.\n\n\nA neural network is a mathematical function that maps inputs to outputs by composing many simpler functions called neurons. Each neuron computes a weighted sum of its inputs and passes the result through a non-linear function called an activation function.\nMathematically, a single neuron computes:\noutput = activation( w₁x₁ + w₂x₂ + ... + wₙxₙ + b )\nWhere: - x₁, x₂, ..., xₙ are input features - w₁, w₂, ..., wₙ are learnable weights - b is a learnable bias - activation(·) is a non-linear function like ReLU, sigmoid, or tanh\n\n\n\nWithout non-linearity, stacking multiple layers reduces to a single linear transformation (matrix multiplication collapses into one matrix), making the network no more expressive than logistic regression. Non-linearities allow networks to approximate arbitrarily complex functions — this is the Universal Approximation Theorem.\n\n\n\nTraining means adjusting weights w to minimize a loss function — a measure of how wrong our predictions are. We use gradient descent: compute the derivative of the loss with respect to every weight, then nudge weights in the direction that reduces loss.\nw ← w - η · ∂L/∂w\nWhere η (eta) is the learning rate — a small number like 0.001 that controls step size.\nBackpropagation is the algorithm that efficiently computes all these derivatives using the chain rule of calculus, propagating error gradients backward through the network from output to input.\n\n\n\nAll data in deep learning lives in tensors — multi-dimensional arrays. A scalar is a 0-D tensor, a vector is 1-D, a matrix is 2-D, and so on. PyTorch and TensorFlow both operate on tensors and automatically track gradients through them.\nimport torch\n\nx = torch.tensor([[1.0, 2.0], [3.0, 4.0]], requires_grad=True)\ny = (x ** 2).sum()\ny.backward()\nprint(x.grad)  # dy/dx = 2x → [[2., 4.], [6., 8.]]\nWith that foundation, let us begin.\n\n ## Chapter 1: Transformer Models — The Architecture That Changed Everything\n\n\n\nBefore 2017, sequence modeling was dominated by RNNs and LSTMs. These architectures process sequences token by token, left to right. This creates two crushing limitations:\n\nSequential bottleneck: You cannot process token 10 until you have processed tokens 1–9. This kills parallelism during training.\nVanishing long-range dependencies: Information from token 1 must travel through hundreds of hidden states to influence token 500. Gradients vanish; the network forgets.\n\nThe paper “Attention Is All You Need” (Vaswani et al., 2017) proposed a radical idea: throw away recurrence entirely. Replace it with a mechanism called self-attention that allows every token to directly attend to every other token in the sequence simultaneously.\nThis was not an incremental improvement — it was a paradigm shift. Every modern large language model (GPT, BERT, T5, Claude, Gemini) is built on this architecture.\n\n\n\n\n\nImagine you are at a library. You have a query (what you’re looking for), the library has shelves of keys (catalog labels), and each shelf contains values (actual books). You match your query against every key, find the best matches, and retrieve the corresponding values — possibly a weighted blend of multiple books.\nAttention does exactly this with vectors:\n\nQuery (Q): What am I looking for?\nKey (K): What does each token advertise about itself?\nValue (V): What information does each token actually carry?\n\nFor a sequence of n tokens, each token produces a query vector, a key vector, and a value vector. The output for each token is a weighted sum of all value vectors, where the weights are determined by how well that token’s query matches each other token’s key.\n\n\n\nGiven matrices Q (queries), K (keys), and V (values), attention is computed as:\nAttention(Q, K, V) = softmax( Q·Kᵀ / √dₖ ) · V\nLet us break this down step by step.\nStep 1 — Compute similarity scores:\nscores = Q · Kᵀ     # shape: [n_tokens × n_tokens]\nscores[i][j] measures how much token i should attend to token j. This is just a dot product between the query of token i and the key of token j.\nStep 2 — Scale by √dₖ:\nscores = scores / √dₖ\nWhere dₖ is the dimensionality of the key vectors. Why scale? When dₖ is large, dot products grow large in magnitude, pushing softmax into regions with very small gradients (saturation). Dividing by √dₖ stabilizes training.\nStep 3 — Apply softmax to get attention weights:\nweights = softmax(scores, dim=-1)   # shape: [n_tokens × n_tokens]\nEach row of weights sums to 1. weights[i][j] is the fraction of attention token i pays to token j.\nStep 4 — Weighted sum of values:\noutput = weights · V   # shape: [n_tokens × d_v]\nEach output token is a weighted blend of all value vectors.\n\n\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport math\n\ndef scaled_dot_product_attention(Q, K, V, mask=None):\n    \"\"\"\n    Args:\n        Q: Queries,  shape [batch, heads, seq_len, d_k]\n        K: Keys,     shape [batch, heads, seq_len, d_k]\n        V: Values,   shape [batch, heads, seq_len, d_v]\n        mask: Optional boolean mask, shape [batch, 1, seq_len, seq_len]\n              True = position is masked (ignored)\n    Returns:\n        output:  shape [batch, heads, seq_len, d_v]\n        weights: attention weights [batch, heads, seq_len, seq_len]\n    \"\"\"\n    d_k = Q.size(-1)\n\n    # Step 1 & 2: Scaled similarity scores\n    # Q·Kᵀ → [batch, heads, seq_len_q, seq_len_k]\n    scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(d_k)\n\n    # Step 3: Apply mask (used in decoder to prevent attending to future tokens)\n    if mask is not None:\n        scores = scores.masked_fill(mask, float('-inf'))\n        # -inf → 0 after softmax, effectively ignoring masked positions\n\n    # Step 4: Softmax over the key dimension\n    weights = F.softmax(scores, dim=-1)\n\n    # Step 5: Weighted sum of values\n    output = torch.matmul(weights, V)\n\n    return output, weights\n\n\n# --- Quick test ---\nbatch, heads, seq_len, d_k = 2, 4, 10, 64\nQ = torch.randn(batch, heads, seq_len, d_k)\nK = torch.randn(batch, heads, seq_len, d_k)\nV = torch.randn(batch, heads, seq_len, d_k)\n\nout, w = scaled_dot_product_attention(Q, K, V)\nprint(f\"Output shape: {out.shape}\")    # [2, 4, 10, 64]\nprint(f\"Weights shape: {w.shape}\")     # [2, 4, 10, 10]\nprint(f\"Weights sum (should be 1): {w[0,0,0].sum():.4f}\")\n\n\n\n\nA single attention operation computes one type of relationship between tokens. But language is rich — “bank” relates to “river” in one context and “money” in another. We want the model to capture multiple types of relationships simultaneously.\nMulti-Head Attention runs h independent attention operations (called heads) in parallel, each with its own learned Q, K, V projection matrices, then concatenates and re-projects the results.\nMultiHead(Q, K, V) = Concat(head₁, head₂, ..., headₕ) · Wᴼ\n\nwhere headᵢ = Attention(Q·Wᵢᴼ, K·Wᵢᴷ, V·Wᵢᵛ)\nThe key insight: instead of using d_model-dimensional keys, we project to d_model / h per head. This keeps the total computation roughly constant while multiplying the representational capacity.\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, num_heads):\n        \"\"\"\n        d_model:    Total model dimension (e.g., 512)\n        num_heads:  Number of parallel attention heads (e.g., 8)\n        d_k = d_v = d_model / num_heads = 64 in this example\n        \"\"\"\n        super().__init__()\n        assert d_model % num_heads == 0, \"d_model must be divisible by num_heads\"\n\n        self.d_model = d_model\n        self.num_heads = num_heads\n        self.d_k = d_model // num_heads   # dimension per head\n\n        # Single projection matrices for all heads (efficient)\n        # Instead of h separate W matrices, we use one large W and split\n        self.W_q = nn.Linear(d_model, d_model)   # projects to [batch, seq, d_model]\n        self.W_k = nn.Linear(d_model, d_model)\n        self.W_v = nn.Linear(d_model, d_model)\n        self.W_o = nn.Linear(d_model, d_model)   # output projection\n\n    def split_heads(self, x):\n        \"\"\"\n        Reshape from [batch, seq_len, d_model]\n        to           [batch, num_heads, seq_len, d_k]\n        \"\"\"\n        batch_size, seq_len, _ = x.size()\n        # Reshape: [batch, seq, d_model] → [batch, seq, heads, d_k]\n        x = x.view(batch_size, seq_len, self.num_heads, self.d_k)\n        # Transpose: [batch, seq, heads, d_k] → [batch, heads, seq, d_k]\n        return x.transpose(1, 2)\n\n    def forward(self, query, key, value, mask=None):\n        \"\"\"\n        query, key, value: [batch, seq_len, d_model]\n        mask: optional [batch, 1, seq_len, seq_len]\n        \"\"\"\n        batch_size = query.size(0)\n\n        # 1. Linear projections\n        Q = self.W_q(query)  # [batch, seq, d_model]\n        K = self.W_k(key)\n        V = self.W_v(value)\n\n        # 2. Split into multiple heads\n        Q = self.split_heads(Q)  # [batch, heads, seq, d_k]\n        K = self.split_heads(K)\n        V = self.split_heads(V)\n\n        # 3. Scaled dot-product attention on each head in parallel\n        attn_output, attn_weights = scaled_dot_product_attention(Q, K, V, mask)\n        # attn_output: [batch, heads, seq, d_k]\n\n        # 4. Concatenate heads\n        # Transpose back: [batch, heads, seq, d_k] → [batch, seq, heads, d_k]\n        attn_output = attn_output.transpose(1, 2).contiguous()\n        # Merge heads: [batch, seq, heads, d_k] → [batch, seq, d_model]\n        attn_output = attn_output.view(batch_size, -1, self.d_model)\n\n        # 5. Final output projection\n        output = self.W_o(attn_output)  # [batch, seq, d_model]\n\n        return output, attn_weights\n\n\n# --- Test ---\nd_model, num_heads, seq_len = 512, 8, 20\nmha = MultiHeadAttention(d_model, num_heads)\nx = torch.randn(2, seq_len, d_model)\nout, weights = mha(x, x, x)   # self-attention: Q=K=V=x\nprint(f\"MHA output: {out.shape}\")     # [2, 20, 512]\nprint(f\"Attn weights: {weights.shape}\")  # [2, 8, 20, 20]\n\n\n\nAttention has no built-in notion of order — it treats the input as a set of tokens, not a sequence. We must inject positional information explicitly.\nThe original Transformer used fixed sinusoidal encodings:\nPE(pos, 2i)   = sin(pos / 10000^(2i/d_model))\nPE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))\nWhere pos is the position index and i is the dimension index. Different dimensions encode different frequency sine/cosine waves, allowing the model to distinguish positions and even extrapolate to unseen lengths.\nclass PositionalEncoding(nn.Module):\n    def __init__(self, d_model, max_seq_len=5000, dropout=0.1):\n        super().__init__()\n        self.dropout = nn.Dropout(dropout)\n\n        # Create a matrix of shape [max_seq_len, d_model]\n        pe = torch.zeros(max_seq_len, d_model)\n\n        # Position indices: [0, 1, 2, ..., max_seq_len-1]\n        position = torch.arange(0, max_seq_len).unsqueeze(1).float()\n\n        # Division term: 10000^(2i/d_model), computed in log space for stability\n        div_term = torch.exp(\n            torch.arange(0, d_model, 2).float() * (-math.log(10000.0) / d_model)\n        )\n\n        # Even dimensions: sine, odd dimensions: cosine\n        pe[:, 0::2] = torch.sin(position * div_term)   # 0, 2, 4, ...\n        pe[:, 1::2] = torch.cos(position * div_term)   # 1, 3, 5, ...\n\n        # Add batch dimension and register as non-trainable buffer\n        pe = pe.unsqueeze(0)  # [1, max_seq_len, d_model]\n        self.register_buffer('pe', pe)\n\n    def forward(self, x):\n        \"\"\"\n        x: [batch, seq_len, d_model]\n        Add positional encoding to token embeddings\n        \"\"\"\n        x = x + self.pe[:, :x.size(1), :]\n        return self.dropout(x)\n\n\n\nAfter attention, each position is independently processed by a small 2-layer MLP:\nFFN(x) = max(0, x·W₁ + b₁)·W₂ + b₂\nThe inner dimension is typically 4× the model dimension (e.g., 2048 for d_model=512). This is where most of the “knowledge storage” happens — research has shown that these layers act as key-value memories.\nclass FeedForward(nn.Module):\n    def __init__(self, d_model, d_ff, dropout=0.1):\n        super().__init__()\n        self.net = nn.Sequential(\n            nn.Linear(d_model, d_ff),\n            nn.ReLU(),\n            nn.Dropout(dropout),\n            nn.Linear(d_ff, d_model),\n            nn.Dropout(dropout),\n        )\n\n    def forward(self, x):\n        return self.net(x)\n\n\n\nEach sublayer (attention and FFN) is wrapped in a residual connection and layer normalization:\noutput = LayerNorm(x + Sublayer(x))\nWhy residuals? They create gradient highways — during backprop, gradients can flow directly from output to input without passing through every layer, preventing vanishing gradients in deep networks.\nWhy layer norm? It normalizes the activations across the feature dimension for each token independently, stabilizing training without the batch-size dependency of batch normalization.\n\n\n\nclass TransformerEncoderLayer(nn.Module):\n    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):\n        super().__init__()\n        self.self_attn = MultiHeadAttention(d_model, num_heads)\n        self.ffn = FeedForward(d_model, d_ff, dropout)\n        self.norm1 = nn.LayerNorm(d_model)\n        self.norm2 = nn.LayerNorm(d_model)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, x, mask=None):\n        # --- Sub-layer 1: Self-Attention ---\n        attn_out, _ = self.self_attn(x, x, x, mask)   # Q=K=V=x\n        x = self.norm1(x + self.dropout(attn_out))     # Residual + Norm\n\n        # --- Sub-layer 2: Feed-Forward ---\n        ffn_out = self.ffn(x)\n        x = self.norm2(x + ffn_out)                    # Residual + Norm\n\n        return x\n\n\n\nThe original Transformer was designed for sequence-to-sequence tasks (e.g., translation). It has two components:\n\n\nThe encoder reads the input sequence and produces rich contextual representations. It stacks N encoder layers (typically N=6). Each encoder layer applies self-attention (where every input token can attend to every other input token) and a feed-forward network.\nInput → Embedding + Positional Encoding\n      → [EncoderLayer₁ → EncoderLayer₂ → ... → EncoderLayerₙ]\n      → Encoder Output (contextual representations)\n\n\n\nThe decoder generates the output sequence token by token. Each decoder layer has three sublayers:\n\nMasked Self-Attention: The decoder attends to its own previously generated tokens. The “masking” prevents attending to future positions (causal masking), since at inference time, future tokens don’t exist yet.\nCross-Attention: This is the encoder-decoder bridge. The decoder’s queries attend to the encoder’s keys and values. This is how the decoder “reads” the input — the decoder asks questions (queries) and the encoder provides answers (keys + values).\nFeed-Forward Network: Same as encoder.\n\nclass TransformerDecoderLayer(nn.Module):\n    def __init__(self, d_model, num_heads, d_ff, dropout=0.1):\n        super().__init__()\n        # Sub-layer 1: Masked self-attention (causal)\n        self.self_attn = MultiHeadAttention(d_model, num_heads)\n        # Sub-layer 2: Cross-attention over encoder output\n        self.cross_attn = MultiHeadAttention(d_model, num_heads)\n        # Sub-layer 3: Feed-forward\n        self.ffn = FeedForward(d_model, d_ff, dropout)\n\n        self.norm1 = nn.LayerNorm(d_model)\n        self.norm2 = nn.LayerNorm(d_model)\n        self.norm3 = nn.LayerNorm(d_model)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, x, encoder_output, src_mask=None, tgt_mask=None):\n        \"\"\"\n        x:              Decoder input, [batch, tgt_len, d_model]\n        encoder_output: [batch, src_len, d_model]\n        src_mask:       Padding mask for encoder output\n        tgt_mask:       Causal mask for decoder input (upper-triangular)\n        \"\"\"\n        # --- Sub-layer 1: Masked self-attention ---\n        # Q, K, V all come from decoder input; mask prevents future-peeking\n        attn1, _ = self.self_attn(x, x, x, tgt_mask)\n        x = self.norm1(x + self.dropout(attn1))\n\n        # --- Sub-layer 2: Cross-attention ---\n        # Queries from decoder, Keys & Values from encoder output\n        attn2, cross_weights = self.cross_attn(\n            query=x,\n            key=encoder_output,\n            value=encoder_output,\n            mask=src_mask\n        )\n        x = self.norm2(x + self.dropout(attn2))\n\n        # --- Sub-layer 3: Feed-forward ---\n        ffn_out = self.ffn(x)\n        x = self.norm3(x + ffn_out)\n\n        return x, cross_weights\n\n\ndef make_causal_mask(seq_len, device):\n    \"\"\"\n    Creates an upper-triangular mask to prevent attending to future positions.\n    mask[i][j] = True means token i should NOT attend to token j.\n    \"\"\"\n    mask = torch.triu(torch.ones(seq_len, seq_len, device=device), diagonal=1).bool()\n    return mask.unsqueeze(0).unsqueeze(0)  # [1, 1, seq, seq]\n\n\n\nclass Transformer(nn.Module):\n    def __init__(self, src_vocab, tgt_vocab, d_model=512, num_heads=8,\n                 num_layers=6, d_ff=2048, max_seq_len=512, dropout=0.1):\n        super().__init__()\n\n        # Embeddings (shared or separate for src/tgt)\n        self.src_embedding = nn.Embedding(src_vocab, d_model)\n        self.tgt_embedding = nn.Embedding(tgt_vocab, d_model)\n        self.pos_encoding = PositionalEncoding(d_model, max_seq_len, dropout)\n\n        # Encoder stack\n        self.encoder_layers = nn.ModuleList([\n            TransformerEncoderLayer(d_model, num_heads, d_ff, dropout)\n            for _ in range(num_layers)\n        ])\n\n        # Decoder stack\n        self.decoder_layers = nn.ModuleList([\n            TransformerDecoderLayer(d_model, num_heads, d_ff, dropout)\n            for _ in range(num_layers)\n        ])\n\n        # Final projection to vocabulary\n        self.fc_out = nn.Linear(d_model, tgt_vocab)\n        self.d_model = d_model\n\n    def encode(self, src, src_mask=None):\n        x = self.pos_encoding(self.src_embedding(src) * math.sqrt(self.d_model))\n        for layer in self.encoder_layers:\n            x = layer(x, src_mask)\n        return x  # [batch, src_len, d_model]\n\n    def decode(self, tgt, encoder_output, src_mask=None, tgt_mask=None):\n        x = self.pos_encoding(self.tgt_embedding(tgt) * math.sqrt(self.d_model))\n        for layer in self.decoder_layers:\n            x, _ = layer(x, encoder_output, src_mask, tgt_mask)\n        return x  # [batch, tgt_len, d_model]\n\n    def forward(self, src, tgt, src_mask=None):\n        tgt_mask = make_causal_mask(tgt.size(1), tgt.device)\n        encoder_output = self.encode(src, src_mask)\n        decoder_output = self.decode(tgt, encoder_output, src_mask, tgt_mask)\n        logits = self.fc_out(decoder_output)\n        return logits  # [batch, tgt_len, tgt_vocab]\n\n\n\n\n\nEncoder-only (BERT-style): No decoder. Bidirectional attention — each token sees all others. Used for understanding tasks: classification, NER, question answering. Pretrained with Masked Language Modeling.\nDecoder-only (GPT-style): No encoder. Causal attention only. Used for generation. Pretrained with next-token prediction. This is the architecture behind ChatGPT, Claude, and LLaMA.\nEncoder-decoder (T5, BART): Full architecture as above. Used for translation, summarization, any seq2seq task.\n\n\n\n\nThe power of attention can be understood from three angles:\n\nArbitrary range: Any two tokens can directly influence each other in a single layer. Path length between any pair is O(1), versus O(n) for RNNs.\nDynamic computation: Attention weights are input-dependent. The same word computes different relationships in different contexts. “Bank” in “river bank” has different keys/queries than “bank” in “central bank.”\nInterpretability: Attention weights provide a window into what the model “looks at,” making Transformers more interpretable than RNNs (though this is a simplification — the relationship between attention and model behavior is complex).\n\n\n ## Chapter 2: Graph Neural Networks — Learning on Structured Data\n\n\n\nMost real-world data is not a sequence or a grid — it is a graph:\n\nSocial networks (users = nodes, friendships = edges)\nMolecules (atoms = nodes, bonds = edges)\nKnowledge graphs (entities = nodes, relations = edges)\nRoad networks (intersections = nodes, roads = edges)\nCitation networks (papers = nodes, citations = edges)\nProtein interaction networks\n\nCNNs exploit the grid structure of images. RNNs exploit the sequential structure of language. GNNs are the equivalent framework for arbitrary graph structures.\n\n\n\nA graph G = (V, E) consists of: - V: a set of n nodes (also called vertices) - E: a set of edges between pairs of nodes - X ∈ ℝⁿˣᶠ: a feature matrix where row i is the feature vector of node i - A ∈ ℝⁿˣⁿ: an adjacency matrix where A[i][j] = 1 if there’s an edge from i to j\nA graph can be directed (edges have direction) or undirected (edges are symmetric). Edges can also have features.\n\n\n\nThe central operation in virtually all GNNs is message passing. The intuition: a node should update its representation based on information gathered from its neighbors. This is analogous to asking: “Given who I am and who I am connected to, how should I update my understanding of myself?”\nThe message-passing framework is:\nStep 1 — Message: Each neighbor sends a message to the central node.\nmᵢⱼ = MESSAGE(hᵢᵗ, hⱼᵗ, eᵢⱼ)\nStep 2 — Aggregate: Collect messages from all neighbors into a single summary.\nMᵢᵗ = AGGREGATE({mᵢⱼ : j ∈ N(i)})\nStep 3 — Update: Update the node’s own representation.\nhᵢᵗ⁺¹ = UPDATE(hᵢᵗ, Mᵢᵗ)\nAfter k rounds of message passing, each node’s representation encodes information from its k-hop neighborhood.\n\n\n\nThe seminal GCN paper (Kipf & Welling, 2017) introduced a particularly elegant form of message passing.\nThe Spectral Motivation: Graph convolutions can be derived from spectral graph theory as a localized approximation of graph signal filtering. But the practical result is beautifully simple.\nFor a single GCN layer:\nH^(l+1) = σ( D̃^(-1/2) Ã D̃^(-1/2) H^(l) W^(l) )\nWhere: - Ã = A + I (adjacency matrix with added self-loops) - D̃[i][i] = Σⱼ Ã[i][j] (degree matrix of Ã) - D̃^(-1/2) Ã D̃^(-1/2) is the symmetric normalized adjacency - W^(l) is the learnable weight matrix for layer l - σ is an activation function (e.g., ReLU)\nThe D̃^(-1/2) Ã D̃^(-1/2) term normalizes by node degree, preventing high-degree nodes from drowning out low-degree nodes. The Ã = A + I self-loops ensure each node also retains its own features.\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass GCNLayer(nn.Module):\n    def __init__(self, in_features, out_features):\n        super().__init__()\n        self.linear = nn.Linear(in_features, out_features, bias=False)\n        # Initialize weights following Glorot uniform\n        nn.init.xavier_uniform_(self.linear.weight)\n\n    def forward(self, H, A_hat):\n        \"\"\"\n        H:     Node feature matrix [n_nodes, in_features]\n        A_hat: Pre-normalized adjacency D̃^(-1/2) Ã D̃^(-1/2) [n_nodes, n_nodes]\n        \"\"\"\n        # Aggregate neighbor features: A_hat @ H\n        aggregated = torch.mm(A_hat, H)           # [n_nodes, in_features]\n        # Linear transformation + activation\n        return self.linear(aggregated)            # [n_nodes, out_features]\n\n\ndef normalize_adjacency(A):\n    \"\"\"\n    Compute D̃^(-1/2) Ã D̃^(-1/2) for GCN.\n    A: adjacency matrix [n, n] (can be torch.Tensor or scipy sparse)\n    \"\"\"\n    n = A.size(0)\n    # Add self-loops: Ã = A + I\n    A_tilde = A + torch.eye(n, device=A.device)\n    # Compute degree matrix D̃\n    D_tilde = A_tilde.sum(dim=1)  # [n] — row sums\n    # D̃^(-1/2) as a vector (we'll apply it as element-wise scaling)\n    D_inv_sqrt = D_tilde.pow(-0.5)\n    D_inv_sqrt[D_inv_sqrt == float('inf')] = 0  # handle isolated nodes\n    # Symmetric normalization: D̃^(-1/2) Ã D̃^(-1/2)\n    # Equivalent to: scale rows and columns by D_inv_sqrt\n    A_hat = D_inv_sqrt.unsqueeze(1) * A_tilde * D_inv_sqrt.unsqueeze(0)\n    return A_hat\n\n\nclass GCN(nn.Module):\n    def __init__(self, in_features, hidden_dim, num_classes, dropout=0.5):\n        super().__init__()\n        self.gcn1 = GCNLayer(in_features, hidden_dim)\n        self.gcn2 = GCNLayer(hidden_dim, num_classes)\n        self.dropout = nn.Dropout(dropout)\n\n    def forward(self, X, A):\n        A_hat = normalize_adjacency(A)\n        # Layer 1: GCN + ReLU + Dropout\n        h = F.relu(self.gcn1(X, A_hat))\n        h = self.dropout(h)\n        # Layer 2: GCN (no activation — logits for classification)\n        logits = self.gcn2(h, A_hat)\n        return logits  # [n_nodes, num_classes]\n\n\n# --- Test ---\nn_nodes, in_features, hidden, n_classes = 100, 16, 64, 7\nX = torch.randn(n_nodes, in_features)\n# Random graph (Erdős–Rényi)\nA = (torch.rand(n_nodes, n_nodes) &gt; 0.9).float()\nA = (A + A.T).clamp(0, 1)  # make symmetric (undirected)\n\nmodel = GCN(in_features, hidden, n_classes)\nlogits = model(X, A)\nprint(f\"Node logits shape: {logits.shape}\")  # [100, 7]\n\n\n\nGCN uses fixed structural weights (based on degree). But not all neighbors are equally important. GAT (Veličković et al., 2018) learns to weight neighbors dynamically — just like Transformer attention.\nFor each edge (i, j), GAT computes an attention coefficient:\neᵢⱼ = LeakyReLU( aᵀ [Whᵢ || Whⱼ] )\nαᵢⱼ = softmax_j(eᵢⱼ) = exp(eᵢⱼ) / Σ_{k∈N(i)} exp(eᵢₖ)\nThen aggregates:\nhᵢ' = σ( Σ_{j∈N(i)} αᵢⱼ · W·hⱼ )\nclass GATLayer(nn.Module):\n    def __init__(self, in_features, out_features, num_heads=1,\n                 dropout=0.6, alpha=0.2, concat=True):\n        \"\"\"\n        in_features:  Input feature dimension\n        out_features: Output feature dimension per head\n        num_heads:    Number of attention heads (multi-head GAT)\n        alpha:        Negative slope for LeakyReLU\n        concat:       If True, concatenate heads; if False, average them\n        \"\"\"\n        super().__init__()\n        self.num_heads = num_heads\n        self.out_features = out_features\n        self.concat = concat\n\n        # One weight matrix per head\n        self.W = nn.Parameter(torch.FloatTensor(num_heads, in_features, out_features))\n        # Attention vector per head: [2 * out_features] → scalar\n        self.a = nn.Parameter(torch.FloatTensor(num_heads, 2 * out_features))\n\n        self.leaky_relu = nn.LeakyReLU(alpha)\n        self.dropout = nn.Dropout(dropout)\n\n        # Initialize\n        nn.init.xavier_uniform_(self.W)\n        nn.init.xavier_uniform_(self.a.unsqueeze(0))\n\n    def forward(self, H, adj):\n        \"\"\"\n        H:   [n_nodes, in_features]\n        adj: [n_nodes, n_nodes] adjacency matrix (binary)\n        \"\"\"\n        n = H.size(0)\n        outputs = []\n\n        for k in range(self.num_heads):\n            # Linear transformation: [n, in_features] @ [in_features, out_features]\n            Wh = H @ self.W[k]  # [n, out_features]\n\n            # Compute attention scores for all pairs (i, j)\n            # Expand for broadcasting: Wh_i for source, Wh_j for target\n            Wh_repeat_i = Wh.unsqueeze(1).expand(n, n, -1)  # [n, n, out]\n            Wh_repeat_j = Wh.unsqueeze(0).expand(n, n, -1)  # [n, n, out]\n\n            # Concatenate along feature dim: [n, n, 2*out]\n            concat_features = torch.cat([Wh_repeat_i, Wh_repeat_j], dim=-1)\n\n            # Apply attention vector: [n, n, 2*out] @ [2*out] → [n, n]\n            e = self.leaky_relu((concat_features * self.a[k]).sum(dim=-1))\n\n            # Mask non-edges with -inf (so they get 0 after softmax)\n            mask = (adj == 0)\n            e = e.masked_fill(mask, float('-inf'))\n\n            # Softmax over neighbors\n            alpha = F.softmax(e, dim=1)    # [n, n]\n            alpha = self.dropout(alpha)\n\n            # Weighted aggregation\n            h_prime = alpha @ Wh           # [n, out_features]\n            outputs.append(h_prime)\n\n        if self.concat:\n            return F.elu(torch.cat(outputs, dim=-1))      # [n, heads*out]\n        else:\n            return F.elu(torch.stack(outputs, dim=0).mean(0))  # [n, out]\n\n\n\nPyTorch Geometric (PyG) provides an elegant base class for message passing. Let’s implement it from scratch to understand the mechanics:\n# Using PyTorch Geometric's MessagePassing base class\nfrom torch_geometric.nn import MessagePassing\nfrom torch_geometric.utils import add_self_loops, degree\n\nclass CustomGCNConv(MessagePassing):\n    \"\"\"\n    A from-scratch GCN convolution using PyG's MessagePassing framework.\n    aggr='add' means we sum incoming messages (GCN uses sum, not mean)\n    \"\"\"\n    def __init__(self, in_channels, out_channels):\n        super().__init__(aggr='add')\n        self.lin = nn.Linear(in_channels, out_channels, bias=False)\n\n    def forward(self, x, edge_index):\n        \"\"\"\n        x:          [n_nodes, in_channels]\n        edge_index: [2, n_edges] — COO format edges (source, target)\n        \"\"\"\n        # Step 1: Add self-loops\n        edge_index, _ = add_self_loops(edge_index, num_nodes=x.size(0))\n\n        # Step 2: Linear transform of node features\n        x = self.lin(x)   # [n_nodes, out_channels]\n\n        # Step 3: Compute degree-based normalization\n        row, col = edge_index\n        deg = degree(col, x.size(0), dtype=x.dtype)  # [n_nodes]\n        deg_inv_sqrt = deg.pow(-0.5)\n        norm = deg_inv_sqrt[row] * deg_inv_sqrt[col]  # per-edge norm factor\n\n        # Step 4: Propagate messages\n        # This calls message() → aggregate() → update() internally\n        return self.propagate(edge_index, x=x, norm=norm)\n\n    def message(self, x_j, norm):\n        \"\"\"\n        Called for each edge. x_j is the source node feature.\n        norm: per-edge normalization factor.\n        Returns the message from source node j to target node i.\n        \"\"\"\n        return norm.view(-1, 1) * x_j   # scale source features by norm\n\n    def update(self, aggr_out):\n        \"\"\"\n        Called after aggregation. aggr_out is the aggregated message.\n        We just return it here; in more complex models, apply an MLP.\n        \"\"\"\n        return aggr_out\n\n\n\nReal graphs change over time: friendships form and dissolve, transactions occur at specific times, proteins interact dynamically. Static GNNs cannot capture this temporal dimension. Two main paradigms handle it:\n\n\nThe graph is observed as a sequence of snapshots at discrete time steps: G₁, G₂, …, Gₜ. We apply a GNN to each snapshot and use an RNN/LSTM to model temporal evolution.\nimport torch\nimport torch.nn as nn\n\nclass TemporalGCN(nn.Module):\n    \"\"\"\n    For each time step, applies a GCN, then uses GRU to propagate temporal info.\n    Architecture: GCN(t) → GRU → node embeddings at time t\n    \"\"\"\n    def __init__(self, node_features, gcn_out, gru_hidden, num_classes):\n        super().__init__()\n        # Shared GCN applied at each time step\n        self.gcn = GCN(node_features, gcn_out, gcn_out)\n        # GRU takes GCN output at each step and updates temporal state\n        self.gru = nn.GRU(gcn_out, gru_hidden, batch_first=True)\n        # Classification head\n        self.classifier = nn.Linear(gru_hidden, num_classes)\n\n    def forward(self, X_sequence, A_sequence):\n        \"\"\"\n        X_sequence: List of T feature matrices, each [n_nodes, node_features]\n        A_sequence: List of T adjacency matrices, each [n_nodes, n_nodes]\n        Returns:    Node predictions at the final time step\n        \"\"\"\n        T = len(X_sequence)\n        n_nodes = X_sequence[0].size(0)\n\n        # Apply GCN at each snapshot → collect temporal node embeddings\n        gcn_outputs = []\n        for t in range(T):\n            h_t = self.gcn(X_sequence[t], A_sequence[t])   # [n_nodes, gcn_out]\n            gcn_outputs.append(h_t)\n\n        # Stack into sequence: [n_nodes, T, gcn_out]\n        # Each node has a sequence of GCN embeddings through time\n        node_sequences = torch.stack(gcn_outputs, dim=1)\n\n        # GRU across time for each node\n        gru_out, _ = self.gru(node_sequences)     # [n_nodes, T, gru_hidden]\n        final_embeddings = gru_out[:, -1, :]      # [n_nodes, gru_hidden]\n\n        return self.classifier(final_embeddings)  # [n_nodes, num_classes]\n\n\n\nInteractions happen at arbitrary continuous timestamps, not discrete snapshots. The graph evolves as a stream of (source, target, time, features) events. This calls for more sophisticated methods like:\nTemporal Point Processes: Model the time between events as a stochastic process.\nTGNN (Temporal Graph Neural Network): Maintain per-node memory states that are updated upon each interaction. The seminal model here is TGN (Temporal Graph Networks, Rossi et al., 2020).\nclass TGNMemory(nn.Module):\n    \"\"\"\n    Simplified TGN memory module.\n    Each node has a memory vector that gets updated when it interacts.\n    \"\"\"\n    def __init__(self, num_nodes, memory_dim, message_dim, time_dim=1):\n        super().__init__()\n        self.memory_dim = memory_dim\n        # Per-node memory: initialized to zeros\n        self.register_buffer('memory', torch.zeros(num_nodes, memory_dim))\n        # Time encoder: maps scalar time delta to a vector\n        self.time_encoder = nn.Linear(1, time_dim)\n        # Message function: [memory_src || memory_dst || time_enc || edge_feat] → message\n        self.message_fn = nn.Linear(2 * memory_dim + time_dim + message_dim, memory_dim)\n        # GRU cell to update memory\n        self.memory_updater = nn.GRUCell(memory_dim, memory_dim)\n\n    def get_memory(self, node_ids):\n        return self.memory[node_ids]\n\n    def update_memory(self, src_ids, dst_ids, timestamps, edge_features):\n        \"\"\"\n        Update memory for source and destination nodes after an interaction.\n        \"\"\"\n        src_mem = self.memory[src_ids]      # [batch, memory_dim]\n        dst_mem = self.memory[dst_ids]\n        time_enc = self.time_encoder(timestamps.unsqueeze(-1))  # [batch, time_dim]\n\n        # Compute messages for src and dst\n        msg_src = self.message_fn(\n            torch.cat([src_mem, dst_mem, time_enc, edge_features], dim=-1)\n        )\n        msg_dst = self.message_fn(\n            torch.cat([dst_mem, src_mem, time_enc, edge_features], dim=-1)\n        )\n\n        # Update memories with GRU\n        self.memory[src_ids] = self.memory_updater(msg_src, src_mem)\n        self.memory[dst_ids] = self.memory_updater(msg_dst, dst_mem)\n\n\n\n\nNode-level tasks output per-node predictions. For graph-level tasks (e.g., “Is this molecule toxic?”), we need to pool all node representations into a single graph embedding.\nGlobal Mean/Max/Sum Pooling: Simple aggregation.\nfrom torch_geometric.nn import global_mean_pool, global_max_pool\n\n# After computing node embeddings h:\ngraph_embedding = global_mean_pool(h, batch)  \n# batch: [n_nodes] tensor indicating which graph each node belongs to\nHierarchical Pooling (DiffPool): Learn to coarsen the graph level-by-level, similar to how CNNs downsample images.\n\n ## Chapter 3: Convolutional Neural Networks — Vision from First Principles\n\n\n\nConsider a 224×224 RGB image. As a flat vector, this has 224 × 224 × 3 = 150,528 dimensions. A single fully-connected layer mapping to 1000 hidden units would need 150,528 × 1,000 = 150 million parameters — and we’d have multiple such layers. This is computationally intractable and prone to catastrophic overfitting.\nWorse, fully-connected layers are not equivariant to translation: if a cat moves 10 pixels to the right, the network sees a completely different input and must learn the same cat detector all over again.\nCNNs solve both problems through: 1. Local connectivity: Each neuron only connects to a small spatial region (the receptive field) 2. Weight sharing: The same filter (set of weights) is applied at every position 3. Hierarchical features: Early layers detect edges, middle layers detect textures, deep layers detect objects\n\n\n\n\n\nA convolution slides a small filter (kernel) across the input, computing a dot product at each position. The result is a feature map indicating where and how strongly that filter’s pattern is present.\nFor a 2D input I and kernel K of size f×f, the output at position (i, j) is:\nOutput[i][j] = Σₘ Σₙ I[i+m][j+n] × K[m][n]\nIn CNNs, we have multiple input channels (e.g., 3 for RGB) and multiple output filters (e.g., 64). For input X of shape [C_in, H, W] and kernel K of shape [C_out, C_in, f, f]:\nOutput[c_out, i, j] = Σ_{c_in} Σₘ Σₙ X[c_in, i+m, j+n] × K[c_out, c_in, m, n]\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\n\ndef manual_conv2d(input, kernel, stride=1, padding=0):\n    \"\"\"\n    Manual implementation of 2D convolution to understand internals.\n    input:  [batch, C_in, H, W]\n    kernel: [C_out, C_in, kH, kW]\n    Returns: [batch, C_out, H_out, W_out]\n    \"\"\"\n    batch, C_in, H, W = input.shape\n    C_out, _, kH, kW = kernel.shape\n\n    # Apply padding\n    if padding &gt; 0:\n        input = F.pad(input, [padding]*4)  # pad all 4 sides\n\n    # Output spatial dimensions\n    H_out = (H + 2*padding - kH) // stride + 1\n    W_out = (W + 2*padding - kW) // stride + 1\n\n    output = torch.zeros(batch, C_out, H_out, W_out)\n\n    for b in range(batch):\n        for c_out in range(C_out):\n            for i in range(H_out):\n                for j in range(W_out):\n                    h_start = i * stride\n                    w_start = j * stride\n                    # Extract patch\n                    patch = input[b, :, h_start:h_start+kH, w_start:w_start+kW]\n                    # Element-wise multiply and sum (dot product)\n                    output[b, c_out, i, j] = (patch * kernel[c_out]).sum()\n\n    return output\n\n\n# Verify against PyTorch's F.conv2d\nx = torch.randn(1, 1, 5, 5)\nk = torch.randn(1, 1, 3, 3)\nour_result = manual_conv2d(x, k)\npytorch_result = F.conv2d(x, k)\nprint(f\"Max difference: {(our_result - pytorch_result).abs().max():.6f}\")  # ~0\n\n\n\nWithout padding, convolutions shrink the spatial dimensions: an n×n input with an f×f kernel produces (n-f+1)×(n-f+1) output. Deep networks would rapidly collapse to size 1.\nValid padding (padding=0): No padding, output is smaller than input. Same padding (padding=(f-1)/2): Pad so output is same size as input.\nWith f=3, padding=1: H_out = H_in\nWith f=5, padding=2: H_out = H_in\nGeneral rule: padding = (f-1) / 2 for odd kernel sizes\n\n\n\nStride controls how many pixels the filter jumps at each step. stride=1 moves one pixel at a time (default). stride=2 moves two pixels, halving the output size.\nH_out = floor((H_in + 2*padding - f) / stride) + 1\nStride &gt; 1 is an alternative to pooling for downsampling — it’s differentiable and can be learned.\n\n\n\nDilation inserts spaces between kernel elements, effectively increasing the receptive field without adding parameters and without losing spatial resolution.\nA 3×3 kernel with dilation=2 has the same number of parameters but sees a 5×5 area (with gaps). Dilation=1 is standard convolution.\nEffective kernel size = f + (f-1)(d-1) = d*(f-1) + 1\nFor f=3, d=2: effective size = 5\nFor f=3, d=4: effective size = 9\nDilated convolutions are widely used in semantic segmentation (DeepLab) and audio generation (WaveNet).\nclass ConvBlock(nn.Module):\n    \"\"\"\n    A comprehensive conv block showcasing padding, stride, and dilation.\n    \"\"\"\n    def __init__(self, in_channels, out_channels, kernel_size=3,\n                 stride=1, padding=1, dilation=1):\n        super().__init__()\n        # When dilation &gt; 1, adjust padding to keep output size stable:\n        # effective_padding = dilation * (kernel_size - 1) / 2\n        self.conv = nn.Conv2d(\n            in_channels, out_channels,\n            kernel_size=kernel_size,\n            stride=stride,\n            padding=padding,\n            dilation=dilation,\n            bias=False   # Usually no bias when using BatchNorm\n        )\n        self.bn = nn.BatchNorm2d(out_channels)\n        self.relu = nn.ReLU(inplace=True)\n\n    def forward(self, x):\n        return self.relu(self.bn(self.conv(x)))\n\n\n# Demonstrate different convolution configurations\nx = torch.randn(1, 32, 56, 56)\n\n# Standard conv: 56 → 56 (same padding)\nstd_conv = ConvBlock(32, 64, kernel_size=3, stride=1, padding=1, dilation=1)\nprint(std_conv(x).shape)    # [1, 64, 56, 56]\n\n# Strided conv: 56 → 28 (downsampling by 2)\nstrided = ConvBlock(32, 64, kernel_size=3, stride=2, padding=1, dilation=1)\nprint(strided(x).shape)     # [1, 64, 28, 28]\n\n# Dilated conv: 56 → 56 (same size, larger receptive field)\ndilated = ConvBlock(32, 64, kernel_size=3, stride=1, padding=2, dilation=2)\nprint(dilated(x).shape)     # [1, 64, 56, 56]\n\n\n\n\nPooling reduces spatial dimensions, making representations more compact and somewhat translation-invariant.\nMax Pooling: Takes the maximum value in each pooling window. Picks the most active feature. Most commonly used.\nAverage Pooling: Takes the mean. Smoother but less sharp.\nGlobal Average Pooling (GAP): Averages across all spatial positions, producing a [batch, channels] tensor from [batch, channels, H, W]. Used at the end of modern CNNs instead of fully-connected layers — drastically reduces parameters and acts as regularization.\nclass PoolingDemo(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.max_pool = nn.MaxPool2d(kernel_size=2, stride=2)     # H,W → H/2, W/2\n        self.avg_pool = nn.AvgPool2d(kernel_size=2, stride=2)\n        self.global_avg = nn.AdaptiveAvgPool2d((1, 1))           # H,W → 1, 1\n\n    def forward(self, x):\n        print(f\"Input:      {x.shape}\")\n        print(f\"MaxPool:    {self.max_pool(x).shape}\")\n        print(f\"AvgPool:    {self.avg_pool(x).shape}\")\n        g = self.global_avg(x)\n        print(f\"GlobalAvg:  {g.shape}\")\n        print(f\"Flattened:  {g.flatten(1).shape}\")\n        return g.flatten(1)\n\ndemo = PoolingDemo()\nx = torch.randn(4, 64, 28, 28)\nout = demo(x)\n\n\n\nLet us build a modern, fully-functional CNN for image classification, following the design principles of ResNet.\n\n\nBefore ResNet (2015), training very deep networks (&gt;20 layers) was notoriously difficult. Gradients exponentially shrink as they propagate back through many layers, making early layers receive near-zero gradients and thus barely learning.\n\n\n\nHe et al. (2015) introduced the residual block:\noutput = F(x, {Wᵢ}) + x\nInstead of learning the desired mapping H(x), the network learns the residual F(x) = H(x) - x. The identity shortcut + x creates a gradient highway: during backprop, the gradient can flow directly through the identity connection, bypassing the conv layers.\nclass ResidualBlock(nn.Module):\n    \"\"\"\n    Standard ResNet residual block.\n    If in_channels != out_channels, uses a 1×1 conv as shortcut projection.\n    \"\"\"\n    expansion = 1  # Basic block doesn't expand channels\n\n    def __init__(self, in_channels, out_channels, stride=1):\n        super().__init__()\n\n        # Main path: 3×3 conv → BN → ReLU → 3×3 conv → BN\n        self.conv1 = nn.Conv2d(in_channels, out_channels,\n                               kernel_size=3, stride=stride, padding=1, bias=False)\n        self.bn1 = nn.BatchNorm2d(out_channels)\n\n        self.conv2 = nn.Conv2d(out_channels, out_channels,\n                               kernel_size=3, stride=1, padding=1, bias=False)\n        self.bn2 = nn.BatchNorm2d(out_channels)\n\n        self.relu = nn.ReLU(inplace=True)\n\n        # Shortcut: if dimensions change, project to match\n        self.shortcut = nn.Sequential()\n        if stride != 1 or in_channels != out_channels:\n            self.shortcut = nn.Sequential(\n                nn.Conv2d(in_channels, out_channels,\n                          kernel_size=1, stride=stride, bias=False),\n                nn.BatchNorm2d(out_channels)\n            )\n\n    def forward(self, x):\n        # Main path\n        residual = self.shortcut(x)    # Save for skip connection (may be projected)\n\n        out = self.relu(self.bn1(self.conv1(x)))\n        out = self.bn2(self.conv2(out))  # No ReLU yet — add residual first\n\n        out += residual                # Residual addition\n        out = self.relu(out)           # ReLU after addition\n\n        return out\n\n\nclass ResNet(nn.Module):\n    \"\"\"\n    Simplified ResNet for CIFAR-10 (32×32 images, 10 classes).\n    Adapted from ResNet architecture, scaled for small images.\n    \"\"\"\n    def __init__(self, num_classes=10):\n        super().__init__()\n\n        # Initial feature extraction\n        self.stem = nn.Sequential(\n            nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False),\n            nn.BatchNorm2d(64),\n            nn.ReLU(inplace=True)\n        )\n        # Note: No max pooling here (images are 32×32, not 224×224)\n\n        # Residual stages\n        self.stage1 = self._make_stage(64, 64, num_blocks=2, stride=1)\n        self.stage2 = self._make_stage(64, 128, num_blocks=2, stride=2)\n        self.stage3 = self._make_stage(128, 256, num_blocks=2, stride=2)\n        self.stage4 = self._make_stage(256, 512, num_blocks=2, stride=2)\n\n        # Global average pooling + classifier\n        self.global_pool = nn.AdaptiveAvgPool2d((1, 1))\n        self.classifier = nn.Linear(512, num_classes)\n\n        # Weight initialization\n        self._init_weights()\n\n    def _make_stage(self, in_channels, out_channels, num_blocks, stride):\n        \"\"\"Create a stage: first block may downsample, rest maintain size.\"\"\"\n        blocks = [ResidualBlock(in_channels, out_channels, stride=stride)]\n        for _ in range(1, num_blocks):\n            blocks.append(ResidualBlock(out_channels, out_channels, stride=1))\n        return nn.Sequential(*blocks)\n\n    def _init_weights(self):\n        for m in self.modules():\n            if isinstance(m, nn.Conv2d):\n                # He initialization: accounts for ReLU non-linearity\n                nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')\n            elif isinstance(m, nn.BatchNorm2d):\n                nn.init.constant_(m.weight, 1)\n                nn.init.constant_(m.bias, 0)\n\n    def forward(self, x):\n        # x: [batch, 3, 32, 32]\n        x = self.stem(x)           # [batch, 64, 32, 32]\n        x = self.stage1(x)         # [batch, 64, 32, 32]\n        x = self.stage2(x)         # [batch, 128, 16, 16]\n        x = self.stage3(x)         # [batch, 256, 8, 8]\n        x = self.stage4(x)         # [batch, 512, 4, 4]\n        x = self.global_pool(x)    # [batch, 512, 1, 1]\n        x = x.flatten(1)           # [batch, 512]\n        return self.classifier(x)  # [batch, num_classes]\n\n\n# --- Full training loop ---\ndef train_cnn(model, train_loader, num_epochs=10, lr=0.1):\n    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\n    model = model.to(device)\n\n    # SGD with momentum and weight decay — the classic combo for CNNs\n    optimizer = torch.optim.SGD(\n        model.parameters(), lr=lr,\n        momentum=0.9, weight_decay=1e-4, nesterov=True\n    )\n    # Cosine annealing: smoothly decay LR to 0\n    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=num_epochs)\n    criterion = nn.CrossEntropyLoss(label_smoothing=0.1)  # Label smoothing regularization\n\n    for epoch in range(num_epochs):\n        model.train()\n        total_loss, correct, total = 0, 0, 0\n\n        for images, labels in train_loader:\n            images, labels = images.to(device), labels.to(device)\n            optimizer.zero_grad()\n            logits = model(images)\n            loss = criterion(logits, labels)\n            loss.backward()\n            # Gradient clipping: prevents exploding gradients\n            torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n            optimizer.step()\n\n            total_loss += loss.item()\n            correct += (logits.argmax(1) == labels).sum().item()\n            total += labels.size(0)\n\n        scheduler.step()\n        acc = 100 * correct / total\n        print(f\"Epoch {epoch+1}/{num_epochs} | Loss: {total_loss/len(train_loader):.3f} | Acc: {acc:.2f}%\")\n\n    return model\n\n\n# Quick architecture check\nmodel = ResNet(num_classes=10)\nx = torch.randn(4, 3, 32, 32)\nout = model(x)\nprint(f\"Output shape: {out.shape}\")   # [4, 10]\nparam_count = sum(p.numel() for p in model.parameters())\nprint(f\"Parameters: {param_count:,}\")\n\n\n\n\nBatch Normalization (Ioffe & Szegedy, 2015) normalizes the activations within a mini-batch:\nx̂ᵢ = (xᵢ - μ_B) / √(σ²_B + ε)\nyᵢ = γ·x̂ᵢ + β\nWhere μ_B and σ²_B are the batch mean and variance, ε is a small constant for numerical stability, and γ, β are learnable scale and shift parameters.\nWhy it helps: 1. Reduces internal covariate shift — the distribution of activations shifts as earlier layers update, forcing later layers to constantly re-adapt. BN stabilizes these distributions. 2. Acts as regularization (reduces need for dropout) 3. Allows higher learning rates 4. Reduces sensitivity to initialization\nDuring inference, batch statistics are replaced by running averages computed during training (model.eval() mode in PyTorch).\n\n\n\n\n\n\n\n\n\n\n\nPrinciple\nImplementation\nWhy\n\n\n\n\nResidual connections\nout = F(x) + x\nSolves vanishing gradients\n\n\nBatchNorm before activation\nBN → ReLU\nStabilizes training\n\n\nGlobal average pooling\nAdaptiveAvgPool2d(1,1)\nReduces parameters, regularizes\n\n\nNo bias with BatchNorm\nbias=False\nBN subsumes bias\n\n\nHe initialization\nkaiming_normal_\nCorrect variance for ReLU\n\n\nLabel smoothing\nCrossEntropyLoss(label_smoothing=0.1)\nPrevents overconfident predictions\n\n\nWeight decay\nSGD with weight_decay=1e-4\nL2 regularization\n\n\n\n\n ## Chapter 4: Recurrent Neural Networks — Sequential Memory\n\n\n\nMany problems are inherently sequential: speech recognition (audio frames over time), text generation (tokens in order), time-series forecasting (measurements at timesteps). Unlike CNNs processing fixed-size windows, RNNs maintain a hidden state — a summary of everything they’ve seen so far.\n\n\n\nThe simplest RNN updates its hidden state at each step:\nhₜ = tanh(Wₕₕ·hₜ₋₁ + Wₓₕ·xₜ + b)\nyₜ = Wₕᵧ·hₜ + bᵧ\nThe same weight matrices Wₕₕ, Wₓₕ, Wₕᵧ are applied at every timestep — this is weight sharing across time.\nThe Problem: Vanilla RNNs cannot learn long-range dependencies. Backpropagation through time (BPTT) requires multiplying gradients through hundreds of timesteps. If |Wₕₕ| &lt; 1, gradients vanish exponentially. If |Wₕₕ| &gt; 1, gradients explode.\n\n\n\nThe LSTM (Hochreiter & Schmidhuber, 1997) solves the vanishing gradient problem with a revolutionary idea: a cell state cₜ that can carry information over long distances with minimal transformation.\nThe cell state is modified by three gates — learned sigmoid-activated filters that decide what to forget, write, and output.\n\n\nForget Gate: How much of the old cell state to keep?\nfₜ = σ(Wf·[hₜ₋₁, xₜ] + bf)\nInput Gate: What new information to write to the cell state?\niₜ = σ(Wᵢ·[hₜ₋₁, xₜ] + bᵢ)\nc̃ₜ = tanh(Wc·[hₜ₋₁, xₜ] + bc)   # Candidate cell values\nCell State Update:\ncₜ = fₜ ⊙ cₜ₋₁ + iₜ ⊙ c̃ₜ\n⊙ is element-wise multiplication. The forget gate erases old info; the input gate writes new info.\nOutput Gate: What to expose as hidden state?\noₜ = σ(Wo·[hₜ₋₁, xₜ] + bo)\nhₜ = oₜ ⊙ tanh(cₜ)\nThe key insight: gradients can flow through cₜ via the addition operation (gradient of addition is 1), not multiplication — so they don’t vanish! The forget gate can learn to be near 1 for important information, creating near-perfect gradient highways across time.\n\n\n\nclass LSTMCell(nn.Module):\n    \"\"\"\n    Single LSTM cell — processes one timestep.\n    Implementing from scratch to understand all gate mechanics.\n    \"\"\"\n    def __init__(self, input_size, hidden_size):\n        super().__init__()\n        self.hidden_size = hidden_size\n\n        # All four gates computed in one efficient matrix operation\n        # Output size is 4 * hidden_size: [f_gate | i_gate | c_candidate | o_gate]\n        self.W_ih = nn.Linear(input_size, 4 * hidden_size)   # Input → gates\n        self.W_hh = nn.Linear(hidden_size, 4 * hidden_size)  # Hidden → gates\n\n        self._init_weights()\n\n    def _init_weights(self):\n        # Orthogonal initialization for hidden-to-hidden weights\n        nn.init.orthogonal_(self.W_hh.weight)\n        # Forget gate bias initialized to 1 — makes it remember by default\n        nn.init.constant_(self.W_ih.bias[self.hidden_size:2*self.hidden_size], 1.0)\n        nn.init.constant_(self.W_hh.bias[self.hidden_size:2*self.hidden_size], 1.0)\n\n    def forward(self, x, h_prev, c_prev):\n        \"\"\"\n        x:      [batch, input_size]   — current input\n        h_prev: [batch, hidden_size]  — previous hidden state\n        c_prev: [batch, hidden_size]  — previous cell state\n\n        Returns:\n        h_next: [batch, hidden_size]\n        c_next: [batch, hidden_size]\n        \"\"\"\n        # Compute all gates in one pass (4x efficient vs. separate matrices)\n        gates = self.W_ih(x) + self.W_hh(h_prev)  # [batch, 4*hidden]\n\n        # Split into 4 equal parts\n        f_gate, i_gate, c_cand, o_gate = gates.chunk(4, dim=1)\n        # Each: [batch, hidden_size]\n\n        # Apply gate activations\n        f = torch.sigmoid(f_gate)    # Forget: 0=forget, 1=keep\n        i = torch.sigmoid(i_gate)    # Input: 0=ignore, 1=write\n        g = torch.tanh(c_cand)       # Candidate values: [-1, 1]\n        o = torch.sigmoid(o_gate)    # Output: 0=hide, 1=expose\n\n        # Update cell state: forget old + write new\n        c_next = f * c_prev + i * g\n\n        # Compute hidden state: gated tanh of cell state\n        h_next = o * torch.tanh(c_next)\n\n        return h_next, c_next\n\n\nclass LSTM(nn.Module):\n    \"\"\"\n    Multi-layer LSTM that processes full sequences.\n    \"\"\"\n    def __init__(self, input_size, hidden_size, num_layers=1,\n                 dropout=0.0, bidirectional=False):\n        super().__init__()\n        self.hidden_size = hidden_size\n        self.num_layers = num_layers\n        self.bidirectional = bidirectional\n        self.num_directions = 2 if bidirectional else 1\n\n        # Create a cell for each layer and direction\n        self.cells = nn.ModuleList()\n        for layer in range(num_layers):\n            for direction in range(self.num_directions):\n                in_size = input_size if layer == 0 else hidden_size * self.num_directions\n                self.cells.append(LSTMCell(in_size, hidden_size))\n\n        self.dropout = nn.Dropout(dropout) if dropout &gt; 0 else nn.Identity()\n\n    def forward(self, x, initial_state=None):\n        \"\"\"\n        x: [batch, seq_len, input_size]\n        Returns:\n            output: [batch, seq_len, hidden_size * num_directions]\n            (h_n, c_n): final states [num_layers*directions, batch, hidden_size]\n        \"\"\"\n        batch_size, seq_len, _ = x.shape\n        device = x.device\n\n        # Initialize hidden and cell states to zeros\n        if initial_state is None:\n            h = torch.zeros(self.num_layers * self.num_directions,\n                            batch_size, self.hidden_size, device=device)\n            c = torch.zeros_like(h)\n        else:\n            h, c = initial_state\n\n        layer_input = x  # [batch, seq, input_size]\n\n        h_list, c_list = [], []\n\n        for layer_idx in range(self.num_layers):\n            # Forward direction\n            fwd_cell = self.cells[layer_idx * self.num_directions]\n            fwd_h = h[layer_idx * self.num_directions]\n            fwd_c = c[layer_idx * self.num_directions]\n\n            fwd_outputs = []\n            for t in range(seq_len):\n                fwd_h, fwd_c = fwd_cell(layer_input[:, t, :], fwd_h, fwd_c)\n                fwd_outputs.append(fwd_h)\n\n            fwd_seq = torch.stack(fwd_outputs, dim=1)  # [batch, seq, hidden]\n\n            if self.bidirectional:\n                # Backward direction: process sequence in reverse\n                bwd_cell = self.cells[layer_idx * 2 + 1]\n                bwd_h = h[layer_idx * 2 + 1]\n                bwd_c = c[layer_idx * 2 + 1]\n\n                bwd_outputs = []\n                for t in reversed(range(seq_len)):\n                    bwd_h, bwd_c = bwd_cell(layer_input[:, t, :], bwd_h, bwd_c)\n                    bwd_outputs.append(bwd_h)\n                bwd_outputs.reverse()  # Reorder to match forward direction\n                bwd_seq = torch.stack(bwd_outputs, dim=1)\n\n                layer_output = torch.cat([fwd_seq, bwd_seq], dim=-1)\n                h_list.extend([fwd_h, bwd_h])\n                c_list.extend([fwd_c, bwd_c])\n            else:\n                layer_output = fwd_seq\n                h_list.append(fwd_h)\n                c_list.append(fwd_c)\n\n            # Apply dropout between layers (not on final layer)\n            if layer_idx &lt; self.num_layers - 1:\n                layer_input = self.dropout(layer_output)\n            else:\n                layer_input = layer_output\n\n        h_n = torch.stack(h_list, dim=0)   # [num_layers*directions, batch, hidden]\n        c_n = torch.stack(c_list, dim=0)\n\n        return layer_output, (h_n, c_n)\n\n\n# --- Test ---\nlstm = LSTM(input_size=10, hidden_size=32, num_layers=2, dropout=0.2, bidirectional=True)\nx = torch.randn(4, 20, 10)   # [batch=4, seq=20, input=10]\noutput, (h_n, c_n) = lstm(x)\nprint(f\"Output shape:   {output.shape}\")   # [4, 20, 64] (32*2 bidirectional)\nprint(f\"Final h shape:  {h_n.shape}\")      # [4, 4, 32]  (2 layers * 2 directions)\n\n\n\nLSTMs shine on problems requiring long-range memory. Key applications:\nLanguage Modeling: Predict the next token given all previous tokens.\nclass LSTMLanguageModel(nn.Module):\n    def __init__(self, vocab_size, embed_dim, hidden_size, num_layers, dropout=0.5):\n        super().__init__()\n        self.embedding = nn.Embedding(vocab_size, embed_dim)\n        self.lstm = nn.LSTM(embed_dim, hidden_size, num_layers,\n                            dropout=dropout, batch_first=True)\n        self.drop = nn.Dropout(dropout)\n        self.decoder = nn.Linear(hidden_size, vocab_size)\n        # Tie weights: embedding and decoder share parameters\n        # This is a critical regularization technique for language models\n        self.decoder.weight = self.embedding.weight\n\n    def forward(self, x, hidden=None):\n        \"\"\"\n        x:      [batch, seq_len] — token indices\n        Returns logits [batch, seq_len, vocab_size] and new hidden state\n        \"\"\"\n        emb = self.drop(self.embedding(x))           # [batch, seq, embed_dim]\n        lstm_out, hidden = self.lstm(emb, hidden)    # [batch, seq, hidden]\n        lstm_out = self.drop(lstm_out)\n        logits = self.decoder(lstm_out)              # [batch, seq, vocab_size]\n        return logits, hidden\n\n    def detach_hidden(self, hidden):\n        \"\"\"Detach hidden state to prevent backprop through entire history.\"\"\"\n        return tuple(h.detach() for h in hidden)\n\n\n\n\nThe GRU (Cho et al., 2014) is a simplification of the LSTM. It merges the forget and input gates into a single update gate and eliminates the separate cell state.\nUpdate Gate: Controls how much of the previous hidden state to keep.\nzₜ = σ(Wz·[hₜ₋₁, xₜ])\nReset Gate: Controls how much of the previous hidden state influences the candidate.\nrₜ = σ(Wr·[hₜ₋₁, xₜ])\nCandidate Hidden State:\nh̃ₜ = tanh(W·[rₜ⊙hₜ₋₁, xₜ])\nFinal Update:\nhₜ = (1 - zₜ) ⊙ hₜ₋₁ + zₜ ⊙ h̃ₜ\nWhen zₜ ≈ 0: the hidden state is almost unchanged (the unit forgets nothing — pure memory). When zₜ ≈ 1: the hidden state is completely replaced by the new candidate.\nclass GRUCell(nn.Module):\n    \"\"\"\n    Single GRU cell. Fewer parameters than LSTM, often comparable performance.\n    \"\"\"\n    def __init__(self, input_size, hidden_size):\n        super().__init__()\n        self.hidden_size = hidden_size\n\n        # Update and reset gates\n        self.W_z = nn.Linear(input_size + hidden_size, hidden_size)\n        self.W_r = nn.Linear(input_size + hidden_size, hidden_size)\n        # Candidate hidden state\n        self.W_h = nn.Linear(input_size + hidden_size, hidden_size)\n\n    def forward(self, x, h_prev):\n        \"\"\"\n        x:      [batch, input_size]\n        h_prev: [batch, hidden_size]\n        Returns: h_next [batch, hidden_size]\n        \"\"\"\n        combined = torch.cat([x, h_prev], dim=1)   # [batch, input+hidden]\n\n        # Compute gates\n        z = torch.sigmoid(self.W_z(combined))      # Update gate\n        r = torch.sigmoid(self.W_r(combined))      # Reset gate\n\n        # Candidate: reset gate modulates how much past state to use\n        combined_r = torch.cat([x, r * h_prev], dim=1)\n        h_candidate = torch.tanh(self.W_h(combined_r))\n\n        # Final hidden state: interpolate between old and new\n        h_next = (1 - z) * h_prev + z * h_candidate\n\n        return h_next\n\n\nclass GRUSequenceModel(nn.Module):\n    \"\"\"\n    GRU-based model for sequence classification (e.g., sentiment analysis).\n    \"\"\"\n    def __init__(self, vocab_size, embed_dim, hidden_size,\n                 num_layers, num_classes, dropout=0.3):\n        super().__init__()\n        self.embedding = nn.Embedding(vocab_size, embed_dim, padding_idx=0)\n        self.gru = nn.GRU(embed_dim, hidden_size, num_layers,\n                          batch_first=True, dropout=dropout,\n                          bidirectional=True)\n        # Attention over all hidden states (instead of just final state)\n        self.attention = nn.Linear(hidden_size * 2, 1)\n        self.classifier = nn.Sequential(\n            nn.Dropout(dropout),\n            nn.Linear(hidden_size * 2, hidden_size),\n            nn.ReLU(),\n            nn.Dropout(dropout),\n            nn.Linear(hidden_size, num_classes)\n        )\n\n    def forward(self, x, lengths=None):\n        \"\"\"\n        x:       [batch, seq_len] — padded token ids\n        lengths: [batch] — actual sequence lengths (for packing)\n        \"\"\"\n        emb = self.embedding(x)           # [batch, seq, embed]\n\n        # Pack padded sequences for efficiency (skip padding in GRU)\n        if lengths is not None:\n            emb = nn.utils.rnn.pack_padded_sequence(\n                emb, lengths.cpu(), batch_first=True, enforce_sorted=False\n            )\n\n        gru_out, _ = self.gru(emb)        # [batch, seq, 2*hidden]\n\n        if lengths is not None:\n            gru_out, _ = nn.utils.rnn.pad_packed_sequence(gru_out, batch_first=True)\n\n        # Attention mechanism over sequence dimension\n        attn_scores = self.attention(gru_out).squeeze(-1)  # [batch, seq]\n        if lengths is not None:\n            # Mask padding positions\n            for i, l in enumerate(lengths):\n                attn_scores[i, l:] = float('-inf')\n        attn_weights = F.softmax(attn_scores, dim=1).unsqueeze(-1)  # [batch, seq, 1]\n        context = (gru_out * attn_weights).sum(dim=1)               # [batch, 2*hidden]\n\n        return self.classifier(context)   # [batch, num_classes]\n\n\n\n\n\n\n\n\n\n\n\nFactor\nLSTM\nGRU\n\n\n\n\nParameters\nMore (4 gate matrices)\nFewer (3 gate matrices)\n\n\nLong sequences\nSlightly better (separate cell state)\nClose, often comparable\n\n\nSmall datasets\nMay overfit more\nBetter generalization\n\n\nTraining speed\nSlower\n~30% faster\n\n\nMemory\nHigher\nLower\n\n\nBest for\nLanguage modeling, complex sequences\nShorter sequences, resource-constrained settings\n\n\n\n\n ## Chapter 5: Generative Adversarial Networks — Learning to Create\n\n\n\nA GAN (Goodfellow et al., 2014) involves two networks locked in a competitive game:\n\nThe Generator (G): A forger who creates fake artwork. It takes random noise z as input and outputs synthetic data (images, audio, text).\nThe Discriminator (D): An art critic who tries to distinguish real artwork from the forger’s fakes. It takes an input sample and outputs a probability that it’s real.\n\nThey are trained adversarially: - G tries to fool D by generating more realistic fakes - D tries to get better at detecting fakes\nThe remarkable result: through this adversarial pressure, G learns to generate incredibly realistic data — without ever directly seeing the training data distribution. G only knows whether D was fooled.\n\n\n\nThe GAN minimax game is formalized as:\nmin_G max_D V(D, G) = E_{x~p_data}[log D(x)] + E_{z~p_z}[log(1 - D(G(z)))]\nBreaking this down: - E_{x~p_data}[log D(x)]: Discriminator should output high probability (close to 1) for real data x. log D(x) is maximized when D(x) → 1. - E_{z~p_z}[log(1 - D(G(z)))]: Discriminator should output low probability for fake data G(z). log(1 - D(G(z))) is maximized when D(G(z)) → 0.\nDiscriminator wants to maximize this (correctly classify real and fake). Generator wants to minimize this (fool the discriminator into classifying fakes as real).\nThe non-saturating trick: In practice, instead of minimizing log(1 - D(G(z))), the generator minimizes -log(D(G(z))) (equivalent to maximizing log(D(G(z)))). This provides stronger gradients early in training when the generator is terrible and the discriminator easily rejects its outputs.\n\n\n\n\n\nDCGAN (Radford et al., 2015) established the architectural guidelines that made GANs stable to train:\nGenerator (Decoder-like): - Takes latent vector z and upsamples to image resolution - Uses transposed convolutions (fractionally strided convolutions) for upsampling - BatchNorm on all layers except output - ReLU activations, tanh on output layer\nDiscriminator (Encoder-like): - Takes image and downsamples to scalar - Uses strided convolutions instead of pooling - BatchNorm on all layers except input - LeakyReLU activations, sigmoid on output\nclass DCGANGenerator(nn.Module):\n    \"\"\"\n    Generator for 64×64 images.\n    Architecture: z [100] → linear → reshape → 4 transposed conv blocks → 64×64 RGB\n    \"\"\"\n    def __init__(self, latent_dim=100, img_channels=3, feature_maps=64):\n        super().__init__()\n        self.latent_dim = latent_dim\n        ngf = feature_maps  # Base number of feature maps\n\n        # Project and reshape noise vector\n        self.project = nn.Sequential(\n            nn.Linear(latent_dim, ngf * 8 * 4 * 4),\n            nn.Unflatten(1, (ngf * 8, 4, 4))\n        )\n        # Note: Unflatten reshapes to [batch, ngf*8, 4, 4]\n\n        # Transposed convolution blocks: each doubles spatial size\n        # 4×4 → 8×8 → 16×16 → 32×32 → 64×64\n        self.blocks = nn.Sequential(\n            # Block 1: 4×4 → 8×8\n            self._tconv_block(ngf * 8, ngf * 4),\n            # Block 2: 8×8 → 16×16\n            self._tconv_block(ngf * 4, ngf * 2),\n            # Block 3: 16×16 → 32×32\n            self._tconv_block(ngf * 2, ngf),\n            # Block 4: 32×32 → 64×64 (output layer — no BN, tanh activation)\n            nn.ConvTranspose2d(ngf, img_channels,\n                               kernel_size=4, stride=2, padding=1, bias=False),\n            nn.Tanh()  # Output in [-1, 1] (match normalized images)\n        )\n\n    def _tconv_block(self, in_ch, out_ch):\n        \"\"\"Transposed conv block: upsample by 2×.\"\"\"\n        return nn.Sequential(\n            nn.ConvTranspose2d(in_ch, out_ch,\n                               kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(out_ch),\n            nn.ReLU(inplace=True)\n        )\n\n    def forward(self, z):\n        \"\"\"z: [batch, latent_dim]\"\"\"\n        x = self.project(z)    # [batch, ngf*8, 4, 4]\n        return self.blocks(x)  # [batch, 3, 64, 64]\n\n\nclass DCGANDiscriminator(nn.Module):\n    \"\"\"\n    Discriminator for 64×64 images.\n    Architecture: 64×64 RGB → 4 conv blocks → linear → scalar\n    \"\"\"\n    def __init__(self, img_channels=3, feature_maps=64):\n        super().__init__()\n        ndf = feature_maps\n        # No BatchNorm on first layer (input is raw pixels)\n        self.model = nn.Sequential(\n            # Block 1: 64×64 → 32×32\n            nn.Conv2d(img_channels, ndf, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.LeakyReLU(0.2, inplace=True),  # LeakyReLU prevents dead neurons\n\n            # Block 2: 32×32 → 16×16\n            nn.Conv2d(ndf, ndf * 2, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 2),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Block 3: 16×16 → 8×8\n            nn.Conv2d(ndf * 2, ndf * 4, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 4),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Block 4: 8×8 → 4×4\n            nn.Conv2d(ndf * 4, ndf * 8, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(ndf * 8),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            # Output: 4×4 → 1×1 → scalar\n            nn.Conv2d(ndf * 8, 1, kernel_size=4, stride=1, padding=0, bias=False),\n            nn.Flatten(),\n            nn.Sigmoid()  # Output probability: real (1) or fake (0)\n        )\n\n    def forward(self, x):\n        \"\"\"x: [batch, 3, 64, 64]\"\"\"\n        return self.model(x).squeeze()  # [batch]\n\n\n\n\nGAN training is a two-player game — we alternate between optimizing the discriminator and the generator.\ndef weights_init(m):\n    \"\"\"DCGAN weight initialization: Conv weights from N(0, 0.02)\"\"\"\n    classname = m.__class__.__name__\n    if classname.find('Conv') != -1:\n        nn.init.normal_(m.weight.data, 0.0, 0.02)\n    elif classname.find('BatchNorm') != -1:\n        nn.init.normal_(m.weight.data, 1.0, 0.02)\n        nn.init.constant_(m.bias.data, 0)\n\n\ndef train_gan(\n    generator, discriminator, dataloader,\n    latent_dim=100, num_epochs=200, lr=0.0002,\n    beta1=0.5,  # Adam beta1: lower than default for GANs\n    device='cuda'\n):\n    \"\"\"\n    Full DCGAN training loop with all the stability tricks.\n    \"\"\"\n    generator = generator.to(device)\n    discriminator = discriminator.to(device)\n\n    # Apply DCGAN weight initialization\n    generator.apply(weights_init)\n    discriminator.apply(weights_init)\n\n    # Binary cross-entropy loss for adversarial training\n    criterion = nn.BCELoss()\n\n    # Separate optimizers for G and D\n    # Key: lower beta1=0.5 (instead of default 0.9) reduces momentum, helps stability\n    opt_G = torch.optim.Adam(generator.parameters(),\n                             lr=lr, betas=(beta1, 0.999))\n    opt_D = torch.optim.Adam(discriminator.parameters(),\n                             lr=lr, betas=(beta1, 0.999))\n\n    # Fixed noise for monitoring training progress\n    fixed_noise = torch.randn(64, latent_dim, device=device)\n\n    # Label conventions\n    real_label = 1.0\n    fake_label = 0.0\n\n    # Tracking metrics\n    G_losses, D_losses = [], []\n\n    print(\"Starting GAN Training...\")\n    for epoch in range(num_epochs):\n        for batch_idx, (real_images, _) in enumerate(dataloader):\n            batch_size = real_images.size(0)\n            real_images = real_images.to(device)\n\n            # ==========================================\n            # STEP 1: Train the Discriminator\n            # Goal: maximize log(D(x)) + log(1 - D(G(z)))\n            # ==========================================\n            discriminator.zero_grad()\n\n            # --- Train on real batch ---\n            real_labels = torch.full((batch_size,), real_label, device=device)\n            # Forward pass real images through D\n            pred_real = discriminator(real_images)\n            # Loss: D should output 1 for real images\n            loss_D_real = criterion(pred_real, real_labels)\n            loss_D_real.backward()\n\n            # --- Train on fake batch ---\n            # Generate fake images\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            fake_labels = torch.full((batch_size,), fake_label, device=device)\n            # Note: .detach() — don't backprop through G when training D\n            pred_fake = discriminator(fake_images.detach())\n            # Loss: D should output 0 for fake images\n            loss_D_fake = criterion(pred_fake, fake_labels)\n            loss_D_fake.backward()\n\n            # Total discriminator loss\n            loss_D = loss_D_real + loss_D_fake\n            opt_D.step()\n\n            # ==========================================\n            # STEP 2: Train the Generator\n            # Goal: maximize log(D(G(z))) [non-saturating]\n            # ==========================================\n            generator.zero_grad()\n\n            # Generate new fake images (or reuse — but new is more stable)\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            # For generator training, we WANT D to think these are real\n            # So we use real_label=1 as target — generator succeeds when D(G(z))=1\n            pred_G = discriminator(fake_images)  # No .detach() — we want gradients through G\n            loss_G = criterion(pred_G, real_labels)  # \"Fool D into saying these are real\"\n            loss_G.backward()\n            opt_G.step()\n\n            # Logging\n            G_losses.append(loss_G.item())\n            D_losses.append(loss_D.item())\n\n        if (epoch + 1) % 10 == 0:\n            print(f\"Epoch [{epoch+1}/{num_epochs}] \"\n                  f\"Loss_D: {loss_D.item():.4f}  \"\n                  f\"Loss_G: {loss_G.item():.4f}  \"\n                  f\"D(x): {pred_real.mean().item():.3f}  \"\n                  f\"D(G(z)): {pred_fake.mean().item():.3f}/{pred_G.mean().item():.3f}\")\n\n    return G_losses, D_losses\n\n\n\nGANs are notoriously hard to train. Three fundamental problems arise:\n\n\nThe generator learns to produce a small variety of samples (or even just one) that consistently fool the discriminator, ignoring the full data distribution.\nSymptoms: All generated faces look similar; generated digits all look like “1”.\nSolutions: - Minibatch discrimination: Discriminator compares samples within a mini-batch, penalizing when all fake images are similar. - Unrolled GANs: Generator optimizes against future discriminator states. - Wasserstein GAN (WGAN): Replaces JS divergence with Wasserstein distance — more stable gradient signal.\n\n\n\nWhen the discriminator becomes too good, it outputs near-0 for all fakes, and log(1 - D(G(z))) ≈ log(1) = 0 — zero gradient for the generator.\nSolution: WGAN, non-saturating loss, gradient penalty.\n\n\n\nIf D or G is much stronger than the other, training breaks down.\nSolution: Train D multiple times per G update, or vice versa. Two-timescale update rules (TTUR): different learning rates.\n\n\n\n\nWGAN replaces the discriminator with a critic that outputs an unrestricted real number (not a probability). The loss becomes:\nLoss_Critic = E[Critic(fake)] - E[Critic(real)]   ← Critic minimizes\nLoss_G      = -E[Critic(G(z))]                    ← Generator maximizes critic score\nThe 1-Lipschitz constraint on the critic (required by Wasserstein theory) is enforced via gradient penalty:\nGP = E[(||∇_x̂ Critic(x̂)||₂ - 1)²]\nWhere x̂ is a random interpolation between real and fake: x̂ = ε·x_real + (1-ε)·x_fake.\nclass WGANGPCritic(nn.Module):\n    \"\"\"\n    WGAN-GP Critic (discriminator without sigmoid — outputs raw score).\n    No BatchNorm! (incompatible with gradient penalty). Use LayerNorm or SpectralNorm.\n    \"\"\"\n    def __init__(self, img_channels=3, feature_maps=64):\n        super().__init__()\n        ndf = feature_maps\n        self.model = nn.Sequential(\n            # No BN in critic for WGAN-GP\n            nn.Conv2d(img_channels, ndf, 4, 2, 1),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf, ndf*2, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*2, affine=True),  # InstanceNorm instead of BatchNorm\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*2, ndf*4, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*4, affine=True),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*4, ndf*8, 4, 2, 1),\n            nn.InstanceNorm2d(ndf*8, affine=True),\n            nn.LeakyReLU(0.2, inplace=True),\n\n            nn.Conv2d(ndf*8, 1, 4, 1, 0),\n            nn.Flatten()\n            # NO sigmoid! Critic outputs raw real number\n        )\n\n    def forward(self, x):\n        return self.model(x).squeeze()\n\n\ndef gradient_penalty(critic, real_images, fake_images, device):\n    \"\"\"\n    Compute WGAN-GP gradient penalty.\n    Enforces 1-Lipschitz constraint by penalizing gradient norm.\n    \"\"\"\n    batch_size = real_images.size(0)\n\n    # Random interpolation coefficient: ε ~ Uniform[0,1]\n    epsilon = torch.rand(batch_size, 1, 1, 1, device=device)\n\n    # Interpolated samples: x̂ = ε·real + (1-ε)·fake\n    interpolated = epsilon * real_images + (1 - epsilon) * fake_images.detach()\n    interpolated.requires_grad_(True)\n\n    # Critic score for interpolated samples\n    critic_score = critic(interpolated)\n\n    # Compute gradients of critic scores w.r.t. interpolated samples\n    gradients = torch.autograd.grad(\n        outputs=critic_score,\n        inputs=interpolated,\n        grad_outputs=torch.ones_like(critic_score),\n        create_graph=True,   # Need second-order gradients for optimization\n        retain_graph=True,\n    )[0]\n\n    # Gradient norm: flatten spatial dims, compute L2 norm per sample\n    gradients = gradients.view(batch_size, -1)\n    gradient_norm = gradients.norm(2, dim=1)           # [batch]\n\n    # Penalty: (||∇||₂ - 1)² — penalize deviations from unit gradient norm\n    penalty = ((gradient_norm - 1) ** 2).mean()\n\n    return penalty\n\n\ndef train_wgan_gp(generator, critic, dataloader,\n                  latent_dim=100, num_epochs=100,\n                  n_critic=5,    # Train critic n_critic times per generator update\n                  lambda_gp=10,  # GP weight\n                  lr=1e-4, device='cuda'):\n    \"\"\"\n    WGAN-GP training loop.\n    \"\"\"\n    generator, critic = generator.to(device), critic.to(device)\n\n    # Note: WGAN uses Adam with lower learning rate and beta1=0\n    opt_G = torch.optim.Adam(generator.parameters(), lr=lr, betas=(0.0, 0.9))\n    opt_C = torch.optim.Adam(critic.parameters(),    lr=lr, betas=(0.0, 0.9))\n\n    for epoch in range(num_epochs):\n        for real_images, _ in dataloader:\n            batch_size = real_images.size(0)\n            real_images = real_images.to(device)\n\n            # === Train Critic (n_critic times per G update) ===\n            for _ in range(n_critic):\n                noise = torch.randn(batch_size, latent_dim, device=device)\n                fake_images = generator(noise).detach()\n\n                # Wasserstein loss: maximize E[C(real)] - E[C(fake)]\n                loss_C = (critic(fake_images).mean()   # C wants fake scores LOW\n                         - critic(real_images).mean()  # C wants real scores HIGH\n                         + lambda_gp * gradient_penalty(critic, real_images, fake_images, device))\n\n                opt_C.zero_grad()\n                loss_C.backward()\n                opt_C.step()\n\n            # === Train Generator ===\n            noise = torch.randn(batch_size, latent_dim, device=device)\n            fake_images = generator(noise)\n\n            # G wants critic to score its fakes as HIGH as possible\n            loss_G = -critic(fake_images).mean()\n\n            opt_G.zero_grad()\n            loss_G.backward()\n            opt_G.step()\n\n        if (epoch + 1) % 10 == 0:\n            print(f\"Epoch {epoch+1}: Loss_C={loss_C.item():.4f} | Loss_G={loss_G.item():.4f}\")\n\n\n\nStandard GANs generate random samples — we can’t control what class or attribute is generated. Conditional GANs (Mirza & Osindero, 2014) condition both G and D on additional information y (a class label, text description, or reference image).\nclass ConditionalGenerator(nn.Module):\n    \"\"\"Generator conditioned on a class label.\"\"\"\n    def __init__(self, latent_dim, num_classes, embed_dim, img_size):\n        super().__init__()\n        self.label_emb = nn.Embedding(num_classes, embed_dim)\n        in_dim = latent_dim + embed_dim\n\n        self.model = nn.Sequential(\n            nn.Linear(in_dim, 256),\n            nn.LeakyReLU(0.2),\n            nn.BatchNorm1d(256),\n            nn.Linear(256, 512),\n            nn.LeakyReLU(0.2),\n            nn.BatchNorm1d(512),\n            nn.Linear(512, img_size * img_size),\n            nn.Tanh()\n        )\n        self.img_size = img_size\n\n    def forward(self, noise, labels):\n        \"\"\"\n        noise:  [batch, latent_dim]\n        labels: [batch] — class indices\n        \"\"\"\n        label_embed = self.label_emb(labels)           # [batch, embed_dim]\n        x = torch.cat([noise, label_embed], dim=1)    # [batch, latent+embed]\n        img = self.model(x)\n        return img.view(img.size(0), 1, self.img_size, self.img_size)\n\n\n\n\n\n\n\n\n\n\n\nGAN Variant\nKey Innovation\nApplication\n\n\n\n\nDCGAN\nConv architecture guidelines\nImage generation\n\n\nWGAN-GP\nWasserstein loss + gradient penalty\nStable training\n\n\ncGAN\nConditioning on labels/text\nControlled generation\n\n\nPix2Pix\nImage-to-image with paired data\nStyle transfer, colorization\n\n\nCycleGAN\nCycle consistency loss, no paired data\nDomain adaptation\n\n\nStyleGAN2\nStyle-based generator, W-space\nHigh-fidelity face synthesis\n\n\nBigGAN\nLarge-scale, class-conditional\nImageNet generation\n\n\nGauGAN\nSPADE normalization\nPhoto-realistic scene synthesis\n\n\n\n\n\n\nUnlike supervised learning, we can’t use simple accuracy. GAN evaluation is an open research problem. Key metrics:\nFréchet Inception Distance (FID): The most widely used metric. Computes the Fréchet distance between the distributions of Inception features from real and generated images. Lower is better.\n# Conceptual FID calculation (uses scipy in practice)\ndef compute_fid(mu_real, sigma_real, mu_fake, sigma_fake):\n    \"\"\"\n    FID = ||μ_r - μ_g||² + Tr(Σ_r + Σ_g - 2(Σ_r·Σ_g)^(1/2))\n    \"\"\"\n    from scipy.linalg import sqrtm\n    import numpy as np\n    diff = mu_real - mu_fake\n    covmean = sqrtm(sigma_real @ sigma_fake)\n    if np.iscomplexobj(covmean):\n        covmean = covmean.real\n    fid = (diff @ diff + np.trace(sigma_real + sigma_fake - 2 * covmean))\n    return fid\nInception Score (IS): Measures both quality (sharp predictions for single class) and diversity (uniform marginal distribution across classes). Higher is better.\nPrecision and Recall: Precision measures sample quality; recall measures coverage of the real data manifold."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#chapter-6-putting-it-all-together-the-unified-view",
    "href": "Resources/DeepLearningAdv/index.html#chapter-6-putting-it-all-together-the-unified-view",
    "title": "DLA",
    "section": "",
    "text": "After diving deep into five paradigms, we can identify universal principles:\n\nResiduals everywhere: Skip connections solve vanishing gradients and ease optimization. Used in Transformers (residual connections), ResNets, and modern RNNs.\nNormalization is essential: Layer Norm (Transformer), Batch Norm (CNNs), Instance Norm (GANs) — each architecture uses the right normalization for its structure.\nAttention is general aggregation: Multi-head attention in Transformers, graph attention in GATs, temporal attention in GRUs — all are forms of learned weighted aggregation.\nScale matters: More parameters, more data, and more compute consistently improve performance across all architectures (the “bitter lesson”).\nInductive biases: Good architectures encode assumptions about data structure. CNNs assume spatial locality. RNNs assume temporal order. GNNs assume relational structure. Transformers make fewer assumptions (more general, but need more data).\n\n\n\n\n\nTransformer = Fully-connected GNN: Self-attention is message passing where every node can attend to every other node. Transformers are GNNs on a complete graph with learned edge weights.\nGNN on grid = CNN: When the graph is a regular 2D grid with local connections, graph convolution reduces to standard convolution.\nAttention in RNNs: The Bahdanau attention mechanism (precursor to Transformers) is attention added on top of encoder-decoder RNNs.\nGAN generator = Decoder: The Transformer decoder, CNN decoder (transposed convolutions), and GAN generator all take compact representations and expand them into rich outputs.\n\n\n\n\n# Universal training best practices\n\n# 1. Learning rate scheduling — never use constant LR\nscheduler = torch.optim.lr_scheduler.OneCycleLR(\n    optimizer, max_lr=0.01,\n    steps_per_epoch=len(dataloader),\n    epochs=num_epochs\n)\n\n# 2. Gradient clipping — essential for RNNs, useful everywhere\ntorch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n\n# 3. Mixed precision training — 2x speed, half memory\nfrom torch.cuda.amp import autocast, GradScaler\nscaler = GradScaler()\nwith autocast():\n    output = model(input)\n    loss = criterion(output, target)\nscaler.scale(loss).backward()\nscaler.step(optimizer)\nscaler.update()\n\n# 4. Exponential Moving Average of weights — better generalization\nfrom torch.optim.swa_utils import AveragedModel\nema_model = AveragedModel(model, multi_avg_fn=torch.optim.swa_utils.get_ema_multi_avg_fn(0.999))\n\n# 5. Data augmentation (for vision tasks)\nimport torchvision.transforms as T\naugment = T.Compose([\n    T.RandomHorizontalFlip(),\n    T.RandomCrop(32, padding=4),\n    T.ColorJitter(brightness=0.4, contrast=0.4, saturation=0.4),\n    T.ToTensor(),\n    T.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])\n])"
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#conclusion-where-to-go-from-here",
    "href": "Resources/DeepLearningAdv/index.html#conclusion-where-to-go-from-here",
    "title": "DLA",
    "section": "",
    "text": "You now have expert-level knowledge of the five fundamental pillars of modern deep learning. Here is a structured roadmap for further mastery:\nImmediate Next Steps:\n\nImplement and train on real data: Theory becomes knowledge through practice. Train a GCN on Cora (citation network), a CNN on CIFAR-10, a Transformer on a translation task.\nRead the original papers: “Attention Is All You Need”, “Deep Residual Learning for Image Recognition”, “Semi-supervised Classification with GCN”, “Generative Adversarial Nets”, “LSTM (1997)”. Reading primaries deepens understanding enormously.\nExplore PyTorch Geometric for GNNs and HuggingFace Transformers for production-grade Transformer implementations.\n\nAdvanced Topics to Explore:\n\nDiffusion Models: The current state-of-the-art for image generation, surpassing GANs in quality and stability. Based on gradually denoising Gaussian noise.\nGraph Transformers: Combining attention mechanisms with graph structure — the current frontier of GNN research.\nState Space Models (Mamba): A new paradigm for sequence modeling, potentially replacing Transformers for long-context tasks.\nNeural Architecture Search (NAS): Automating the design of neural network architectures.\nFoundation Models: Pretraining massive models on internet-scale data and fine-tuning for downstream tasks (BERT, GPT, SAM, etc.).\n\nThe field moves fast. The architectures in this article have already transformed industries — autonomous driving, drug discovery, natural language processing, computer vision, and beyond. Master these foundations, and you will be equipped not just to use these tools, but to push them forward.\n\nThis article covered: Scaled Dot-Product Attention, Multi-Head Attention, Positional Encoding, Transformer Encoder-Decoder, GCN, GAT, Message Passing Neural Networks, Temporal GNNs, TGN Memory, Convolution operations (padding, stride, dilation), ResNets, LSTM from scratch, GRU from scratch, DCGAN, WGAN-GP, Conditional GAN — all with complete, runnable PyTorch implementations."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#overview",
    "href": "Resources/DeepLearningAdv/index.html#overview",
    "title": "DLA",
    "section": "Overview",
    "text": "Overview\nModel optimization sits at the intersection of theory and systems engineering. The goal is always the same: shrink the computational and memory cost of inference without catastrophically degrading accuracy. The three main pillars — Quantization, Pruning, and Knowledge Distillation — each attack the problem from a different angle, and in practice they are stacked on top of each other to achieve deployment-grade models."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#part-1-quantization",
    "href": "Resources/DeepLearningAdv/index.html#part-1-quantization",
    "title": "DLA",
    "section": "Part 1 — Quantization",
    "text": "Part 1 — Quantization\n\nThe Core Idea\nA standard neural network stores weights and activations as 32-bit floating-point numbers (float32). Quantization replaces these with lower-bit representations — typically int8 (8-bit integer), int4, or even binary — reducing memory footprint and enabling faster arithmetic on hardware that has native integer multiply-accumulate units (most edge accelerators, mobile NPUs, and modern x86/ARM CPUs).\nThe fundamental trade-off: reduced numerical precision introduces quantization error, which manifests as a degradation in model accuracy. The craft is in minimizing this error.\n\n\nQuantization Arithmetic\nA floating-point tensor x is mapped to an integer x_q via:\nx_q = round( x / S ) + Z\nWhere: - S (scale) — a float that controls the granularity of the quantization grid. - Z (zero-point) — an integer offset that maps the float zero exactly to an integer.\nReconstruction is:\nx ≈ S * (x_q − Z)\nThe quantization error for a single value is bounded by S/2 — smaller scale means finer resolution but narrower representable range.\nSymmetric vs Asymmetric quantization: - Symmetric: Z = 0, range is [-127, 127] for int8. Simpler hardware implementation. - Asymmetric: Z ≠ 0, range is [0, 255] for uint8. Better utilization of the bit budget for skewed distributions (e.g., ReLU activations that are all non-negative).\nGranularity levels: - Per-tensor: one (S, Z) pair for the whole tensor. Cheapest, but coarse. - Per-channel (per-axis): one (S, Z) per output channel of a weight tensor. Critical for convolutional layers where channel weight distributions can vary significantly. - Per-group: one (S, Z) per small group of elements (e.g., 128 weights). Used in 4-bit quantization (GPTQ, AWQ) to recover accuracy.\n\n\n\nPost-Training Quantization (PTQ)\nPTQ quantizes a model after it has been fully trained, with no or minimal re-training. This is the dominant approach when you don’t have the resources or the original training setup to re-train.\n\nApproach 1 — Weight-Only Quantization (Static)\nWeights are quantized offline once and stored in int8. During inference, weights are dequantized back to float16 before each multiply-accumulate. Memory savings are large (weights dominate model size), but compute savings are minimal since the actual math still happens in float.\nUse case: LLM serving where memory bandwidth, not FLOPs, is the bottleneck. Models like LLaMA with AWQ/GPTQ fall here.\n\n\nApproach 2 — Dynamic Quantization\nWeights are quantized statically. Activations are quantized dynamically at runtime on each forward pass — the scale and zero-point are computed per-activation-tensor on the fly. No calibration dataset needed.\nUse case: LSTMs, Transformers where activation ranges are highly input-dependent.\nTrade-off: The runtime scale computation adds CPU overhead, but you get better accuracy than purely static activation quantization.\n\n\nApproach 3 — Static Quantization (Full Integer)\nBoth weights and activations are quantized to integers. Activations require calibration to pre-determine their scales (see below). The result is a fully integer inference graph — compute happens in integer arithmetic end-to-end, giving maximum hardware speedup.\nUse case: Edge inference on mobile NPUs, ARM Cortex-M, dedicated accelerators. This is what TFLite and ONNX Runtime target for on-device deployment.\n\n\n\n\nCalibrating Quantized Models\nCalibration is the process of determining the optimal (S, Z) for activations by running a calibration dataset — a small representative subset (typically 100–1000 samples) — through the original float model and observing the activation ranges.\n\nMin-Max Calibration\nThe simplest approach: S = (max_observed - min_observed) / 255. Sensitive to outliers — a single activation spike inflates the range, wasting precision on low-frequency extreme values.\n\n\nPercentile Calibration\nClip the range at the 99.99th percentile (or tunable percentile) before computing scale. Discards tail outliers, compressing the remaining dynamic range into the quantization grid more efficiently.\n\n\nKL Divergence Calibration (Entropy Calibration)\nChooses the float range that minimizes the KL divergence between the original float activation distribution and the quantized representation. This is the default in TensorRT and NVIDIA’s PTQ pipeline. It iteratively searches over threshold values and selects the one that best preserves the information content of the distribution.\nAlgorithmic sketch:\nbest_threshold = None\nbest_kl = float('inf')\nfor threshold in candidate_thresholds:\n    quantized_dist = quantize_and_dequantize(activation_hist, threshold)\n    kl = kl_divergence(activation_hist, quantized_dist)\n    if kl &lt; best_kl:\n        best_kl = kl\n        best_threshold = threshold\n\n\nMSE Calibration\nMinimizes the mean squared error between float and dequantized activations over the calibration set. Often outperforms KL calibration for activation distributions with multiple peaks or unusual shapes.\n\n\nLayer-Sensitivity Analysis\nNot all layers tolerate quantization equally. The first and last layers (embedding, classifier head) tend to be the most sensitive. Standard practice: run a sensitivity sweep — quantize one layer at a time, measure accuracy drop. Layers with outsized accuracy degradation are kept in float (mixed-precision quantization).\nMixed-precision example: Keep the first convolution and final linear layer in float16, quantize all intermediate layers to int8. This is the strategy used by NVIDIA’s Automatic Mixed Precision (AMP) inference."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#part-2-model-pruning",
    "href": "Resources/DeepLearningAdv/index.html#part-2-model-pruning",
    "title": "DLA",
    "section": "Part 2 — Model Pruning",
    "text": "Part 2 — Model Pruning\n\nThe Core Idea\nNeural networks are over-parameterized by design — they are trained with more capacity than strictly necessary so that optimization converges to good solutions. Pruning exploits this by identifying and removing parameters that contribute little to the output, introducing sparsity into the weight tensors.\nThe Lottery Ticket Hypothesis (Frankle & Carlin, 2019) provides theoretical grounding: within any large over-parameterized network exists a smaller winning ticket subnetwork that, if trained from scratch with the same initialization, would reach comparable accuracy. Pruning is approximately finding this ticket post-hoc.\n\n\nIdentifying Unimportant Weights\n\nMagnitude-Based Pruning (Unstructured)\nThe simplest and most widely used criterion: prune weights with the smallest absolute values. Intuition — small magnitude weights contribute little to the pre-activation, so zeroing them causes minimal perturbation.\nthreshold = torch.quantile(torch.abs(weight), prune_ratio)\nmask = (torch.abs(weight) &gt;= threshold).float()\npruned_weight = weight * mask\nLimitation: Magnitude is a proxy for importance, not a direct measure. A small weight multiplied by a large activation can be more important than a large weight receiving near-zero input.\n\n\nGradient-Based Saliency (Taylor Expansion)\nA first-order Taylor expansion approximates the change in loss ΔL when a weight w_i is zeroed:\nΔL ≈ |∂L/∂w_i * w_i|\nThis is the gradient-magnitude product — it captures both the size of the weight and how much the loss is currently changing with respect to it. Prune weights where |g_i * w_i| is smallest.\nAdvantage: More accurate importance estimate. Requires one forward+backward pass over a calibration set.\n\n\nActivation-Based Neuron Pruning (Structured)\nFor structured pruning (removing entire neurons, channels, or attention heads rather than individual weights), the relevant quantity is the average activation magnitude of a neuron across the calibration set:\nimportance(j) = (1/N) Σ_i |a_j(x_i)|\nNeurons with near-zero average activation are candidates for removal. For convolutional filters, L1 or L2 norm of the filter’s activation feature map is used.\nWhy structured pruning matters: Unstructured sparsity (zeroing individual weights) does not yield speedups on standard hardware without sparse tensor libraries. Structured pruning removes entire channels or layers, producing a smaller dense network that immediately benefits from conventional matrix multiply acceleration.\n\n\n\n\nIterative Pruning Strategies\nOne-shot pruning — removing a large fraction of weights all at once — causes severe accuracy degradation. Iterative pruning interleaves pruning steps with fine-tuning (re-training) steps, allowing the remaining weights to compensate for the removed ones.\n\nIterative Magnitude Pruning (IMP)\nThe canonical approach from Frankle & Carlin:\n\nTrain the network to convergence.\nPrune the bottom p% of weights by magnitude.\nReset remaining weights to their values at initialization (or after an early warm-up phase).\nRe-train from this reset state with the pruning mask applied.\nRepeat from step 2 until the target sparsity is reached.\n\nEach round applies a small pruning rate (e.g., 10–20%) rather than a large single shot. The sparsity schedule after n rounds with per-round rate p is:\nsparsity_n = 1 - (1 - p)^n\nPractical note: Resetting to initialization (as in the Lottery Ticket original) is expensive and not always beneficial. In practice, the Fine-Pruning variant simply fine-tunes from the pruned weights without resetting, which is faster and performs comparably for most applications.\n\n\nGradual Magnitude Pruning (GMP)\nInstead of discrete pruning rounds, GMP continuously increases the sparsity mask during training following a cubic sparsity schedule:\ns(t) = s_f * [1 - (1 - (t - t_0) / (t_n - t_0))^3]\nWhere s_f is the final target sparsity, t_0 is the pruning start step, and t_n is the end step. The cubic shape spares the early training (when the loss landscape is steep) and the late training (when fine-tuning is most beneficial), concentrating pruning in the middle.\nUsed by the torch.nn.utils.prune API and integrated into SparseML / Neural Magic workflows.\n\n\nMaintaining Accuracy — Practical Strategies\nLearning rate rewinding: After each pruning round, reset the learning rate to an earlier value in the schedule (not to the initial LR, which is too large). This allows the remaining network to efficiently adapt without destabilizing training.\nPruning masks on gradients: During fine-tuning, apply the pruning mask to gradients as well so that zeroed weights stay zeroed and do not “grow back” through gradient updates.\nRegularization: L1 regularization on weights during training encourages sparsity organically, producing weight distributions that are easier to prune with less accuracy loss.\nSecond-order pruning (OBD/OBS): Optimal Brain Damage and Optimal Brain Surgeon use the Hessian of the loss to estimate the impact of removing each weight. Computationally expensive but highly accurate — relevant for extreme compression regimes."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#part-3-knowledge-distillation",
    "href": "Resources/DeepLearningAdv/index.html#part-3-knowledge-distillation",
    "title": "DLA",
    "section": "Part 3 — Knowledge Distillation",
    "text": "Part 3 — Knowledge Distillation\n\nThe Core Idea\nKnowledge Distillation (Hinton et al., 2015) transfers the “knowledge” of a large, high-capacity teacher model into a smaller, efficient student model. The key insight is that the teacher’s soft probability distribution over all classes — not just its hard label prediction — is vastly more informative for training. A soft distribution that assigns 0.85 probability to “cat” and 0.12 to “lynx” tells the student about the inter-class similarity structure that a one-hot label [0, 0, 1, 0, ...] completely destroys.\n\n\n\nTraining Student Models with Teacher Logits\nThe student is trained on a composite loss:\nL_total = α * L_distill + (1 - α) * L_CE\nWhere: - L_CE = standard cross-entropy between student logits and ground-truth hard labels. - L_distill = KL divergence (or cross-entropy) between student and teacher soft distributions. - α = loss weighting hyperparameter (typically 0.7–0.9 early in training, can be annealed).\nThe distillation loss specifically is:\nL_distill = T² * KL( σ(z_T / T) || σ(z_S / T) )\nWhere z_T are teacher logits, z_S are student logits, T is the temperature, and σ is softmax. The T² factor compensates for the reduced gradient magnitude at high temperatures.\n\n\n\nTemperature in Distillation\nTemperature T is the most critical hyperparameter in distillation. It controls the softness of the probability distributions shared by the teacher.\nAt T = 1, the softmax is standard — if the teacher is confident, the output is a near one-hot distribution, which is barely more informative than the hard label.\nAt T &gt; 1, the softmax is softened — probabilities spread across all classes, revealing the teacher’s internal similarity structure. The logit ratio between class i and class j in the limit of large T approximates:\np_i / p_j ≈ exp((z_i - z_j) / T) → 1 + (z_i - z_j) / T for large T\nSo at high temperature, the student learns from the logit differences (the relative affinities), not just the argmax.\nPractical guidance on temperature: - T = 1: No softening benefit. Equivalent to training on pseudo-labels. - T = 3–5: Typical sweet spot for image classification (CIFAR, ImageNet). Enough to reveal inter-class structure without over-diffusing. - T = 10–20: Used for large language models and complex structured tasks. Distributes probability mass more uniformly. - Very high T (&gt; 20): Over-smoothing — all classes become nearly equiprobable. The teacher’s signal degrades to noise.\nAnnealing temperature: Start with high T to bootstrap the student on a rich soft signal, then reduce T over training to sharpen focus on correct classes. Not universally helpful but beneficial for very small student models.\n\n\n\nAdjusting Temperature and Loss Weighting\nThe interplay between α, (1-α), and T determines the training dynamics:\n\n\n\n\n\n\n\nScenario\nRecommended Strategy\n\n\n\n\nStudent is much smaller than teacher\nHigh α (0.9+), moderate T (4–8). Lean heavily on soft labels.\n\n\nLimited labeled data\nHigh α. Distillation loss acts as a form of regularization / label smoothing.\n\n\nStudent near teacher capacity\nLower α (0.3–0.5). Hard labels become more important — the student can fit the full distribution.\n\n\nImbalanced classes\nReduce α for tail classes; distillation may propagate teacher’s existing bias.\n\n\nSequence-to-sequence / LLMs\nToken-level KL divergence. Temperature 2–5 works well for autoregressive distillation.\n\n\n\nDistilBERT recipe as a reference: α = 0.5 splitting equally between soft cross-entropy (T=2) and masked LM hard label loss, plus a cosine embedding loss on the hidden states of student vs teacher. This multi-objective approach adds an intermediate-layer alignment term that enriches the soft signal beyond logits alone.\nIntermediate Feature Distillation: Beyond the final logits, you can distill from intermediate layers. Common approaches: - FitNets: Student middle layers are trained to match teacher’s hidden representations via an MSE loss. - Attention Transfer (AT): Student’s attention maps (sum of squared activation maps across channels) are trained to match teacher’s. - CRD (Contrastive Representation Distillation): Frames distillation as a contrastive learning problem — student representations of the same example should be close to the teacher’s and far from other examples.\n\n\n\nOptimizing Memory Footprint for Target Deployment Platforms\nDistillation produces an accurate student; quantization and pruning compress it further. The full optimization stack for a deployment target looks like:\nTrain large teacher → Distill into small student →\nPrune student to target sparsity → Quantize pruned model →\nCompile with hardware-specific backend\n\nTarget Platform Considerations\nMobile (ARM Cortex-A / Apple Neural Engine): - Prefer int8 static quantization (TFLite, Core ML). - Structured pruning to width/depth multiples that align with SIMD vector widths (128-bit NEON = 16x int8 in parallel). - Depthwise-separable convolutions in the student architecture (MobileNet-style) reduce FLOPs by ~8–9× vs standard conv. - Avoid dynamic shapes — static batch size = 1 enables maximum compiler optimization.\nEdge Microcontrollers (Cortex-M, RISC-V): - int8 or even int4 quantization. Some targets require int16 for activations due to lack of int8 SIMD. - No heap allocation — all tensors must be statically sized. Use TFLite Micro’s arena allocator. - Weight-sharing (applying k-means clustering to weights) reduces unique weight values, enabling simpler lookup tables.\nServer GPU (NVIDIA): - FP16 or BF16 for training, int8 for inference via TensorRT. - Tensor Core alignment: weight dimensions must be multiples of 8 (FP16) or 16 (int8) for Tensor Core utilization. - Structured sparsity with 2:4 pattern: NVIDIA Ampere+ supports 50% structured sparsity natively — for every group of 4 weights, exactly 2 must be zero. The torch.nn.utils.prune.l1_unstructured + APEX sparse library combination achieves 2× speedup with minimal accuracy loss.\nBrowser / WebAssembly (ONNX.js, TensorFlow.js): - float16 weights (halve download size), float32 compute (JS typed arrays are 32-bit). - Limit model parameter count to &lt; 10M for reasonable load times. - Prefer small, factored architectures (e.g., EfficientNet-Lite, MobileViT-XXS) over pruned large models.\n\n\nMeasuring and Targeting Memory\nKey metrics for deployment:\n\n\n\n\n\n\n\n\nMetric\nDefinition\nTarget Tool\n\n\n\n\nModel size (disk)\nSum of compressed parameter storage\nos.path.getsize(), TFLite flatbuffer\n\n\nPeak memory (runtime)\nMax simultaneous tensor allocation during inference\nTFLite Micro arena estimation\n\n\nActivation memory\nMemory for storing intermediate activations\nProfile by layer with torch.profiler\n\n\nWorking set\nModel size + activation memory + runtime overhead\nHardware BSP memory map\n\n\n\nOperator fusion: Before measuring final footprint, apply graph-level optimizations: fuse BatchNorm into the preceding Conv (reduces memory by eliminating the BN parameter tensors), fuse Conv+ReLU into a single op (reduces activation storage). These are handled automatically by TorchScript, ONNX Runtime, and TensorRT.\nQuantization-aware training (QAT) as a final refinement: After PTQ, if the accuracy gap is too large, QAT inserts fake quantization nodes into the compute graph during re-training. These nodes simulate the rounding error of integer arithmetic in the forward pass while maintaining float gradients, allowing the weights to adapt to the quantization grid. QAT typically recovers 0.5–2% accuracy over PTQ at a cost of re-training for ~10–25% of the original training duration."
  },
  {
    "objectID": "Resources/DeepLearningAdv/index.html#summary-the-optimization-stack-in-practice",
    "href": "Resources/DeepLearningAdv/index.html#summary-the-optimization-stack-in-practice",
    "title": "DLA",
    "section": "Summary — The Optimization Stack in Practice",
    "text": "Summary — The Optimization Stack in Practice\n\n\n\n\n\n\n\n\n\nTechnique\nPrimary Benefit\nKey Hyperparameters\nWatch-out\n\n\n\n\nPTQ (int8 static)\n4× memory reduction, 2–4× inference speedup\nCalibration dataset size, per-channel vs per-tensor, calibration algorithm\nSensitive layers need float fallback\n\n\nWeight-only quant (int4)\n8× weight compression\nGroup size (128 typical), calibration method (AWQ, GPTQ)\nActivation compute still float; no compute speedup\n\n\nUnstructured pruning\nReduces parameter count, enables compression\nSparsity target, pruning schedule, fine-tune LR\nNo FLOP reduction without sparse kernels\n\n\nStructured pruning\nReduces FLOPs and latency on standard hardware\nChannel prune ratio, iterative rounds\nArchitectural change — requires re-evaluation\n\n\nKnowledge distillation\nBetter accuracy at given model size\nTemperature T, α, student architecture\nTeacher must be large/good enough to distill from\n\n\nQAT\nRecovers accuracy after aggressive quantization\nQuantization scheme, re-training duration\nRequires original training pipeline\n\n\n\nThe best results consistently come from combining all three: distill a small student, prune it to the target sparsity, then quantize and compile for the deployment hardware. Each stage compounds the compression, and accuracy is best preserved when each stage begins from the output of the previous one rather than applying them independently.\n\nEnd of Training Material"
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html",
    "href": "Resources/Core5/Architecture/index.html",
    "title": "VLM Architecture",
    "section": "",
    "text": "Every line in this document is written so that the reader walks away with the ability to draw any modern VLM on a whiteboard, justify every design choice with a paper, and code each component from a blank .py file. We move from first principles to the latest 2024–2025 architectures (LLaVA-NeXT, InternVL-2, Qwen2-VL, Florence-2, Idefics-3, Chameleon, Molmo).\n\n\n\n\nA Vision-Language Model (VLM) is a single differentiable function\n\\[\nf_\\theta : (\\mathcal{I}, \\mathcal{T}) \\;\\longrightarrow\\; \\mathcal{Y}\n\\]\nthat consumes an image (or video) \\(\\mathcal{I}\\) and a text \\(\\mathcal{T}\\), and emits some target \\(\\mathcal{Y}\\) — a class label, a similarity score, a caption, an answer, a bounding box, a segmentation mask, or another image. Internally every modern VLM decomposes into four building blocks:\n\nVisual encoder \\(E_v\\) — turns pixels into a sequence (or set) of visual tokens \\(\\mathbf{V} \\in \\mathbb{R}^{N_v \\times d_v}\\).\nText encoder / language model \\(E_t\\) — turns sub-word tokens into textual representations \\(\\mathbf{T} \\in \\mathbb{R}^{N_t \\times d_t}\\).\nFusion module \\(F\\) — aligns \\(\\mathbf{V}\\) and \\(\\mathbf{T}\\) in a shared space and lets them interact.\nTask head \\(H\\) — projects the fused representation to the output space.\n\nEvery architecture you will ever read about — CLIP, ALIGN, BLIP, BLIP-2, Flamingo, LLaVA, MiniGPT-4, Qwen-VL, Kosmos, PaLI, Florence-2, Chameleon — is a particular choice for these four blocks.\nWe will now master every block.\n\n\n\n\n\nComparing Model Architectures\nLearning Representations\nCustomizing Visual Encoders\nExtracting Visual Embeddings\nCustomizing Text Encoders\nExtracting Textual Embeddings\nImplementing Custom Fusion Modules\nAssembling Multimodal Architecture\nReading List & References\n\n\n\n\n\n\nModern VLMs split cleanly into four design families. Memorise this taxonomy — examiners love it.\n\n\n\n\n\n\n\n\n\n\n\n\n\nFamily\nCanonical model\nVision encoder\nFusion\nDecoder\nTrained for\n\n\n\n\nDual-encoder (contrastive)\nCLIP, ALIGN, SigLIP\nViT-L/14\nnone (dot-product)\nnone\nRetrieval, zero-shot classification\n\n\nEncoder-decoder (fusion-in-encoder)\nViLBERT, LXMERT, ALBEF, BLIP\nViT or Faster-RCNN\ncross-attention layers\nsmall text decoder\nVQA, captioning, ITM\n\n\nFrozen-LLM (projection-style)\nFlamingo, BLIP-2, LLaVA, MiniGPT-4\nfrozen ViT (CLIP/EVA)\nQ-Former / MLP / Resampler\nfrozen or LoRA LLM\nOpen-ended dialog, instruction following\n\n\nEarly-fusion / unified tokenization\nFuyu, Chameleon, Gemini-Nano, Unified-IO\nlinear patch projector\nnone (single transformer)\nsingle transformer\nAny-to-any modality\n\n\n\nThe progression is historical: dual-encoders proved alignment was learnable from scale; encoder-decoders showed cross-attention beats concatenation for VQA; frozen-LLM models exploited LLM emergent abilities while paying a tiny compute price; early-fusion models removed the modality boundary entirely.\n\n\n\nCLIP — Contrastive Language–Image Pre-training — is the bedrock. Two encoders, one objective:\n\\[\n\\mathcal{L}_{\\text{CLIP}} = \\tfrac{1}{2}\\!\\left[ \\mathcal{L}_{i\\to t} + \\mathcal{L}_{t \\to i} \\right], \\qquad\n\\mathcal{L}_{i\\to t} = -\\frac{1}{B}\\sum_{i=1}^B \\log \\frac{\\exp(\\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_i \\rangle / \\tau)}{\\sum_{j=1}^B \\exp(\\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_j \\rangle / \\tau)}\n\\]\nwhere \\(\\hat{\\mathbf{v}}_i\\) and \\(\\hat{\\mathbf{t}}_i\\) are L2-normalised image and text embeddings, \\(\\tau\\) is a learnable temperature, and \\(B\\) is the mini-batch size. Read Jay Alammar’s “Illustrated CLIP” and Lilian Weng’s “Contrastive Representation Learning” post for visual intuition.\n# Minimal CLIP forward pass\nimport torch, torch.nn as nn, torch.nn.functional as F\n\nclass MiniCLIP(nn.Module):\n    def __init__(self, vision_enc, text_enc, d=512):\n        super().__init__()\n        self.v = vision_enc\n        self.t = text_enc\n        self.v_proj = nn.Linear(vision_enc.out_dim, d, bias=False)\n        self.t_proj = nn.Linear(text_enc.out_dim, d, bias=False)\n        self.logit_scale = nn.Parameter(torch.tensor(2.6592))  # log(1/0.07)\n\n    def forward(self, images, tokens):\n        v = F.normalize(self.v_proj(self.v(images)), dim=-1)\n        t = F.normalize(self.t_proj(self.t(tokens)), dim=-1)\n        logits = self.logit_scale.exp() * v @ t.T\n        return logits, logits.T   # image-to-text, text-to-image\nKey properties:\n\nMassive negative samples: with batch size \\(B\\), every image sees \\(B-1\\) negatives at no extra cost.\nModality gap: the two embedding clouds occupy distinct sub-regions of the unit sphere even after training (Liang et al., 2022). The cosine similarities are still discriminative but the absolute geometry is not aligned — relevant when you want to use CLIP embeddings in downstream classifiers.\nScaling laws: CLIP improves roughly as \\(\\text{acc} \\propto \\log(N_{\\text{params}}) + \\log(N_{\\text{samples}})\\).\n\n\n\n\nSigLIP replaces the row-wise softmax with a pair-wise sigmoid:\n\\[\n\\mathcal{L}_{\\text{SigLIP}} = -\\frac{1}{B} \\sum_{i=1}^B \\sum_{j=1}^B \\log \\sigma\\!\\big(z_{ij}\\cdot ( \\tau \\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_j\\rangle + b )\\big)\n\\]\nwith \\(z_{ij} = +1\\) if \\(i = j\\) else \\(-1\\), and a learnable bias \\(b\\). Why is this important?\n\nThe softmax in CLIP forces global normalisation across the batch — every device must hold all logits during back-prop. SigLIP factorises and tolerates tiny batch sizes (down to 256) while matching CLIP at 32 k.\nRobust to label-noise because each pair is independently labelled.\nAll modern VLMs (PaliGemma, Idefics-3, Molmo, InternVL-2) use SigLIP-So400m as the vision encoder.\n\n\n\n\nBLIP introduces a Multi-modal Mixture-of-Encoder-Decoder (MED): one ViT plus a text transformer that can act as\n\nUnimodal text encoder (for ITC — image-text contrastive)\nImage-grounded text encoder (for ITM — image-text matching)\nImage-grounded text decoder (for LM — captioning)\n\nAll three modes share weights except for the self-attention and cross-attention layers, with prompt tokens distinguishing the role. Training combines three losses:\n\\[\n\\mathcal{L}_{\\text{BLIP}} = \\mathcal{L}_{\\text{ITC}} + \\mathcal{L}_{\\text{ITM}} + \\mathcal{L}_{\\text{LM}}.\n\\]\nBLIP also introduces CapFilt — a captioner and filter trained on the same model that bootstraps clean captions from noisy web alt-text. Many “synthetic-caption” papers (LLaVA-1.5, ShareGPT-4V) descend from this idea.\n\n\n\nBLIP-2 freezes a vision encoder (ViT-g, EVA-CLIP) and a language model (OPT, Flan-T5) and learns only a tiny Q-Former: 32 learnable query tokens that cross-attend to image features.\nPre-training has two stages:\n\nRepresentation learning — train Q-Former with ITC + ITM + ITG losses against the frozen vision encoder.\nGenerative learning — connect Q-Former output to the frozen LLM via a linear projection and train on captioning.\n\nQ-Former parameter count: ~188 M, versus 1.2 B for the LLM and 1 B for the ViT-g. This is the architectural pattern that lets the field iterate on “a tiny adapter, frozen giants on both sides.”\n\n\n\nLLaVA proves that a single linear layer (LLaVA-1) or a 2-layer MLP (LLaVA-1.5) is sufficient to connect a frozen CLIP-ViT-L/14 to a frozen Vicuna/LLaMA-2. Training is two-stage:\n\nFeature alignment: train only the projector on 558 k LAION-CC-SBU caption pairs.\nInstruction tuning: unfreeze the LLM and projector, train on 150 k GPT-4-generated multimodal instructions.\n\nLLaVA’s insight is that the LLM is already a multimodal reasoner if you can speak its language — and the projector’s job is to translate visual tokens into something the LLM treats as foreign-language tokens.\nLLaVA-NeXT (1.6) and LLaVA-OneVision extend this with AnyRes dynamic tiling: an image is split into a 2×2 or 3×3 grid of 336² patches plus a global thumbnail, each tile is encoded separately, and tokens are concatenated. This unlocks 672² or 1008² resolution without retraining the ViT.\n\n\n\nFlamingo interleaves gated cross-attention blocks inside a frozen LLM:\n\\[\n\\mathbf{h}^{(l+1)} = \\mathbf{h}^{(l)} + \\tanh(\\alpha_l) \\cdot \\text{CrossAttn}(\\mathbf{h}^{(l)}, \\mathbf{V}) + \\text{FFN}(\\cdot)\n\\]\nThe scalar gate \\(\\alpha_l\\) is initialised to 0 so the LLM behaves identically to its uni-modal self at the start of training, then gradually opens. A Perceiver Resampler compresses arbitrary numbers of image features into 64 fixed tokens per image, allowing interleaved image-text sequences.\nFlamingo handles few-shot multimodal prompting natively because the LLM still sees a single token stream.\n\n\n\nChameleon (Meta, 2024) and Fuyu-8B (Adept, 2023) eliminate the separate vision encoder. Pixels are turned into discrete tokens (Chameleon via VQ-VAE, Fuyu via a linear patch projector) and prepended to text tokens in a single decoder-only transformer.\nPros: no modality gap, generation across modalities, scaling laws are identical to text LLMs.\nCons: vision pre-training of a giant transformer is enormously expensive and discards the inductive biases of CNN/ViT — generally requires 10× more data.\n\n\n\nA complete comparison must touch:\n\nVision encoder choice and resolution (ViT-L/14 @ 224 vs SigLIP-So400m @ 384 vs AnyRes 672).\nLLM backbone and parameter count (Vicuna-7B vs LLaMA-2-13B vs Qwen-2.5-72B vs Phi-3).\nFusion topology (concatenation / cross-attn / Q-Former / MLP).\nTraining data scale and composition (image-text pairs, OCR, charts, video, agentic web traces).\nPre-training objectives (ITC, ITM, LM, NTP, masked-anywhere).\nToken budget per image (1, 32, 64, 144, 256, 729, 2880…).\nResolution strategy (fixed / dynamic tiling / native any-resolution / native-pack).\nBenchmark numbers (MMMU, MathVista, DocVQA, ChartQA, MMBench, VQAv2, AI2D, BLINK).\n\nMemorise this list; it is the rubric for any architectural-comparison question.\n\n\n\n\n\n\n\n\nA representation is a vector that carries information about the input in a form that is useful to downstream tasks while being invariant to nuisance variation. Bengio’s 2013 “Representation Learning: A Review and New Perspectives” defines the three desiderata: smoothness, multiple explanatory factors, and hierarchy. Lilian Weng’s “Self-Supervised Learning” post (Nov 2019, updated 2021) is the canonical free tutorial.\nThere are four ways a VLM learns representations.\n\n\n\nThe InfoNCE objective (van den Oord et al., 2018) is the workhorse:\n\\[\n\\mathcal{L}_{\\text{InfoNCE}} = -\\mathbb{E}\\!\\left[ \\log \\frac{\\exp(s(q, k_+)/\\tau)}{\\exp(s(q,k_+)/\\tau) + \\sum_{k_-} \\exp(s(q,k_-)/\\tau)} \\right]\n\\]\nwhere \\(q\\) is a query embedding, \\(k_+\\) is the positive (paired) key, and the sum runs over \\(N-1\\) negatives. As \\(N \\to \\infty\\), InfoNCE lower-bounds the mutual information \\(I(q;k_+)\\).\nFor VLMs the query/positive pair is an (image, text) sample; the negatives are the other items in the batch (CLIP) or items in a queue (MoCo / ALBEF). The key engineering questions are:\n\nHow many negatives? More is better up to ~32 k for CLIP; the marginal gain plateaus.\nHard-negative mining: ALBEF samples cross-modal negatives weighted by current similarity scores — this dramatically accelerates convergence.\nTemperature \\(\\tau\\): smaller \\(\\tau\\) → sharper distribution → harder optimisation but better separation. Setting \\(\\tau\\) learnable (CLIP) often beats fixed.\nMomentum encoder: a slow EMA copy of the encoder produces consistent keys (He et al., MoCo). ALBEF uses a momentum distillation loss too.\n\n\n\n\nA binary cross-entropy classifier sits on top of the fused [CLS] token and predicts “is this image-text pair a real match?” Half the batch is real pairs, half are hard negatives sampled with weights proportional to current similarity. ITM is cheap and effective but only useful when the model is encoder-style (it needs cross-attention).\n\n\n\nThis is plain next-token prediction conditioned on the image:\n\\[\n\\mathcal{L}_{\\text{LM}} = -\\sum_{t=1}^T \\log p_\\theta(w_t \\mid w_{&lt;t}, \\mathbf{V}).\n\\]\nIt produces a generative model from the very start. Required for any model that must caption or chat. LLaVA and BLIP-2 use a masked variant where the loss is computed only on assistant tokens, not user-instruction tokens — this is the “selective masking” detail you should always mention.\n\n\n\nMask 50–75 % of image patches and 15 % of text tokens; predict the missing ones from the remaining context. The image targets can be:\n\nRaw pixels (MAE).\nDiscrete visual tokens from a pre-trained dVAE (BEiT, BEiT-3).\nFeatures from a separate teacher (data2vec, EVA — predicts CLIP features).\n\nMasked modelling rewards local representations, while contrastive learning rewards global invariances — combining them (FLAVA, BEiT-3) gives both.\n\n\n\nFlorence-2 (Microsoft, 2024) demonstrates that prompting a single sequence-to-sequence model with task-specific prompts (&lt;OD&gt; for detection, &lt;CAPTION&gt; for captioning, &lt;OCR&gt; for text reading) is sufficient — provided the pre-training data covers all tasks. The representation learned is the shared encoder hidden state.\n\n\n\nWang & Isola (2020) show that contrastive losses can be decomposed into\n\\[\n\\mathcal{L}_{\\text{align}} = \\mathbb{E}\\big[\\|f(x) - f(x^+)\\|^2\\big], \\qquad\n\\mathcal{L}_{\\text{uniform}} = \\log \\mathbb{E}\\big[\\exp(-2 \\|f(x) - f(y)\\|^2)\\big].\n\\]\nA good encoder minimises both: positives collapse together (alignment), random samples spread on the unit sphere (uniformity). This decomposition is a great mental model for why contrastive learning works.\n\n\n\nThree protocols to evaluate a frozen representation:\n\nLinear probe: freeze the encoder, train a linear classifier on a labelled dataset.\nk-NN probe: classify by the dominant label among the \\(k\\) nearest training embeddings.\nZero-shot: in CLIP-style models, build a classifier by encoding text prompts and computing dot products.\n\nA model that wins on zero-shot but loses on linear probe is producing a task-specific representation rather than a general one. Watch for this in papers.\n\n\n\n\nRetrieval / zero-shot classification → InfoNCE only (CLIP, SigLIP).\nVQA / classification head → InfoNCE + ITM + masked LM (ALBEF, BLIP).\nOpen-ended dialog / OCR / detection → generative LM on top of a frozen contrastive backbone (BLIP-2, LLaVA, Florence-2).\nAny-to-any / image generation included → unified tokenisation + LM (Chameleon, Unified-IO 2).\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nFamily\nExamples\nInductive bias\nToken count for 224²\nWhen to use\n\n\n\n\nCNN\nResNet-50, ConvNeXt-XL, RegNetY\nLocality, translation equivariance\n49 (after 7×7 pool)\nEdge devices, dense prediction\n\n\nVision Transformer (ViT)\nViT-L/14, DeiT, EVA-02\nAlmost none; global attention\n256 (16² patches) or 196 (14²)\nAnything that scales\n\n\nHybrid / Hierarchical\nSwin, ConvNeXt-V2, BEiT-3\nHierarchical pooling + windowed attention\nvaries\nObject detection, segmentation\n\n\n\nA VLM almost always uses a ViT today because (a) it produces a clean sequence of tokens that drops straight into a transformer-based fusion module, and (b) it scales with data better than CNNs.\n\n\n\nGiven an image \\(x \\in \\mathbb{R}^{H \\times W \\times 3}\\) and patch size \\(P\\):\n\nPatchify: reshape into \\(N = HW/P^2\\) patches of size \\(P \\times P \\times 3 = 3P^2\\) floats, then linearly project to dimension \\(d\\). This is a convolution with kernel \\(P\\) and stride \\(P\\).\nPrepend [CLS] token: a learnable vector \\(\\mathbf{z}_{\\text{cls}} \\in \\mathbb{R}^d\\).\nAdd positional embeddings \\(\\mathbf{p} \\in \\mathbb{R}^{(N+1)\\times d}\\) — learned (ViT) or sinusoidal (CLIP-RN) or RoPE-2D (Qwen2-VL).\nL transformer blocks: each is\n\\[\n\\mathbf{z}^\\prime = \\mathbf{z} + \\text{MHSA}(\\text{LN}(\\mathbf{z})), \\quad \\mathbf{z} = \\mathbf{z}^\\prime + \\text{MLP}(\\text{LN}(\\mathbf{z}^\\prime)).\n\\]\nFinal LN, output: either the [CLS] embedding or all \\(N\\) patch embeddings.\n\nVariant cheat-sheet:\n\nViT-B/16: 12 layers, 12 heads, \\(d=768\\) — 86 M params.\nViT-L/14: 24 layers, 16 heads, \\(d=1024\\) — 304 M params.\nViT-H/14: 32 layers, 16 heads, \\(d=1280\\) — 632 M params.\nViT-g/14: 40 layers, 16 heads, \\(d=1408\\) — 1.0 B params.\nSigLIP-So400m/14: 27 layers, 16 heads, \\(d=1152\\) — 400 M params, optimal “shape” by SoViT (Alabdulmohsin et al., 2023).\nEVA-02-Large: ViT-L with RoPE-2D + xattn + sub-LN + masked image modelling pre-training; the strongest ~300 M parameter encoder publicly.\n\n\n\n\nimport torch, torch.nn as nn\nfrom transformers import CLIPVisionModel, SiglipVisionModel\n\n# 1) CLIP-ViT-L/14 from OpenAI weights\nclip = CLIPVisionModel.from_pretrained(\"openai/clip-vit-large-patch14-336\")\n# 2) SigLIP-So400m from Google (used by Gemini-Nano, PaliGemma, Idefics-3)\nsig  = SiglipVisionModel.from_pretrained(\"google/siglip-so400m-patch14-384\")\n\n# Both return BaseModelOutputWithPooling: .last_hidden_state [B, 1+N, d], .pooler_output [B, d]\nimg = torch.randn(2, 3, 336, 336)\nout = clip(pixel_values=img)\nprint(out.last_hidden_state.shape, out.pooler_output.shape)\n\n\n\nYou will be asked to modify an encoder. Six standard surgeries:\n\n\nTo support higher resolution without re-training, interpolate positional embeddings:\ndef interpolate_pos_embed(pos_embed, new_grid):\n    # pos_embed: [1, 1+N_old, d]; new_grid: int H_new == W_new in patches\n    cls, grid = pos_embed[:, :1], pos_embed[:, 1:]\n    N_old = grid.shape[1]; gs_old = int(N_old**0.5)\n    grid = grid.reshape(1, gs_old, gs_old, -1).permute(0, 3, 1, 2)\n    grid = torch.nn.functional.interpolate(grid, size=(new_grid, new_grid),\n                                           mode=\"bicubic\", align_corners=False)\n    grid = grid.permute(0, 2, 3, 1).reshape(1, new_grid * new_grid, -1)\n    return torch.cat([cls, grid], dim=1)\nCLIP-ViT-L/14 trained at 224 (16² patches) generalises to 336 (24²) with this trick alone.\n\n\n\nLLaVA-NeXT and Qwen2-VL split a high-res image into multiple ViT inputs.\ndef anyres_tiles(image, base=336, candidate_grids=((1,1),(1,2),(2,1),(2,2),(1,3),(3,1))):\n    H, W = image.shape[-2:]\n    best = max(candidate_grids,\n               key=lambda g: min(W / (g[1]*base), H / (g[0]*base)))\n    gh, gw = best\n    image = torch.nn.functional.interpolate(\n        image[None], size=(gh*base, gw*base), mode=\"bicubic\", align_corners=False)[0]\n    tiles = image.unfold(1, base, base).unfold(2, base, base)         # [3, gh, gw, base, base]\n    tiles = tiles.permute(1, 2, 0, 3, 4).reshape(gh*gw, 3, base, base)\n    thumb = torch.nn.functional.interpolate(\n        image[None], size=(base, base), mode=\"bicubic\", align_corners=False)[0]\n    return torch.cat([thumb[None], tiles], dim=0)                     # [1+gh*gw, 3, b, b]\n\n\n\nReplace the 1-D positional embedding with rotary embeddings indexed by \\((y, x)\\):\n\\[\n\\text{RoPE}_{2D}(\\mathbf{q}_{y,x}) = \\begin{pmatrix} R_y & 0 \\\\ 0 & R_x \\end{pmatrix} \\mathbf{q}_{y,x}\n\\]\nwhere \\(R_y\\) and \\(R_x\\) are block-diagonal rotation matrices on half of the channels each. This is how Qwen2-VL and InternVL-2 support truly arbitrary resolutions.\n\n\n\nAdd 4–8 extra learnable tokens to the input that are discarded at output. Empirically removes the “artifacts” in attention maps reported by Caron et al. and improves dense prediction.\n\n\n\nDynamicViT, ATS, ToMe — remove or merge tokens whose attention score is below a threshold to cut FLOPs by 30–50 %. ToMe (token-merging) is the most popular: at each layer, find the \\(r\\) most similar token pairs by cosine and average them.\n\n\n\nConcatenate features from layers \\(\\{l_1, l_2, l_3\\}\\) — e.g. layer 8 (textures), 16 (parts), 24 (objects). VAR, VeCLIP and LLaVA-1.5 with high-res use this trick.\n\n\n\n\nWhen you need a low-token-count encoder for mobile or for video, ConvNeXt-V2 and EfficientFormer-V2 produce 49 or 196 tokens at much lower FLOPs. For dense prediction (detection, segmentation) the hierarchical encoders matter — Swin-Transformer uses shifted windows, InternImage uses deformable convolutions; the InternVL series uses these as the vision tower.\n\n\n\n\ntorch.compile(encoder, mode=\"reduce-overhead\") removes 20–30 % wall-clock on H100 for ViT-L.\nUse torch.nn.functional.scaled_dot_product_attention (PyTorch ≥ 2.0) — it dispatches to Flash-Attention 2 automatically.\nFreeze the encoder with for p in encoder.parameters(): p.requires_grad = False; encoder.eval() — the .eval() matters because of BatchNorm/Dropout (Dropout is no-op but LayerNorm running stats are not stored in CLIP-style ViTs, so .eval() is safe).\nCast pre-trained weights to bf16 with model.to(torch.bfloat16) when training fusion — the tiny accuracy loss is invisible and memory drops 50 %.\n\n\n\n\n\n\n\n\n\nA ViT block-by-block output has shape [B, 1+N, d]. Five canonical extraction strategies:\n\n\n\n\n\n\n\n\n\nStrategy\nTensor used\nPros\nCons\n\n\n\n\nCLS token\nhidden_states[-1][:, 0]\nOne vector per image, fast\nLoses spatial info\n\n\nMean pool of patches\nhidden_states[-1][:, 1:].mean(1)\nRobust, no CLS-collapse\nLoses CLS bias\n\n\nAll patch tokens\nhidden_states[-1][:, 1:]\nSpatial reasoning possible\n\\(N\\) tokens to fuse\n\n\nPenultimate layer patches\nhidden_states[-2][:, 1:]\nLess “task-specialised” than the final\nOne layer to specify\n\n\nMulti-scale concat\n[h[l1], h[l2], h[l3]]\nCaptures textures + objects\n2-3× tokens\n\n\n\nCLIP’s image_features uses CLS + a final projection layer. LLaVA-1.5 uses penultimate-layer patch tokens (24×24 = 576 tokens), justified empirically: the last layer is too task-specialised to the contrastive objective.\n\n\n\nfrom transformers import CLIPVisionModel\nimport torch.nn as nn\n\nclass VisualFeatureExtractor(nn.Module):\n    def __init__(self, model_name=\"openai/clip-vit-large-patch14-336\", select_layer=-2):\n        super().__init__()\n        self.vit = CLIPVisionModel.from_pretrained(model_name, output_hidden_states=True)\n        self.select_layer = select_layer  # -2 == penultimate\n\n    @torch.no_grad()\n    def forward(self, pixel_values):\n        out = self.vit(pixel_values=pixel_values, output_hidden_states=True)\n        feats = out.hidden_states[self.select_layer][:, 1:]   # drop CLS, [B, N, d]\n        return feats\n\n\n\nThe projector \\(P : \\mathbb{R}^{d_v} \\to \\mathbb{R}^{d_t}\\) converts visual feature dimension to LLM embedding dimension.\n1. Linear (LLaVA-1) — nn.Linear(1024, 4096). Two-step training: warm-up only this layer, then unfreeze everything.\n2. MLP-2 (LLaVA-1.5) — Linear → GELU → Linear. +1.0 MMMU vs linear.\n3. Q-Former (BLIP-2) — a 12-layer BERT initialised from BERT-base, with 32 learnable query tokens that cross-attend to image features and self-attend to each other. Output: 32 vectors regardless of input resolution.\nclass QFormer(nn.Module):\n    def __init__(self, n_queries=32, d=768, n_layers=12, n_heads=12):\n        super().__init__()\n        self.queries = nn.Parameter(torch.randn(1, n_queries, d) * 0.02)\n        self.layers = nn.ModuleList([QFormerLayer(d, n_heads) for _ in range(n_layers)])\n\n    def forward(self, vision_tokens):\n        q = self.queries.expand(vision_tokens.size(0), -1, -1)\n        for layer in self.layers:\n            q = layer(q, vision_tokens)\n        return q                                      # [B, 32, d]\n4. Perceiver Resampler (Flamingo) — like Q-Former but the queries also see learned positional embeddings indexed by time-of-frame (videos!), and the cross-attention is gated.\n5. Pixel-Shuffle (InternVL-2) — concatenate \\(2 \\times 2\\) neighbouring patch tokens then linearly project. Cuts token count 4× while keeping local information. Used in InternVL-2 to bring 24×24 down to 12×12 = 144 tokens.\n6. C-Abstractor (Honeybee) — convolutions on the 2-D patch grid then flatten. Preserves locality, modest parameter count.\n\n\n\nThere are three schools:\n\nUse the ViT’s own pos-embed and add nothing: the LLM’s RoPE handles tokens by their position in the sequence but not their spatial position. This is what LLaVA does.\nAdd 2-D positional information via the projector (interleave spatial coordinates).\nUse M-RoPE / RoPE-2D in the LLM itself (Qwen2-VL): the LLM is re-trained to interpret rotary frequencies along temporal, height and width axes.\n\n\n\n\nAfter training, run a linear probe: freeze the encoder + projector and train a single linear classifier on ImageNet-1k. If accuracy drops more than 5 % vs the original ViT, your projector is destroying information.\n\n\n\n\n\n\n\n\nThe first question is what role does text play in your VLM?\n\nSymmetric retrieval (CLIP-style) → bidirectional encoder (BERT/RoBERTa) or a causal Transformer with attention-mask = bidirectional only for the [EOS] vector. CLIP uses a causal Transformer with EOS pooling.\nEncoder-decoder VQA (BLIP-style) → BERT-base initialised, repurposed via prompt tokens.\nGenerative dialog (LLaVA-style) → a full LLM (Vicuna, LLaMA, Mistral, Phi, Qwen, Gemma).\n\n\n\n\nThree production algorithms:\n\n\n\n\n\n\n\n\n\nAlgorithm\nUsed by\nVocab\nNotes\n\n\n\n\nBPE\nGPT-2, CLIP, LLaMA-1\n32 k–50 k\nGreedy merging of frequent pairs\n\n\nWordPiece\nBERT\n30 k\nLikelihood-based merging\n\n\nSentencePiece / Unigram\nT5, LLaMA-2, Mistral, Gemma\n32 k–128 k\nProbabilistic, language-agnostic\n\n\n\nFor VLMs, the tokenizer must include special tokens for image placeholders, e.g. &lt;image&gt;, &lt;video&gt;, &lt;roi&gt;, &lt;grounding&gt;. LLaVA reserves the &lt;image&gt; token; Kosmos-2 reserves &lt;box&gt; and &lt;phrase&gt;; Florence-2 reserves &lt;OD&gt;, &lt;OCR&gt;, &lt;CAPTION&gt;.\n\n\n\nA bidirectional encoder:\nfrom transformers import AutoTokenizer, BertModel\ntok = AutoTokenizer.from_pretrained(\"bert-base-uncased\")\nbert = BertModel.from_pretrained(\"bert-base-uncased\")\nout = bert(**tok([\"a dog on grass\"], return_tensors=\"pt\", padding=True))\nprint(out.last_hidden_state.shape, out.pooler_output.shape)\nThe pooler_output is a Linear(d, d) → tanh applied to the [CLS] vector — only meaningful when fine-tuned with a classification head. For retrieval you usually use mean pooling of all token embeddings with attention mask.\n\n\n\nfrom transformers import CLIPTextModel, CLIPTokenizer\ntok = CLIPTokenizer.from_pretrained(\"openai/clip-vit-large-patch14\")\ntext = CLIPTextModel.from_pretrained(\"openai/clip-vit-large-patch14\")\ninputs = tok([\"a photo of a cat\"], padding=True, return_tensors=\"pt\")\nout = text(**inputs)\n# CLIP pools at the EOS position\neos_idx = inputs.input_ids.argmax(dim=-1)\npooled = out.last_hidden_state[torch.arange(eos_idx.size(0)), eos_idx]\nThis pooling-at-EOS detail is famously subtle: because of greedy BPE, the largest token id in the sequence is the EOS token (49407 in CLIP’s tokenizer), so argmax selects it. If you fine-tune CLIP with a tokenizer that has a different id for EOS, you must change this.\n\n\n\nDecoder-only causal LMs dominate. You should know the parameter count, context length, tokenizer, and the position-embedding scheme for each:\n\n\n\n\n\n\n\n\n\n\nModel\nParams\nContext\nTokenizer\nPos-Embed\n\n\n\n\nLLaMA-2\n7 / 13 / 70 B\n4 k\nSentencePiece-BPE, 32 k\nRoPE θ=10000\n\n\nLLaMA-3 / 3.1 / 3.2\n8 / 70 / 405 B\n8 k / 128 k\ntiktoken, 128 k\nRoPE θ=500000\n\n\nMistral-7B / Mixtral-8x7B\n7 / 56 B\n32 k\nBPE, 32 k\nRoPE + sliding window\n\n\nQwen2 / Qwen2.5\n0.5 / 1.5 / 7 / 14 / 32 / 72 B\n128 k\nBPE, 152 k\nRoPE w/ YARN\n\n\nGemma / Gemma-2\n2 / 9 / 27 B\n8 k\nSentencePiece, 256 k\nRoPE\n\n\nPhi-3\n3.8 / 7 / 14 B\n4 k–128 k\ntiktoken, 32 k\nRoPE w/ LongRoPE\n\n\n\n\n\n\nJust like the visual side:\n\nAdd special tokens: tokenizer.add_special_tokens({\"additional_special_tokens\": [\"&lt;image&gt;\", \"&lt;im_patch&gt;\"]}) then model.resize_token_embeddings(len(tokenizer)). Critical: new embeddings are initialised randomly — initialise them to the mean of all existing token embeddings to stabilise training.\nExtend context with RoPE re-scaling: replace \\(\\theta = 10000\\) with \\(\\theta = 500000\\) (NTK-aware scaling) or use YaRN (Peng et al., 2023). Done by Qwen2-VL to support 32 k visual+text tokens.\nReplace the LLM with a Mixture-of-Experts version: same fusion module, swap the dense FFN for a routed FFN. Used by MoE-LLaVA, DeepSeek-VL2.\nAdd LoRA adapters to specific layers instead of full fine-tuning (see Optimization doc).\nFreeze the LM entirely and rely on the projector to “speak” the LM’s language — the BLIP-2 / LLaVA-1 approach.\n\n\n\n\nInside the LLM, visual tokens occupy specific positions in the sequence. There are three masking conventions:\n\nPure causal: visual tokens are causally masked just like text. Easiest, default.\nVisual-prefix bidirectional: visual tokens can attend to all other visual tokens but not text after them; text is causal. Used by some early models.\nBidirectional vision, causal text (Idefics-3): better preserves the ViT’s information.\n\nThe implementation detail is the attention mask, not a different module.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nPooling\nFormula\nUse case\n\n\n\n\nCLS / [SOS]\nh[:, 0]\nBERT classifiers\n\n\nEOS\nh[arange(B), eos_idx]\nCLIP retrieval\n\n\nMean (mask-aware)\n\\(\\frac{\\sum_t m_t h_t}{\\sum_t m_t}\\)\nSentence-BERT, MPNet\n\n\nMax\n\\(\\max_t h_t\\)\nrarely used\n\n\nLast-token\nh[:, -1]\nLLM-based retrievers (E5-Mistral, GTE-Qwen2)\n\n\nWeighted (attention)\n\\(\\sum_t \\alpha_t h_t\\)\nlearned, slightly better\n\n\n\nPyTorch mean-pool with mask:\ndef mean_pool(hidden, attention_mask):\n    mask = attention_mask.unsqueeze(-1).float()\n    return (hidden * mask).sum(1) / mask.sum(1).clamp(min=1e-6)\n\n\n\nWhen you want a single vector per sentence, the Sentence-BERT family (Reimers & Gurevych, 2019) fine-tunes BERT with a siamese contrastive loss. MRL (Kusupati et al., 2022) trains the same embedding to be useful when truncated to multiple dimensions \\(\\{64, 128, 256, 512, 768\\}\\) — relevant for VLMs because shorter embeddings mean cheaper retrieval at deployment time.\n\n\n\nWhen the LLM is the fusion module (LLaVA, Qwen-VL), there is no separate text embedding — the LLM’s own token embeddings are the textual representations. Your fusion module simply produces visual tokens with the same dimensionality and the LLM concatenates them. The “embedding extraction” question becomes: do I want the embedding before any attention, after the first layer, or at the final hidden state?\nFor downstream tasks where you need a fixed-size text vector from a decoder-only LM, use the last token’s hidden state (E5-Mistral approach), or apply an extra &lt;|END|&gt; token and pool there.\n\n\n\nimport torch.nn.functional as F\n\ndef encode_text(text_model, tokenizer, sentences, device=\"cuda\"):\n    tok = tokenizer(sentences, padding=True, truncation=True,\n                    max_length=77, return_tensors=\"pt\").to(device)\n    with torch.no_grad():\n        out = text_model(**tok)\n    eos_idx = tok.input_ids.argmax(dim=-1)\n    h = out.last_hidden_state\n    pooled = h[torch.arange(h.size(0), device=device), eos_idx]\n    return F.normalize(pooled, dim=-1)\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nType\nWhere fusion happens\nExample\n\n\n\n\nNo fusion (late)\nDot-product after both encoders\nCLIP, SigLIP\n\n\nCross-attention fusion\nInside one transformer\nFlamingo, ALBEF, BLIP\n\n\nConcatenative fusion\nTokens prepended to LLM\nLLaVA, MiniGPT-4, Idefics\n\n\nResampler fusion\nFew learned queries summarise vision then concat\nBLIP-2, Flamingo\n\n\nAdapter fusion\nSmall modules inserted into LLM\nLLaMA-Adapter, MMICL\n\n\nUnified fusion\nSingle transformer over (vision_tokens, text_tokens)\nFuyu, Chameleon\n\n\n\n\n\n\nMulti-head attention with separate sources for Q and K/V:\n\\[\n\\text{CrossAttn}(\\mathbf{Q}, \\mathbf{K}, \\mathbf{V}) = \\text{softmax}\\!\\left(\\frac{\\mathbf{Q}\\mathbf{K}^\\top}{\\sqrt{d_k}}\\right)\\mathbf{V}, \\qquad \\mathbf{Q}=\\mathbf{X}_t W_Q,\\; \\mathbf{K}=\\mathbf{X}_v W_K,\\; \\mathbf{V}=\\mathbf{X}_v W_V.\n\\]\nPyTorch implementation:\nclass CrossAttention(nn.Module):\n    def __init__(self, d_text, d_vis, n_heads=8, d_head=64):\n        super().__init__()\n        d = n_heads * d_head\n        self.n_heads = n_heads; self.d_head = d_head\n        self.to_q = nn.Linear(d_text, d, bias=False)\n        self.to_k = nn.Linear(d_vis,  d, bias=False)\n        self.to_v = nn.Linear(d_vis,  d, bias=False)\n        self.to_out = nn.Linear(d, d_text)\n\n    def forward(self, x_text, x_vis, mask=None):\n        B, Nt, _ = x_text.shape\n        Nv = x_vis.shape[1]\n        q = self.to_q(x_text).view(B, Nt, self.n_heads, self.d_head).transpose(1,2)\n        k = self.to_k(x_vis ).view(B, Nv, self.n_heads, self.d_head).transpose(1,2)\n        v = self.to_v(x_vis ).view(B, Nv, self.n_heads, self.d_head).transpose(1,2)\n        attn = torch.nn.functional.scaled_dot_product_attention(q, k, v, attn_mask=mask)\n        attn = attn.transpose(1,2).reshape(B, Nt, -1)\n        return self.to_out(attn)\nNote the use of scaled_dot_product_attention — PyTorch 2 dispatches to Flash-Attention 2 when shapes and dtypes align.\n\n\n\nAdd a learnable gate so the LLM starts as its uni-modal self:\nclass GatedXAttn(nn.Module):\n    def __init__(self, d_text, d_vis, n_heads):\n        super().__init__()\n        self.xattn = CrossAttention(d_text, d_vis, n_heads)\n        self.alpha_xattn = nn.Parameter(torch.zeros(1))\n        self.ff   = nn.Sequential(nn.Linear(d_text, 4*d_text), nn.GELU(), nn.Linear(4*d_text, d_text))\n        self.alpha_ff    = nn.Parameter(torch.zeros(1))\n\n    def forward(self, x_text, x_vis):\n        x_text = x_text + torch.tanh(self.alpha_xattn) * self.xattn(x_text, x_vis)\n        x_text = x_text + torch.tanh(self.alpha_ff)    * self.ff(x_text)\n        return x_text\nInsert one such block every \\(K\\) LLM layers (Flamingo: \\(K=4\\) for the smaller variant). The initial zero-gate is critical: it makes the pre-trained LLM’s behaviour an exact starting point.\n\n\n\nQ-Former has two pathways because it is trained with three losses (ITC, ITM, ITG) that need different attention masks. A single forward pass routes the queries through different self-attention masks depending on the task token:\n\nFor ITC: queries can only see each other; text can only see text. The two CLS representations contrast.\nFor ITM: queries see image features and full bidirectional text — used to compute a binary score.\nFor ITG: queries see image features; text is causally masked (so the model can be used as a captioner).\n\nclass QFormerLayer(nn.Module):\n    def __init__(self, d=768, n_heads=12):\n        super().__init__()\n        self.self_attn  = nn.MultiheadAttention(d, n_heads, batch_first=True)\n        self.cross_attn = nn.MultiheadAttention(d, n_heads, batch_first=True)\n        self.ff = nn.Sequential(nn.Linear(d, 4*d), nn.GELU(), nn.Linear(4*d, d))\n        self.ln1, self.ln2, self.ln3 = nn.LayerNorm(d), nn.LayerNorm(d), nn.LayerNorm(d)\n\n    def forward(self, q, v):\n        h = self.ln1(q)\n        q = q + self.self_attn(h, h, h, need_weights=False)[0]\n        h = self.ln2(q)\n        q = q + self.cross_attn(h, v, v, need_weights=False)[0]\n        q = q + self.ff(self.ln3(q))\n        return q\n\n\n\nclass MLPProjector(nn.Module):\n    def __init__(self, d_vis, d_lm):\n        super().__init__()\n        self.proj = nn.Sequential(\n            nn.Linear(d_vis, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        )\n    def forward(self, x):                              # x: [B, N, d_vis]\n        return self.proj(x)                            # -&gt; [B, N, d_lm]\nUsed by 90 % of recent open-source VLMs. Initialization tip: zero-init the second linear’s bias and use Kaiming for weights to keep activations bounded.\n\n\n\nInternVL-2’s trick to cut visual tokens 4×:\ndef pixel_shuffle_2d(x, scale=2):\n    # x: [B, N, d] with N = h*w; halve spatial, multiply channels by scale**2\n    B, N, d = x.shape\n    h = w = int(N**0.5)\n    x = x.view(B, h, w, d)\n    x = x.view(B, h//scale, scale, w//scale, scale, d).permute(0, 1, 3, 2, 4, 5)\n    x = x.reshape(B, (h//scale) * (w//scale), d * scale * scale)\n    return x\n\nclass PixelShuffleProjector(nn.Module):\n    def __init__(self, d_vis, d_lm, scale=2):\n        super().__init__()\n        self.scale = scale\n        self.proj = nn.Sequential(\n            nn.LayerNorm(d_vis * scale * scale),\n            nn.Linear(d_vis * scale * scale, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        )\n    def forward(self, x):\n        return self.proj(pixel_shuffle_2d(x, self.scale))\n\n\n\nInsert a low-rank adapter into the LLM’s FFN or attention layers, with conditioning from visual features:\n\\[\n\\Delta \\mathbf{h} = \\alpha \\cdot \\text{cross-attn}(\\mathbf{h}, \\mathbf{V}) \\cdot B^\\top A^\\top\n\\]\nwhere \\(A \\in \\mathbb{R}^{r \\times d}\\), \\(B \\in \\mathbb{R}^{d \\times r}\\), \\(r \\ll d\\). LLaMA-Adapter-V2 does exactly this.\n\n\n\n\n\n\n\n\n\n\nSituation\nRecommended fusion\n\n\n\n\nTrain a CLIP-replacement on 1 B pairs\nNone — dual encoder + InfoNCE/SigLIP\n\n\nTrain a chatbot on a frozen LLM with limited compute\nMLP projector + LoRA\n\n\nTrain a model that must answer about 100s of frames\nQ-Former + temporal pos-embed\n\n\nNeed few-shot in-context learning across images\nGated cross-attention (Flamingo)\n\n\nWant highest token efficiency (long doc + image)\nPixel-shuffle + AnyRes\n\n\nNeed image generation too\nUnified VQ-VAE tokens + LM (Chameleon)\n\n\n\n\n\n\n\nForgetting LayerNorm on the projector input — visual features and LM embeddings live on different scales; un-normalised, the LM ignores them.\nWrong sequence length — never forget to pad and use attention_mask.\nPosition confusion: when you prepend visual tokens, the LLM’s RoPE indexes them as if they were text. If your LLM has RoPE θ=10000 trained on 4 k context, suddenly seeing 576 extra tokens can blow context length.\nMixed dtypes: keep projector in bfloat16 matching the LLM. A float32 projector silently up-casts the entire compute graph.\n\n\n\n\n\n\n\n\n\nEvery modern projection-style VLM is assembled the same way. Memorise these seven steps.\n\nPick the vision encoder. Default: SigLIP-So400m/14 @ 384. Faster + smaller: CLIP-ViT-L/14 @ 336. Best dense: DINOv2-Giant.\nPick the LLM backbone. Default: Qwen2.5-7B-Instruct. Tiny: Phi-3-mini. Huge: LLaMA-3-70B.\nPick the resolution strategy. Single-tile 384², or AnyRes 4-tile, or native any-resolution with M-RoPE.\nPick the projector. Default: 2-layer MLP. Token-efficient: Pixel-shuffle ×2 + MLP. Few queries: Q-Former-32.\nDecide what is frozen. Stage 1 (alignment): freeze ViT and LLM, train projector. Stage 2 (instruction): unfreeze LLM (or LoRA), keep ViT frozen or unfreeze the last 4 layers.\nChoose the training data. Pre-train on captions (LAION-2B, COYO, DataComp). Instruction-tune on LLaVA-Mix-665k, ShareGPT-4V, LLaVA-OneVision-mid.\nChoose evaluation suite. MMMU, MathVista, DocVQA, ChartQA, AI2D, MMBench, BLINK, VQAv2, OK-VQA, TextVQA.\n\n\n\n\nimport torch\nimport torch.nn as nn\nfrom transformers import (\n    AutoTokenizer, AutoModelForCausalLM,\n    SiglipVisionModel, SiglipImageProcessor,\n)\n\n\nclass VLM(nn.Module):\n    \"\"\"A LLaVA-style VLM: SigLIP vision tower + MLP projector + frozen-or-LoRA LLM.\"\"\"\n\n    def __init__(\n        self,\n        vision_name=\"google/siglip-so400m-patch14-384\",\n        llm_name=\"Qwen/Qwen2.5-7B-Instruct\",\n        image_token=\"&lt;image&gt;\",\n        select_layer=-2,\n    ):\n        super().__init__()\n        self.tokenizer = AutoTokenizer.from_pretrained(llm_name)\n        if image_token not in self.tokenizer.get_vocab():\n            self.tokenizer.add_special_tokens({\"additional_special_tokens\": [image_token]})\n        self.image_token_id = self.tokenizer.convert_tokens_to_ids(image_token)\n\n        self.vision = SiglipVisionModel.from_pretrained(vision_name)\n        self.processor = SiglipImageProcessor.from_pretrained(vision_name)\n        self.llm = AutoModelForCausalLM.from_pretrained(\n            llm_name, torch_dtype=torch.bfloat16, attn_implementation=\"flash_attention_2\"\n        )\n        self.llm.resize_token_embeddings(len(self.tokenizer))\n\n        d_vis = self.vision.config.hidden_size\n        d_lm = self.llm.config.hidden_size\n        self.projector = nn.Sequential(\n            nn.LayerNorm(d_vis),\n            nn.Linear(d_vis, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        ).to(torch.bfloat16)\n\n        self.select_layer = select_layer\n\n    def freeze_for_alignment(self):\n        for p in self.vision.parameters():\n            p.requires_grad = False\n        for p in self.llm.parameters():\n            p.requires_grad = False\n\n    def encode_vision(self, pixel_values):\n        out = self.vision(pixel_values=pixel_values, output_hidden_states=True)\n        feats = out.hidden_states[self.select_layer]                    # [B, N, d_vis]\n        return self.projector(feats)                                     # [B, N, d_lm]\n\n    def _embed_inputs(self, input_ids, pixel_values):\n        \"\"\"Replace every &lt;image&gt; token's embedding with vision tokens.\"\"\"\n        inputs_embeds = self.llm.get_input_embeddings()(input_ids)       # [B, T, d_lm]\n        vision_embeds = self.encode_vision(pixel_values)                 # [B, N, d_lm]\n        merged, new_attn_mask = [], []\n        for b in range(input_ids.size(0)):\n            img_pos = (input_ids[b] == self.image_token_id).nonzero(as_tuple=True)[0]\n            if len(img_pos) == 0:\n                merged.append(inputs_embeds[b]); continue\n            assert len(img_pos) == 1, \"Use one &lt;image&gt; per sample in this minimal demo\"\n            p = img_pos.item()\n            merged.append(torch.cat([inputs_embeds[b, :p],\n                                     vision_embeds[b],\n                                     inputs_embeds[b, p+1:]], dim=0))\n        merged = nn.utils.rnn.pad_sequence(merged, batch_first=True)\n        return merged\n\n    def forward(self, input_ids, pixel_values, labels=None):\n        inputs_embeds = self._embed_inputs(input_ids, pixel_values)\n        outputs = self.llm(inputs_embeds=inputs_embeds, labels=labels)\n        return outputs\nThis is essentially the architecture of LLaVA-1.5, Bunny, Imp, and most open-source 7B-class VLMs published in 2024. Read it line by line until you can rewrite it from memory.\n\n\n\nfrom PIL import Image\nmodel = VLM().eval().to(\"cuda\")\nmodel.freeze_for_alignment()\n\nimage = Image.open(\"dog.jpg\").convert(\"RGB\")\npixel_values = model.processor(images=image, return_tensors=\"pt\")[\"pixel_values\"].to(\"cuda\", dtype=torch.bfloat16)\n\nprompt = \"&lt;image&gt;\\nQuestion: What animal is in the picture?\\nAnswer:\"\ntok = model.tokenizer(prompt, return_tensors=\"pt\").to(\"cuda\")\n\nwith torch.no_grad():\n    out = model(input_ids=tok.input_ids, pixel_values=pixel_values)\nprint(out.logits.shape)\n\n\n\nThe differences from LLaVA-1.5:\n\nprocessor returns multiple tiles plus a thumbnail.\nencode_vision runs the ViT on each tile (with possible gradient checkpointing), then concatenates patch tokens.\nA newline token is inserted between rows of tiles so the LLM can reason about layout.\n\ndef encode_anyres(self, tiles):\n    # tiles: [B, T, 3, H, W] where T = num_tiles_per_image (variable)\n    B, T = tiles.shape[:2]\n    tiles_flat = tiles.flatten(0, 1)\n    feats = self.encode_vision(tiles_flat)                  # [B*T, N, d_lm]\n    feats = feats.view(B, T, -1, feats.size(-1))            # [B, T, N, d_lm]\n    return feats.reshape(B, T*feats.size(2), -1)            # concat tiles\n\n\n\nQwen2-VL drops the tiling entirely. The ViT is retrained to operate on arbitrary \\(H \\times W\\) via M-RoPE (multi-dimensional rotary embeddings). The vision encoder outputs a variable number of patch tokens; each is given a (t, y, x) triple consumed by the LLM’s M-RoPE. The trick that makes the LM happy: split RoPE channels into thirds — one for temporal, one for height, one for width.\n\n\n\nFor multiple images, each gets its own &lt;image&gt; placeholder and its own visual-token block:\n&lt;image&gt;{img1}&lt;image&gt;{img2} What is different between these two pictures?\nFor video, sample \\(T\\) frames, run each through the ViT, concat with temporal embeddings, optionally compress via Q-Former with shared queries across frames (Video-LLaMA, Video-ChatGPT).\n\n\n\nBefore launching a multi-day run, verify:\n\nOne sample forward — loss.backward() does not throw.\nLoss decreases on a tiny set — overfit 16 samples and watch loss → ~0.\nVisual token count matches — print inputs_embeds.shape.\n&lt;image&gt; token is masked in labels — the LLM should not be supervised to predict the image token.\nNo mixed-dtype warnings — everything bf16 except the vision encoder LayerNorms which can stay fp32.\n\n\n\n\n\nToken efficiency vs accuracy — is 144 tokens ever enough for OCR / charts? Most evidence says no; pixel-shuffle helps but DocVQA still wants ≥ 729 tokens.\nGenerative vs discriminative pre-training — does masked image modelling (BEiT-3, EVA) beat contrastive (SigLIP) when used as a VLM tower? Currently a tie; depends on downstream task.\nEncoder-free VLMs — Fuyu / Chameleon throw away the ViT. The 2024 evidence is that they need 5–10× the data; not yet a free lunch.\nModality gap — does it matter for downstream? Liang et al. 2022 say yes, but recent work (Schrodi et al. 2024) argues the gap is partly a measurement artefact.\nVisual tokenisation for generation — VQ-VAEs vs continuous tokens vs Magvit-v2 finite-scalar quantisation.\n\n\n\n\n\n\n\n\n\n\nJay Alammar, “The Illustrated Transformer” — required prerequisite.\nJay Alammar, “The Illustrated CLIP” and “The Illustrated Stable Diffusion”.\nLilian Weng, “Contrastive Representation Learning” (May 2021).\nLilian Weng, “Self-Supervised Representation Learning” (Nov 2019 / Jun 2021).\nLilian Weng, “Generalized Visual Language Models” (Jun 2022).\nLilian Weng, “Attention? Attention!”.\nSebastian Raschka, “Understanding Multimodal LLMs” (2024) — code-level walkthrough.\nYannic Kilcher’s YouTube paper reviews for CLIP, Flamingo, BLIP-2, LLaVA.\nHuggingFace blog: “Vision Language Models Explained” (April 2024), “Idefics 3” (Aug 2024).\n\n\n\n\n\nRadford et al., Learning Transferable Visual Models From Natural Language Supervision (CLIP), 2021.\nJia et al., Scaling Up Visual and Vision-Language Representation Learning With Noisy Text Supervision (ALIGN), 2021.\nZhai et al., Sigmoid Loss for Language Image Pre-Training (SigLIP), 2023.\nLi et al., ALBEF: Align before Fuse, NeurIPS 2021.\nLi et al., BLIP: Bootstrapping Language-Image Pre-training, ICML 2022.\nLi et al., BLIP-2: Bootstrapping Language-Image Pre-training with Frozen Encoders and Large Language Models, ICML 2023.\nAlayrac et al., Flamingo: a Visual Language Model for Few-Shot Learning, NeurIPS 2022.\nLiu et al., Visual Instruction Tuning (LLaVA), NeurIPS 2023.\nLiu et al., Improved Baselines with Visual Instruction Tuning (LLaVA-1.5), 2023.\nLiu et al., LLaVA-NeXT technical report, 2024.\nBai et al., Qwen-VL and Qwen2-VL technical reports, 2023 / 2024.\nChen et al., InternVL and InternVL 2.5 technical reports, 2023 / 2024.\nLaurençon et al., Idefics2 / Idefics3, HuggingFace, 2024.\nXiao et al., Florence-2, 2024.\nTeam Chameleon (Meta), Chameleon: Mixed-Modal Early-Fusion Foundation Models, 2024.\nBavishi et al., Fuyu-8B, Adept, 2023.\nDosovitskiy et al., An Image Is Worth 16×16 Words (ViT), ICLR 2021.\nHe et al., Masked Autoencoders Are Scalable Vision Learners (MAE), CVPR 2022.\nCaron et al., Emerging Properties in Self-Supervised Vision Transformers (DINO), ICCV 2021.\nOquab et al., DINOv2, 2023.\nDarcet et al., Vision Transformers Need Registers, ICLR 2024.\nvan den Oord et al., Representation Learning with Contrastive Predictive Coding (InfoNCE), 2018.\n\n\n\n\n\ntorch.nn.functional.scaled_dot_product_attention\ntorch.nn.MultiheadAttention\ntorch.compile\ntransformers.CLIPVisionModel, transformers.SiglipVisionModel, transformers.AutoModelForCausalLM\ntransformers.AutoProcessor (handles both image and text preprocessing)\naccelerate.Accelerator (covered in Deployment doc)\n\n\n\n\nLAION-2B-en, LAION-COCO, COYO-700M, DataComp-1B, RedCaps, CC12M, SBU, Visual Genome, COCO Captions, TextCaps, DocVQA, ChartQA, OCR-VQA, A-OKVQA, GQA, VQAv2, OK-VQA, MMMU, MathVista, AI2D, MMBench, MMBench-CN, MMVet, BLINK, RealWorldQA, ScienceQA, ShareGPT-4V, ALLaVA, LLaVA-OneVision-Data, Cauldron (Idefics-2’s instruction mix).\n\n\n\nA complete study cycle is:\n\nRead end to end once without coding.\nRe-read with PyTorch open and run each snippet.\nPick three architectures (e.g. CLIP, BLIP-2, LLaVA-NeXT) and re-implement them from scratch in a single notebook.\nRe-read the Reading List and pick five papers per family.\nWrite a one-page comparison of any two papers using the eight-point rubric from §1.9.\n\nIf you can do all five, you are an architecture expert.\n\nEnd of Vision-Language Model Architecture document."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#what-is-a-vision-language-model-really",
    "href": "Resources/Core5/Architecture/index.html#what-is-a-vision-language-model-really",
    "title": "VLM Architecture",
    "section": "",
    "text": "A Vision-Language Model (VLM) is a single differentiable function\n\\[\nf_\\theta : (\\mathcal{I}, \\mathcal{T}) \\;\\longrightarrow\\; \\mathcal{Y}\n\\]\nthat consumes an image (or video) \\(\\mathcal{I}\\) and a text \\(\\mathcal{T}\\), and emits some target \\(\\mathcal{Y}\\) — a class label, a similarity score, a caption, an answer, a bounding box, a segmentation mask, or another image. Internally every modern VLM decomposes into four building blocks:\n\nVisual encoder \\(E_v\\) — turns pixels into a sequence (or set) of visual tokens \\(\\mathbf{V} \\in \\mathbb{R}^{N_v \\times d_v}\\).\nText encoder / language model \\(E_t\\) — turns sub-word tokens into textual representations \\(\\mathbf{T} \\in \\mathbb{R}^{N_t \\times d_t}\\).\nFusion module \\(F\\) — aligns \\(\\mathbf{V}\\) and \\(\\mathbf{T}\\) in a shared space and lets them interact.\nTask head \\(H\\) — projects the fused representation to the output space.\n\nEvery architecture you will ever read about — CLIP, ALIGN, BLIP, BLIP-2, Flamingo, LLaVA, MiniGPT-4, Qwen-VL, Kosmos, PaLI, Florence-2, Chameleon — is a particular choice for these four blocks.\nWe will now master every block."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#table-of-contents",
    "href": "Resources/Core5/Architecture/index.html#table-of-contents",
    "title": "VLM Architecture",
    "section": "",
    "text": "Comparing Model Architectures\nLearning Representations\nCustomizing Visual Encoders\nExtracting Visual Embeddings\nCustomizing Text Encoders\nExtracting Textual Embeddings\nImplementing Custom Fusion Modules\nAssembling Multimodal Architecture\nReading List & References"
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#comparing-model-architectures",
    "href": "Resources/Core5/Architecture/index.html#comparing-model-architectures",
    "title": "VLM Architecture",
    "section": "",
    "text": "Modern VLMs split cleanly into four design families. Memorise this taxonomy — examiners love it.\n\n\n\n\n\n\n\n\n\n\n\n\n\nFamily\nCanonical model\nVision encoder\nFusion\nDecoder\nTrained for\n\n\n\n\nDual-encoder (contrastive)\nCLIP, ALIGN, SigLIP\nViT-L/14\nnone (dot-product)\nnone\nRetrieval, zero-shot classification\n\n\nEncoder-decoder (fusion-in-encoder)\nViLBERT, LXMERT, ALBEF, BLIP\nViT or Faster-RCNN\ncross-attention layers\nsmall text decoder\nVQA, captioning, ITM\n\n\nFrozen-LLM (projection-style)\nFlamingo, BLIP-2, LLaVA, MiniGPT-4\nfrozen ViT (CLIP/EVA)\nQ-Former / MLP / Resampler\nfrozen or LoRA LLM\nOpen-ended dialog, instruction following\n\n\nEarly-fusion / unified tokenization\nFuyu, Chameleon, Gemini-Nano, Unified-IO\nlinear patch projector\nnone (single transformer)\nsingle transformer\nAny-to-any modality\n\n\n\nThe progression is historical: dual-encoders proved alignment was learnable from scale; encoder-decoders showed cross-attention beats concatenation for VQA; frozen-LLM models exploited LLM emergent abilities while paying a tiny compute price; early-fusion models removed the modality boundary entirely.\n\n\n\nCLIP — Contrastive Language–Image Pre-training — is the bedrock. Two encoders, one objective:\n\\[\n\\mathcal{L}_{\\text{CLIP}} = \\tfrac{1}{2}\\!\\left[ \\mathcal{L}_{i\\to t} + \\mathcal{L}_{t \\to i} \\right], \\qquad\n\\mathcal{L}_{i\\to t} = -\\frac{1}{B}\\sum_{i=1}^B \\log \\frac{\\exp(\\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_i \\rangle / \\tau)}{\\sum_{j=1}^B \\exp(\\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_j \\rangle / \\tau)}\n\\]\nwhere \\(\\hat{\\mathbf{v}}_i\\) and \\(\\hat{\\mathbf{t}}_i\\) are L2-normalised image and text embeddings, \\(\\tau\\) is a learnable temperature, and \\(B\\) is the mini-batch size. Read Jay Alammar’s “Illustrated CLIP” and Lilian Weng’s “Contrastive Representation Learning” post for visual intuition.\n# Minimal CLIP forward pass\nimport torch, torch.nn as nn, torch.nn.functional as F\n\nclass MiniCLIP(nn.Module):\n    def __init__(self, vision_enc, text_enc, d=512):\n        super().__init__()\n        self.v = vision_enc\n        self.t = text_enc\n        self.v_proj = nn.Linear(vision_enc.out_dim, d, bias=False)\n        self.t_proj = nn.Linear(text_enc.out_dim, d, bias=False)\n        self.logit_scale = nn.Parameter(torch.tensor(2.6592))  # log(1/0.07)\n\n    def forward(self, images, tokens):\n        v = F.normalize(self.v_proj(self.v(images)), dim=-1)\n        t = F.normalize(self.t_proj(self.t(tokens)), dim=-1)\n        logits = self.logit_scale.exp() * v @ t.T\n        return logits, logits.T   # image-to-text, text-to-image\nKey properties:\n\nMassive negative samples: with batch size \\(B\\), every image sees \\(B-1\\) negatives at no extra cost.\nModality gap: the two embedding clouds occupy distinct sub-regions of the unit sphere even after training (Liang et al., 2022). The cosine similarities are still discriminative but the absolute geometry is not aligned — relevant when you want to use CLIP embeddings in downstream classifiers.\nScaling laws: CLIP improves roughly as \\(\\text{acc} \\propto \\log(N_{\\text{params}}) + \\log(N_{\\text{samples}})\\).\n\n\n\n\nSigLIP replaces the row-wise softmax with a pair-wise sigmoid:\n\\[\n\\mathcal{L}_{\\text{SigLIP}} = -\\frac{1}{B} \\sum_{i=1}^B \\sum_{j=1}^B \\log \\sigma\\!\\big(z_{ij}\\cdot ( \\tau \\langle \\hat{\\mathbf{v}}_i, \\hat{\\mathbf{t}}_j\\rangle + b )\\big)\n\\]\nwith \\(z_{ij} = +1\\) if \\(i = j\\) else \\(-1\\), and a learnable bias \\(b\\). Why is this important?\n\nThe softmax in CLIP forces global normalisation across the batch — every device must hold all logits during back-prop. SigLIP factorises and tolerates tiny batch sizes (down to 256) while matching CLIP at 32 k.\nRobust to label-noise because each pair is independently labelled.\nAll modern VLMs (PaliGemma, Idefics-3, Molmo, InternVL-2) use SigLIP-So400m as the vision encoder.\n\n\n\n\nBLIP introduces a Multi-modal Mixture-of-Encoder-Decoder (MED): one ViT plus a text transformer that can act as\n\nUnimodal text encoder (for ITC — image-text contrastive)\nImage-grounded text encoder (for ITM — image-text matching)\nImage-grounded text decoder (for LM — captioning)\n\nAll three modes share weights except for the self-attention and cross-attention layers, with prompt tokens distinguishing the role. Training combines three losses:\n\\[\n\\mathcal{L}_{\\text{BLIP}} = \\mathcal{L}_{\\text{ITC}} + \\mathcal{L}_{\\text{ITM}} + \\mathcal{L}_{\\text{LM}}.\n\\]\nBLIP also introduces CapFilt — a captioner and filter trained on the same model that bootstraps clean captions from noisy web alt-text. Many “synthetic-caption” papers (LLaVA-1.5, ShareGPT-4V) descend from this idea.\n\n\n\nBLIP-2 freezes a vision encoder (ViT-g, EVA-CLIP) and a language model (OPT, Flan-T5) and learns only a tiny Q-Former: 32 learnable query tokens that cross-attend to image features.\nPre-training has two stages:\n\nRepresentation learning — train Q-Former with ITC + ITM + ITG losses against the frozen vision encoder.\nGenerative learning — connect Q-Former output to the frozen LLM via a linear projection and train on captioning.\n\nQ-Former parameter count: ~188 M, versus 1.2 B for the LLM and 1 B for the ViT-g. This is the architectural pattern that lets the field iterate on “a tiny adapter, frozen giants on both sides.”\n\n\n\nLLaVA proves that a single linear layer (LLaVA-1) or a 2-layer MLP (LLaVA-1.5) is sufficient to connect a frozen CLIP-ViT-L/14 to a frozen Vicuna/LLaMA-2. Training is two-stage:\n\nFeature alignment: train only the projector on 558 k LAION-CC-SBU caption pairs.\nInstruction tuning: unfreeze the LLM and projector, train on 150 k GPT-4-generated multimodal instructions.\n\nLLaVA’s insight is that the LLM is already a multimodal reasoner if you can speak its language — and the projector’s job is to translate visual tokens into something the LLM treats as foreign-language tokens.\nLLaVA-NeXT (1.6) and LLaVA-OneVision extend this with AnyRes dynamic tiling: an image is split into a 2×2 or 3×3 grid of 336² patches plus a global thumbnail, each tile is encoded separately, and tokens are concatenated. This unlocks 672² or 1008² resolution without retraining the ViT.\n\n\n\nFlamingo interleaves gated cross-attention blocks inside a frozen LLM:\n\\[\n\\mathbf{h}^{(l+1)} = \\mathbf{h}^{(l)} + \\tanh(\\alpha_l) \\cdot \\text{CrossAttn}(\\mathbf{h}^{(l)}, \\mathbf{V}) + \\text{FFN}(\\cdot)\n\\]\nThe scalar gate \\(\\alpha_l\\) is initialised to 0 so the LLM behaves identically to its uni-modal self at the start of training, then gradually opens. A Perceiver Resampler compresses arbitrary numbers of image features into 64 fixed tokens per image, allowing interleaved image-text sequences.\nFlamingo handles few-shot multimodal prompting natively because the LLM still sees a single token stream.\n\n\n\nChameleon (Meta, 2024) and Fuyu-8B (Adept, 2023) eliminate the separate vision encoder. Pixels are turned into discrete tokens (Chameleon via VQ-VAE, Fuyu via a linear patch projector) and prepended to text tokens in a single decoder-only transformer.\nPros: no modality gap, generation across modalities, scaling laws are identical to text LLMs.\nCons: vision pre-training of a giant transformer is enormously expensive and discards the inductive biases of CNN/ViT — generally requires 10× more data.\n\n\n\nA complete comparison must touch:\n\nVision encoder choice and resolution (ViT-L/14 @ 224 vs SigLIP-So400m @ 384 vs AnyRes 672).\nLLM backbone and parameter count (Vicuna-7B vs LLaMA-2-13B vs Qwen-2.5-72B vs Phi-3).\nFusion topology (concatenation / cross-attn / Q-Former / MLP).\nTraining data scale and composition (image-text pairs, OCR, charts, video, agentic web traces).\nPre-training objectives (ITC, ITM, LM, NTP, masked-anywhere).\nToken budget per image (1, 32, 64, 144, 256, 729, 2880…).\nResolution strategy (fixed / dynamic tiling / native any-resolution / native-pack).\nBenchmark numbers (MMMU, MathVista, DocVQA, ChartQA, MMBench, VQAv2, AI2D, BLINK).\n\nMemorise this list; it is the rubric for any architectural-comparison question."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#learning-representations",
    "href": "Resources/Core5/Architecture/index.html#learning-representations",
    "title": "VLM Architecture",
    "section": "",
    "text": "A representation is a vector that carries information about the input in a form that is useful to downstream tasks while being invariant to nuisance variation. Bengio’s 2013 “Representation Learning: A Review and New Perspectives” defines the three desiderata: smoothness, multiple explanatory factors, and hierarchy. Lilian Weng’s “Self-Supervised Learning” post (Nov 2019, updated 2021) is the canonical free tutorial.\nThere are four ways a VLM learns representations.\n\n\n\nThe InfoNCE objective (van den Oord et al., 2018) is the workhorse:\n\\[\n\\mathcal{L}_{\\text{InfoNCE}} = -\\mathbb{E}\\!\\left[ \\log \\frac{\\exp(s(q, k_+)/\\tau)}{\\exp(s(q,k_+)/\\tau) + \\sum_{k_-} \\exp(s(q,k_-)/\\tau)} \\right]\n\\]\nwhere \\(q\\) is a query embedding, \\(k_+\\) is the positive (paired) key, and the sum runs over \\(N-1\\) negatives. As \\(N \\to \\infty\\), InfoNCE lower-bounds the mutual information \\(I(q;k_+)\\).\nFor VLMs the query/positive pair is an (image, text) sample; the negatives are the other items in the batch (CLIP) or items in a queue (MoCo / ALBEF). The key engineering questions are:\n\nHow many negatives? More is better up to ~32 k for CLIP; the marginal gain plateaus.\nHard-negative mining: ALBEF samples cross-modal negatives weighted by current similarity scores — this dramatically accelerates convergence.\nTemperature \\(\\tau\\): smaller \\(\\tau\\) → sharper distribution → harder optimisation but better separation. Setting \\(\\tau\\) learnable (CLIP) often beats fixed.\nMomentum encoder: a slow EMA copy of the encoder produces consistent keys (He et al., MoCo). ALBEF uses a momentum distillation loss too.\n\n\n\n\nA binary cross-entropy classifier sits on top of the fused [CLS] token and predicts “is this image-text pair a real match?” Half the batch is real pairs, half are hard negatives sampled with weights proportional to current similarity. ITM is cheap and effective but only useful when the model is encoder-style (it needs cross-attention).\n\n\n\nThis is plain next-token prediction conditioned on the image:\n\\[\n\\mathcal{L}_{\\text{LM}} = -\\sum_{t=1}^T \\log p_\\theta(w_t \\mid w_{&lt;t}, \\mathbf{V}).\n\\]\nIt produces a generative model from the very start. Required for any model that must caption or chat. LLaVA and BLIP-2 use a masked variant where the loss is computed only on assistant tokens, not user-instruction tokens — this is the “selective masking” detail you should always mention.\n\n\n\nMask 50–75 % of image patches and 15 % of text tokens; predict the missing ones from the remaining context. The image targets can be:\n\nRaw pixels (MAE).\nDiscrete visual tokens from a pre-trained dVAE (BEiT, BEiT-3).\nFeatures from a separate teacher (data2vec, EVA — predicts CLIP features).\n\nMasked modelling rewards local representations, while contrastive learning rewards global invariances — combining them (FLAVA, BEiT-3) gives both.\n\n\n\nFlorence-2 (Microsoft, 2024) demonstrates that prompting a single sequence-to-sequence model with task-specific prompts (&lt;OD&gt; for detection, &lt;CAPTION&gt; for captioning, &lt;OCR&gt; for text reading) is sufficient — provided the pre-training data covers all tasks. The representation learned is the shared encoder hidden state.\n\n\n\nWang & Isola (2020) show that contrastive losses can be decomposed into\n\\[\n\\mathcal{L}_{\\text{align}} = \\mathbb{E}\\big[\\|f(x) - f(x^+)\\|^2\\big], \\qquad\n\\mathcal{L}_{\\text{uniform}} = \\log \\mathbb{E}\\big[\\exp(-2 \\|f(x) - f(y)\\|^2)\\big].\n\\]\nA good encoder minimises both: positives collapse together (alignment), random samples spread on the unit sphere (uniformity). This decomposition is a great mental model for why contrastive learning works.\n\n\n\nThree protocols to evaluate a frozen representation:\n\nLinear probe: freeze the encoder, train a linear classifier on a labelled dataset.\nk-NN probe: classify by the dominant label among the \\(k\\) nearest training embeddings.\nZero-shot: in CLIP-style models, build a classifier by encoding text prompts and computing dot products.\n\nA model that wins on zero-shot but loses on linear probe is producing a task-specific representation rather than a general one. Watch for this in papers.\n\n\n\n\nRetrieval / zero-shot classification → InfoNCE only (CLIP, SigLIP).\nVQA / classification head → InfoNCE + ITM + masked LM (ALBEF, BLIP).\nOpen-ended dialog / OCR / detection → generative LM on top of a frozen contrastive backbone (BLIP-2, LLaVA, Florence-2).\nAny-to-any / image generation included → unified tokenisation + LM (Chameleon, Unified-IO 2)."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#customizing-visual-encoders",
    "href": "Resources/Core5/Architecture/index.html#customizing-visual-encoders",
    "title": "VLM Architecture",
    "section": "",
    "text": "Family\nExamples\nInductive bias\nToken count for 224²\nWhen to use\n\n\n\n\nCNN\nResNet-50, ConvNeXt-XL, RegNetY\nLocality, translation equivariance\n49 (after 7×7 pool)\nEdge devices, dense prediction\n\n\nVision Transformer (ViT)\nViT-L/14, DeiT, EVA-02\nAlmost none; global attention\n256 (16² patches) or 196 (14²)\nAnything that scales\n\n\nHybrid / Hierarchical\nSwin, ConvNeXt-V2, BEiT-3\nHierarchical pooling + windowed attention\nvaries\nObject detection, segmentation\n\n\n\nA VLM almost always uses a ViT today because (a) it produces a clean sequence of tokens that drops straight into a transformer-based fusion module, and (b) it scales with data better than CNNs.\n\n\n\nGiven an image \\(x \\in \\mathbb{R}^{H \\times W \\times 3}\\) and patch size \\(P\\):\n\nPatchify: reshape into \\(N = HW/P^2\\) patches of size \\(P \\times P \\times 3 = 3P^2\\) floats, then linearly project to dimension \\(d\\). This is a convolution with kernel \\(P\\) and stride \\(P\\).\nPrepend [CLS] token: a learnable vector \\(\\mathbf{z}_{\\text{cls}} \\in \\mathbb{R}^d\\).\nAdd positional embeddings \\(\\mathbf{p} \\in \\mathbb{R}^{(N+1)\\times d}\\) — learned (ViT) or sinusoidal (CLIP-RN) or RoPE-2D (Qwen2-VL).\nL transformer blocks: each is\n\\[\n\\mathbf{z}^\\prime = \\mathbf{z} + \\text{MHSA}(\\text{LN}(\\mathbf{z})), \\quad \\mathbf{z} = \\mathbf{z}^\\prime + \\text{MLP}(\\text{LN}(\\mathbf{z}^\\prime)).\n\\]\nFinal LN, output: either the [CLS] embedding or all \\(N\\) patch embeddings.\n\nVariant cheat-sheet:\n\nViT-B/16: 12 layers, 12 heads, \\(d=768\\) — 86 M params.\nViT-L/14: 24 layers, 16 heads, \\(d=1024\\) — 304 M params.\nViT-H/14: 32 layers, 16 heads, \\(d=1280\\) — 632 M params.\nViT-g/14: 40 layers, 16 heads, \\(d=1408\\) — 1.0 B params.\nSigLIP-So400m/14: 27 layers, 16 heads, \\(d=1152\\) — 400 M params, optimal “shape” by SoViT (Alabdulmohsin et al., 2023).\nEVA-02-Large: ViT-L with RoPE-2D + xattn + sub-LN + masked image modelling pre-training; the strongest ~300 M parameter encoder publicly.\n\n\n\n\nimport torch, torch.nn as nn\nfrom transformers import CLIPVisionModel, SiglipVisionModel\n\n# 1) CLIP-ViT-L/14 from OpenAI weights\nclip = CLIPVisionModel.from_pretrained(\"openai/clip-vit-large-patch14-336\")\n# 2) SigLIP-So400m from Google (used by Gemini-Nano, PaliGemma, Idefics-3)\nsig  = SiglipVisionModel.from_pretrained(\"google/siglip-so400m-patch14-384\")\n\n# Both return BaseModelOutputWithPooling: .last_hidden_state [B, 1+N, d], .pooler_output [B, d]\nimg = torch.randn(2, 3, 336, 336)\nout = clip(pixel_values=img)\nprint(out.last_hidden_state.shape, out.pooler_output.shape)\n\n\n\nYou will be asked to modify an encoder. Six standard surgeries:\n\n\nTo support higher resolution without re-training, interpolate positional embeddings:\ndef interpolate_pos_embed(pos_embed, new_grid):\n    # pos_embed: [1, 1+N_old, d]; new_grid: int H_new == W_new in patches\n    cls, grid = pos_embed[:, :1], pos_embed[:, 1:]\n    N_old = grid.shape[1]; gs_old = int(N_old**0.5)\n    grid = grid.reshape(1, gs_old, gs_old, -1).permute(0, 3, 1, 2)\n    grid = torch.nn.functional.interpolate(grid, size=(new_grid, new_grid),\n                                           mode=\"bicubic\", align_corners=False)\n    grid = grid.permute(0, 2, 3, 1).reshape(1, new_grid * new_grid, -1)\n    return torch.cat([cls, grid], dim=1)\nCLIP-ViT-L/14 trained at 224 (16² patches) generalises to 336 (24²) with this trick alone.\n\n\n\nLLaVA-NeXT and Qwen2-VL split a high-res image into multiple ViT inputs.\ndef anyres_tiles(image, base=336, candidate_grids=((1,1),(1,2),(2,1),(2,2),(1,3),(3,1))):\n    H, W = image.shape[-2:]\n    best = max(candidate_grids,\n               key=lambda g: min(W / (g[1]*base), H / (g[0]*base)))\n    gh, gw = best\n    image = torch.nn.functional.interpolate(\n        image[None], size=(gh*base, gw*base), mode=\"bicubic\", align_corners=False)[0]\n    tiles = image.unfold(1, base, base).unfold(2, base, base)         # [3, gh, gw, base, base]\n    tiles = tiles.permute(1, 2, 0, 3, 4).reshape(gh*gw, 3, base, base)\n    thumb = torch.nn.functional.interpolate(\n        image[None], size=(base, base), mode=\"bicubic\", align_corners=False)[0]\n    return torch.cat([thumb[None], tiles], dim=0)                     # [1+gh*gw, 3, b, b]\n\n\n\nReplace the 1-D positional embedding with rotary embeddings indexed by \\((y, x)\\):\n\\[\n\\text{RoPE}_{2D}(\\mathbf{q}_{y,x}) = \\begin{pmatrix} R_y & 0 \\\\ 0 & R_x \\end{pmatrix} \\mathbf{q}_{y,x}\n\\]\nwhere \\(R_y\\) and \\(R_x\\) are block-diagonal rotation matrices on half of the channels each. This is how Qwen2-VL and InternVL-2 support truly arbitrary resolutions.\n\n\n\nAdd 4–8 extra learnable tokens to the input that are discarded at output. Empirically removes the “artifacts” in attention maps reported by Caron et al. and improves dense prediction.\n\n\n\nDynamicViT, ATS, ToMe — remove or merge tokens whose attention score is below a threshold to cut FLOPs by 30–50 %. ToMe (token-merging) is the most popular: at each layer, find the \\(r\\) most similar token pairs by cosine and average them.\n\n\n\nConcatenate features from layers \\(\\{l_1, l_2, l_3\\}\\) — e.g. layer 8 (textures), 16 (parts), 24 (objects). VAR, VeCLIP and LLaVA-1.5 with high-res use this trick.\n\n\n\n\nWhen you need a low-token-count encoder for mobile or for video, ConvNeXt-V2 and EfficientFormer-V2 produce 49 or 196 tokens at much lower FLOPs. For dense prediction (detection, segmentation) the hierarchical encoders matter — Swin-Transformer uses shifted windows, InternImage uses deformable convolutions; the InternVL series uses these as the vision tower.\n\n\n\n\ntorch.compile(encoder, mode=\"reduce-overhead\") removes 20–30 % wall-clock on H100 for ViT-L.\nUse torch.nn.functional.scaled_dot_product_attention (PyTorch ≥ 2.0) — it dispatches to Flash-Attention 2 automatically.\nFreeze the encoder with for p in encoder.parameters(): p.requires_grad = False; encoder.eval() — the .eval() matters because of BatchNorm/Dropout (Dropout is no-op but LayerNorm running stats are not stored in CLIP-style ViTs, so .eval() is safe).\nCast pre-trained weights to bf16 with model.to(torch.bfloat16) when training fusion — the tiny accuracy loss is invisible and memory drops 50 %."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#extracting-visual-embeddings",
    "href": "Resources/Core5/Architecture/index.html#extracting-visual-embeddings",
    "title": "VLM Architecture",
    "section": "",
    "text": "A ViT block-by-block output has shape [B, 1+N, d]. Five canonical extraction strategies:\n\n\n\n\n\n\n\n\n\nStrategy\nTensor used\nPros\nCons\n\n\n\n\nCLS token\nhidden_states[-1][:, 0]\nOne vector per image, fast\nLoses spatial info\n\n\nMean pool of patches\nhidden_states[-1][:, 1:].mean(1)\nRobust, no CLS-collapse\nLoses CLS bias\n\n\nAll patch tokens\nhidden_states[-1][:, 1:]\nSpatial reasoning possible\n\\(N\\) tokens to fuse\n\n\nPenultimate layer patches\nhidden_states[-2][:, 1:]\nLess “task-specialised” than the final\nOne layer to specify\n\n\nMulti-scale concat\n[h[l1], h[l2], h[l3]]\nCaptures textures + objects\n2-3× tokens\n\n\n\nCLIP’s image_features uses CLS + a final projection layer. LLaVA-1.5 uses penultimate-layer patch tokens (24×24 = 576 tokens), justified empirically: the last layer is too task-specialised to the contrastive objective.\n\n\n\nfrom transformers import CLIPVisionModel\nimport torch.nn as nn\n\nclass VisualFeatureExtractor(nn.Module):\n    def __init__(self, model_name=\"openai/clip-vit-large-patch14-336\", select_layer=-2):\n        super().__init__()\n        self.vit = CLIPVisionModel.from_pretrained(model_name, output_hidden_states=True)\n        self.select_layer = select_layer  # -2 == penultimate\n\n    @torch.no_grad()\n    def forward(self, pixel_values):\n        out = self.vit(pixel_values=pixel_values, output_hidden_states=True)\n        feats = out.hidden_states[self.select_layer][:, 1:]   # drop CLS, [B, N, d]\n        return feats\n\n\n\nThe projector \\(P : \\mathbb{R}^{d_v} \\to \\mathbb{R}^{d_t}\\) converts visual feature dimension to LLM embedding dimension.\n1. Linear (LLaVA-1) — nn.Linear(1024, 4096). Two-step training: warm-up only this layer, then unfreeze everything.\n2. MLP-2 (LLaVA-1.5) — Linear → GELU → Linear. +1.0 MMMU vs linear.\n3. Q-Former (BLIP-2) — a 12-layer BERT initialised from BERT-base, with 32 learnable query tokens that cross-attend to image features and self-attend to each other. Output: 32 vectors regardless of input resolution.\nclass QFormer(nn.Module):\n    def __init__(self, n_queries=32, d=768, n_layers=12, n_heads=12):\n        super().__init__()\n        self.queries = nn.Parameter(torch.randn(1, n_queries, d) * 0.02)\n        self.layers = nn.ModuleList([QFormerLayer(d, n_heads) for _ in range(n_layers)])\n\n    def forward(self, vision_tokens):\n        q = self.queries.expand(vision_tokens.size(0), -1, -1)\n        for layer in self.layers:\n            q = layer(q, vision_tokens)\n        return q                                      # [B, 32, d]\n4. Perceiver Resampler (Flamingo) — like Q-Former but the queries also see learned positional embeddings indexed by time-of-frame (videos!), and the cross-attention is gated.\n5. Pixel-Shuffle (InternVL-2) — concatenate \\(2 \\times 2\\) neighbouring patch tokens then linearly project. Cuts token count 4× while keeping local information. Used in InternVL-2 to bring 24×24 down to 12×12 = 144 tokens.\n6. C-Abstractor (Honeybee) — convolutions on the 2-D patch grid then flatten. Preserves locality, modest parameter count.\n\n\n\nThere are three schools:\n\nUse the ViT’s own pos-embed and add nothing: the LLM’s RoPE handles tokens by their position in the sequence but not their spatial position. This is what LLaVA does.\nAdd 2-D positional information via the projector (interleave spatial coordinates).\nUse M-RoPE / RoPE-2D in the LLM itself (Qwen2-VL): the LLM is re-trained to interpret rotary frequencies along temporal, height and width axes.\n\n\n\n\nAfter training, run a linear probe: freeze the encoder + projector and train a single linear classifier on ImageNet-1k. If accuracy drops more than 5 % vs the original ViT, your projector is destroying information."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#customizing-text-encoders",
    "href": "Resources/Core5/Architecture/index.html#customizing-text-encoders",
    "title": "VLM Architecture",
    "section": "",
    "text": "The first question is what role does text play in your VLM?\n\nSymmetric retrieval (CLIP-style) → bidirectional encoder (BERT/RoBERTa) or a causal Transformer with attention-mask = bidirectional only for the [EOS] vector. CLIP uses a causal Transformer with EOS pooling.\nEncoder-decoder VQA (BLIP-style) → BERT-base initialised, repurposed via prompt tokens.\nGenerative dialog (LLaVA-style) → a full LLM (Vicuna, LLaMA, Mistral, Phi, Qwen, Gemma).\n\n\n\n\nThree production algorithms:\n\n\n\n\n\n\n\n\n\nAlgorithm\nUsed by\nVocab\nNotes\n\n\n\n\nBPE\nGPT-2, CLIP, LLaMA-1\n32 k–50 k\nGreedy merging of frequent pairs\n\n\nWordPiece\nBERT\n30 k\nLikelihood-based merging\n\n\nSentencePiece / Unigram\nT5, LLaMA-2, Mistral, Gemma\n32 k–128 k\nProbabilistic, language-agnostic\n\n\n\nFor VLMs, the tokenizer must include special tokens for image placeholders, e.g. &lt;image&gt;, &lt;video&gt;, &lt;roi&gt;, &lt;grounding&gt;. LLaVA reserves the &lt;image&gt; token; Kosmos-2 reserves &lt;box&gt; and &lt;phrase&gt;; Florence-2 reserves &lt;OD&gt;, &lt;OCR&gt;, &lt;CAPTION&gt;.\n\n\n\nA bidirectional encoder:\nfrom transformers import AutoTokenizer, BertModel\ntok = AutoTokenizer.from_pretrained(\"bert-base-uncased\")\nbert = BertModel.from_pretrained(\"bert-base-uncased\")\nout = bert(**tok([\"a dog on grass\"], return_tensors=\"pt\", padding=True))\nprint(out.last_hidden_state.shape, out.pooler_output.shape)\nThe pooler_output is a Linear(d, d) → tanh applied to the [CLS] vector — only meaningful when fine-tuned with a classification head. For retrieval you usually use mean pooling of all token embeddings with attention mask.\n\n\n\nfrom transformers import CLIPTextModel, CLIPTokenizer\ntok = CLIPTokenizer.from_pretrained(\"openai/clip-vit-large-patch14\")\ntext = CLIPTextModel.from_pretrained(\"openai/clip-vit-large-patch14\")\ninputs = tok([\"a photo of a cat\"], padding=True, return_tensors=\"pt\")\nout = text(**inputs)\n# CLIP pools at the EOS position\neos_idx = inputs.input_ids.argmax(dim=-1)\npooled = out.last_hidden_state[torch.arange(eos_idx.size(0)), eos_idx]\nThis pooling-at-EOS detail is famously subtle: because of greedy BPE, the largest token id in the sequence is the EOS token (49407 in CLIP’s tokenizer), so argmax selects it. If you fine-tune CLIP with a tokenizer that has a different id for EOS, you must change this.\n\n\n\nDecoder-only causal LMs dominate. You should know the parameter count, context length, tokenizer, and the position-embedding scheme for each:\n\n\n\n\n\n\n\n\n\n\nModel\nParams\nContext\nTokenizer\nPos-Embed\n\n\n\n\nLLaMA-2\n7 / 13 / 70 B\n4 k\nSentencePiece-BPE, 32 k\nRoPE θ=10000\n\n\nLLaMA-3 / 3.1 / 3.2\n8 / 70 / 405 B\n8 k / 128 k\ntiktoken, 128 k\nRoPE θ=500000\n\n\nMistral-7B / Mixtral-8x7B\n7 / 56 B\n32 k\nBPE, 32 k\nRoPE + sliding window\n\n\nQwen2 / Qwen2.5\n0.5 / 1.5 / 7 / 14 / 32 / 72 B\n128 k\nBPE, 152 k\nRoPE w/ YARN\n\n\nGemma / Gemma-2\n2 / 9 / 27 B\n8 k\nSentencePiece, 256 k\nRoPE\n\n\nPhi-3\n3.8 / 7 / 14 B\n4 k–128 k\ntiktoken, 32 k\nRoPE w/ LongRoPE\n\n\n\n\n\n\nJust like the visual side:\n\nAdd special tokens: tokenizer.add_special_tokens({\"additional_special_tokens\": [\"&lt;image&gt;\", \"&lt;im_patch&gt;\"]}) then model.resize_token_embeddings(len(tokenizer)). Critical: new embeddings are initialised randomly — initialise them to the mean of all existing token embeddings to stabilise training.\nExtend context with RoPE re-scaling: replace \\(\\theta = 10000\\) with \\(\\theta = 500000\\) (NTK-aware scaling) or use YaRN (Peng et al., 2023). Done by Qwen2-VL to support 32 k visual+text tokens.\nReplace the LLM with a Mixture-of-Experts version: same fusion module, swap the dense FFN for a routed FFN. Used by MoE-LLaVA, DeepSeek-VL2.\nAdd LoRA adapters to specific layers instead of full fine-tuning (see Optimization doc).\nFreeze the LM entirely and rely on the projector to “speak” the LM’s language — the BLIP-2 / LLaVA-1 approach.\n\n\n\n\nInside the LLM, visual tokens occupy specific positions in the sequence. There are three masking conventions:\n\nPure causal: visual tokens are causally masked just like text. Easiest, default.\nVisual-prefix bidirectional: visual tokens can attend to all other visual tokens but not text after them; text is causal. Used by some early models.\nBidirectional vision, causal text (Idefics-3): better preserves the ViT’s information.\n\nThe implementation detail is the attention mask, not a different module."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#extracting-textual-embeddings",
    "href": "Resources/Core5/Architecture/index.html#extracting-textual-embeddings",
    "title": "VLM Architecture",
    "section": "",
    "text": "Pooling\nFormula\nUse case\n\n\n\n\nCLS / [SOS]\nh[:, 0]\nBERT classifiers\n\n\nEOS\nh[arange(B), eos_idx]\nCLIP retrieval\n\n\nMean (mask-aware)\n\\(\\frac{\\sum_t m_t h_t}{\\sum_t m_t}\\)\nSentence-BERT, MPNet\n\n\nMax\n\\(\\max_t h_t\\)\nrarely used\n\n\nLast-token\nh[:, -1]\nLLM-based retrievers (E5-Mistral, GTE-Qwen2)\n\n\nWeighted (attention)\n\\(\\sum_t \\alpha_t h_t\\)\nlearned, slightly better\n\n\n\nPyTorch mean-pool with mask:\ndef mean_pool(hidden, attention_mask):\n    mask = attention_mask.unsqueeze(-1).float()\n    return (hidden * mask).sum(1) / mask.sum(1).clamp(min=1e-6)\n\n\n\nWhen you want a single vector per sentence, the Sentence-BERT family (Reimers & Gurevych, 2019) fine-tunes BERT with a siamese contrastive loss. MRL (Kusupati et al., 2022) trains the same embedding to be useful when truncated to multiple dimensions \\(\\{64, 128, 256, 512, 768\\}\\) — relevant for VLMs because shorter embeddings mean cheaper retrieval at deployment time.\n\n\n\nWhen the LLM is the fusion module (LLaVA, Qwen-VL), there is no separate text embedding — the LLM’s own token embeddings are the textual representations. Your fusion module simply produces visual tokens with the same dimensionality and the LLM concatenates them. The “embedding extraction” question becomes: do I want the embedding before any attention, after the first layer, or at the final hidden state?\nFor downstream tasks where you need a fixed-size text vector from a decoder-only LM, use the last token’s hidden state (E5-Mistral approach), or apply an extra &lt;|END|&gt; token and pool there.\n\n\n\nimport torch.nn.functional as F\n\ndef encode_text(text_model, tokenizer, sentences, device=\"cuda\"):\n    tok = tokenizer(sentences, padding=True, truncation=True,\n                    max_length=77, return_tensors=\"pt\").to(device)\n    with torch.no_grad():\n        out = text_model(**tok)\n    eos_idx = tok.input_ids.argmax(dim=-1)\n    h = out.last_hidden_state\n    pooled = h[torch.arange(h.size(0), device=device), eos_idx]\n    return F.normalize(pooled, dim=-1)"
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#implementing-custom-fusion-modules",
    "href": "Resources/Core5/Architecture/index.html#implementing-custom-fusion-modules",
    "title": "VLM Architecture",
    "section": "",
    "text": "Type\nWhere fusion happens\nExample\n\n\n\n\nNo fusion (late)\nDot-product after both encoders\nCLIP, SigLIP\n\n\nCross-attention fusion\nInside one transformer\nFlamingo, ALBEF, BLIP\n\n\nConcatenative fusion\nTokens prepended to LLM\nLLaVA, MiniGPT-4, Idefics\n\n\nResampler fusion\nFew learned queries summarise vision then concat\nBLIP-2, Flamingo\n\n\nAdapter fusion\nSmall modules inserted into LLM\nLLaMA-Adapter, MMICL\n\n\nUnified fusion\nSingle transformer over (vision_tokens, text_tokens)\nFuyu, Chameleon\n\n\n\n\n\n\nMulti-head attention with separate sources for Q and K/V:\n\\[\n\\text{CrossAttn}(\\mathbf{Q}, \\mathbf{K}, \\mathbf{V}) = \\text{softmax}\\!\\left(\\frac{\\mathbf{Q}\\mathbf{K}^\\top}{\\sqrt{d_k}}\\right)\\mathbf{V}, \\qquad \\mathbf{Q}=\\mathbf{X}_t W_Q,\\; \\mathbf{K}=\\mathbf{X}_v W_K,\\; \\mathbf{V}=\\mathbf{X}_v W_V.\n\\]\nPyTorch implementation:\nclass CrossAttention(nn.Module):\n    def __init__(self, d_text, d_vis, n_heads=8, d_head=64):\n        super().__init__()\n        d = n_heads * d_head\n        self.n_heads = n_heads; self.d_head = d_head\n        self.to_q = nn.Linear(d_text, d, bias=False)\n        self.to_k = nn.Linear(d_vis,  d, bias=False)\n        self.to_v = nn.Linear(d_vis,  d, bias=False)\n        self.to_out = nn.Linear(d, d_text)\n\n    def forward(self, x_text, x_vis, mask=None):\n        B, Nt, _ = x_text.shape\n        Nv = x_vis.shape[1]\n        q = self.to_q(x_text).view(B, Nt, self.n_heads, self.d_head).transpose(1,2)\n        k = self.to_k(x_vis ).view(B, Nv, self.n_heads, self.d_head).transpose(1,2)\n        v = self.to_v(x_vis ).view(B, Nv, self.n_heads, self.d_head).transpose(1,2)\n        attn = torch.nn.functional.scaled_dot_product_attention(q, k, v, attn_mask=mask)\n        attn = attn.transpose(1,2).reshape(B, Nt, -1)\n        return self.to_out(attn)\nNote the use of scaled_dot_product_attention — PyTorch 2 dispatches to Flash-Attention 2 when shapes and dtypes align.\n\n\n\nAdd a learnable gate so the LLM starts as its uni-modal self:\nclass GatedXAttn(nn.Module):\n    def __init__(self, d_text, d_vis, n_heads):\n        super().__init__()\n        self.xattn = CrossAttention(d_text, d_vis, n_heads)\n        self.alpha_xattn = nn.Parameter(torch.zeros(1))\n        self.ff   = nn.Sequential(nn.Linear(d_text, 4*d_text), nn.GELU(), nn.Linear(4*d_text, d_text))\n        self.alpha_ff    = nn.Parameter(torch.zeros(1))\n\n    def forward(self, x_text, x_vis):\n        x_text = x_text + torch.tanh(self.alpha_xattn) * self.xattn(x_text, x_vis)\n        x_text = x_text + torch.tanh(self.alpha_ff)    * self.ff(x_text)\n        return x_text\nInsert one such block every \\(K\\) LLM layers (Flamingo: \\(K=4\\) for the smaller variant). The initial zero-gate is critical: it makes the pre-trained LLM’s behaviour an exact starting point.\n\n\n\nQ-Former has two pathways because it is trained with three losses (ITC, ITM, ITG) that need different attention masks. A single forward pass routes the queries through different self-attention masks depending on the task token:\n\nFor ITC: queries can only see each other; text can only see text. The two CLS representations contrast.\nFor ITM: queries see image features and full bidirectional text — used to compute a binary score.\nFor ITG: queries see image features; text is causally masked (so the model can be used as a captioner).\n\nclass QFormerLayer(nn.Module):\n    def __init__(self, d=768, n_heads=12):\n        super().__init__()\n        self.self_attn  = nn.MultiheadAttention(d, n_heads, batch_first=True)\n        self.cross_attn = nn.MultiheadAttention(d, n_heads, batch_first=True)\n        self.ff = nn.Sequential(nn.Linear(d, 4*d), nn.GELU(), nn.Linear(4*d, d))\n        self.ln1, self.ln2, self.ln3 = nn.LayerNorm(d), nn.LayerNorm(d), nn.LayerNorm(d)\n\n    def forward(self, q, v):\n        h = self.ln1(q)\n        q = q + self.self_attn(h, h, h, need_weights=False)[0]\n        h = self.ln2(q)\n        q = q + self.cross_attn(h, v, v, need_weights=False)[0]\n        q = q + self.ff(self.ln3(q))\n        return q\n\n\n\nclass MLPProjector(nn.Module):\n    def __init__(self, d_vis, d_lm):\n        super().__init__()\n        self.proj = nn.Sequential(\n            nn.Linear(d_vis, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        )\n    def forward(self, x):                              # x: [B, N, d_vis]\n        return self.proj(x)                            # -&gt; [B, N, d_lm]\nUsed by 90 % of recent open-source VLMs. Initialization tip: zero-init the second linear’s bias and use Kaiming for weights to keep activations bounded.\n\n\n\nInternVL-2’s trick to cut visual tokens 4×:\ndef pixel_shuffle_2d(x, scale=2):\n    # x: [B, N, d] with N = h*w; halve spatial, multiply channels by scale**2\n    B, N, d = x.shape\n    h = w = int(N**0.5)\n    x = x.view(B, h, w, d)\n    x = x.view(B, h//scale, scale, w//scale, scale, d).permute(0, 1, 3, 2, 4, 5)\n    x = x.reshape(B, (h//scale) * (w//scale), d * scale * scale)\n    return x\n\nclass PixelShuffleProjector(nn.Module):\n    def __init__(self, d_vis, d_lm, scale=2):\n        super().__init__()\n        self.scale = scale\n        self.proj = nn.Sequential(\n            nn.LayerNorm(d_vis * scale * scale),\n            nn.Linear(d_vis * scale * scale, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        )\n    def forward(self, x):\n        return self.proj(pixel_shuffle_2d(x, self.scale))\n\n\n\nInsert a low-rank adapter into the LLM’s FFN or attention layers, with conditioning from visual features:\n\\[\n\\Delta \\mathbf{h} = \\alpha \\cdot \\text{cross-attn}(\\mathbf{h}, \\mathbf{V}) \\cdot B^\\top A^\\top\n\\]\nwhere \\(A \\in \\mathbb{R}^{r \\times d}\\), \\(B \\in \\mathbb{R}^{d \\times r}\\), \\(r \\ll d\\). LLaMA-Adapter-V2 does exactly this.\n\n\n\n\n\n\n\n\n\n\nSituation\nRecommended fusion\n\n\n\n\nTrain a CLIP-replacement on 1 B pairs\nNone — dual encoder + InfoNCE/SigLIP\n\n\nTrain a chatbot on a frozen LLM with limited compute\nMLP projector + LoRA\n\n\nTrain a model that must answer about 100s of frames\nQ-Former + temporal pos-embed\n\n\nNeed few-shot in-context learning across images\nGated cross-attention (Flamingo)\n\n\nWant highest token efficiency (long doc + image)\nPixel-shuffle + AnyRes\n\n\nNeed image generation too\nUnified VQ-VAE tokens + LM (Chameleon)\n\n\n\n\n\n\n\nForgetting LayerNorm on the projector input — visual features and LM embeddings live on different scales; un-normalised, the LM ignores them.\nWrong sequence length — never forget to pad and use attention_mask.\nPosition confusion: when you prepend visual tokens, the LLM’s RoPE indexes them as if they were text. If your LLM has RoPE θ=10000 trained on 4 k context, suddenly seeing 576 extra tokens can blow context length.\nMixed dtypes: keep projector in bfloat16 matching the LLM. A float32 projector silently up-casts the entire compute graph."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#assembling-multimodal-architecture",
    "href": "Resources/Core5/Architecture/index.html#assembling-multimodal-architecture",
    "title": "VLM Architecture",
    "section": "",
    "text": "Every modern projection-style VLM is assembled the same way. Memorise these seven steps.\n\nPick the vision encoder. Default: SigLIP-So400m/14 @ 384. Faster + smaller: CLIP-ViT-L/14 @ 336. Best dense: DINOv2-Giant.\nPick the LLM backbone. Default: Qwen2.5-7B-Instruct. Tiny: Phi-3-mini. Huge: LLaMA-3-70B.\nPick the resolution strategy. Single-tile 384², or AnyRes 4-tile, or native any-resolution with M-RoPE.\nPick the projector. Default: 2-layer MLP. Token-efficient: Pixel-shuffle ×2 + MLP. Few queries: Q-Former-32.\nDecide what is frozen. Stage 1 (alignment): freeze ViT and LLM, train projector. Stage 2 (instruction): unfreeze LLM (or LoRA), keep ViT frozen or unfreeze the last 4 layers.\nChoose the training data. Pre-train on captions (LAION-2B, COYO, DataComp). Instruction-tune on LLaVA-Mix-665k, ShareGPT-4V, LLaVA-OneVision-mid.\nChoose evaluation suite. MMMU, MathVista, DocVQA, ChartQA, AI2D, MMBench, BLINK, VQAv2, OK-VQA, TextVQA.\n\n\n\n\nimport torch\nimport torch.nn as nn\nfrom transformers import (\n    AutoTokenizer, AutoModelForCausalLM,\n    SiglipVisionModel, SiglipImageProcessor,\n)\n\n\nclass VLM(nn.Module):\n    \"\"\"A LLaVA-style VLM: SigLIP vision tower + MLP projector + frozen-or-LoRA LLM.\"\"\"\n\n    def __init__(\n        self,\n        vision_name=\"google/siglip-so400m-patch14-384\",\n        llm_name=\"Qwen/Qwen2.5-7B-Instruct\",\n        image_token=\"&lt;image&gt;\",\n        select_layer=-2,\n    ):\n        super().__init__()\n        self.tokenizer = AutoTokenizer.from_pretrained(llm_name)\n        if image_token not in self.tokenizer.get_vocab():\n            self.tokenizer.add_special_tokens({\"additional_special_tokens\": [image_token]})\n        self.image_token_id = self.tokenizer.convert_tokens_to_ids(image_token)\n\n        self.vision = SiglipVisionModel.from_pretrained(vision_name)\n        self.processor = SiglipImageProcessor.from_pretrained(vision_name)\n        self.llm = AutoModelForCausalLM.from_pretrained(\n            llm_name, torch_dtype=torch.bfloat16, attn_implementation=\"flash_attention_2\"\n        )\n        self.llm.resize_token_embeddings(len(self.tokenizer))\n\n        d_vis = self.vision.config.hidden_size\n        d_lm = self.llm.config.hidden_size\n        self.projector = nn.Sequential(\n            nn.LayerNorm(d_vis),\n            nn.Linear(d_vis, d_lm),\n            nn.GELU(),\n            nn.Linear(d_lm, d_lm),\n        ).to(torch.bfloat16)\n\n        self.select_layer = select_layer\n\n    def freeze_for_alignment(self):\n        for p in self.vision.parameters():\n            p.requires_grad = False\n        for p in self.llm.parameters():\n            p.requires_grad = False\n\n    def encode_vision(self, pixel_values):\n        out = self.vision(pixel_values=pixel_values, output_hidden_states=True)\n        feats = out.hidden_states[self.select_layer]                    # [B, N, d_vis]\n        return self.projector(feats)                                     # [B, N, d_lm]\n\n    def _embed_inputs(self, input_ids, pixel_values):\n        \"\"\"Replace every &lt;image&gt; token's embedding with vision tokens.\"\"\"\n        inputs_embeds = self.llm.get_input_embeddings()(input_ids)       # [B, T, d_lm]\n        vision_embeds = self.encode_vision(pixel_values)                 # [B, N, d_lm]\n        merged, new_attn_mask = [], []\n        for b in range(input_ids.size(0)):\n            img_pos = (input_ids[b] == self.image_token_id).nonzero(as_tuple=True)[0]\n            if len(img_pos) == 0:\n                merged.append(inputs_embeds[b]); continue\n            assert len(img_pos) == 1, \"Use one &lt;image&gt; per sample in this minimal demo\"\n            p = img_pos.item()\n            merged.append(torch.cat([inputs_embeds[b, :p],\n                                     vision_embeds[b],\n                                     inputs_embeds[b, p+1:]], dim=0))\n        merged = nn.utils.rnn.pad_sequence(merged, batch_first=True)\n        return merged\n\n    def forward(self, input_ids, pixel_values, labels=None):\n        inputs_embeds = self._embed_inputs(input_ids, pixel_values)\n        outputs = self.llm(inputs_embeds=inputs_embeds, labels=labels)\n        return outputs\nThis is essentially the architecture of LLaVA-1.5, Bunny, Imp, and most open-source 7B-class VLMs published in 2024. Read it line by line until you can rewrite it from memory.\n\n\n\nfrom PIL import Image\nmodel = VLM().eval().to(\"cuda\")\nmodel.freeze_for_alignment()\n\nimage = Image.open(\"dog.jpg\").convert(\"RGB\")\npixel_values = model.processor(images=image, return_tensors=\"pt\")[\"pixel_values\"].to(\"cuda\", dtype=torch.bfloat16)\n\nprompt = \"&lt;image&gt;\\nQuestion: What animal is in the picture?\\nAnswer:\"\ntok = model.tokenizer(prompt, return_tensors=\"pt\").to(\"cuda\")\n\nwith torch.no_grad():\n    out = model(input_ids=tok.input_ids, pixel_values=pixel_values)\nprint(out.logits.shape)\n\n\n\nThe differences from LLaVA-1.5:\n\nprocessor returns multiple tiles plus a thumbnail.\nencode_vision runs the ViT on each tile (with possible gradient checkpointing), then concatenates patch tokens.\nA newline token is inserted between rows of tiles so the LLM can reason about layout.\n\ndef encode_anyres(self, tiles):\n    # tiles: [B, T, 3, H, W] where T = num_tiles_per_image (variable)\n    B, T = tiles.shape[:2]\n    tiles_flat = tiles.flatten(0, 1)\n    feats = self.encode_vision(tiles_flat)                  # [B*T, N, d_lm]\n    feats = feats.view(B, T, -1, feats.size(-1))            # [B, T, N, d_lm]\n    return feats.reshape(B, T*feats.size(2), -1)            # concat tiles\n\n\n\nQwen2-VL drops the tiling entirely. The ViT is retrained to operate on arbitrary \\(H \\times W\\) via M-RoPE (multi-dimensional rotary embeddings). The vision encoder outputs a variable number of patch tokens; each is given a (t, y, x) triple consumed by the LLM’s M-RoPE. The trick that makes the LM happy: split RoPE channels into thirds — one for temporal, one for height, one for width.\n\n\n\nFor multiple images, each gets its own &lt;image&gt; placeholder and its own visual-token block:\n&lt;image&gt;{img1}&lt;image&gt;{img2} What is different between these two pictures?\nFor video, sample \\(T\\) frames, run each through the ViT, concat with temporal embeddings, optionally compress via Q-Former with shared queries across frames (Video-LLaMA, Video-ChatGPT).\n\n\n\nBefore launching a multi-day run, verify:\n\nOne sample forward — loss.backward() does not throw.\nLoss decreases on a tiny set — overfit 16 samples and watch loss → ~0.\nVisual token count matches — print inputs_embeds.shape.\n&lt;image&gt; token is masked in labels — the LLM should not be supervised to predict the image token.\nNo mixed-dtype warnings — everything bf16 except the vision encoder LayerNorms which can stay fp32.\n\n\n\n\n\nToken efficiency vs accuracy — is 144 tokens ever enough for OCR / charts? Most evidence says no; pixel-shuffle helps but DocVQA still wants ≥ 729 tokens.\nGenerative vs discriminative pre-training — does masked image modelling (BEiT-3, EVA) beat contrastive (SigLIP) when used as a VLM tower? Currently a tie; depends on downstream task.\nEncoder-free VLMs — Fuyu / Chameleon throw away the ViT. The 2024 evidence is that they need 5–10× the data; not yet a free lunch.\nModality gap — does it matter for downstream? Liang et al. 2022 say yes, but recent work (Schrodi et al. 2024) argues the gap is partly a measurement artefact.\nVisual tokenisation for generation — VQ-VAEs vs continuous tokens vs Magvit-v2 finite-scalar quantisation."
  },
  {
    "objectID": "Resources/Core5/Architecture/index.html#reading-list-references",
    "href": "Resources/Core5/Architecture/index.html#reading-list-references",
    "title": "VLM Architecture",
    "section": "",
    "text": "Jay Alammar, “The Illustrated Transformer” — required prerequisite.\nJay Alammar, “The Illustrated CLIP” and “The Illustrated Stable Diffusion”.\nLilian Weng, “Contrastive Representation Learning” (May 2021).\nLilian Weng, “Self-Supervised Representation Learning” (Nov 2019 / Jun 2021).\nLilian Weng, “Generalized Visual Language Models” (Jun 2022).\nLilian Weng, “Attention? Attention!”.\nSebastian Raschka, “Understanding Multimodal LLMs” (2024) — code-level walkthrough.\nYannic Kilcher’s YouTube paper reviews for CLIP, Flamingo, BLIP-2, LLaVA.\nHuggingFace blog: “Vision Language Models Explained” (April 2024), “Idefics 3” (Aug 2024).\n\n\n\n\n\nRadford et al., Learning Transferable Visual Models From Natural Language Supervision (CLIP), 2021.\nJia et al., Scaling Up Visual and Vision-Language Representation Learning With Noisy Text Supervision (ALIGN), 2021.\nZhai et al., Sigmoid Loss for Language Image Pre-Training (SigLIP), 2023.\nLi et al., ALBEF: Align before Fuse, NeurIPS 2021.\nLi et al., BLIP: Bootstrapping Language-Image Pre-training, ICML 2022.\nLi et al., BLIP-2: Bootstrapping Language-Image Pre-training with Frozen Encoders and Large Language Models, ICML 2023.\nAlayrac et al., Flamingo: a Visual Language Model for Few-Shot Learning, NeurIPS 2022.\nLiu et al., Visual Instruction Tuning (LLaVA), NeurIPS 2023.\nLiu et al., Improved Baselines with Visual Instruction Tuning (LLaVA-1.5), 2023.\nLiu et al., LLaVA-NeXT technical report, 2024.\nBai et al., Qwen-VL and Qwen2-VL technical reports, 2023 / 2024.\nChen et al., InternVL and InternVL 2.5 technical reports, 2023 / 2024.\nLaurençon et al., Idefics2 / Idefics3, HuggingFace, 2024.\nXiao et al., Florence-2, 2024.\nTeam Chameleon (Meta), Chameleon: Mixed-Modal Early-Fusion Foundation Models, 2024.\nBavishi et al., Fuyu-8B, Adept, 2023.\nDosovitskiy et al., An Image Is Worth 16×16 Words (ViT), ICLR 2021.\nHe et al., Masked Autoencoders Are Scalable Vision Learners (MAE), CVPR 2022.\nCaron et al., Emerging Properties in Self-Supervised Vision Transformers (DINO), ICCV 2021.\nOquab et al., DINOv2, 2023.\nDarcet et al., Vision Transformers Need Registers, ICLR 2024.\nvan den Oord et al., Representation Learning with Contrastive Predictive Coding (InfoNCE), 2018.\n\n\n\n\n\ntorch.nn.functional.scaled_dot_product_attention\ntorch.nn.MultiheadAttention\ntorch.compile\ntransformers.CLIPVisionModel, transformers.SiglipVisionModel, transformers.AutoModelForCausalLM\ntransformers.AutoProcessor (handles both image and text preprocessing)\naccelerate.Accelerator (covered in Deployment doc)\n\n\n\n\nLAION-2B-en, LAION-COCO, COYO-700M, DataComp-1B, RedCaps, CC12M, SBU, Visual Genome, COCO Captions, TextCaps, DocVQA, ChartQA, OCR-VQA, A-OKVQA, GQA, VQAv2, OK-VQA, MMMU, MathVista, AI2D, MMBench, MMBench-CN, MMVet, BLINK, RealWorldQA, ScienceQA, ShareGPT-4V, ALLaVA, LLaVA-OneVision-Data, Cauldron (Idefics-2’s instruction mix).\n\n\n\nA complete study cycle is:\n\nRead end to end once without coding.\nRe-read with PyTorch open and run each snippet.\nPick three architectures (e.g. CLIP, BLIP-2, LLaVA-NeXT) and re-implement them from scratch in a single notebook.\nRe-read the Reading List and pick five papers per family.\nWrite a one-page comparison of any two papers using the eight-point rubric from §1.9.\n\nIf you can do all five, you are an architecture expert.\n\nEnd of Vision-Language Model Architecture document."
  },
  {
    "objectID": "Resources/RAG/index.html#module-1-introducing-rag",
    "href": "Resources/RAG/index.html#module-1-introducing-rag",
    "title": "RAG",
    "section": "MODULE 1: INTRODUCING RAG",
    "text": "MODULE 1: INTRODUCING RAG\n\n1.1 Define Retrieval-Augmented Generation (RAG) and Explain Its Purpose\nDefinition. Retrieval-Augmented Generation (RAG) is an architecture that augments a generative language model (LLM) with an external knowledge source at inference time. Instead of relying only on the parameters learned during pretraining, the model first retrieves relevant passages from an external corpus (documents, web pages, internal wikis, databases) and then generates an answer conditioned on both the user’s query and the retrieved context.\nThe term was coined in the 2020 paper “Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks” (Lewis et al., Facebook AI). The original architecture combined a dense retriever (DPR) with a seq2seq generator (BART). Today, “RAG” is used as an umbrella term for any pipeline of the form:\nuser query  →  retrieve relevant context  →  prompt LLM with (query + context)  →  generated answer\nPurpose of RAG. RAG exists to decouple knowledge from language ability:\n\nThe LLM provides the language-modeling capability (understanding the query, composing fluent answers, reasoning).\nThe retrieval system provides the knowledge, kept outside the model weights so it can be updated, audited, scoped, and attributed without retraining.\n\nConcretely, a RAG system is designed to:\n\nGround generated answers in verifiable source documents (reducing hallucination).\nInject knowledge the LLM never saw (private documents, post-training events, domain-specific corpora).\nProvide citations so users can audit each claim.\nKeep responses fresh by updating the index instead of retraining the model.\nReduce cost compared with continually fine-tuning the LLM on new knowledge.\n\nMental model. Think of the LLM as an extremely well-spoken intern with no internet access, and the retriever as the librarian who hands them the three most relevant pages before they’re asked to write an answer.\n\n\n\n1.2 Why RAG Enhances LLMs by Retrieving Relevant External Knowledge\nA standalone LLM stores everything it “knows” inside its weights (parametric memory). This is powerful but has structural limitations that RAG directly addresses.\nProblem 1 — Knowledge cutoff. An LLM is frozen at the date its training data was collected. Anything that happened after that date is invisible to the model. Retrieving from a continuously updated index gives access to current information without retraining.\nProblem 2 — Hallucination. When asked about topics on which the LLM has weak or no memory, it fills the gap with plausible-sounding but fabricated content. By placing actual passages in the prompt and instructing the model to answer only from those passages, RAG converts open-ended generation into a constrained reading-comprehension task, which dramatically reduces fabrication.\nProblem 3 — Lack of attribution. A pure LLM cannot reliably say where a fact came from. RAG returns explicit document IDs/URLs alongside each retrieved chunk, so the answer can carry citations.\nProblem 4 — Private/proprietary knowledge. Most enterprise knowledge (internal wikis, contracts, support tickets, codebases) was never in the pretraining corpus. Putting that data behind a retriever is the cheapest, safest, most auditable way to make an LLM “know” it. Sensitive data stays in your store; only the relevant snippets enter the prompt.\nProblem 5 — Long-tail facts. LLMs memorize popular facts well and rare facts poorly. Retrieval shines on the long tail because a document only needs to exist in the corpus — it does not need to have been seen many times during pretraining.\nProblem 6 — Cost and freshness of fine-tuning. Continually fine-tuning a multi-billion-parameter model to absorb new knowledge is expensive, slow, brittle (catastrophic forgetting), and not auditable. Updating a vector index is cheap, instant, and revertible.\nProblem 7 — Source control and governance. You can add, remove, or redact a document from the index in seconds. You cannot easily “remove” a fact from a model’s weights.\nWhy retrieval works at all (the mechanism in one line): the user’s query and each candidate document are projected into the same vector space; documents whose vectors lie closest to the query vector are semantically most related and are surfaced as context."
  },
  {
    "objectID": "Resources/RAG/index.html#module-2-comparing-rag-and-traditional-llms",
    "href": "Resources/RAG/index.html#module-2-comparing-rag-and-traditional-llms",
    "title": "RAG",
    "section": "MODULE 2: COMPARING RAG AND TRADITIONAL LLMs",
    "text": "MODULE 2: COMPARING RAG AND TRADITIONAL LLMs\n\n2.1 Differentiating RAG-based and Non-Retrieval-Based Generative Models\nA non-retrieval-based (vanilla) LLM answers using only its parametric memory. A RAG-based system augments the same LLM with a retrieval step that fetches grounding context from an external store.\n\n\n\n\n\n\n\n\nDimension\nVanilla / Non-retrieval LLM\nRAG-based System\n\n\n\n\nKnowledge source\nFrozen in model weights (parametric)\nExternal, indexed corpus (non-parametric) + parametric base\n\n\nKnowledge freshness\nStuck at training cutoff\nUpdated by re-indexing documents\n\n\nHallucination risk\nHigh, especially for long-tail / out-of-domain facts\nLower; model is constrained to provided context\n\n\nAttribution\nNone (cannot cite sources reliably)\nEach chunk has an ID/URL → answer can carry citations\n\n\nUpdating cost\nFine-tuning or full retraining\nRe-embed and upsert documents\n\n\nPrivate data\nMust be fine-tuned in (risky, leaks possible)\nStays in your store; only relevant chunks enter prompt\n\n\nInference cost per query\nOne LLM call\nOne (or more) embedding / retrieval call(s) + one LLM call with longer prompt\n\n\nLatency\nLower (single call)\nHigher (retrieval adds ~tens to hundreds of ms; longer prompt → more tokens to process)\n\n\nDeterminism / auditability\nOpaque\nRetrieval logs show exactly which chunks were used\n\n\nFailure mode\nConfident wrong answers\n“I don’t know” when context is missing or irrelevant\n\n\nBest for\nStyle, reasoning, common knowledge\nKnowledge-intensive Q&A, enterprise search, support bots\n\n\n\nArchitectural difference in one line. Vanilla LLM = decoder over a query. RAG = retriever + decoder over (query, retrieved context).\nKey vocabulary distinction.\n\nParametric memory — knowledge baked into model weights during training.\nNon-parametric memory — knowledge stored outside the model, retrieved on demand. RAG combines both.\n\n\n\n\n2.2 Identifying Scenarios Where RAG Is Beneficial Over Standard LLMs\nChoose RAG when one or more of these apply. These are the situational stems most likely to appear on the assessment.\nChoose RAG when…\n\nThe knowledge changes often — news, prices, stock levels, schedules, regulations. The vanilla model goes stale; the index doesn’t.\nThe knowledge is private/proprietary — internal documentation, customer records, contracts, code. You don’t want to fine-tune it into weights you may need to share or revoke.\nCitations are required — legal, medical, scientific, journalistic, compliance use cases where every claim must be traceable.\nThe domain is long-tail or specialized — rare diseases, niche statutes, obscure APIs. The base model never saw enough of it to memorize.\nHallucination is costly — customer-facing support, healthcare, finance. Grounding in retrieved text is the cheapest hallucination defense.\nYou need fine-grained access control — different users should see different subsets of knowledge. Filter the retriever by metadata; the LLM never sees what the user shouldn’t.\nYou have a large knowledge base that doesn’t fit in the context window — millions of pages of documentation. Retrieval picks the few relevant chunks per query.\nYou need fast iteration on knowledge — content changes daily; retraining every day is infeasible.\n\nPrefer a standard (non-RAG) LLM when…\n\nThe task is purely stylistic or compositional (rewriting, summarization of the input itself, creative writing) — no external facts are needed.\nThe required knowledge is general-purpose and stable, well within the model’s training distribution (e.g., “explain how a binary search works”).\nLatency budgets are extremely tight and the answer doesn’t depend on any external corpus.\nThe corpus is so small that you can simply paste it into the prompt every time.\n\nPrefer fine-tuning instead of (or in addition to) RAG when…\n\nYou need to change the style, format, or behaviour of the model, not its knowledge. RAG cannot teach a model to follow a new output schema or speak in a brand voice; fine-tuning can.\nThe knowledge is so deeply intertwined with reasoning that retrieval cannot surface the right chunk reliably.\n\nIn practice, mature systems often combine both: fine-tune for behaviour, RAG for knowledge."
  },
  {
    "objectID": "Resources/RAG/index.html#module-3-describing-core-rag-components",
    "href": "Resources/RAG/index.html#module-3-describing-core-rag-components",
    "title": "RAG",
    "section": "MODULE 3: DESCRIBING CORE RAG COMPONENTS",
    "text": "MODULE 3: DESCRIBING CORE RAG COMPONENTS\n\n3.1 Roles of the Retriever, Retriever Store, and Generator\nA RAG system has three core components. Know each by name and by responsibility — the assessment may test this with match-the-pairs or fill-in-the-blank items.\n1. Retriever\n\nRole: Given a user query, return the top-k most relevant pieces of context from the store.\nResponsibilities:\n\nConvert the query into a representation comparable with stored documents (e.g., dense embedding, sparse term vector, or both).\nExecute a similarity search against the store.\nApply any filters (metadata, recency, access control).\nReturn ranked candidates, optionally with scores.\n\nImplementations: dense retrievers (BERT-family bi-encoders, OpenAI/Cohere/BGE embedders), sparse retrievers (BM25, TF-IDF), or hybrid combinations.\n\n2. Retriever store (a.k.a. document store, vector store, index)\n\nRole: Persist the corpus in a form the retriever can search efficiently.\nResponsibilities:\n\nStore the original document chunks (text + metadata such as source URL, author, timestamp, ACLs).\nStore the indexed representation: an inverted index for sparse retrieval (BM25), and/or a vector index for dense retrieval (FAISS, Chroma, etc.).\nSupport insert / update / delete (so the corpus can evolve) and metadata filtering.\n\nExamples: FAISS, Chroma, Elasticsearch (BM25), and similar.\n\n3. Generator\n\nRole: Produce the final natural-language answer using the user’s query and the retrieved context.\nResponsibilities:\n\nAccept a prompt of the form “Use the following context to answer the question …  … ”.\nSynthesize an answer grounded in that context.\nOptionally produce citations referencing the chunks it used.\nRefuse / say “I don’t know” when the context is insufficient (when prompted to do so).\n\nImplementations: any instruction-following LLM (GPT-4 family, Claude, Llama, Mistral, Gemini, etc.).\n\n\n\n\n3.2 How These Components Interact to Improve Response Quality\nThe three components form a pipeline. There are two phases — indexing (offline) and querying (online).\nIndexing phase (offline, run when documents are added or changed):\n\nLoad raw documents from sources (PDFs, HTML, Markdown, databases).\nSplit each document into chunks of manageable size.\nEmbed each chunk using an embedding model (for dense retrieval) and/or tokenize it for the inverted index (for sparse retrieval).\nWrite (chunk_text, embedding, metadata) into the retriever store.\n\nQuery phase (online, runs per user request):\n\nUser query arrives at the system.\nRetriever embeds (or tokenizes) the query and searches the store for the top-k most similar chunks.\nRetriever store returns those chunks plus their metadata.\nThe pipeline constructs a prompt that contains the system instruction, the retrieved context, and the original query.\nGenerator (the LLM) reads the prompt and produces an answer, optionally with citations.\nThe answer is returned to the user.\n\nHow each component improves response quality:\n\nThe retriever narrows the LLM’s attention to a small slice of highly relevant text, so the model spends its context window on signal rather than noise.\nThe retriever store, by being external and updatable, keeps the knowledge fresh, scoped, and auditable.\nThe generator turns retrieved snippets — which on their own are unstructured passages — into a coherent, contextual, query-specific answer.\n\nWhy all three are necessary. Drop the retriever and you’re back to a vanilla LLM that hallucinates on long-tail questions. Drop the store and the retriever has nothing to search. Drop the generator and you’ve built a search engine, not a conversational answer.\nInteraction diagram (text form):\n                      ┌──────────────┐\n   user query  ───▶   │  Retriever   │  ──── embed query\n                      └──────┬───────┘\n                             │ similarity search\n                             ▼\n                      ┌──────────────┐\n                      │  Retriever   │  returns top-k chunks\n                      │    Store     │     + metadata\n                      └──────┬───────┘\n                             │\n                             ▼\n                      ┌──────────────┐\n   prompt builder ─▶  │  Generator   │ ──▶ grounded answer\n   (query + chunks)   │    (LLM)     │      (+ citations)\n                      └──────────────┘"
  },
  {
    "objectID": "Resources/RAG/index.html#module-4-using-vector-databases-embeddings",
    "href": "Resources/RAG/index.html#module-4-using-vector-databases-embeddings",
    "title": "RAG",
    "section": "MODULE 4: USING VECTOR DATABASES & EMBEDDINGS",
    "text": "MODULE 4: USING VECTOR DATABASES & EMBEDDINGS\n\n4.1 How Embeddings Work and Their Role in Similarity-Based Retrieval\nDefinition. An embedding is a fixed-length numerical vector that represents the meaning of a piece of text (or image, audio, etc.) in a continuous vector space. Texts with similar meaning have vectors that lie close together; unrelated texts lie far apart.\nA good embedding model has the property that:\nsimilarity(embed(\"how to reset my password\"),\n           embed(\"steps for changing the login passcode\"))   →  high\n\nsimilarity(embed(\"how to reset my password\"),\n           embed(\"recipe for chocolate cake\"))                →  low\nHow they are produced. An embedding model is a neural network — typically a transformer encoder (e.g., BERT-family) — trained with a contrastive objective so that semantically related text pairs end up nearby in the output vector space and unrelated pairs end up far apart. The encoder reads the text and emits one vector (commonly 384, 768, 1024, 1536, or 3072 dimensions).\nSimilarity metrics used at retrieval time:\n\nCosine similarity — (a·b) / (‖a‖·‖b‖). Measures angle, not magnitude. Range [−1, 1]; higher is more similar. The default for most text embeddings (which are usually L2-normalized).\nDot product — a·b. Equivalent to cosine when vectors are normalized; otherwise also reflects magnitude. Common with FAISS for speed.\nEuclidean (L2) distance — ‖a − b‖. Smaller is more similar. Less common for text embeddings.\n\nRole in similarity-based retrieval (the full mechanism):\n\nOffline: embed every chunk in the corpus and store the vectors in a vector index.\nOnline: embed the incoming query with the same model.\nUse the similarity metric to find the top-k chunk vectors closest to the query vector.\nReturn those chunks as the retrieval result.\n\nBecause the comparison is over vectors, the retriever finds passages that are semantically related even when they share no surface words with the query — for example, “how do I cancel my subscription” matches a chunk about “terminating membership”.\nImportant practical rule. Always embed queries with the same embedding model that was used to embed the documents; otherwise the vectors are not comparable.\n\n\n\n4.2 Differentiating Various Embedding Models\nEmbedding models differ along several axes that matter for choosing the right one (this is also exactly what skill 9.1 will ask you to weigh).\nDifferentiating axes:\n\n\n\n\n\n\n\n\nAxis\nWhat it means\nWhy it matters\n\n\n\n\nModality\nText-only, multimodal (text + image), code, etc.\nMust match your data.\n\n\nArchitecture\nBi-encoder (one vector per text, fast) vs. cross-encoder (joint scoring of query+doc, slow but accurate)\nBi-encoders for retrieval; cross-encoders for re-ranking.\n\n\nDimensionality\n384, 768, 1024, 1536, 3072 …\nHigher dim = often higher accuracy, but more storage and slower search.\n\n\nContext length\nMax tokens the model accepts\nCaps your chunk size.\n\n\nLanguage coverage\nEnglish-only vs. multilingual\nCritical for non-English corpora.\n\n\nDomain specialization\nGeneral web text vs. code / biomedical / legal\nDomain-specific models often beat general ones in that domain.\n\n\nProvider / hosting\nAPI (OpenAI, Cohere, Voyage, Google) vs. self-hosted open-weight (BGE, E5, GTE, Instructor, SBERT/MiniLM)\nTrade off cost, latency, privacy.\n\n\nLicensing\nCommercial-use? On-prem allowed?\nMatters for enterprise.\n\n\n\nTwo important model families:\n\nBi-encoder (a.k.a. dual encoder). Encodes query and document independently into vectors; similarity is computed by a cheap dot product. Used as the actual retriever because the document vectors can be pre-computed and indexed. Examples: SBERT/MiniLM, BGE, E5, GTE, OpenAI text-embedding-3-*, Cohere embed-*.\nCross-encoder. Takes (query, document) as a single input and outputs a relevance score directly. Far more accurate but cannot pre-index — it must run a forward pass per candidate. Used for re-ranking the top-N from a bi-encoder. Examples: cross-encoder/ms-marco-MiniLM, BGE-reranker, Cohere Rerank.\n\nGeneral vs. specialized. A general embedding model trained on broad web text is a safe default. For very technical corpora (legal contracts, medical literature, source code), a domain-specialized embedder — or a general embedder fine-tuned on your domain — typically gives meaningfully better retrieval (covered in 9.2).\nOne-line picker. Start with a strong general bi-encoder, profile recall on your own evaluation set, and only switch to a heavier / domain-specific / fine-tuned model when the numbers demand it."
  },
  {
    "objectID": "Resources/RAG/index.html#module-5-applying-indexing-techniques",
    "href": "Resources/RAG/index.html#module-5-applying-indexing-techniques",
    "title": "RAG",
    "section": "MODULE 5: APPLYING INDEXING TECHNIQUES",
    "text": "MODULE 5: APPLYING INDEXING TECHNIQUES\n\n5.1 Comparing BM25, FAISS, and Chroma for Indexing and Retrieving Documents\nThese three are the canonical names the syllabus calls out. They are not all the same kind of thing — knowing the category each falls into is the most common assessment trap.\nBM25 — sparse, lexical, keyword-based.\n\nCategory: A ranking function (not a database) built on top of an inverted index of terms. Implementations live in Lucene/Elasticsearch/OpenSearch, Whoosh, rank_bm25, etc.\nHow it works: For each document it stores which terms occur and how often. At query time, it scores each document by Σ IDF(term) · TF-normalization(term, doc), rewarding documents that contain rare query terms many times while penalising over-long documents.\nStrengths: No training; instantaneous to update; very fast; transparent (you can see exactly which terms matched); excellent on queries that rely on rare keywords, names, IDs, error codes.\nWeaknesses: Purely lexical. A query about “car” will not match a document that only talks about “automobile”. No semantic understanding, no synonym handling, no paraphrase robustness.\nUse it when: keywords matter (codes, legal terms, product SKUs); when you have no embeddings; or as the sparse half of a hybrid retriever.\n\nFAISS — dense vector similarity search library (Facebook AI Similarity Search).\n\nCategory: A library (not a managed database) for fast similarity search over large vector collections. You give it vectors; you get back nearest neighbors.\nHow it works: Builds an Approximate Nearest Neighbor (ANN) index over your embeddings. Supports many index types: flat (exact), IVF (Inverted File — clusters vectors and only searches a few clusters per query), HNSW (Hierarchical Navigable Small World graph — graph-based ANN, very fast), PQ (Product Quantization — compresses vectors to save memory).\nStrengths: Extremely fast on millions–billions of vectors; many index types to trade speed for accuracy; runs on CPU or GPU; battle-tested.\nWeaknesses: Pure search library — no persistence, no metadata filtering, no REST API, no built-in document storage. You have to maintain the original text and metadata yourself.\nUse it when: you need maximum-performance ANN search and are willing to wire up your own storage / serving layer.\n\nChroma — dense vector database.\n\nCategory: A vector database — an end-to-end system that stores embeddings together with the original text and metadata, and exposes a simple API.\nHow it works: Wraps an ANN backend (e.g., HNSW) and adds persistence, collections, metadata filtering, and a Python-first API. Includes a default embedding function so you can hand it raw text.\nStrengths: Easy to get started; persists data; metadata filters; embedding function bundled; designed specifically for LLM/RAG workflows.\nWeaknesses: Less raw throughput than tuned FAISS at very large scale; relatively young; fewer ANN knobs.\nUse it when: you want a one-stop vector DB for a RAG prototype or small-to-medium production workload, with minimal plumbing.\n\nSide-by-side comparison:\n\n\n\n\n\n\n\n\n\nAspect\nBM25\nFAISS\nChroma\n\n\n\n\nWhat it is\nLexical ranking function over an inverted index\nANN search library over dense vectors\nVector database\n\n\nRepresentation\nSparse term vectors (TF-IDF-like)\nDense embeddings\nDense embeddings (+ stored text/metadata)\n\n\nMatches on\nExact / stemmed keyword overlap\nSemantic similarity\nSemantic similarity\n\n\nHandles synonyms / paraphrase\nNo\nYes (depends on embeddings)\nYes (depends on embeddings)\n\n\nStores documents/metadata\nYes (e.g., via Lucene)\nNo (vectors only)\nYes\n\n\nMetadata filtering\nYes\nManual / not built-in\nBuilt-in\n\n\nUpdate cost\nCheap\nCheap (or rebuild for some index types)\nCheap\n\n\nBest at\nRare keywords, IDs, names\nMassive-scale ANN\nEasy-to-use RAG store\n\n\nTypical role in RAG\nSparse retriever / hybrid component\nUnderlying ANN engine\nDrop-in vector store\n\n\n\nOne-line rule. BM25 = keywords. FAISS = a fast ANN engine you wrap yourself. Chroma = a friendly vector DB built on top of an ANN engine.\n\n\n\n5.2 How Indexing Strategies Affect Retrieval Speed and Accuracy\nEvery index choice is a point on the speed / accuracy / memory triangle. You cannot maximize all three at once.\nFlat (exact) index. - How: Stores all vectors as-is; at query time computes similarity to every vector (brute force). - Accuracy: Perfect (top-k is exact). - Speed: Slow — O(N · d) per query, where N = number of vectors, d = dimensionality. - Memory: Highest — every full vector stored. - Use when: Corpus is small (≲10⁵ vectors) or you need a ground-truth baseline to evaluate ANN indexes against.\nIVF (Inverted File) index. - How: Cluster the vectors into nlist partitions (centroids learned via k-means). At query time, look up the query’s nearest nprobe centroids and search only inside those partitions. - Accuracy: Tunable via nprobe. Higher nprobe = more clusters searched = higher recall, slower. - Speed: Much faster than flat (sub-linear in N). - Memory: Same as flat unless combined with PQ. - Use when: Mid-to-large corpora where you can afford a small recall loss for a big speedup.\nHNSW (Hierarchical Navigable Small World) graph index. - How: Builds a multi-layer graph where each node is a vector connected to its nearest neighbors. Search greedily walks the graph from the top layer down. - Accuracy: Very high recall, often &gt;95% of exact at sensible parameters. - Speed: Extremely fast at query time — logarithmic-ish. - Memory: Higher than IVF because the graph itself is stored. - Use when: You want the best speed-vs-accuracy default. Used under the hood by Chroma and many vector DBs. - Knobs: M (graph degree — higher = more accurate but more memory) and efSearch (search effort at query time — higher = higher recall but slower).\nProduct Quantization (PQ) / IVF-PQ. - How: Compress each vector into a short code by quantizing sub-vectors against learned codebooks. Drastically reduces memory and speeds up distance computations. - Accuracy: Lossy — recall drops with stronger compression. - Speed: Very fast, especially when memory-bound. - Memory: The lowest — vectors can be 8–32× smaller. - Use when: Corpus is huge (10⁸+ vectors) and memory is the bottleneck.\nInverted index (BM25). - Speed: Very fast (postings lists per term). - Accuracy: Exact for the lexical scoring function. But its “accuracy” relative to user intent is limited because it only matches surface terms. - Memory: Modest — proportional to vocabulary × postings, far less than dense vectors.\nHow each strategy trades off — summary table:\n\n\n\n\n\n\n\n\n\n\nIndex\nRecall vs. exact\nQuery speed\nMemory\nUpdate friendliness\n\n\n\n\nFlat (brute force)\n1.0\nSlow\nHighest\nTrivial\n\n\nIVF (flat)\ntunable, ~0.9–0.99\nFast\nSame as flat\nRe-train centroids occasionally\n\n\nIVF-PQ\nlower (lossy)\nVery fast\nVery low\nSame\n\n\nHNSW\nvery high (&gt;0.95)\nVery fast\nHigh (graph)\nIncremental inserts supported\n\n\nBM25 (inverted)\nexact for lexical, but only lexical\nVery fast\nLow–medium\nTrivial inserts\n\n\n\nPractical guidance:\n\nSmall corpus (a few thousand chunks): a flat index is fine; you’ll never notice the cost.\nMedium corpus (10⁵–10⁷ chunks): HNSW (used by Chroma and most vector DBs) is the default sweet spot.\nHuge corpus (10⁸+ chunks): IVF-PQ or similar memory-aware index; accept a modest recall hit for tractable memory.\nHeavy keyword / ID lookups: keep BM25 alongside (hybrid — see 11.1).\n\nThe core trade-off in one sentence: moving from exact to approximate search buys you orders of magnitude in speed and memory at the cost of a small, controllable drop in recall."
  },
  {
    "objectID": "Resources/RAG/index.html#module-6-identifying-rag-pipeline-types",
    "href": "Resources/RAG/index.html#module-6-identifying-rag-pipeline-types",
    "title": "RAG",
    "section": "MODULE 6: IDENTIFYING RAG PIPELINE TYPES",
    "text": "MODULE 6: IDENTIFYING RAG PIPELINE TYPES\n\n6.1 Identifying Different Retrieval Strategies\n“Retrieval strategy” = how the retriever decides which chunks to return. The main families, ordered from simplest to most elaborate:\n1. Sparse / lexical retrieval (e.g., BM25, TF-IDF). Matches on terms in the query and document. Excellent on rare keywords (names, codes, identifiers); blind to synonyms and paraphrases.\n2. Dense / semantic retrieval (embedding-based). Both query and documents are embedded with the same model; nearest-neighbor search returns semantically related chunks. Handles paraphrase and synonyms; can miss exact-keyword matches that don’t show up clearly in vector space.\n3. Hybrid retrieval (sparse + dense). Run both retrievers in parallel and merge their results — commonly by score fusion (e.g., weighted sum after normalisation, or Reciprocal Rank Fusion). Captures the strengths of both: rare-term precision from BM25, semantic recall from embeddings. The default “strong” baseline in modern RAG.\n4. Retrieve-then-rerank (two-stage retrieval). First stage: a fast bi-encoder (and/or BM25) returns a generous top-N (say 50–100). Second stage: a slow but accurate cross-encoder re-scores those N candidates and keeps the top-k (say 5–10) for the LLM. Big quality win for small latency cost because the cross-encoder only runs on N items, not the full corpus.\n5. Filtered / metadata-aware retrieval. Apply structured filters (date range, document type, language, tenant, ACL) before or alongside the similarity search, so the LLM only sees chunks the user is allowed to see and that are relevant in time/source.\n6. Query transformation strategies. Modify the query before retrieval to improve recall: - Query rewriting — the LLM rewrites a messy user question into a cleaner search query. - Multi-query — the LLM generates several paraphrased queries; results from all are merged. - Step-back / decomposition — for complex questions, break the query into sub-questions and retrieve for each. - HyDE (Hypothetical Document Embeddings) — ask the LLM to draft a plausible answer first, then embed and retrieve using that draft.\n7. Multi-hop / iterative retrieval. The system retrieves, reads, decides what’s still missing, formulates a follow-up query, and retrieves again — repeating until enough evidence is assembled. Needed for questions that require chaining facts across documents.\n\n\n\n6.2 Selecting the Appropriate Retrieval Method Based on the Use Case\nA short decision guide; the assessment likes situational questions of the form “Given this use case, which retrieval strategy fits best?”\n\n\n\n\n\n\n\n\nUse case / signal\nRecommended strategy\nWhy\n\n\n\n\nLookups dominated by exact codes, names, error IDs\nBM25 / sparse (or hybrid leaning on BM25)\nKeywords are the strongest signal; embeddings can dilute them.\n\n\nFree-form natural-language questions over prose\nDense / semantic\nHandles paraphrase, synonyms, conceptual matches.\n\n\nMixed traffic (some keyword, some natural language)\nHybrid (BM25 + dense)\nBest general-purpose default.\n\n\nTop-k must be tight and highly precise (long, expensive LLM call)\nRetrieve-then-rerank with a cross-encoder\nCross-encoder cleans up the top of the list.\n\n\nMulti-tenant, permissioned, or time-sensitive corpus\nFiltered retrieval (dense or hybrid + metadata filters)\nEnforces access control and recency at the retriever.\n\n\nVague or messy user queries\nAdd query rewriting / multi-query / HyDE in front of any base retriever\nImproves recall on poorly formed queries.\n\n\nQuestions whose answer requires combining facts from multiple documents\nMulti-hop / iterative retrieval\nSingle-shot retrieval cannot assemble the chain.\n\n\nTiny corpus (≲ a few hundred chunks)\nJust put it all in the prompt; or flat dense index\nRetrieval gymnastics aren’t needed.\n\n\nHuge corpus (10⁸+ chunks)\nDense (IVF-PQ / HNSW) + hybrid + rerank\nNeed ANN for scale, BM25 for precision, rerank for quality.\n\n\n\nSelection heuristic in three questions:\n\nDo my users search with keywords or with natural language? → leans BM25 vs dense.\nIs the top of the result list good enough as-is, or do I need to clean it up? → decides whether to add a re-ranker.\nDoes a single retrieval round contain enough evidence, or do I need to chain? → decides single-shot vs multi-hop."
  },
  {
    "objectID": "Resources/RAG/index.html#module-7-evaluating-retrieval-performance",
    "href": "Resources/RAG/index.html#module-7-evaluating-retrieval-performance",
    "title": "RAG",
    "section": "MODULE 7: EVALUATING RETRIEVAL PERFORMANCE",
    "text": "MODULE 7: EVALUATING RETRIEVAL PERFORMANCE\n\n7.1 Defining Precision@K, Recall@K, MRR, and F1-score\nFor each query, you have a ground-truth set of relevant documents and a ranked list of K returned documents.\nLet: - K = number of results examined. - relevant@K = number of returned items in the top-K that are actually relevant. - total_relevant = total number of relevant documents that exist for this query in the corpus.\nPrecision@K — Of the K results I returned, what fraction are actually relevant?\nPrecision@K = relevant@K / K\n\nMeasures how clean the top-K list is.\nRange: [0, 1]. Higher is better.\nInsensitive to documents you missed outside the top-K.\nUse when the cost of showing irrelevant results is high (e.g., you can only feed 5 chunks to the LLM and noise hurts the answer).\n\nRecall@K — Of all the relevant documents that exist, what fraction did I surface in the top-K?\nRecall@K = relevant@K / total_relevant\n\nMeasures how complete the retrieval is.\nRange: [0, 1]. Higher is better.\nInsensitive to how many irrelevant items are mixed in.\nUse when missing a relevant document is costly (e.g., legal discovery, compliance search, or any RAG step where a re-ranker / LLM downstream can ignore noise but cannot recover a missing fact).\n\nMRR — Mean Reciprocal Rank. On average, how high in the ranking does the first relevant result appear?\nFor a single query whose first relevant result is at rank rᵢ, the reciprocal rank is 1/rᵢ (and 0 if no relevant result appears). Averaged over a set of Q queries:\nMRR = (1/Q) · Σᵢ (1 / rᵢ)\n\nRange: [0, 1]. 1.0 means the first hit is always relevant; 0.5 means it’s typically at rank 2; etc.\nCares only about the rank of the first relevant result, not how many relevant results follow.\nUse when there is essentially one correct answer per query (factoid QA, “find the right document”) and where the user only looks at / the LLM only uses the very top result.\n\nF1-score (in retrieval). Harmonic mean of precision and recall — a single number that punishes imbalance between the two.\nF1 = 2 · (Precision · Recall) / (Precision + Recall)\n\nRange: [0, 1]. Higher is better.\nVariant most relevant here: F1@K uses Precision@K and Recall@K.\nUseful when you want a single metric that balances both, and don’t want a system that wins on recall by returning the entire corpus or wins on precision by returning one obvious result.\n\nQuick numeric example. Suppose 6 relevant documents exist for a query, you return K=5, and 3 of the 5 are relevant, with the first relevant one at rank 2.\n\nPrecision@5 = 3/5 = 0.60\nRecall@5 = 3/6 = 0.50\nReciprocal Rank for this query = 1/2 = 0.50\nF1@5 = 2 · 0.60 · 0.50 / (0.60 + 0.50) ≈ 0.545\n\n\n\n\n7.2 Choosing the Right Metric Based on Task Requirements\nPick the metric whose definition matches the cost structure of your task.\n\n\n\n\n\n\n\n\nTask / requirement\nBest primary metric\nReasoning\n\n\n\n\nFactoid QA — one document holds the answer; user only needs the first hit\nMRR\nCaptures “did the right thing come up first?”\n\n\nTop-k feeds an LLM with a tight context window — every wasted slot hurts the answer\nPrecision@K\nPenalises irrelevant chunks crowding out good ones.\n\n\nLegal / compliance / discovery — missing a relevant doc is unacceptable\nRecall@K (often with large K)\nPenalises gaps in coverage.\n\n\nBalanced quality with no clear asymmetry between false positives and false negatives\nF1@K\nSingle score balancing precision and recall.\n\n\nCost of false positives ≠ cost of false negatives\nUse Precision and Recall separately, not F1\nF1 weights them equally; report both so the trade-off is visible.\n\n\nComparing two retrievers / picking K\nSweep K and plot Precision@K, Recall@K, MRR\nLets you see the actual trade-off curve, not just one point.\n\n\n\nPicking K itself:\n\nSmall K (3–5) when downstream LLM context is tight or each chunk is expensive.\nLarger K (20–100) when followed by a re-ranker that will trim the list.\nFor Recall@K specifically, K must be large enough that meaningful coverage is achievable.\n\nEnd-to-end vs. retriever-only evaluation. Precision@K / Recall@K / MRR / F1 evaluate retrieval in isolation. A high-recall retriever can still feed a bad final answer if the LLM ignores the context. In practice, measure both: retrieval metrics for the retriever, plus answer-level metrics (exact match, F1 over answer tokens, or human/LLM grading) for the full system."
  },
  {
    "objectID": "Resources/RAG/index.html#module-8-building-a-rag-pipeline",
    "href": "Resources/RAG/index.html#module-8-building-a-rag-pipeline",
    "title": "RAG",
    "section": "MODULE 8: BUILDING A RAG PIPELINE",
    "text": "MODULE 8: BUILDING A RAG PIPELINE\n\n8.1 Constructing a Basic RAG Pipeline Using LangChain, LlamaIndex, or Custom Retrieval Logic\nA basic RAG pipeline always has the same six logical stages, regardless of which framework you use:\n\nLoad documents from a source.\nSplit them into chunks.\nEmbed each chunk and store it in a vector store.\nRetrieve top-k chunks for an incoming query.\nPrompt the LLM with the query + retrieved context.\nGenerate the answer.\n\nSkeleton in LangChain (Python):\nfrom langchain_community.document_loaders import TextLoader\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\nfrom langchain_openai import OpenAIEmbeddings, ChatOpenAI\nfrom langchain_community.vectorstores import Chroma\nfrom langchain.chains import RetrievalQA\n\ndocs = TextLoader(\"docs/handbook.txt\").load()\n\nsplitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)\nchunks = splitter.split_documents(docs)\n\nvectorstore = Chroma.from_documents(\n    documents=chunks,\n    embedding=OpenAIEmbeddings(model=\"text-embedding-3-small\"),\n    persist_directory=\"./chroma_db\",\n)\n\nretriever = vectorstore.as_retriever(search_kwargs={\"k\": 4})\n\nqa = RetrievalQA.from_chain_type(\n    llm=ChatOpenAI(model=\"gpt-4o-mini\", temperature=0),\n    retriever=retriever,\n    return_source_documents=True,\n)\n\nanswer = qa.invoke({\"query\": \"What is the parental-leave policy?\"})\nprint(answer[\"result\"])\nSkeleton in LlamaIndex (Python):\nfrom llama_index.core import SimpleDirectoryReader, VectorStoreIndex, Settings\nfrom llama_index.embeddings.openai import OpenAIEmbedding\nfrom llama_index.llms.openai import OpenAI\n\nSettings.embed_model = OpenAIEmbedding(model=\"text-embedding-3-small\")\nSettings.llm         = OpenAI(model=\"gpt-4o-mini\", temperature=0)\n\ndocuments = SimpleDirectoryReader(\"docs/\").load_data()\nindex     = VectorStoreIndex.from_documents(documents)\n\nquery_engine = index.as_query_engine(similarity_top_k=4)\nprint(query_engine.query(\"What is the parental-leave policy?\"))\nSkeleton with custom retrieval logic (no framework):\nimport numpy as np\nfrom openai import OpenAI\n\nclient = OpenAI()\n\ndef embed(text: str) -&gt; np.ndarray:\n    v = client.embeddings.create(model=\"text-embedding-3-small\", input=text).data[0].embedding\n    return np.array(v, dtype=np.float32)\n\ndef chunk(text: str, size: int = 500, overlap: int = 50) -&gt; list[str]:\n    out, i = [], 0\n    while i &lt; len(text):\n        out.append(text[i : i + size])\n        i += size - overlap\n    return out\n\nwith open(\"docs/handbook.txt\") as f:\n    chunks = chunk(f.read())\nchunk_vecs = np.stack([embed(c) for c in chunks])\n\ndef retrieve(query: str, k: int = 4) -&gt; list[str]:\n    q = embed(query)\n    sims = chunk_vecs @ q / (np.linalg.norm(chunk_vecs, axis=1) * np.linalg.norm(q) + 1e-12)\n    top = np.argsort(-sims)[:k]\n    return [chunks[i] for i in top]\n\ndef answer(query: str) -&gt; str:\n    context = \"\\n\\n\".join(retrieve(query))\n    prompt  = (\n        \"Answer the question using ONLY the context below. \"\n        \"If the answer is not in the context, say you don't know.\\n\\n\"\n        f\"CONTEXT:\\n{context}\\n\\nQUESTION: {query}\"\n    )\n    resp = client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[{\"role\": \"user\", \"content\": prompt}],\n        temperature=0,\n    )\n    return resp.choices[0].message.content\nAll three implementations execute the same six logical stages — only the syntactic wrapping differs.\n\n\n\n8.2 Implementing a Simple End-to-End Retrieval and Generation Loop\nThe minimum viable RAG loop, broken into its ordered steps. Memorize the order — assessment items often ask you to reorder these.\n1. INGEST\n   1a. Load raw documents from source.\n   1b. Clean / normalize text (remove boilerplate, fix encoding).\n   1c. Split into chunks (size and overlap chosen — see Module 10).\n   1d. Embed each chunk with the embedding model.\n   1e. Upsert (chunk_text, embedding, metadata) into the vector store.\n\n2. QUERY\n   2a. User submits a question.\n   2b. (Optional) Rewrite or expand the query.\n   2c. Embed the query with the SAME embedding model.\n   2d. Search the vector store for top-k nearest chunks.\n   2e. (Optional) Apply metadata filters / re-rank.\n\n3. GENERATE\n   3a. Build a prompt: system instructions + retrieved context + user question.\n   3b. Call the LLM.\n   3c. Parse and return the answer (optionally with citations to chunk IDs).\nMinimal end-to-end loop (framework-agnostic pseudocode):\ndef rag_answer(question: str) -&gt; str:\n    q_emb   = embed(question)\n    chunks  = vector_store.search(q_emb, k=4)\n    context = \"\\n\\n\".join(c.text for c in chunks)\n    prompt  = build_prompt(question, context)\n    return llm.generate(prompt)\nSample prompt template (a strong default):\nYou are a helpful assistant. Use ONLY the context below to answer the question.\nIf the answer is not in the context, reply: \"I don't know based on the provided context.\"\nCite the source IDs in square brackets when you use information from a chunk.\n\nCONTEXT:\n[1] {chunk_1_text}\n[2] {chunk_2_text}\n[3] {chunk_3_text}\n\nQUESTION: {user_question}\n\nANSWER:\nWhat each step contributes to quality.\n\nCleaning prevents boilerplate (nav bars, footers) from drowning out real content.\nChunking decides how much of the surrounding context travels with each retrieved hit.\nSame-model embedding for query and docs is mandatory; otherwise the vectors live in different spaces.\nThe “answer ONLY from context” instruction is what actually reduces hallucination at generation time.\n\n\n\n\n8.3 Identifying Different Packages to Construct a RAG Pipeline\nKnow the major options at a glance — the assessment may test “which of these is a RAG framework?” style items.\n\n\n\n\n\n\n\n\n\nPackage\nType\nWhat it gives you\nWhen to pick it\n\n\n\n\nLangChain\nRAG / LLM orchestration framework (Python, JS)\nLoaders, splitters, vector-store wrappers, retrievers, chains, agents — broad, modular\nGeneral-purpose orchestration; chaining retrieval with tools and agents\n\n\nLlamaIndex\nData-framework for LLMs (Python, TS)\nStrong document ingestion, indexing, query engines, response synthesizers\nDocument-heavy RAG where ingestion and indexing are the bulk of the work\n\n\nHaystack (deepset)\nEnd-to-end NLP / RAG framework\nPipelines for retrieval, QA, summarization; mature evaluation\nProduction NLP pipelines, especially with strong evaluation needs\n\n\nChroma\nVector database\nStores embeddings + text + metadata; simple API\nThe store layer of a RAG pipeline\n\n\nFAISS\nANN search library\nFast similarity search over dense vectors\nThe ANN engine under a custom or framework-built store\n\n\nSentence-Transformers (SBERT)\nEmbedding model library\nPretrained bi-encoders and cross-encoders\nSelf-hosted embeddings and re-rankers\n\n\nOpenAI / Cohere / Voyage SDKs\nHosted embedding + LLM APIs\nProduction-grade models behind an API\nWhen you don’t want to self-host models\n\n\n\nMental grouping. Frameworks (LangChain, LlamaIndex, Haystack) orchestrate the pipeline. Vector stores (Chroma, FAISS, and similar) hold the knowledge. Embedding / LLM providers (OpenAI, Cohere, SBERT, etc.) supply the models. A real RAG system picks one item from each group."
  },
  {
    "objectID": "Resources/RAG/index.html#module-9-selecting-embedding-model",
    "href": "Resources/RAG/index.html#module-9-selecting-embedding-model",
    "title": "RAG",
    "section": "MODULE 9: SELECTING EMBEDDING MODEL",
    "text": "MODULE 9: SELECTING EMBEDDING MODEL\n\n9.1 Choosing the Right Embedding Model Based on Accuracy, Cost, and Latency Trade-offs\nThere is no universally “best” embedding model — only the best one for your corpus, your latency budget, and your cost ceiling. Score each candidate along three axes.\nAccuracy.\n\nMeasure on your own data, not just public leaderboards. Build a small evaluation set of (query, relevant chunk) pairs from your corpus and compute Recall@K / MRR per candidate model.\nLarger / higher-dimensional models generally retrieve better, with diminishing returns.\nDomain match matters more than raw size: a 384-dim model trained on code may beat a 1536-dim general model on code.\nMultilingual matters if any meaningful share of queries or documents is non-English.\n\nCost.\n\nHosted (API) embeddings: charged per token embedded. Costs accumulate from two places — bulk re-embedding of the corpus (one-time but big) and per-query embedding (small but continuous).\nSelf-hosted (open-weight) embeddings: no per-token fee, but you pay for GPU/CPU, engineering, and ops.\nHigher dimensions cost more to store (vector size × number of chunks) and more to search.\nRe-ranking with a cross-encoder costs more per query than bi-encoder retrieval — budget for it explicitly.\n\nLatency.\n\nPer-query embedding latency: typically tens of ms for hosted APIs; sub-10 ms for small self-hosted models on a GPU.\nLarger embedding dimension → slightly slower vector search.\nIf you re-rank with a cross-encoder, that step usually dominates per-query latency.\nIndex choice (Module 5) affects search latency more than embedding choice does.\n\nDecision matrix:\n\n\n\n\n\n\n\nConstraint dominates\nReasonable default\n\n\n\n\nAccuracy above all (research, evaluation, recall-critical legal)\nA large hosted embedder + cross-encoder reranker\n\n\nCost above all (huge corpus, tight budget)\nCompact open-weight bi-encoder (e.g., MiniLM-class, 384-dim), self-hosted\n\n\nLatency above all (interactive UX, real-time)\nSmall embedder + HNSW index + no reranker (or a small one)\n\n\nBalanced production system\nMid-size hosted embedder (e.g., 768/1024-dim) with rerank on top of a hybrid retriever\n\n\n\nPractical workflow:\n\nPick 3–5 candidate embedding models spanning sizes/providers.\nEmbed a sample of your corpus and your eval queries with each.\nMeasure Recall@K and MRR on your eval set.\nMeasure per-query latency and project monthly cost at your traffic.\nPick the smallest / cheapest model that hits your accuracy bar.\n\nAnti-patterns.\n\nPicking the model that tops a public leaderboard without measuring on your own data.\nSwitching embedding models without re-embedding the entire corpus (vectors from different models are not comparable).\nMaxing out dimensionality “to be safe” and then complaining about cost and latency.\n\n\n\n\n9.2 Comparing the Effectiveness of Pretrained vs Fine-tuned Embedding Models\nPretrained embedding model. A model trained by someone else (OpenAI, Cohere, BAAI, Microsoft, Google, etc.) on a large general-purpose corpus. You use it out of the box.\n\nPros: Zero training effort, immediate use, strong general performance, well-supported.\nCons: Trained on general web text — may underperform on highly specialized vocabulary (legal, biomedical, internal product jargon, code identifiers); cannot capture domain conventions it never saw.\n\nFine-tuned embedding model. A pretrained model further trained on your data — typically with a contrastive objective over (query, relevant_passage) pairs from your domain, often with hard negatives.\n\nPros: Aligns the vector space to your notion of similarity; usually meaningfully better Recall@K and MRR on the domain it was fine-tuned for; can specialise on terminology, document structure, and user query style.\nCons: Requires labeled (or weakly labeled) training pairs; engineering effort; risk of overfitting to a small dataset; risk of catastrophic forgetting on general queries; must be re-fine-tuned as the domain evolves.\n\nEffectiveness comparison.\n\n\n\n\n\n\n\n\nAspect\nPretrained\nFine-tuned\n\n\n\n\nOut-of-domain quality\nGood baseline\nOften worse if fine-tuned narrowly\n\n\nIn-domain quality\nDecent → poor on niches\nOften substantially better\n\n\nCost to obtain\nFree (or API fee)\nEngineering + compute + labels\n\n\nTime to deploy\nMinutes\nDays to weeks\n\n\nRisk of regression\nLow\nReal (overfit, forget)\n\n\nMaintenance\nVendor handles upgrades\nYou handle drift and retraining\n\n\n\nWhen to fine-tune (rules of thumb):\n\nYour domain vocabulary is meaningfully different from general web text (legal, medical, internal product names, code).\nYou can assemble at least a few hundred to a few thousand (query, relevant_passage) pairs — even weak supervision (e.g., from search logs or LLM-generated questions) often suffices.\nA strong pretrained baseline has already been measured on your eval set and falls short of the bar.\nThe retrieval quality bottleneck has been isolated to the embedder — not to chunking, not to ranking, not to the LLM.\n\nWhen to stick with pretrained:\n\nYou don’t have evaluation data yet — fine-tuning without an eval set is flying blind.\nYour domain is mainstream and the pretrained model already meets your bar.\nThe expected lift over the pretrained baseline is small relative to the engineering cost.\n\nA common third path: keep a pretrained bi-encoder for first-stage retrieval (cheap, broad, low-maintenance) and add a fine-tuned cross-encoder re-ranker on top (focused, expensive per item but only run on the top-N)."
  },
  {
    "objectID": "Resources/RAG/index.html#module-10-optimizing-chunking-strategies",
    "href": "Resources/RAG/index.html#module-10-optimizing-chunking-strategies",
    "title": "RAG",
    "section": "MODULE 10: OPTIMIZING CHUNKING STRATEGIES",
    "text": "MODULE 10: OPTIMIZING CHUNKING STRATEGIES\n\n10.1 Differentiating Between Fixed-Size and Semantic Chunking Approaches\nFixed-size chunking.\n\nDefinition: Cut the text into chunks of a predetermined size — measured in characters, tokens, words, or sentences — usually with a fixed overlap between consecutive chunks.\nHow it works: Walk through the document; emit a chunk every N units, starting N − overlap units after the previous chunk’s start.\nPros: Trivial to implement, deterministic, very fast, predictable token cost per chunk.\nCons: Boundary-blind — happily cuts in the middle of a sentence, a code block, a table row, or a logical idea. The retrieved chunk may start mid-thought and end mid-thought.\nMitigations: Add overlap (commonly 10–20% of chunk size) so adjacent chunks share some text; prefer a recursive splitter that tries to break on paragraph → sentence → word boundaries first and only falls back to a hard cut.\n\nSemantic chunking.\n\nDefinition: Cut the text where the meaning changes, not where a fixed counter rolls over. Boundaries fall between distinct ideas, sections, or topics.\nHow it works (common approach): Embed each sentence (or small unit); walk through the document, and start a new chunk wherever the cosine similarity between consecutive sentences drops below a threshold — i.e., the topic just shifted. Variations: split by document structure (Markdown headings, HTML sections), by speaker turns, or by an LLM that proposes semantic boundaries.\nPros: Each chunk is internally coherent, which improves retrieval precision (the chunk is “about one thing”) and improves the LLM’s ability to use it (no awkward mid-sentence starts).\nCons: More expensive (per-sentence embedding or LLM calls during ingestion); chunk sizes vary widely; harder to bound context-window cost per retrieved chunk.\n\nSide-by-side comparison:\n\n\n\n\n\n\n\n\nAspect\nFixed-size\nSemantic\n\n\n\n\nBoundary rule\nCounter-based (chars/tokens)\nMeaning shift / structure\n\n\nChunk size\nUniform\nVariable\n\n\nImplementation cost\nTrivial\nModerate to high (extra embeddings or LLM calls)\n\n\nRisk of cutting mid-idea\nHigh\nLow\n\n\nRetrieval precision\nLower (noisier chunks)\nHigher\n\n\nBest when\nHomogeneous prose, prototypes, tight budgets\nHeterogeneous documents (long PDFs, mixed headings, transcripts)\n\n\n\nMiddle-ground option: recursive structure-aware splitting. Splits first on document structure (headings, paragraphs, sentences) and only resorts to a hard character/token cut as a fallback. Cheap like fixed-size, much cleaner boundaries. Often the strongest default before reaching for truly semantic chunking.\n\n\n\n10.2 Optimizing Chunk Size for Better Retrieval Accuracy While Minimizing Context-Window Usage\nChunk size is the single highest-leverage knob in a RAG pipeline. It directly trades retrieval precision against context completeness and token cost.\nEffects of chunk size:\n\nSmall chunks (≈100–300 tokens).\n\nPros: Each chunk is tightly focused → higher Precision@K; cheaper context window per chunk; the LLM’s attention isn’t spread across irrelevant text.\nCons: Important context may be split across chunks; relevant information may sit just outside the retrieved chunk; the same topic gets fragmented into many similar chunks (hurts diversity).\n\nLarge chunks (≈1000–2000+ tokens).\n\nPros: Each chunk carries more surrounding context, so the LLM has more to work with; fewer chunks total in the store.\nCons: Lower Precision@K (the chunk also contains a lot of irrelevant text); fewer distinct chunks fit in the context window; embeddings of very long chunks are “averaged” and lose specificity, so retrieval relevance drops.\n\nOverlap.\n\n10–20% overlap between adjacent chunks is the typical default; protects against information that would otherwise straddle a chunk boundary.\nToo much overlap = duplicated content in the index → wasted storage and the retriever returning two near-duplicate chunks for the same query.\n\n\nContext-window arithmetic to keep in mind.\ntokens_used_per_query ≈ system_prompt + (k × chunk_size) + query + answer_buffer\nSo picking k and chunk_size is a joint decision: if your model has an 8k context and you reserve 1k for system + question + answer, you have ~7k tokens left for retrieved chunks. With chunk_size = 500 you can comfortably feed k = 10–12; with chunk_size = 1500 you can only fit k ≈ 4. The first option gives more diversity, the second gives more depth per chunk.\nOptimization workflow:\n\nBuild a small evaluation set: queries paired with the chunks (or documents) that contain the correct answer.\nSweep a grid of chunk sizes (e.g., 128, 256, 512, 1024, 2048 tokens) × overlaps (0, 10%, 20%).\nFor each combination, rebuild the index and measure Recall@K, Precision@K (or MRR) and the average tokens consumed per query.\nPlot accuracy vs. tokens-used; pick the smallest chunk size whose accuracy is within a small margin of the best.\nRe-check on a held-out query set so you’re not overfitting the eval queries.\n\nPractical defaults (good starting points, not laws):\n\nGeneral prose: 300–500 tokens, 10–15% overlap.\nLong technical PDFs / specs: 500–800 tokens, 10–20% overlap, structure-aware splitter.\nConversational transcripts: split on speaker turns, then merge to a target size.\nSource code: split on function / class boundaries, never mid-statement.\n\nWhy “smaller and more chunks” is not automatically better. As chunks get smaller, individual chunks lose context the LLM needs to interpret them; retrieval can return five tiny snippets that, separately, don’t actually contain the answer even though together they would. The sweet spot is “small enough to be on-topic, large enough to stand alone.”"
  },
  {
    "objectID": "Resources/RAG/index.html#module-11-optimizing-retrieval-performance",
    "href": "Resources/RAG/index.html#module-11-optimizing-retrieval-performance",
    "title": "RAG",
    "section": "MODULE 11: OPTIMIZING RETRIEVAL PERFORMANCE",
    "text": "MODULE 11: OPTIMIZING RETRIEVAL PERFORMANCE\n\n11.1 Improving Retrieval Results Using Re-Ranking Methods or Hybrid Search\nTwo of the highest-impact, well-understood upgrades over plain dense retrieval.\nA. Hybrid search (sparse + dense fusion).\nIdea. Run a sparse retriever (BM25) and a dense retriever (embeddings) in parallel over the same corpus, then fuse their result lists. You get keyword precision from BM25 and semantic recall from embeddings.\nWhy it helps. BM25 nails queries that depend on exact tokens (product codes, names, error strings) that embeddings often dilute. Dense retrieval nails paraphrases and conceptual matches that BM25 misses entirely. Their failure modes are nearly disjoint, so fusion is robust.\nHow to fuse — two common methods:\n\nScore fusion (weighted sum). Normalize each retriever’s scores (e.g., min-max) and combine them: combined = α · dense + (1 − α) · sparse. Tune α on a held-out set (often around 0.5–0.7 in favor of dense for natural-language queries).\nRank fusion (Reciprocal Rank Fusion, RRF). Ignore raw scores; combine by ranks:\n\nRRF_score(doc) = Σ over retrievers   1 / (k + rank_in_that_retriever(doc))\nk is a small constant (commonly 60). RRF is robust because it sidesteps cross-retriever score-scale issues.\nB. Re-ranking with a cross-encoder.\nIdea. First-stage retriever (bi-encoder and/or BM25) returns a generous top-N (say 50–100). A heavier cross-encoder then scores each (query, candidate) pair jointly and reorders them; keep the top-k (say 5–10) for the LLM.\nWhy it helps. A bi-encoder encodes query and document independently; it must commit to a single vector per document, which can miss subtle query-document interactions. A cross-encoder reads them together with full attention and produces a much more reliable relevance score. The cost — one forward pass per candidate — is manageable because it runs only on the top-N, not the whole corpus.\nWhen to add a re-ranker: when Precision@k matters (small k feeding an expensive LLM call), when the first-stage retrieval is high-recall but noisy, or when downstream answer quality is bottlenecked by retrieval quality rather than coverage.\nHow re-ranking and hybrid combine in practice (a strong production recipe):\nquery\n  ├── BM25         → top 100 by lexical score\n  └── dense (bi-encoder) → top 100 by cosine\n\nmerge via RRF → top 100 unique candidates\n\ncross-encoder rerank → top 5–10\n\nfeed to LLM\nThis pattern — hybrid retrieve → fuse → re-rank → generate — is the modern default for serious RAG systems.\n\n\n\n11.2 Tuning Retriever Hyperparameters to Boost Relevance\nA retriever exposes several knobs. Tune each against the same offline eval set (queries + relevance labels), measuring Recall@K, Precision@K, MRR, and end-to-end answer quality.\nHyperparameters worth tuning:\n\n\n\n\n\n\n\n\nKnob\nWhat it controls\nHow to think about tuning\n\n\n\n\nk (top-k)\nHow many chunks to return / send to the LLM\nToo small → misses relevant chunks (low recall). Too large → noise dilutes the LLM’s prompt (low precision, higher cost). Sweep k and watch both recall and answer quality.\n\n\nSimilarity score threshold\nMinimum similarity to accept a chunk\nSet so that clearly irrelevant chunks are dropped even if they fall in the top-k. Useful when “no good match” should mean “no chunk”, not “the best of a bad bunch”.\n\n\nHybrid fusion weight (α) / RRF constant\nSparse vs. dense balance\nTune α on a held-out set; for keyword-heavy queries lower α (give BM25 more weight), for natural-language queries higher α.\n\n\nNumber of first-stage candidates (N)\nHow many candidates the re-ranker sees\nLarger N → better chance the true positive is in the candidate pool, but slower and pricier reranking. Pick the smallest N at which recall plateaus.\n\n\nRe-ranker top_k_after_rerank\nFinal size of context handed to the LLM\nConstrained by context window and answer-quality measurements.\n\n\nChunk size and overlap\nGranularity and redundancy of the index\nRe-tune whenever the embedder, document set, or query style changes (see 10.2).\n\n\nEmbedding model choice\nThe vector space itself\nTreat as a hyperparameter; benchmark candidates on your data (see 9.1).\n\n\nIndex parameters (HNSW M, efSearch; IVF nlist, nprobe; PQ codes)\nSpeed-vs-accuracy on ANN search\nRaise efSearch / nprobe until recall stops improving meaningfully; then back off for latency.\n\n\nMetadata filters\nPre-filter by date, source, language, tenant\nUse to enforce recency / access control; effectively raises precision on filtered slices.\n\n\nQuery transformations\nRewrite, multi-query, HyDE\nTreat as on/off knobs; measure whether they actually help on your eval set (they don’t always).\n\n\n\nTuning workflow:\n\nLock down an evaluation set of queries with known relevant chunks; without this, “tuning” is just vibes.\nEstablish a baseline with default settings; record Recall@K, Precision@K, MRR, answer quality.\nOne knob at a time. Sweep one hyperparameter through a small grid; pick the value that maximises the chosen metric.\nRe-evaluate end-to-end. A change that helps retrieval metrics can still hurt answer quality (e.g., bigger k that adds noisy chunks). Always check both.\nIterate in the order of expected impact: chunk size → embedding model → k → hybrid fusion → re-ranker on/off → ANN index params.\nLock and monitor. Once tuned, lock the configuration and monitor metrics in production; re-tune when the corpus, query distribution, or models change.\n\nCommon pitfalls:\n\nTuning everything at once — you can’t tell what helped.\nTuning on the same set you’ll later report numbers on — you’ll overfit.\nOptimising retrieval metrics in isolation while ignoring final answer quality.\nCranking k arbitrarily high to “play it safe” — pollutes the LLM context with noise and increases cost.\nMixing embedding models between query and index without re-embedding — silently destroys retrieval quality."
  },
  {
    "objectID": "Resources/Neet/neet.html",
    "href": "Resources/Neet/neet.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "In our achievement-obsessed world, society, relatives, and even parents judge kids by exam ranks. Coaching factories exploit this monetarily generating unorganic competition and unrealistic expectations. This breeds dishonesty along with which corrupt or ignorant enough authorities aid in shattering the dreams of the deserving, leaving a psychological scar. The future of India lies in its youth. Do we even care how many truly aspire vs being forced into rat-race? Can we think about a student of financially weak background who is made to believe that cracking this exam is sole path to financial freedom, sabotaging self-discovery. Counselling has become a mere formality of choosing an institute to study in. Personalized career and skill guidance is remote to technologically and economically deprived. The government, capable of spending money on printing advertisements, is not subsidizing education for bright minds who can innovate, cure, develop and build India. We need more IIT/IISc like institutes in many more domains and eventually we need to compete for top spot."
  },
  {
    "objectID": "Resources/jspuzz/feb26.html",
    "href": "Resources/jspuzz/feb26.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "https://www.janestreet.com/puzzles/subtiles-2-index/\nRefer this puzzle. I want you to write its detailed solution step by step with brevity. Use the image (download it and refer the cells with their position (row, column) to write the solution.)\nHow I found the solution is\nFirst of all grid contains 169 cells, assuming somehow we can fill all the cells, we get n(n+1)&lt;=1692 where we fill numbers unto n. This gives us value that n has to be 17 since n must be integer. Thus we know that all the equations in the grid should resolve to a number between 1 to 17. Now we need to solve them for the same.\nFew equations that I primaraily take cue from are (You find and refer them with their cells, in fact I want you to use markdown the create the grid and code your equations in a way they get rendered correctly in quarto qmd.) square roots, log and powers. Since b^2 is present b has to an integer among +-1,+-2,+-3, +-4. but (b-1)^2 removes -4 from consideration.\nBecause a-c is denominator a!=c. c-1 is denominator so c!=1. Now there is a cell with square root of c-a which means c&gt;a and in another cell where value is b/(a-c), a-c is denominator which is negative, so to make overall number positive, we need to eliminate positive possibilities of b. So b is now -1/-2/-3.\nNow there is a element c^b and b is negative integer which suggests c has to be of form 1/d where d is integer &gt;1. and since we have a cell with log(base c) a gives away that a=c^k where k is an integer among 2 to 17 beacuse c&gt;a. There is a cell in which neumerator is postive as it is square root and denominator is a which means a is positive implying c is also positive.\nThere is a cell with a^b - 12/a where if b=-1, we have -11/a which won’t work. So remove b=-1. Now there is also b/a-1. Which means if we put a as c^k = (1/d)^k we get -bdk/(dk-1). Now, let’s take b=-2 so 2dk/(dk-1) has to be an integer –&gt; d^k has to be 2 or 3 and with current constraints on d and k we cannot have it. So b=-2 has to be ruled out. Only b=-3 is left. So we have here 3*dk/(dk-1) so d^k should be either 2 or 4. d^k=2 won’t work so d^k=4 and deterministicly we can conclude that d=2 and k=2. Thus c = 1/2 and a = 1/4.\na= 1/4, b=-3, c=1/2. We can now fill labeled cells.\n//table\nNow we have values at 37 cells. R4C11 has to be 12. R11C6 has to 14. Now just to connect exisiting 12 numbers we must exhaust all remaining 12. There are only 2 ways to do this. (In 3rd way we cut off 6s). But we can surely confirm R5,R6,R7 in C10 to be 12."
  },
  {
    "objectID": "Resources/CleanIndia/index.html",
    "href": "Resources/CleanIndia/index.html",
    "title": "स्वच्छ भारतासाठी मी काय करू शकतो?",
    "section": "",
    "text": "‘निर्मलं सदा सुंदरम् ।’ \nआज आपण आधुनिक युगात जगत आहोत. ए.पी.जे. अब्दुल कलाम यांनी दाखवलेले भारताला जागतिक महासत्ता बनविण्याचे स्वप्न साकार करायचे असेल तर प्रत्येक भारतीयाने स्वतः पासून स्वच्छतेस सुरूवात करणे क्रमप्राप्त ठरते. दुसरा काय करतो यापेक्षा मी काय करतो महत्त्वाचे ! म्हणून, मी व्यक्तिगत स्वच्छतेबरोबरच माझे घर व घराभोवतालच्या परिसराच्या स्वच्छतेस प्राधान्य देईन. याकरिता, माझ्या गल्लीतील आणि प्रभागातील जनतेस स्वच्छतेचे महत्त्व पटवून देईन.\nमी माझ्या घरातील केरकचरा, घनकचरा, रद्दी आणि ई-वेस्ट यांची योग्य ती विल्हेवाट लावीन. यासाठी प्लास्टिकचा वापर करणार नाही. शक्य तितका ओला कचरा शेतामध्ये गाडून त्यापासून पर्यावरणपूरक सेंद्रिय खताची निर्मिती करीन. माझ्या घरगुती वापरातून बाहेर पडणारे पाणी भूमिगतरीत्या ड्रेनेजमध्ये सोडीन व मार्गात ते पाणी कोठेही तुंबणार नाही याची काळजी घेईन. आपण आपल्या प्रत्येक कृतीतून काहीना काही निर्माण होतच असते. मग आपण आपल्या कृतीतून स्वच्छताच का नाही निर्माण करायची?\nमी शाळेत असताना कागदाची कपटे कचराकुंडीतच टाकेन. मी शाळेचा परिसर स्वच्छ ठेवीन व यासाठी मित्रांची मदत घेईन. अविघटनशीन पदार्थांच्या बाबतीत ‘Reduce-Reuse-Recycle’ या त्रिसूत्रीचा वापर करीन. ‘स्वच्छतेतून समृद्धीकडे’ हा विचार लक्षात घेऊन मी स्वच्छता अभियान माझ्या पातळीवर राबवीन. ‘टाकाऊ पासून टिकाऊ’ या मूलमंत्राचा अंगीकार करीन. आपल्या धार्मिक परंपरा जोपासतानाही परिसर अस्वच्छ होऊ देणार नाही. असे म्हणतात की ‘Cleanliness is next to godliness.’ हीच स्वच्छता जीवनात अंगीकारून जीवन सुंदर बनायला हवे.\n२ ऑक्टोबर २०१९ रोजी महात्मा गांधीची १५०वी जयंती आहे. या पार्श्वभूमीवर २ ऑक्टोबर २०१४ रोजी ‘स्वच्छ भारत अभियान’ सुरू करण्यात आले आहे. यात प्रत्येक भारतीयाने सहभागी व्हावे. मी फक्त २ ऑक्टोबर रोजीच नाही तर पूर्ण वर्षभर स्वच्छतेसाठी व्रतस्थ राहीन. भारताला स्वच्छ व सुंदर बनवण्यात शक्य तितके योगदान देईन. कारण, ‘I had become and I will remain a part & parcel of स्वच्छ भारत मिशन.’ चला तर मग भारताला स्वच्छ व सुंदर बनवूया…"
  },
  {
    "objectID": "Resources/resources.html",
    "href": "Resources/resources.html",
    "title": "Notes",
    "section": "",
    "text": "Though there is dark but as you are lying in our minds the bark of dogs will show us the way\nThough there is extremism but as you are lying in our minds the shots of gun will teach us tolerance\nThough many in Indian villages sleep without dinner but as you are lying in our mind we will be winner against hunger\nThough we have reached the Mars the world is still under the threat of wars but as you are lying in our minds the kind and sensible people will come together\nBapu though many of us are blind but as you are lying in our mind the kind god will blow the wind and we will find our way\n-Avikskhit Nachiketa"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "I am Madhav Kadam (𑘦𑘰𑘠𑘪 𑘎𑘟𑘦 /  माधव कदम ). I am working as SWE at Samsung R&D Institute India Bangalore in Samsung Pay / Digital Wallet team after completing my B.Tech. in Computer Science and Engineering along with a minor in Economics from the Indian Institute of Technology, Indore.\nI am an avid reader. My reading spectrum stretches from math-heavy research papers to fictional novels. Both extremes of this spectrum help me understand everything in between much better. I focus my research on significant challenges and topics that amuse me. Generative AI, robotics and computer vision fascinate me, and at the same time, I am inclined to study the privacy, security and economic aspects of its development as well as applications. I constantly try to keep myself updated on my domains of keen interest (Economics, Literature, Algorithms, Biology, Law and Technology).\nI express myself with a pen a lot resulting in attempts for writing poems and jotting down thoughts regularly. Have put a few of them here too. All this has led me to have an eye for spotting inefficiencies, and come up with ways to terminate them. I enjoy engaging in thought-provoking debates and discussions. I am big-time foodie and nature lover, always seeking new culinary experiences(vegetarian) and outdoor adventures.",
    "crumbs": [
      "Home",
      "Madhav Kadam"
    ]
  },
  {
    "objectID": "cv/cv.html",
    "href": "cv/cv.html",
    "title": "Madhav Kadam",
    "section": "",
    "text": "Experience\n\n\n\n\n\n\nSoftware Engineer\n\n\nSamsung Research (Digital Wallet Team)\n\n\n\nJuly 2025 - Present\n\n\n\n\n\nDeveloped POC for OCR-based “Add Anything to Wallet” tool as an independent project and integrated it with Samsung Wallet.\n\n\nDeveloped LLM as an on-device judge pipeline for multimodal usecases.\n\n\nImplemented solution for digitizing NFC-based Office IDs for internal use.\n\n\n\n\n\n\n\n\nResearch Intern (SURI)\n\n\nArizona State University\n\n\n\nMay 2025 - July 2025\n\n\n\n\n\nConducted literature reviews on Privacy and Network Security of various categories of Android applications.\n\n\nApplied static and dynamic analysis methodologies to evaluate and report on the network security and privacy posture of shortlisted rental applications.\n\n\n\n\n\n\n\n\nSummer Intern\n\n\nSamsung Research (Digital Wallet Team)\n\n\n\nJune 2024 - Aug 2024\n\n\n\n\n\nReceived Advanced Developer PPO.\n\n\nDeveloped AI-driven image generation models that align with Samsung themes for NRU promotion campaign.\n\n\nAutomated entire end-to-end promotional pipeline; Researched and proposed a plan to develop an LLM agent capable of performing tasks for the Samsung Wallet.\n\n\n\n\n\n\n\n\nResearch Intern\n\n\nUniversity of Tartu, Estonia\n\n\n\nDec 2023 - Dec 2024\n\n\n\n\n\nEngineered datasets by scraping and post-processing 15K+ Facebook/Threads posts and 2M+ comments.\n\n\nAnalyzed and selected from various Spam detection and Toxicity quantification models.\n\n\nConducted in-depth descriptive analysis across 10 key criteria.\n\n\n\n\n\n\n\nProjects\n\n\n\n\n\nAdobe Behaviour Simulation Challenge  Code\n\n\nInter-IIT Tech Meet 12.0 (Bronze Medal)\n\n\n\n\n\nLed a team on predicting tweet engagement; Developed a Classifier-Regressor approach using BERTweet embeddings.\n\n\nEmployed fine-tuned SOTA language models (LLaMA-2, NeuralHermes-2.5) with RAG for tweet generation.\n\n\nProposed LogRMSE as an improved evaluation metric for social media engagement.\n\n\n\n\n\n\n\nVitals Extraction Challenge  Code\n\n\nCloudphysician Healthcare (Gold Medal)\n\n\n\n\n\nDeveloped the fastest pipeline (96% accuracy) for identifying and digitizing ICU monitor vitals.\n\n\nImplemented SOTA models like YOLOv8, Mask RCNN, Segformer, Retinanet, and DETR on a custom dataset of 9000 images.\n\n\nEnhanced accuracy with PaddleOCR, SCAN algorithm, and semi-supervised learning.\n\n\n\n\n\n\nOther Key Projects\n\n\n\n\nIITIHub: Application for Streamlined Campus Services.\n\n\nNatural Language to SQL: NLP project.\n\n\nMIPS 16-bit Processor: Implemented architecture in VHDL.\n\n\nOpenGL Amusement Park: 3D graphics simulation project.\n\n\nEfficient Mixed-Integer Planning: For UAVs in Cluttered Environments.\n\n\nFinnacio & RoastIn: Web development projects.\n\n\n\n\n\n\n\nPublications\n\n\n\n\n\n\nScalable Distributed Laplacian Score for Feature Selection\n\n\nInternational Conference on Neural Information Processing (ICONIP)\n\n\n\nDec 2024\n\n\n\n\n\n\nRelevant Coursework\n\n\n\n\n\nComputer Science: Distributed Network Algorithms, Parallel Computing, Computational Intelligence, Data Structures & Algorithms, Operating Systems, Compiler Techniques, Computer Networks, Computer Graphics, Database Systems, Blockchain, Computer Vision, Cryptography & Network Security, Machine Learning.\n\n\nMathematics: Optimization Algorithms, Differential Equations, Calculus, Linear Algebra, Complex Analysis, Numerical Methods, Algebraic Graph Theory.\n\n\nEconomics: Intermediate Microeconomics, Indian Economy, Development Economics, International Economics.\n\n\n\n\n\n\nLeadership & Extracurriculars\n\n\n\n\n\nCo-Head, Cynaptics (AI/ML Club): Led a team of 60+ students in research, projects, and competitions.\n\n\nPlacement Coordinator: CS Department, Training and Placement Cell, IIT Indore.\n\n\nMember, Academic Council: IIT Indore (A.Y. 2022-2023).\n\n\nCo-Head, Debating Society: Mentored 100+ students; Winner of Annual Debating Championship.\n\n\nSocial Impact: Taught in government schools of underprivileged localities (AVANA Club).\n\n\nSpeaker: Delivered a talk on “Interpretable, Explainable & Trustworthy AI” (Apr 2023)."
  },
  {
    "objectID": "Poems/Hindi/असफलता.html",
    "href": "Poems/Hindi/असफलता.html",
    "title": "असफलता",
    "section": "",
    "text": "पहली दफा जब असफलता को चूमा था\nअश्कों की बारिशों में दिल खोलकर मैं झूमा था\n\nपहली दफा चुकी थी अटकले\nऔर शर्म से झुकी हुईं थी नजर\nडर से रुकी थी धड़कने\nऔर आंसुओं की सूखी हुईं थी नहर\n\nस्वयं पर अविश्वास में मन मेरा डूबा था\nबदला हुआ लोकमन समय का अजूबा था\nलोकमान्यता के लिए मैं भूखा था\nअंदर से टूटा और बाहर से मैं रूखा था\n\nदिल को समझाया यह वक्त भी निकल जायेगा\nअसफलता का सख्त लोहा भी पिघल जायेगा\nउसके लिए कई गुना ज्यादा तपना होगा\nतभी तो पहला पायदान फिरसे अपना होगा\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Hindi",
      "<span class=\"hindi-kalam\">असफलता</span>"
    ]
  },
  {
    "objectID": "Poems/Hindi/परिस्थिती.html",
    "href": "Poems/Hindi/परिस्थिती.html",
    "title": "परिस्थिती",
    "section": "",
    "text": "लक्ष्मी देवी का जिन पर वरदान नहीं होता\nशिक्षण उनका आसान नहीं होता\nमां-बाप दिन में कमाते हैं तब रात को खाते हैं\nझुग्गियों में पौष्टिक पकवान नहीं होता\nमानता हूं हर इंसान समान नहीं होता\nकिंतु इम्तिहान में परिस्थिती प्रमाण नहीं होता\nकैसे स्पर्धा करें उनसे जो पलंगतकिये पे सोते हैं\nबेचारों के पास छोटा सा मकान नहीं होता\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Hindi",
      "<span class=\"hindi-kalam\">परिस्थिती</span>"
    ]
  },
  {
    "objectID": "Poems/disclaimer.html",
    "href": "Poems/disclaimer.html",
    "title": "Disclaimer",
    "section": "",
    "text": "Dear Reader,\nAll these poems are simple words, imperfect and small,\nThey are meant to express, not to judge or extol.\nIf they cause you any pain,\nIt was never the intent of my brain.\nUse Sidebar or Index",
    "crumbs": [
      "Home",
      "Poems",
      "Disclaimer"
    ]
  },
  {
    "objectID": "Poems/Marathi/दिवाळी.html",
    "href": "Poems/Marathi/दिवाळी.html",
    "title": "दिवाळी",
    "section": "",
    "text": "आली आली हो दिवाळी उठा पहाटे सकाळी उठणे लावा अंगाला रहा प्रसन्न त्रिकाळी\nदिवाळी सणांची हो राणी साळीची सुरू कापणी अंघोळ घालती भावांना त्यांच्या लाडक्या बहिणी\nघाई करती हो बाळ आई दे मज फराळ तुळशीविवाहपर्यंत पुरतो हा बराच काळ\nदिवाळीतला अमावस मोकळा मोकळा दिवस धनसंपत्तीची मागणी लक्ष्मी मातेच्या चरणी\nनंतर पाडवा येतो सुख समृद्धी देतो मागतकरी त्या दिवशीला फराळ मागून नेतो\nबहीण ओवाळी भावाला भाऊ जाई तिच्या गावाला भाऊबीजेच्या दिवशी मने भेटती मनाला\nतुळशीविवाह नंतर आकाश कंदील उतरतात आराशी दिव्यांच्या मगच बंद होतात\nजाते जाते हो दिवाळी गुलाबी थंडी मग हिवाळी जाळल्या जातात मग काठ्या पेटू लागतात शेकोट्या\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">दिवाळी</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/शेतकऱ्याचे मनोगत.html",
    "href": "Poems/Marathi/शेतकऱ्याचे मनोगत.html",
    "title": "शेतकऱ्याचे मनोगत",
    "section": "",
    "text": "तोडली तुम्ही झाडे पृथ्वी केली उजाड\nउभारिले कारखाने पण शेती राहिली ओसाड\nमोठी होती आमची चूक मारल्या आम्ही बोरी\nगावची पोहण्या पोरं खोदल्या अगणित विहिरी\nआम्हाला तर उगमली तुम्ही समजून घ्या\nह्या शेतकऱ्याचे मनोगत तुम्ही ऐकून घ्या\n\nनको आम्हाला कर्जमाफी थोडं द्या तंत्रज्ञान\nआम्ही उभारू व्यवसाय नवे संगं मदतीला विज्ञान\nनिसर्गराजा लहरी आहे त्याने थोडी साथ द्यावी\nआमच्या तकलीफेवर थोडी सरकारने मात द्यावी\nआम्ही सामूहिक शेती शिकतो सहकाराचं तंत्र द्या\nह्या शेतकऱ्याचे मनोगत तुम्ही ऐकून घ्या\n\nमायबाप शासन आहे फार आहेत अपेक्षा\nएक आहे त्यातील नको आम्हाला उपेक्षा\nकंबर कसून घाम पुसून आम्ही राहू तयार\nथोड्याशा ठिबकाच्या जीवावर हिरवं करू शिवार\nएक विनंती देशात पुन्हा हरितक्रांती घडू द्या\nह्या शेतकऱ्याचे मनोगत तुम्ही ऐकून घ्या\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">शेतकऱ्याचे मनोगत</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/शिवबा.html",
    "href": "Poems/Marathi/शिवबा.html",
    "title": "शिवबा",
    "section": "",
    "text": "शिवरामकृष्ण विक्रमादित्य\nअशोका आणि मौर्य\nयां पश्चात आसमंतात\nतळपला तो शिवसुर्य\nडोंगराळ प्रांत पाहून\nमनोमनी बेतलं\nहिंदवी स्वराज्य स्थापनकरण्या\nशिरहातावर घेतलं\nकेली रायरेश्वरी प्रतिज्ञा\nहाती घेतला तोरणा\nयुक्तिवादनी कूटनीती\nशिवरायांच्या धोरणा\nलावली जावळी निकाली\nफाडले अफजलाचे पोट\nदिला सिद्धीला चकवा\nतोडिली शायिस्ताची बोट\nजयसिंगाशी तह\nशहाच्या हातावर तुरी\nक्षत्रिय कुलावतंस राजाने\nधारीला राजमुकुट शिरी\nछत्रपती असे दक्ष\nलक्ष केले क्रुरतेला\nमाझे कोटीदा प्रणाम\nशिवबांच्या शूरतेला\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">शिवबा</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/कविता खरंच काय असते.html",
    "href": "Poems/Marathi/कविता खरंच काय असते.html",
    "title": "कविता खरंच काय असते",
    "section": "",
    "text": "कविता खरंच काय असते\nएक हजार पानांचे ज्ञान असलेला एक ताव असते\nकल्पना सागरात घेऊन जाणारी नाव असते\nनिरागस चेहऱ्यावरचा नकळत टिपलेला भाव असते\nमनाच्या कोपऱ्यात वसलेलं एक गाव असते\nज्ञान संपादन आणि काव्यानंदाची ती हाव असते\nबांधकामात मग्न असलेल्या गवंड्याची ती वीट असते\nजात्यावर बसलेल्या आईचं ती पीठ असते\nमागच्या बाकावरील दंगेखोरांची ती कमेंट असते\nबेकार्‍याच्या नोकरीचं पहिलं पेमेंट असते\nमाझ्यासारख्या अनेकांसाठी ती कर्म असते\nविवांसारख्या व्यक्तींचा ती धर्म असते\nदुःख रुपी थंडीत मिळालेली ती शाल असते\nसंकट रुपी युद्धात मिळालेली ती ढाल असते\nव्याख्यान वर्षातला अधिकमास असते\nचेतनागुनोक्ती बरोबरचा सहवास असते\nप्रवासातला चांगला टाईमपास असते\nरसिक वाचकांचा जणू श्वास असते\nकधी शांत कधी रौद्र अशी सरिता असते\nआणि हे सगळं लिहिण्याचा खटाटोप म्हणजे कविता असते\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">कविता खरंच काय असते</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/मी शेतकरी.html",
    "href": "Poems/Marathi/मी शेतकरी.html",
    "title": "मी शेतकरी",
    "section": "",
    "text": "राबतो मी शेतात कसतो सरीवर सरी माझे दुःख मांडण्या लिहितोय मी शेतकरी\nमी पहाटं उठतो काकडं देवाच गातो कांदा भाकर खातो काळ्या मातीत रातो\nखत पाणी देऊन हिरवं पीक येते कधी गारपीट हुती कधी नदी वाहून नेते\nआमच्या कष्टानेच तुमचं पोट रोज भरत दिलं आम्हाला सहाय्य तर का डोळ्यात खुपतं\nपीक चांगलं येण्यासाठी मरमर मरतोय माझं पृथ्वीवरचे वजं झटकन कमी करतोय\nशेवटचा प्रयत्न हा माझे बांधव वाचावे आमच्या हालांनी डोळ्यात तुमच्या पाणी साचावे\nशेवटचा प्रश्न हा कोण आहे आमचा वाली चला जातो देवा घरी जायची माझी वेळ झाली\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">मी शेतकरी</span>"
    ]
  },
  {
    "objectID": "Poems/Marathi/कर्मर्तव्य.html",
    "href": "Poems/Marathi/कर्मर्तव्य.html",
    "title": "कर्मर्तव्य",
    "section": "",
    "text": "केल्याने होत आहे आधी केलेचि पाहिजे\nहातामधील खडक तुझ्या उगारायला हवी\nआळसाची बेडी तुला झुगारयला हवी\nसंघर्षाने होत आहे तर लढलेचि पाहिजे\nभविष्य घडवण्यासाठी कंबर तुला कसवायला हवी\nकर्म आणि कर्तव्याची घडी तुला बसवायला हवी\nपरिश्रमाने होत आहे तर श्रमलेची पाहिजे\nचित्तभूमीवर सद्विचार तु पिकवायला हवे\nषड्रिपूधाड येता ते टिकवायला हवे\nआत्मसंयमाने होत आहे तर शिकलेचि पाहिजे\n-अविक्षित नचिकेत",
    "crumbs": [
      "Home",
      "Poems",
      "Marathi",
      "<span class=\"hindi-kalam\">कर्मर्तव्य</span>"
    ]
  },
  {
    "objectID": "MyBlog/How-to-CSE/index.html",
    "href": "MyBlog/How-to-CSE/index.html",
    "title": "How to CS?",
    "section": "",
    "text": "One thing we either care too much about or we don’t care at all about is CGPA/CPI. Some say it has nothing to do with real work/ skills required out there in the world, some say one day before exam is all you need, some say it’s really important that if you regularly attend the classes and make good notes, you are good to go. All these are true up to some extent. Here is real picture what I have observed in last 6 semesters of my CSE Engineering.\nSo, most of the colleges start first year with a weird combo of everything, you like it or not (Situation may change with NEP). We like PCM when we study for Competitive exams because those are essentially oriented to challenge our mind logically but all engineering disciplines are not exactly as logical as what JEE was. There is theory, there is research then there are faculty biases and weird advices from seniors. So, many (including me) discard these subjects in our first year as useless and ruminate how bad our education system is. Now though your argument might not be flawed but to make it work you might need to go through a lengthy official channel so your juniors might benefit. But now you have at your hand, these subjects like Chemistry, Physics ,etc. {You know I love physics} Study these like you studied NCERT chapter from which certainly one or two questions pop and nobody (not really) likes to study them. Study only once well in advance before exam and just give a quick revision before exam, see and solve all PYQs twice (if faculty is in continuity). Study the Math as thoroughly as you can. Give economics a good shot at studying. Give good hand at programming as its essential to develop problem solving. Don’t fall prey to fear mongering of societies to induct you due to peer pressure. Sure try hard to get into top-notch technical clubs that have good track record (Cynaptics Club of IIT Indore😁). First year vacation go for God level CP/ internships (not ones on cheap sites.. networking is all you need (with fair assumption that you will nurture skills) to land a first year internship at good companies). Alas these details and many other things in some other md some other time.\nOne more thing, I started using Tablet in my third year for taking notes, and it doesn’t really makes a difference except it makes it easier to share your notes with your friends or harder to deny to not so friends. Having course maps, in which you just make graphs depicting what chapters, topics, sub-topics are taught, is really helpful in covering entire syllabus for exams and also helps you retrieve right concepts required for answering questions. If you feel slides are enough and if you are attending classes, just write day date and what were Headings in the lecture, will suffice to retain lot of things. Some profs have bias for good handwriting and good presentation in answer sheet ( Something to incorporate if you have luxury to do so). Having a study group is not at all essential but it helps while preparing for exams. One most important thing, don’t panic in exam if you feel you have not studied well since you can take cues from question but to do that you need open and calm brain. Manage sleep cycle well during exams (if you are prepared already, else burn your midnight oil but cover the material). Make sure to breakfast if you have exams in morning or have light lunch 2 hours before exam, if exam is in afternoon session. Reach exam hall on time, settle and cool down. Breathe slowly to gain composure. These are very basic things that our moms tell every exam day, but most of us live in hostels, so thought of mentioning these.\nLet’s move to Second and third year now, here you start with core subjects from CSE. Some of these course include DSA, DBMS, Automata (or TOC as is called), Discrete math, Computer Architecture(🥹 my classmates will understand why), Software Engineering, Computer Networks, Operating Systems, Parallel Computing, ML, CV, Computational Intelligence, Optimization algorithms. Now, one thing all these course will offer you is good base especially if your university conducts good Labs associated with them. These courses are pretty standard and you can study them on your own if you wish. But what I have felt is attending few classes is certainly going to help you in terms of content. Either make your own notes, if slides are not provided, or have a good Note maker good-friend (As I was for many in my class). Why notes matter because profs are likely to teach, speak something not on slides with specific intention of putting these things in exam. If attendance carry marks, don’t miss classes please. Some courses are so damn scoring that attendance will decide final grade. If particular professor is known for strict (many call it vindictive) grading, attend all classes of such courses. PYQs are GODS for these courses. Just, make sure to solve (or study answers from instructor answer manual ) of exercises in the recommended textbook. It always helps those who solve (and ruins those who don’t💀). Now, how much and when is totally dependent on your priorities. One of my brilliant friends prefer having weekly reviews (despite you attend classes or not) to avoid last minute panic and a general sense of what is going on in course whereas I prefer attending classes. Do labs seriously, complete the exercises then and there, don’t wait for deadlines, do labs on your own (unless you are sure that there will be no lab exam and you have all the answers readymade from senior and you hate that subject). These are highly scoring courses. I have friends who have done way lot more things than me and managed to get near full grades. Also there are friends who have pursued everything from fun, joy and memorable experiences, bunked classes and still managed to do pretty good (🤘). Look definition of good is not same for everyone. But good academics mostly will make your way smoother and easier in whatever domain you see yourself going. Be it job market or be it research/ higher education. These two years are also stepping stones in transforming yourself into a skilled individual. So there is much more than academics in these two year and make sure you succeed there. Use all kind of tricks to save time from acads and to invest in yourself from solving CP questions / network / read my blogs in class which is ultimately disappointing or in a useless(for you) lab. Make sure to not get into unnecessary conflict with any prof / TAs.\n\nThis blog is written without any help from GenAI models. I will try to write another how-to-cs in future.\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“How to CS?” 2024. July 25, 2024. https://madhaviit.github.io/MyBlog/How-to-CSE/.",
    "crumbs": [
      "Madhav_Speaks",
      "How to CS?"
    ]
  },
  {
    "objectID": "MyBlog/Guardrails/index.html",
    "href": "MyBlog/Guardrails/index.html",
    "title": "Guardrails",
    "section": "",
    "text": "The rise of generative AI has ushered in a technological revolution reshaping how we interact with and leverage information. Long before ChatGPT captured global attention, I was fascinated by the immense potential of this field, researching cutting-edge applications like diffusion models for medical imaging. I have seen these technologies employed very innovatively. For example, learning the vocabulary of a new language with AI-curated multimodal content is engaging and enjoyable. Chatbots have become much more efficient in addressing customers’ primary concerns. Medical Imaging and Diffusion models are producing results with outstanding accuracy. Many social issues are of grave concern to me and can be solved with enough research and recent advances in Tech put in the right direction, like farmers’ distress due to lack of guidance and inequality of opportunity in Education. But this is just the brighter side.\nThe issue that concerns me the most is the dark patterns in the recommendation algorithms – a problem worsened by the complex generative AI models, the consequences of which I dread. Focused, advanced research in this field is urgently needed. A Netflix documentary “The Social Dilemma” exposed these dangers before the mass proliferation of generative AI, but today’s reality is even more alarming. Social networks and online platforms surround our day-to-day lives, serving purposes that range from relationship maintenance and discovering new connections to consuming entertainment and news content and facilitating commerce and self-expression. However, multiple research papers from studies across different nations have established that these very same platforms have also enabled darker use cases – from trolling and abuse to spreading misinformation and propaganda to even the nefarious manipulation of voters.\nTechnological developments in Recommendation algorithms and mighty powerful GenAI models (trained on the corpus of data with serious privacy concerns) present a double-edged sword. On the one hand, it can make students learn better; on the other, it can even radicalize them. Extensive research is crucial to address these systems’ risks to our privacy and autonomy. Recommendation algorithms have resulted in the amplification of extremist content. They are not neutral. After all, recommendation systems are designed to keep us engaged by feeding us more of what we’re likely to click on, watch, or read. The more data they collect about our actions, the better they can predict and influence our future behaviour. But the implications go far beyond just shaping what content we see. By analysing small details, these platforms possess the capability to predict our actions, emotions, beliefs and even vulnerability to a surprising level of accuracy. All this deeply personal data exists in corporate databases, largely unprotected from misuse. Brands already leverage these insights to psychologically compel user actions, undergirding the online advertising industry.\nGenerative models which are trained on web crawls are able to correlate many of these things. By crawling over all the text (and, in recent years, videos and images as well) on the internet, including our social media trails, they can precisely mimic our language, communication styles, and personas. Identity theft has become much easier because of these platforms’ lack of new-age security measures. This ability to produce human-like content at scale enables hyper-personalization and targeting that is difficult to distinguish from authentic human engagement. With the powerful multimodal models, these models can also be tweaked to manufacture very realistic and believable misinformation, fake news, propaganda and armies of bots that can mass manipulate with hyper-personalization. Nation-states and bad actors may leverage this technology for surveillance, social control, and undermining democracy and social cohesion. I have experienced this issue playing a destructive, polarizing role in many events and campaigns. The hate, abuse and trolling one faces on these platforms is simply unbearable for most individuals.\nThus, robust research initiatives are crucially needed across several interrelated domains: human-computer interaction principles, robust authentication mechanisms, ethical web framework designs, unbiased recommendation algorithm development, and the responsible architecture of social networks and generative AI systems. While these endeavours are multifaceted, combining insights from each field can minimize the risks arising from the indiscriminate integration of AI across digital spheres. Ultimately, such collaborative research safeguards authentic human-to-human interactions from corrosive technological influences.\nFirstly, robust authentication systems and privacy-preserving approaches must be developed to safeguard user data and autonomy. Distributed systems can potentially mitigate risks of centralized data misuse by making it more difficult for any single entity to conspire. Moreover, establishing clear AI governance frameworks, regulations, and ethical training standards is imperative to ensure generative AI models are developed and deployed responsibly without perpetuating biases or infringing human rights. Recent initiatives like Snowflake’s transparent open-sourcing of their Arctic LLM represent a welcomed shift towards responsible AI research in the open. By embracing open-source philosophies and rigorous peer scrutiny, generative model development can become more accountable and aligned with ethical principles from the ground up. Secondly, extensive research is needed in human-computer interaction (HCI) principles and user interface (UI) design. This includes exploring mechanisms to provide users transparency into if/when they are interacting with AI systems, what data is being utilized, and how it influences recommendations or content curation. Interface designs should empower users to audit inferences made about them, object to unfair treatment, and manage their digital footprints consciously. Significant advancements must be made in distinguishing human-generated and AI-generated content across modalities like text, images, and videos. While extremely challenging, developing robust detection algorithms is crucial to combat misinformation, deception, and erosion of trust. Complementing this, Web 4.0 frameworks should incorporate intelligent ways to obfuscate personal data from indiscriminate crawling without user consent.\nWithout proactive human-centred interventions, we risk ceding our cognitive sovereignty and sleepwalking into a dystopian future of surveillance, manipulation and social fractioning on a societal scale. Maintaining privacy and human agency must be central priorities as generative AI breakthroughs rapidly reshape human-computer interaction.\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“Guardrails.” 2024. April 22, 2024. https://madhaviit.github.io/MyBlog/Guardrails/.",
    "crumbs": [
      "Madhav_Speaks",
      "Guardrails"
    ]
  },
  {
    "objectID": "MyBlog/India@75/index.html",
    "href": "MyBlog/India@75/index.html",
    "title": "India@75",
    "section": "",
    "text": "Today, we celebrate 75 years of independence, hard-fought and earned through the sacrifices of millions and millions. This is our moment, and we own it. At this auspicious moment, there are tons of opportunities and millions of traps ahead of us. The question is, are we wise enough to avoid the traps and grab the opportunities? Moreover, where do we, as a nation, look ahead?\nLong years ago, we made a tryst with destiny; and now the time comes when we shall redeem our pledge, not wholly or in full measure, but very substantially. At the stroke of the midnight hour, when the world sleeps, India will awake to life and freedom. These were the first few words of the First Prime Minister of India, Pandit Jawaharlal Nehru, from one of his greatest speeches when India became Independent. India needs to try to make herself an outward-looking and utterly free society. Free from fear and full of confidence that we as a nation will march together to greatness where each of us feels included. India must free herself from divisions of religions and castes and devote herself to science and innovation.\n\nWe are about to complete The Azadi Ka Amrit Mahotsav. According to our honourable Prime Minister Shri Narendra Modi, this is an enthralling year filled with the elixir of energy of independence; the elixir of inspiration of the warriors of the freedom struggle; the elixir of new ideas and pledges; and the elixir of Aatmanirbharta. Therefore, this Mahotsav is a festival of awakening of the nation; festival of fulfilling the dream of good governance; and the festival of global peace and development. As we enter the Amrit Kal, let us look at this moment distantly.\n\nA moment comes, which comes but rarely in history, when we step out from the old to the new, when an age ends, and when the soul of a nation, long suppressed, finds utterance. It is fitting that at this solemn moment we take the pledge of dedication to the service of India, and her people, and to the still larger cause of humanity. We must feel the freedom of that long-suppressed soul when we think about acting selfishly against the national interest. We end today a period of ill fortune and India discovers herself again. Everyone must feel the pledge to serve this country in every way possible. We shall not forget the sacrifices of our forefathers who lived every breath to improve our beloved nation. The utterance that the soul found was of democracy and liberal values. We shall embrace it wholeheartedly, keeping aside momentary political gains. The diversity in India shall be celebrated with a sense of unity and not uniformity. There will be chaos in a nation of our size and diversity, but it should be constructive. India must move, slowly or fast, but in the forward direction. No way we can afford to go back.\nThe ambition of the greatest men of our generation has been to wipe every tear from every eye. This is the crux. We are obligated to uplift those who have not been advanced enough to have a life with even a bare minimum standard of living. We need to raise even those bare minimum standards as fast as possible. It is said that education is tigress’ milk. We also need to build world-class institutions for mutual exchange of wisdom from other nations. India should also make space research more collaborative so that even the sky is not our limit.\nTo the people of India, whose representatives we are, we make appeal to join us with faith and confidence in this great adventure. This is no time for petty and destructive criticism, no time for ill will or blaming others. We have to build the noble mansion of free India where all her children may dwell. We shall embark on values of tolerance that we inherited. We shall be aware and brave enough that our tolerance may not be conceived as a weakness. We are a young nation. We must appreciate the successful youth, but we seriously need to look at a generation that has been completely lost in the virtual world, which I fear threatens the real zeal for a better life, education, and opportunities. The voices of women were heard and empowered. Nevertheless, still, we are far from saying that we have liberated the ladies of this nation from patriarchy. Serious concern has been that parliamentary practices are deteriorating year by year. We need a fix on this. We need to deal with environmental challenges while taking entire humanity on board. And finally, we need consciousness. The greatness of India shall lie at the bottom of our hearts, and the urge for betterment should be at the top of our minds. We must celebrate the achievements of these 75 years and look at the future with a clear goal of a world of happiness and prosperity because the idealism of today can be tomorrow’s realism.\n\nThis blog is written without any GenAI models. You can also find this blog on medium",
    "crumbs": [
      "Madhav_Speaks",
      "India@75"
    ]
  },
  {
    "objectID": "MyBlog/India@75/index.html#india75-through-lens-of-panditji",
    "href": "MyBlog/India@75/index.html#india75-through-lens-of-panditji",
    "title": "India@75",
    "section": "",
    "text": "Today, we celebrate 75 years of independence, hard-fought and earned through the sacrifices of millions and millions. This is our moment, and we own it. At this auspicious moment, there are tons of opportunities and millions of traps ahead of us. The question is, are we wise enough to avoid the traps and grab the opportunities? Moreover, where do we, as a nation, look ahead?\nLong years ago, we made a tryst with destiny; and now the time comes when we shall redeem our pledge, not wholly or in full measure, but very substantially. At the stroke of the midnight hour, when the world sleeps, India will awake to life and freedom. These were the first few words of the First Prime Minister of India, Pandit Jawaharlal Nehru, from one of his greatest speeches when India became Independent. India needs to try to make herself an outward-looking and utterly free society. Free from fear and full of confidence that we as a nation will march together to greatness where each of us feels included. India must free herself from divisions of religions and castes and devote herself to science and innovation.\n\nWe are about to complete The Azadi Ka Amrit Mahotsav. According to our honourable Prime Minister Shri Narendra Modi, this is an enthralling year filled with the elixir of energy of independence; the elixir of inspiration of the warriors of the freedom struggle; the elixir of new ideas and pledges; and the elixir of Aatmanirbharta. Therefore, this Mahotsav is a festival of awakening of the nation; festival of fulfilling the dream of good governance; and the festival of global peace and development. As we enter the Amrit Kal, let us look at this moment distantly.\n\nA moment comes, which comes but rarely in history, when we step out from the old to the new, when an age ends, and when the soul of a nation, long suppressed, finds utterance. It is fitting that at this solemn moment we take the pledge of dedication to the service of India, and her people, and to the still larger cause of humanity. We must feel the freedom of that long-suppressed soul when we think about acting selfishly against the national interest. We end today a period of ill fortune and India discovers herself again. Everyone must feel the pledge to serve this country in every way possible. We shall not forget the sacrifices of our forefathers who lived every breath to improve our beloved nation. The utterance that the soul found was of democracy and liberal values. We shall embrace it wholeheartedly, keeping aside momentary political gains. The diversity in India shall be celebrated with a sense of unity and not uniformity. There will be chaos in a nation of our size and diversity, but it should be constructive. India must move, slowly or fast, but in the forward direction. No way we can afford to go back.\nThe ambition of the greatest men of our generation has been to wipe every tear from every eye. This is the crux. We are obligated to uplift those who have not been advanced enough to have a life with even a bare minimum standard of living. We need to raise even those bare minimum standards as fast as possible. It is said that education is tigress’ milk. We also need to build world-class institutions for mutual exchange of wisdom from other nations. India should also make space research more collaborative so that even the sky is not our limit.\nTo the people of India, whose representatives we are, we make appeal to join us with faith and confidence in this great adventure. This is no time for petty and destructive criticism, no time for ill will or blaming others. We have to build the noble mansion of free India where all her children may dwell. We shall embark on values of tolerance that we inherited. We shall be aware and brave enough that our tolerance may not be conceived as a weakness. We are a young nation. We must appreciate the successful youth, but we seriously need to look at a generation that has been completely lost in the virtual world, which I fear threatens the real zeal for a better life, education, and opportunities. The voices of women were heard and empowered. Nevertheless, still, we are far from saying that we have liberated the ladies of this nation from patriarchy. Serious concern has been that parliamentary practices are deteriorating year by year. We need a fix on this. We need to deal with environmental challenges while taking entire humanity on board. And finally, we need consciousness. The greatness of India shall lie at the bottom of our hearts, and the urge for betterment should be at the top of our minds. We must celebrate the achievements of these 75 years and look at the future with a clear goal of a world of happiness and prosperity because the idealism of today can be tomorrow’s realism.\n\nThis blog is written without any GenAI models. You can also find this blog on medium",
    "crumbs": [
      "Madhav_Speaks",
      "India@75"
    ]
  },
  {
    "objectID": "MyBlog/Seniors-talk/index.html",
    "href": "MyBlog/Seniors-talk/index.html",
    "title": "Preparation for Internships",
    "section": "",
    "text": "I hope this message finds you well and enjoying your vacation. You are probably running around, barely having enough time to breathe. However, this is an excellent opportunity to begin creating your resume, CV, cover letter, letter of motivation, research statement, SOP, and all the other things you will need when seeking opportunities. Remember, this is not a one-time, done deal. It is about presenting yourself on paper over a period of time. You must update it continuously throughout your career. The term “resume” means to begin again after a pause. Your resume is a reflection of yourself. It’s a representation of who you are. If provided with an opportunity, your resume should indicate to the reader the context from which you will be resuming. Your resume is the most important document in your career. Recruiters spend an average of six seconds (I doubt but this number is widedly quoted) reviewing a resume before they make the initial decision on candidates. Please find some guidelines shared by our senior colleagues from final year, Niyati Totala and Amogh Dixit at end of this post.\nIn another talk, seniors also discussed about overall process. The resume is only one piece of it. Once you’re past that door, the interview is what actually decides things. So here is crux with fair bit of my opinion as well, honestly, not in like guaranteed roadmap, but what actually seemed to work. Entire process constantly keeps changing and the seniors advised us to keep that in mind.\nMany said CP is the only way. Some say DSA is enough. Some say just grind Leetcode and you’re fine. But, DSA is what placements are currently built on. Competitive Programming is great if you enjoy it as it genuinely sharpens you and it will help you regardless of what you go for. Software, Quant, ML Research, etc. Main requirement for Software roles is that you can think through a problem, write clean code, and explain what you did and why. For that Leetcode (especially hards) is your best friend here. Paid courses are mostly repackaged content you can skip. Don’t fall for that. And please, don’t cheat your way through practice problems. You will be robbing yourself of internalising fundamentals.\nDBMS also keeps showing up in interviews more than people expect. Don’t leave it for last. Interviewbit is well-structured for this (Seniors strongly recommended). Use it. For beginners, resist the urge to immediately look up the solution. Sit with the problem. Be uncomfortable for a bit. As you get more comfortable, cap yourself at around half an hour per problem before moving on, that’s a reasonable balance between productive struggle and not wasting your entire evening on one question.\nWhen it comes to projects, the “why” matters enormously. Why did you build it? What problem were you solving? What would you do differently now? If you can’t answer those, it’ll show. Don’t copy projects. First, its wrong and second, interviewers know popular tutorial projects and they will ask follow-up questions. Prepare a rough questionnaire for yourself of the hardest things someone could ask about your work, and actually think through answers. OOP and basics of C/C++ also comes up more than you’d expect.\n\nThe research / Masters track is a different game\nIf placements aren’t your goal and you’re eyeing Masters or research roles, the prep looks quite different. Here the focus shifts to strong SOP, strong LOR, and a decent CPI. Publications help significantly. Research internships, even small ones, matter a lot and are often better signals than corporate internships for this path (not necessarily imo). Build genuine relationships with professors early based on shared interests. For ML specifically, you can start at a startup to build context, or go directly for a research internship. Both are valid. Don’t wait to feel “ready enough” to reach out to professors, including those outside your campus.\nPlease practice mock interviews. Practice talking through your solutions out loud. It feels awkward. Do it anyway. The gap between understanding something and being able to articulate it under mild pressure is significant. Also, one senior particularly suggested to have some dream companies or programs in mind as they guided her actions, because having something concrete to aim for makes the prep intentional.\n\n\nResume Guidelines\n\nVerify that all links are working correctly.\nInclude CPI, as well as 10th and 12th grades.\nIf it is applicable, include your B.Tech minor.\nIn the experience section, list your previous internships along with the skills you used and briefly describe them.\nComplete 80% of the line so your resume does not appear blank.\nBegin with action words.\nCreate simpler resumes that can pass through ATS easily.\nMention how many people you impacted, such as “Developed a website that was used by 2000 people” or “Improved interaction with customers/stakeholders by 50% (mention figures).”\nFor work experience bullet points, utilize the STAR method : Situation, Task, Action, Result.\nTechnical skills can be listed as Strong, Weak, or through frameworks.\nInclude all of the mathematics courses you have taken. Honesty is key. Do not fabricate anything on your resume.\nTry to obtain internships. Instead, showcase your projects (including GitHub repo or hosted link).\nPlace relevant projects at the top.\nPut internships in chronological order.\nIn the case of research internships, foster relationships with professors, as their letters of recommendation can significantly benefit you.\nBuilding a resume takes time. Keep updating it and approach professors as often as possible. Referrals are vital. Complete the things. If you do so, you are already ahead of the curve.\nDo not be afraid to contact professors for research internships outside campus.\nPOR should be listed in order of relevance.\nInclude your JEE rank and how many people took the exam.\nInclude your Codeforces or CodeChef ranking. Do not include trivial information.\nDescribe what you have accomplished during college.\nBe familiar with every word on your resume.\nUse bullet points whenever possible.\nDo not leave any white spaces on your resume.\nYour resume should be one page only.\nProofread and ask your friends for assistance.\nIf possible, try to tailor your resume for the role you are applying.\n\nSpecial thanks to Sairaj Loke for helping me organize my notes from Resume session. I had two separate writeups earlier, I have combined them into one to present a more coherent picture for reader.\n\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“Preparation for Internships.” 2023. May 11, 2023. https://madhaviit.github.io/MyBlog/Seniors-talk/.",
    "crumbs": [
      "Madhav_Speaks",
      "Preparation for Internships"
    ]
  },
  {
    "objectID": "MyBlog/Broken-Silence/index.html",
    "href": "MyBlog/Broken-Silence/index.html",
    "title": "Broken Silence",
    "section": "",
    "text": "At the nest’s edge, she cooed, when her baby’s chirps pierced through the dawn’s silence as it cracked through the eggshell. Days turned to weeks listening to rustling leaves and flapping wings as she guided him flight lessons, worm hunts, predator evasion. Eventually, silence returned except for the sobs of the gusts.\n\n\n\n\n\n\n\n\n\nReuseCC BY-NC-ND 4.0CitationFor attribution, please cite this work as:\n“Broken Silence.” 2025. February 27, 2025. https://madhaviit.github.io/MyBlog/Broken-Silence/.",
    "crumbs": [
      "Madhav_Speaks",
      "Broken Silence"
    ]
  }
]